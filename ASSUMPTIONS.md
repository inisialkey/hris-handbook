# ASSUMPTIONS.md — Assumption Log

Records decisions made under `CLAUDE.md` Decision hierarchy rule 3 (minor gaps) and the outcome of the `HANDBOOK_SPEC.md` §6 defaults review. Architectural gaps get ADRs (rule 4); this file only points to them.

## Assumptions

| ID | Date | Area | Assumption | Status |
|---|---|---|---|---|
| A-001 | 2026-08-01 | ADR filenames | ADR files are named `docs/adr/ADR-XXXX-<kebab-case-title>.md`, per the §8 tree comment ("ADR-0001-..., kebab-case titles"). | Active |
| A-002 | 2026-08-01 | Manifest structure | `docs/04-database/erd-overview.md` is scheduled in Phase 4, not Phase 1/2: a full-system ERD can only be drawn after all module entities exist. §8 groups it under `docs/04-database/` by folder only; no phase was specified. | Active |
| A-003 | 2026-08-01 | Deployment region (extends D5) | Reference production region is GCP `asia-southeast2` (Jakarta) to satisfy UU PDP data-residency expectations for employee personal data. Manifests stay cloud-portable per D5. | Confirmed 2026-08-01 (with D1–D14) |
| A-004 | 2026-08-01 | Repo scope | `docs/agents/*` (issue-tracker, triage-labels, domain) are pre-existing agent-workflow docs, not handbook content; excluded from `MANIFEST.md`. | Active |
| A-005 | 2026-08-01 | Roles | `HANDBOOK_SPEC.md` §3.3 marks only Super Admin as platform-level. Therefore **System Administrator is tenant-scoped** (technical admin of one tenant: settings, device revocation, audit access, integrations); Super Admin is the sole platform-level role. Final permission catalog in `docs/05-platform/authorization-rbac.md`. | Confirmed 2026-08-02 (grilling) |
| A-006 | 2026-08-01 | Repo layout | Implementation uses **three separate repositories** (backend NestJS, admin web Next.js, mobile Flutter); no monorepo. File/folder conventions in `naming-conventions.md` §11 are app-root-relative; CI/CD doc plans pipelines per repo. Spec never states repo count; "pipelines per app" (§7 Phase 4) is compatible with both. | Confirmed 2026-08-02 (grilling) |
| A-007 | 2026-08-01 | Auth scope (**narrowed to tenant users 2026-08-04**) | **MFA (TOTP/OTP) is out of V1 for tenant users.** Spec §5.8 lists password/PIN/biometric/remember-device but no MFA. **Scope correction 2026-08-04 (ADR-0017):** §5.8 is the *tenant* login surface, and no platform login surface existed when this was written — so this assumption never reached `platform_users`. TOTP is now **mandatory** for platform users, on the ground that one platform credential reaches every tenant's NIK, salary, and bank data. Nothing changes for employees, managers, or any tenant admin role; tenant-side MFA remains the first fast-follow. Schema reserves `sessions.mfa_verified` + credential-type discriminator; remember-device marker doubles as future MFA-skip hook (`docs/adr/ADR-0004-auth-sessions-device-management.md`). | Confirmed 2026-08-02 (grilling; admin-web phishing risk accepted for V1, MFA = first fast-follow) |
| A-008 | 2026-08-01 | Selfie retention | Punch selfies retained **12 months** by default (`attendance.selfie_retention_months`, tenant-tunable), then purged; punch row keeps the sha256 + metadata. Driver: storage cost vs. labor-dispute evidence window — no explicit statutory period identified. Related: `docs/adr/ADR-0009-file-storage-strategy.md`. | Confirmed 2026-08-02 (grilling; attendance.md carries VERIFY marker on the value) |
| A-009 | 2026-08-01 | Observability backends (extends D6) | Log sink = **Cloud Logging** (GKE stdout, zero infra; portable swap = Loki). Trace backend = **Grafana Tempo** via OTel Collector. D6 named the SDK/metrics stack but no sink/backend. Related: `docs/adr/ADR-0011-observability-stack.md`. | Confirmed 2026-08-02 (grilling) |
| A-010 | 2026-08-02 | PostgreSQL version | Reference version **PostgreSQL 16+**: core-schema uses `UNIQUE NULLS NOT DISTINCT` (PG15+) on `uq_user_roles_assignment` and `uq_counters_scope`. Managed PG per D5 (Cloud SQL) supports it. | Confirmed 2026-08-02 (grilling) |
| A-011 | 2026-08-02 | Backend validation library | NestJS transport DTO validation uses **class-validator + class-transformer**: native `ValidationPipe` integration, and Swagger reads the same decorator surface — one declaration for shape, validation, and docs. ADR-0006 listed "Zod / class-validator" without pinning the backend; Zod remains admin-web only (spec §5.2). No cross-repo schema sharing — OpenAPI is the contract (`docs/02-architecture/backend-nestjs.md` §6). | Active |
| A-012 | 2026-08-02 | Mobile library set | Spec §5.1 mandates patterns (DI, offline-first, Result) but not libraries beyond flutter_bloc/Drift. Fixed in `docs/02-architecture/mobile-flutter.md`: **get_it** (DI, manual registration — no injectable codegen), **go_router** (navigation + FCM deep links), **dio** (HTTP, interceptor chain), **Equatable** (state value equality — no freezed; Dart 3 sealed classes cover unions), **decimal** (exact money math; Drift money columns = TEXT + `TypeConverter<Decimal, String>`, coding-standards-flutter §6). Hand-written JSON mapping — no json_serializable (Drift is the repo's only codegen). Adding codegen (injectable/freezed/retrofit) later is additive, not breaking. | Active |
| A-013 | 2026-08-02 | Admin web i18n library | **next-intl** (App Router-native) for the admin app; `id` default + `en` (D12), no locale URL prefix in V1 (authenticated tool, no SEO value). Spec fixed the i18n requirement but not the web library. `docs/02-architecture/admin-nextjs.md` §11. | Active |
| A-014 | 2026-08-02 | JWT signing algorithm | **EdDSA (Ed25519)** with `kid` rotation; private key in api pods only, verifier pins the algorithm (no `alg` negotiation). ADR-0004 fixed the token model but not the algorithm. `docs/03-standards/security-standards.md` §7. | Active |
| A-015 | 2026-08-02 | Admin web test/dev library set | Spec fixes the runtime stack (§5.2) but not test tooling. Fixed in `docs/03-standards/coding-standards-nextjs.md` §9: **Vitest** (unit/hooks/components), **React Testing Library + user-event**, **MSW** (the only API fake — network boundary, keeps the Axios interceptor chain under test), **Playwright** (thin E2E tier). Formatting: **prettier-plugin-tailwindcss**. `cva`/`date-fns` arrive as shadcn dependencies, not new choices. | Active |
| A-016 | 2026-08-02 | Backend money math + test library set | Spec fixes patterns, not these libraries. Fixed in `docs/03-standards/coding-standards-nestjs.md`: **decimal.js** for all money arithmetic (explicit rounding modes — payroll needs `ROUND_HALF_UP`-class control; `number`/`parseFloat` on money = lint error); test stack = **Jest** (Nest-native, decorator metadata works out of the box — deliberate divergence from web's Vitest, A-015) + **supertest** + **Testcontainers** (real PostgreSQL/Redis; RLS leak tests L1–L6 and idempotency replay run against real infra, Drizzle never mocked). | Active |
| A-017 | 2026-08-02 | Email provider (**corrected 2026-08-04**) | All email sits behind an **`EmailPort`** adapter (`docs/05-platform/notification.md`). **Reference implementation is Resend, per `HANDBOOK_SPEC.md` §6 D7.** *Correction:* this entry originally read "spec mandates email as a channel but names no provider" and selected **Amazon SES** — both halves were wrong. D7 names Resend explicitly and states that **Amazon SES is prohibited**, and D1–D14 were confirmed binding on 2026-08-01, so the entry contradicted anchor #1 for two days. Caught while writing `docs/07-operations/environments.md` §10.1, the file that would have hardened it into SPF and DKIM records. The port absorbed the change entirely — state machine, retry model, dedupe key, and all 16 templates unchanged, which is the first time that abstraction has been called upon. Nothing was lost: the original justification was that recipient mailboxes fall outside A-003's residency scope, which is equally true of Resend, and the region advantage claimed was never load-bearing. Sender domain, SPF, DKIM, and DMARC per environment are `environments.md` §10.1; Mailpit stands in locally. **Still undischarged from D7 and flagged for the file-72 audit: the templating clause ("react-email or MJML") is named nowhere in the handbook** — a coding-standards concern, not an environment one. | Active |
| A-018 | 2026-08-02 | Sensitive-value masking format | ADR-0016 mandates masked display but no format. Fixed in `docs/06-modules/employee.md` §4.3: all but the **last 4** characters replaced (`••••••••••••3456`); holder names keep the first word. Last-4 is the Indonesian banking/fintech display convention; enough for the owner to self-verify, useless to a shoulder-surfer. | Active |
| A-019 | 2026-08-02 | Employee master import mode | ADR-0015 supports `upsert`, but `docs/06-modules/employee.md` BR-EMP-012 fixes `employee.master` at **`create_only`** for V1: silent bulk mutation of live identity/bank data via spreadsheet is a fraud/typo amplifier; updates go through the UI (audited per field) or a future `update_only` definition with an explicit field whitelist (§15). | Active |
| A-020 | 2026-08-02 | Religion field captured | `employees.religion` (KTP-official six categories, nullable) exists solely for THR eligibility mapping (payroll.md) and statutory document needs — a UU PDP special-category field; purpose limitation recorded in employee.md §1 and security-standards §9's lawful-basis row. Payroll validates presence before a THR run; the field never appears in list payloads or exports' base column set. | Active |
| A-021 | 2026-08-02 | Mobile schedule window | `docs/06-modules/shift.md` BR-SHF-014 fixes the offline schedule mirror at **today − 30 … today + 60 days**, platform-fixed (not a settings key, per the cluster-F rule that platform mechanics are configuration). Backward span covers the correction/dispute lookback attendance already keeps locally (90-day local history, spec §5.7 — the older tail is server-fetched); forward span covers the longest roster horizon tenants publish in practice (two months). No spec guidance existed. Widening it later is a client constant plus a query bound, not a schema change. | Active |
| A-022 | 2026-08-02 | Attendance day-close timing | `docs/06-modules/attendance.md` BR-ATT-012 closes a date once the **latest punch window that started on it has been shut for 2 hours**, evaluated in branch-local time by an hourly job. Platform-fixed, not a settings key (cluster-F rule: platform mechanics are configuration). The window-relative form is what makes it correct for night shifts — a fixed wall-clock cutoff would close a 22:00–06:00 shift's day before its own out-window expired. Two hours is the drain allowance for a phone that reconnects on the commute home; no spec guidance existed. Lengthening it delays absence detection, shortening it risks closing a day a late drain would have completed — either change is one constant. | Active |
| A-023 | 2026-08-02 | Default geofence radius | `attendance.geofence_radius_m` defaults to **100 m** (tenant/company/branch scoped, so tenants tune it per site). Rationale: consumer GPS in an urban Indonesian setting commonly reports 10–50 m accuracy and worse indoors, so a tighter default would flag honest punches at the door, while a looser one covers the neighbouring building. Not a regulatory figure — an operational default chosen to make `flag` mode's review queue signal rather than noise. Tenants running `strict` are expected to raise it per site. | Active |
| A-024 | 2026-08-02 | Leave-year basis | The leave period is an explicit `[start, end]` date pair selected by `leave.annual_period_basis` (`calendar` \| `anniversary`, company scope, default **`calendar`**) — `docs/06-modules/leave.md` BR-LVE-008. Both bases are in real Indonesian use: the statutory entitlement arises after twelve months of service (anniversary-shaped), while most companies run a calendar leave year with a prorated first year. The spec fixes neither. Calendar is the default because it is what tenants expect on a payroll-aligned product and because HR reporting is annual. Storing dates rather than a year number is what lets one schema serve both; switching a company later affects periods opened after the change, never live ones. | Active |
| A-025 | 2026-08-02 | Carry-over defaults | Seeded `annual` type carries `carry_over_max_days = 6` and `leave.carry_over_expiry_months` defaults to **3** (`docs/06-modules/leave.md` BR-LVE-010, ⚠️ VERIFY marked there). Neither is a statutory figure — UU 13/2003 art. 79 jo. UU Cipta Kerja leaves carry-over to the implementing PP and the PKB, so these are common-practice operating defaults (half the annual quota, one quarter to use it) chosen so a tenant that never touches the setting still behaves sanely. Both are tenant-editable configuration; nothing in the engine assumes the values. | Active |
| A-026 | 2026-08-02 | Half-day leave excluded from V1 | `HANDBOOK_SPEC.md` §10 item 6 lists "half-day rules" as required leave coverage; `docs/06-modules/leave.md` §1 **excludes half-day and hourly leave from V1** and documents the rule as "not available, and here is why". Cause: `attendance_days` carries one whole-day verdict (attendance.md BR-ATT-011), so a half-day leave date would derive `on_leave` and silently contradict the punches from the worked half — a wrong attendance record is worse than a missing feature. Enabling it needs attendance day fractions, already listed in attendance.md §15 as a paired future item. The leave side is built for it: balance and ledger columns are `numeric(6,2)`, so the change is a policy flag plus a UI control, not a migration. **Flagged for user review** — overturning this means amending attendance.md, a completed module. | Active |
| A-027 | 2026-08-02 | Overtime filing window: backdate 7 days, no minimum notice | `overtime.max_backdate_days` defaults to **7** and there is deliberately **no** `overtime.min_notice_days` key (`docs/06-modules/overtime.md` BR-OVT-001). Overtime is ordered same-day far more often than it is planned — "stay two hours tonight" is the normal case — so a minimum-notice window would block reality rather than enforce policy. The backdate window is the mirror control: it exists so genuine unplanned overtime can be filed the next morning while a month-old claim, which has no evidence left that anyone ordered it, cannot. Seven days is an operating default, not a regulatory figure, and it is company-scoped configuration. | Active |
| A-028 | 2026-08-02 | TOIL expires at the leave-period boundary | Time off in lieu is credited into leave's ledger against the seeded `toil` type with `carry_over_max_days = 0`, so unused TOIL expires when its leave period ends (`docs/06-modules/overtime.md` BR-OVT-011, `docs/06-modules/leave.md` §4.2). The redemption deadline a tenant would actually want is per-entry — "take within three months of earning" — but leave's expiry machinery is period-scoped, and adding an `expires_on` column to `leave_ledger_entries` for one consumer is a schema change ahead of a confirmed requirement (both modules' §15 carry it). Whether Indonesian regulation fixes a redemption deadline at all is ⚠️ VERIFY-marked in overtime.md §1; if it does, this becomes a correctness item rather than a convenience one. | Active |
| A-029 | 2026-08-02 | TOIL is credited at multiplier-hours | A `toil` occurrence credits leave with its **multiplier-hours**, not its raw worked hours — two hours of rest-day work at 2× yields four hours of time off (`docs/06-modules/overtime.md` BR-OVT-011). Rationale: crediting raw hours makes conversion a strict pay cut, so no employee would ever choose it and the `employee_choice` mode would be decorative. The statutory basis for time-off-in-lieu, and therefore its lawful conversion rate, is ⚠️ VERIFY-marked in overtime.md §1 — this is the defensible default, not a researched figure. Hours convert to leave days at the same `H` used for tier boundaries, so the arithmetic has one source. | Active |
| A-030 | 2026-08-02 | Proration divisor default: `fixed_divisor` at 21 | `payroll.proration_basis` defaults to **`fixed_divisor`** with `payroll.fixed_daily_divisor = 21` (`docs/06-modules/payroll.md` BR-PAY-013, ⚠️ VERIFY marked there). Rationale: 21 and 25 are the divisors the wage regulations themselves use for a 5-day and 6-day week, so a fixed divisor is the option with a statutory anchor. `working_days` was rejected as the default because it makes two employees on identical salaries earn different daily rates from their rosters, which is defensible but unexplainable on a payslip; `calendar_days` was rejected because it silently changes the daily rate every February. All three remain selectable — the basis is contractual, not statutory. | Active |
| A-031 | 2026-08-02 | Retro window default 24 months | `payroll.retro_window_months` defaults to **24** (`docs/06-modules/payroll.md` BR-PAY-019). Rationale: it must cover at least one full annual tax cycle plus the following December recalculation, so anything under 13 is unusable; 24 gives a year of slack without letting a dirty flag from four years ago land on a current payslip. No regulatory basis is claimed — statutory retention is 10 years (D4), but retention is how long a record is kept, not how far back a correction may still be paid. | Active |
| A-032 | 2026-08-02 | A bounced transfer does not block run close | `docs/06-modules/payroll.md` BR-PAY-017 lets a run reach `closed` with employee rows still `bounced`. Rationale: the obligation, the PPh 21 withholding, and the BPJS contribution are all real from approval onward, and the YTD ledger written at close is what December's recalculation and Form 1721-A1 read — holding the run open for one wrong account number would corrupt the following month's tax position to fix a data-entry error. The unpaid row stays re-issuable after close because payment state and run state are deliberately separate machines. ⚠️ VERIFY-adjacent: whether the tax point follows payment date or payroll period when a bounce crosses a month boundary is marked in payroll.md §3. | Active |
| A-033 | 2026-08-02 | No manager visibility of payroll data, at any scope | `docs/06-modules/payroll.md` §2 grants managers no payroll permission key and no `team` data scope — not for payslips, not for aggregates, not with a headcount floor. Rationale: `HANDBOOK_SPEC.md` §10 item 8 defines no manager payroll surface, so nothing requested is being withheld, and a team-scoped aggregate over a team of one is a per-person salary. If manager cost visibility is wanted, it is a deliberate design in `dashboard-analytics.md` with its own key and its own minimum-headcount rule, not a scope flag inherited from every other module's defaults. `docs/06-modules/tax-pph21.md` §2 carries the same exclusion forward, with a stronger reason: PTKP status discloses marital status and dependants. | Active |
| A-034 | 2026-08-03 | Gross-up tax allowance is classified `non_wage` | The `tunjangan_pajak` component created under the gross-up method carries `wage_category = non_wage` and `income_class = regular` (`docs/06-modules/tax-pph21.md` BR-TAX-010, ⚠️ VERIFY marked there). The rationale is structural rather than regulatory: classifying it `variable_allowance` puts it inside *total wage*, which raises the 75% floor on the overtime hourly basis, which raises overtime pay, which raises the tax, which raises the allowance, which raises the floor again. ADR-0012 requires the pipeline to be a pure function of its snapshot, and that requires an acyclic graph; `non_wage` keeps the allowance downstream of every base that would otherwise feed back into it. If Indonesian wage law says a tax allowance is *upah*, this becomes a real conflict to resolve rather than a classification choice, and the resolution is a code-level ordering change, not a config edit. | Active |
| A-035 | 2026-08-03 | Withholding follows payment date, not payroll period | The tax year, the tax month, and the annual-recalculation trigger are all derived from a run's `payment_date` (`docs/06-modules/tax-pph21.md` BR-TAX-003, BR-TAX-004, ⚠️ VERIFY marked there). Rationale: PPh 21 attaches when income is paid and the monthly filing is made by payment month, so an earned-basis ledger would disagree with the tenant's own twelve filings by one month, permanently, for every tenant paying in arrears. This also settles the marker left open on A-032: the tax point is the run's *declared* payment date, and a bounced transfer re-issued the following month does not move it — a settlement failure is not a re-dating of income. Consequence accepted deliberately: a period paid in January belongs to the new tax year even though most of its work happened in December. | Active |
| A-036 | 2026-08-03 | Retro deltas are taxed in the month that pays them | A retro adjustment entering a live run is income of that run's month and tax year, and a cross-year delta does not amend the prior year's Form 1721-A1 (`docs/06-modules/tax-pph21.md` BR-TAX-012, ⚠️ VERIFY on filing practice). Rationale: the alternative requires re-opening a closed run's tax position, which BR-PAY-018 forbids, and re-issuing a form already filed by the employee. A hybrid rule — same-year deltas re-attributed, cross-year ones not — was rejected because it makes the treatment depend on the date somebody noticed the error rather than on what happened. | Active |
| A-037 | 2026-08-03 | One calculating run per company per payment month | `docs/06-modules/payroll.md` BR-PAY-025 blocks a run from entering `calculating` while another run for the same company and payment month sits in `calculating` or `review` (`PAY_MONTH_RUN_IN_FLIGHT`). Rationale: withholding is computed on the month's cumulative base, so while a second run's numbers are still moving the first's answer is undetermined — and a pure function whose result depends on which job finished first is not one. The operational cost is real and accepted: approve the THR run before calculating the regular one. No regulatory basis is claimed; this is the minimum serialization that makes BR-TAX-007 deterministic. | Active |
| A-038 | 2026-08-03 | Severance entitlement is not calculated in V1 | `docs/06-modules/tax-pph21.md` prices severance correctly on the final tariff, but no module computes the *entitlement* — the UU 13/2003 jo. Cipta Kerja and PP 35/2021 multipliers per termination reason, UPMK by tenure, UPH composition. HR enters the amount as a one-off component. This is adjacent to `HANDBOOK_SPEC.md` §4 item 3's "final settlement on termination", which payroll.md does implement for the prorated period, leave encashment, and outstanding deductions. Rationale: the multiplier table is a feature of its own with roughly twenty termination reasons, and it belongs with payroll's final settlement rather than arriving as a side effect of the tax document. Flagged for review — if statutory severance calculation is expected in V1, it is a payroll.md change, not a tax one. | Active |
| A-039 | 2026-08-03 | JKK risk class is company-scoped | `docs/06-modules/bpjs.md` BR-BPJS-002 carries one JKK risk class per company registration version, not per branch. A tenant running a high-risk plant and a low-risk head office **under one legal entity** therefore contributes at one rate for both. Rationale: BPJS registration attaches to the legal entity, `docs/00-overview/product-overview.md` already declares the company the payroll and statutory boundary, and in practice mixed-risk operations are registered as separate entities — which the multi-company model already carries. Flagged for review: if mixed-risk sites under one entity are common in this market, the class moves to branch scope, which is cheap now and expensive once contribution history exists. | Active |
| A-040 | 2026-08-03 | Employer-borne employee contributions excluded from V1 | *Iuran ditanggung perusahaan* — an employer paying an employee's own BPJS share — is not modelled (`docs/06-modules/bpjs.md` §1, §15). Rationale: the borne amount becomes an employer-paid taxable benefit, which is a second gross-up-shaped mechanism that has to interact with `tax.method` resolution, and it is a separate election per program. Workaround available today: model it as a `non_wage` + `regular` allowance sized to the employee part. The `employer_cost` kind and income-class pairing added this session is most of the machinery it will need. | Active |
| A-041 | 2026-08-03 | No BPJS import definitions in V1 | Neither coverage exclusions nor dependent counts get an ImportDefinition (`docs/06-modules/bpjs.md` §15). Rationale: both tables are sparse by design — a ten-thousand-employee tenant carries perhaps fifty rows in each — and the company registration is a single form entry. A bulk loader is scaffolding for a migration case that has not appeared; a tenant with a large expatriate population is the one that would justify it. | Active |
| A-042 | 2026-08-03 | Absent BPJS configuration warns, it does not fail | A company with no registration version covering the payment date contributes nothing and raises a run-creation warning; the same for an unconfigured branch wage floor (`docs/06-modules/bpjs.md` BR-BPJS-004, BR-BPJS-008). Only an absent *statutory parameter version* fails the run, with `PAY_PARAMETER_MISSING`. Rationale: tenant configuration can legitimately be absent — a company genuinely outside BPJS, a tenant whose every wage clears the local minimum — and blocking payroll over an optional field is the worse failure. The residual risk is real and named: a first run for a newly created company computes zero contributions for everyone, successfully. The preflight entry naming the company is the only thing between that state and a silent one. | Active |
| A-043 | 2026-08-03 | Expense policy limits advise, never block | A claim line over its category's per-line ceiling or period cap is flagged, pinned with the limit it exceeded, and submitted; only a missing required receipt refuses a submission. Blocking would leave an employee whose taxi ran over in a rainstorm no path at all, and an annual medical cap would make a hospital bill submittable only by splitting it across two years. The approval chain is the control, and `overPolicyLineCount` lets a chain route over-budget claims to a stricter approver. Spec §10 item 11 says "limits/policies" without saying they refuse. | Active |
| A-044 | 2026-08-03 | Expense backdate window default | `expense.max_backdate_days` defaults to 90, tenant and company scoped — long enough for a quarterly reconciliation, short enough that a year-old receipt is a conversation rather than a submission. No forward-dating is permitted at any setting: an expense that has not happened is not a reimbursement. Mirrors `overtime.max_backdate_days` (A-027) in shape; the number is a judgement, not a statute. | Active |
| A-045 | 2026-08-03 | Cash advance excluded from V1 | Uang muka / kasbon is out of scope: it is a payment **to** the employee before any expense exists, requiring an outstanding balance per employee that nothing in this product models, settlement matching, and a return path that would make expense emit a `deduction` line into payroll. Spec §10 item 11 names categories, limits, receipts, approval, and the disbursement path — not advances. Sketched in expense-reimbursement.md §15; the most likely first request from an Indonesian admin. | **Active — flagged for review** |
| A-046 | 2026-08-03 | Per-diem and mileage excluded from V1 | A system-computed amount from days × rate or km × rate has no receipt, which makes it an allowance rather than a reimbursement — and allowances are payroll components on the salary package, already built. Adding a rate schedule here would put one concept in two modules. | Active |
| A-047 | 2026-08-03 | No cost-center or project dimension on claims | Branch and department come free from `OrgQueryPort.placements`, and no project or cost-center entity has an owner anywhere in this product. A tagging field with nothing to tag against is a free-text column that reports cannot aggregate. | Active |
| A-048 | 2026-08-03 | Consumables and stock excluded from the asset module | `assets` has one row per physical object and no quantity column (`docs/06-modules/asset.md` BR-AST-001, §15). A stock model needs movements, on-hand balances, and a serialization point on the balance row — the ledger machinery expense was able to delete — and none of it answers the question the module exists for: who holds *this* laptop and what condition was it in when they got it. Consumables are never returned, never handed over, never damaged, and never lost; they fail every noun in spec §10 item 12. A tenant that needs to count pens has an inventory system. | Active |
| A-049 | 2026-08-03 | Asset custody is employee-only | `asset_assignments.employee_id` is NOT NULL — no department, branch, or room may hold an asset (`docs/06-modules/asset.md` BR-AST-016). A handover needs a signatory who can acknowledge receipt, be exited, be notified, and be a respondent when the object goes missing; a department is none of those. A polymorphic holder column would additionally forfeit the referential integrity ADR-0001 §5 explicitly buys. Pool items are either assigned to the person responsible for them or sit unassigned at their branch, which is the honest state. `assets.branch_id` carries location, which is not custody. | Active |
| A-050 | 2026-08-03 | No approval chains and no asset-request flow in the asset module | The first Phase 3 module carrying **employee-facing transactional records** with no approval-engine registry entry — holiday, organization, shift, tax-pph21, and bpjs also register none, but all five are configuration or computation surfaces (`docs/06-modules/asset.md` BR-AST-013, §13). Assignment records a physical fact that has already happened — a chain would make the record lag the object — and retirement and incident resolution are controlled by their own permission keys plus the channel-1 audit diff. An employee requesting a laptop is a *procurement* request whose fulfilment is a purchase this product does not model, and whose subject may not exist in the registry yet. Spec §10 item 12 names registry, assignment/return, condition, handover documents, and the loss/damage flow — no approval among them. | Active |
| A-051 | 2026-08-03 | No depreciation or accounting fields on assets | `purchase_cost`, `purchase_date`, and `warranty_until` are flat reference fields, never book value (`docs/06-modules/asset.md` §1, §15). Depreciation needs a method, a useful life, a book-value series, and journal entries, and no module in this product owns a general ledger to post them to. `warranty_until` drives no reminder either — nobody in HR acts on a warranty date; it is looked up when something breaks, which is why `asset_document` correctly carries no expiry reminders in the document-storage registry. | Active |
| A-052 | 2026-08-03 | Asset damage and loss charges never reach payroll automatically | An incident records `repair_cost`, `charged_amount`, `charged_to_employee_id`, and `recovery_status`; recovering the money is a payroll deduction component entered by hand (`docs/06-modules/asset.md` BR-AST-009, §15). No port, no event, no run line — deliberately **not** the `ExpenseQueryPort` mirror. Three reasons: A-050 removed every approval chain here, so a pushed deduction would cut take-home pay on one administrator's unilateral act with no second signature; statutory wage-deduction ceilings apply across all sources at once (⚠️ VERIFY, asset.md §1) and this module can see exactly one of them; and a ten-thousand-person tenant has perhaps a dozen chargeable incidents a year, which does not earn the pin-and-release apparatus. **Named cost:** `recovery_status = 'recovered'` is self-reported and nothing verifies a deduction ever ran (asset.md §9). The unblocking prerequisite is payroll owning a cross-source deduction ceiling, not the port. | **Active — flagged for review** |
| A-053 | 2026-08-03 | An employee's exit flags outstanding assets, it never blocks or closes them | Terminal `employee.status.changed` sends one `asset.clearance_pending` notification and nothing else (`docs/06-modules/asset.md` BR-AST-012). Blocking is not merely undesirable but unimplementable: employee.md BR-EMP-006's side effects run inside the effectuation transaction, asset is not in it, and ADR-0010 events are asynchronous — there is no moment at which asset could answer first. Auto-closing is worse, because marking an unreturned laptop `returned` destroys the only record that it is missing. **Named cost:** there is no second reminder and no cron (asset.md §12), so a missed notification leaves a silent clearance list; the mitigation available today is the saved filter, and a weekly digest is in §15. Whether final settlement may lawfully be withheld pending asset return carries a ⚠️ VERIFY marker in asset.md §9. | Active |
| A-054 | 2026-08-03 | No careers portal or candidate self-service in V1 | Recruitment has **no public surface at all**: no job listing page, no application form, no candidate account, no status page, and no outbound email to any candidate (`docs/06-modules/recruitment-candidate.md` §1, §15). Spec §10 item 13 says "publishing metadata", which this module delivers as `requisition_publications` — a record of where a vacancy was advertised. Rationale: every endpoint in this platform resolves a tenant from an authenticated identity (ADR-0004) and RLS binds on `TenantContext` (ADR-0002); an unauthenticated write into a tenant table has neither, and a portal additionally needs write rate limiting, AV scanning of files from strangers, and an account system that is not the `users` table. **Named cost:** a candidate has no way to request erasure of their own data — an HR Admin must anonymize on their behalf (recruitment §9). That is the one compliance consequence of the exclusion and it is stated rather than discovered. | **Active — flagged for review** |
| A-055 | 2026-08-03 | No internal-applicant model in recruitment | `candidates` carries no `employee_id` and there is no internal-mobility path (`docs/06-modules/recruitment-candidate.md` BR-REC-016). An internal move is an organization transfer or promotion, which already exists and is already effective-dated. If an existing employee is run through the pipeline anyway, conversion fails on BR-EMP-001's NIK duplicate check with `VAL_DUPLICATE` — **the disaster of two employee rows for one human is prevented one module over, with no column, no code, and no rule added here.** The alternative, an explicit link plus a dedicated refusal, buys a nicer error message for a cross-module FK, a new error code, and a nullable field a recruiter must remember to set. | Active |
| A-056 | 2026-08-03 | No generated offer letter | Offer terms are structured columns; the signed copy comes back as a `candidate_file` scan (`docs/06-modules/recruitment-candidate.md` BR-REC-013, §15). ADR-0014 declares its PDF consumers explicitly — payslips, 1721-A1, report exports, training certificates, asset handover documents — and an offer letter is not among them, so adding one is an ADR amendment. More decisively, an offer letter is a legal instrument whose wording belongs to company counsel and varies by role and contract type, and this platform has **no tenant-editable document template surface anywhere** (notification templates are code-owned by BR-NTF-001). Generating one means either a single hardcoded Indonesian letter every tenant's lawyer must accept unread, or building the template editor the handbook has avoided in every other module. | Active |
| A-057 | 2026-08-03 | Recruitment exports, it does not import | Two ExportDefinitions and no ImportDefinition (`docs/06-modules/recruitment-candidate.md` §13). ADR-0015 names "recruitment/candidate lists" among framework consumers; that phrase is read here as list **exports**, and the reading is logged so a future reader can reverse it in one place rather than rediscover the question. A candidate-list import mints people nobody screened with no CV attached, and BR-REC-005's hard email uniqueness would reject every previously-seen person mid-file for a reason the uploader cannot fix. Returns in §15 with a `requisition_code` column creating candidate and application together, on the `employee.master` three-entity precedent. | Active |
| A-058 | 2026-08-03 | No compensation bands exist for an offer to be validated against | `job_levels` carry a `rank` and no money, and no salary range exists on `positions` or anywhere else in this product (`docs/06-modules/recruitment-candidate.md` §13, §15). So offer approval routes on the **raw figure** plus `jobLevelRank` and `revisionNumber`, and no validator can flag an out-of-band offer the way expense flags an over-policy line. Adding bands is an organization-module change with its own permission and history requirements, not a recruitment field. | Active |
| A-059 | 2026-08-03 | An offered salary never reaches payroll | `job_offers.offered_base_salary` exists to be approved and remembered; nothing pushes it into payroll at conversion (`docs/06-modules/recruitment-candidate.md` BR-REC-013). No components, no port, no event. Rationale: payroll owns the component vocabulary and a second owner would drift from it; a recruiter's offer must not be the act that sets someone's pay; and `UC-EMP-001` writes no salary either, so nothing regresses — payroll setup after a hire is the deliberate separate act it already was. The shape mirrors A-052 from the other direction: asset records money it never moves *out*, recruitment records money it never moves *in*. **Named cost:** the offered figure and the actual payroll salary can diverge with nothing reconciling them, which is a report in recruitment §15 and not a constraint. | Active |
| A-060 | 2026-08-03 | No cross-employee goal cascade and no org-level objectives | `performance_goals.parent_goal_id` is constrained to the **same participant**: nesting expresses one employee's objective over his own key results and nothing else (`docs/06-modules/performance-goals.md` BR-PRF-005, §15). Rationale: company or department objectives make `participant_id` nullable on every goal, so every "my goals" query grows a filter, the participant state machine stops applying to half the rows, and permissions need a second path answering "who edits a company goal" — a role this handbook has never defined. A cheaper `aligned_to_goal_id` between employees is load-bearing in a different way: the moment a manager's goal has children owned by his reports, his score is asked to roll up *from them*, which is a second scoring model beside BR-PRF-011's. **Named cost:** a tenant running true cascaded OKR cannot express the link and puts it in the goal title. This is the likeliest V2 ask in the module and the one that needs its roll-up question answered before any schema. | **Active — flagged for review** |
| A-061 | 2026-08-03 | No competency or behavioral rating section | The appraisal rates goals and nothing else (`docs/06-modules/performance-goals.md` §1). Spec §10 item 14 lists review cycles, the goal model, self/manager review, calibration, rating config, and development outcomes — no competencies. Adding them means a proficiency framework per job level, a second weighted score beside the goal score, a rule for combining the two, and a second thing calibration has to argue about. | Active |
| A-062 | 2026-08-03 | Self and manager review only — no 360 or peer review | `performance_reviews` is keyed `(participant_id, kind)` with exactly two kinds (`docs/06-modules/performance-goals.md` BR-PRF-009). Deliberately unlike `interview_scorecards`, which keys on the interviewer because a panel is genuinely N people: two fixed roles with one seat each must not permit two people filing a manager review. Adding peer review is a new `kind`, a reviewer-keyed index, and an anonymity model §2's visibility rules have no shape for today. | Active |
| A-063 | 2026-08-03 | A rating never reaches pay | No merit matrix, no recommended increase, no bonus factor, no port, no event (`docs/06-modules/performance-goals.md` BR-PRF-019). A merit matrix needs salary bands and a compa-ratio, and A-058 declined bands three days earlier precisely so nothing would grow to depend on them. A bonus factor would need payroll to accept a per-employee number pushed from a module, which ADR-0012 amendment 1 fixed the wording of specifically to prevent — everything statutory is pulled at snapshot. Third module in a row drawing this line from a different side: asset records a cost it never charges (A-052), recruitment a salary it never pays (A-059), performance a rating it never converts. **Named cost:** HR exports `performance.rating` and types the increases into payroll by hand. | Active |
| A-064 | 2026-08-03 | No generated appraisal document | Nothing in this module renders a PDF and nothing stores a file (`docs/06-modules/performance-goals.md` §1, §13). ADR-0014 enumerates its consumers — payslips, 1721-A1, report exports, training certificates, asset handover documents — and an appraisal form is not among them, so adding one is an ADR amendment. It is also the weaker artifact: `acknowledged_at` against an immutable, audited rating is better evidence than a scan, and it is captured natively here, which is the one thing a signed form was going to be for. Same check that produced A-056 one module earlier. | Active |
| A-065 | 2026-08-03 | Goals import; ratings never will | One ImportDefinition and two ExportDefinitions (`docs/06-modules/performance-goals.md` §13). The import earns its place on volume: five goals across five hundred employees is 2,500 rows an HR admin builds in a spreadsheet whether or not the framework accepts one, so the only question is whether it arrives validated — and it commits **per employee**, because BR-PRF-006's balance is a property of a set and row-level partial commit would leave half a goal set live and unbalanced. A rating import is refused on kind rather than volume: a rating is the output of a process this module owns end to end, and loading one mints an outcome with no goals, no review, and no reviewer behind it. | Active |
| A-066 | 2026-08-03 | Appraisal records are retained indefinitely — no purge job | Performance owns no retention setting and no purge cron, the deliberate opposite of A-054's candidate treatment (`docs/06-modules/performance-goals.md` §12). A candidate is a stranger the employer holds data about under a time limit; an appraisal is a personnel record about someone the employer employs, and destroying it removes the evidence a termination dispute is argued from. **Carries the §1 ⚠️ VERIFY**: whether UU PDP 27/2022's employment-record treatment permits this, or imposes a limit that would require the job. If it comes back against, the fix is one row in §12's table plus a settings key — no schema change, because the erasure would be a delete and not recruitment's in-place anonymization. | Active |
| A-067 | 2026-08-03 | No mandatory / compliance training model | No `is_mandatory` flag, no per-employee due date, no completion matrix, no chase list (`docs/06-modules/training.md` §1, §15). It needs two things this module refuses to build alone: an **audience model**, which is `announcement.md`'s job one file later in the manifest, and a **recurrence model** for refresher intervals, which is a second scheduler. The stronger reason is that the compliance question tenants actually ask is *"who currently holds a valid K3 certificate, and whose lapses inside 60 days"* — a query over `training_certifications.expires_on`, made active by `cron.training.reminders` — and "who has not yet taken course X" is the weaker cousin of it. The honest V2 shape is a compliance surface reading this register plus announcement's audiences, not a boolean that implies tracking nothing performs. | Active |
| A-068 | 2026-08-03 | No waitlist | A full session refuses with `TRN_SESSION_FULL` and HR opens another (`docs/06-modules/training.md` BR-TRN-004, §15). Automatic promotion needs an ordering, a trigger on every cancellation path, a notification, a rule for the promoted person who is no longer available, and a race against a manual assignment landing in the same second. What it would buy already exists for free: under BR-TRN-004 pending requests are unbounded, so **the pending set is the waitlist** — and the approver seating the free chair uses judgment FIFO does not have, sending the person whose development item names the course ahead of the person who was curious. Cost: a drop-out two days before pages nobody; the seat sits free until HR looks at the roster that already shows it. | Active |
| A-069 | 2026-08-03 | No training provider or vendor registry | `provider_name` and `trainer_name` are free text, with a nullable `trainer_employee_id` for internal trainers (`docs/06-modules/training.md` BR-TRN-001, §4.1). Asset drew this line first — *"maintenance vendors, repair invoices, and warranty claims — `repair_cost` is a number, not an invoice"* — and recruitment drew it again with `channel: text` for advertising channels. **The cost is real and is written into the module**: "how much did we spend with PT Trainindo this year" fragments across `PT Trainindo`, `PT. Trainindo`, and `Trainindo`, and the enrollment export inherits it. Accepted because a registry only fixes that if someone curates it, and an uncurated registry produces the same three rows with FKs pointing at them; the spend question is answered well by course and category, which are entities. | Active |
| A-070 | 2026-08-03 | Training writes nothing to attendance — off-site days need manual corrections | ⚠️ **Flagged for user review.** No port, no event, no new `attendance_days` status value, and no derivation branch (`docs/06-modules/training.md` BR-TRN-009, §9). In-house training is an ordinary working day and the employee punches normally, so the common case needs nothing. The uncommon case is expensive: **a 20-person 3-day off-site training derives `absent` 60 times and costs 60 attendance corrections.** Rejected alternatives — a `training` status value would make this module the second external writer into a lock-frozen derived record (leave, the first, needed pinned `covered_dates`, a ledger, and a port), and it would assert attendance facts from a *registration*, since an enrollment knows a session has dates and not whether the person travelled; a seeded `training` leave type would either spend balance or need another `unlimited` quota, and would route one absence through two approval chains. The clean V2 is a `business_trip` day type **owned by attendance** that several modules publish into — an attendance amendment, deliberately not smuggled in from here. | Active |
| A-071 | 2026-08-03 | One attendance verdict per enrollment — no `partial`, no per-day roster | `attended` or `no_show`, marked once, with no `training_session_days` table (`docs/06-modules/training.md` BR-TRN-007). A 5-day course someone attends 3 days of is one judgment call by the marker. Per-day rows are a table, a grid, and a lifecycle bought to answer "which day did Budi skip", and none of the module's outputs asks: the certificate is a completion question, the cost is per head and unaffected, and training hours come from the course's `duration_hours`. `partial` is refused separately and for its own reason — the only consumer that would want it is a compliance hours calculation this module does not perform, and a third value with no reader gets filled in inconsistently by markers. Per-day attendance and `partial` return together in §15, because that is the only combination in which either has a reader. | Active |
| A-072 | 2026-08-03 | Training cost is recorded and never moved | One `cost_amount` column on the enrollment, no port into payroll, no component, no deduction, no event carrying an amount (`docs/06-modules/training.md` BR-TRN-011). **Fourth consecutive module drawing this line from a different side** — asset records an acquisition cost it never charges (A-052), recruitment records an offered salary it never pays (A-059), performance records a rating it never converts (A-063). Employee-paid course fees already have a path and it is not this one: `docs/06-modules/expense-reimbursement.md` seeds a `training` category routed to payroll. What this module records is the company's own spend, which is a budget fact and not a payroll input. Paired with §1's first ⚠️ VERIFY on whether employer-paid training is excluded natura or taxable income — if taxable, the fix is a payroll component and a correction to that expense category's default, not a port from here. | Active |
| A-073 | 2026-08-03 | The generated completion certificate has one hardcoded design and no logo | Rendered through `PdfService` per ADR-0014, carrying `companies.legal_name`, the employee, the course, the hours, the dates, and a serial (`docs/06-modules/training.md` BR-TRN-014). **There is no tenant logo column anywhere in this schema** — not in `core-schema.md`, not in `organization.md` — and the platform has no tenant-editable template surface at all, since even notification templates are code-owned by BR-NTF-001. This is the same missing platform capability A-056 named from the offer-letter side, and the reason the two decisions differ is that a completion certificate is **not a legal instrument**: it asserts facts the system already holds, with no wording company counsel must approve, so a clean typographic document correctly attributed to the company is a real artifact rather than a liability. When a tenant logo lands — a system-administration or settings concern — the template picks it up with no schema change here. | Active |
| A-074 | 2026-08-03 | The training catalog is visible to everyone; no audience targeting | Every employee sees every live course and `scheduled` session in their company; `training_sessions.self_enrollment_enabled` removes the Request button and `draft` sessions are invisible (`docs/06-modules/training.md` BR-TRN-018). The decisive argument is timing rather than principle: **targeting is `announcement.md`'s scope and announcement is the very next file in the manifest** (row 58 — "targeting, scheduling, acknowledgment via inbox, push fan-out"), so building an audience model here ships two targeting implementations one file apart, shaped differently, with the second one being the good one. Cost: an employee browsing sees the Senior Leadership Program they will not be joining. If targeting is genuinely needed later, training consumes announcement's rather than growing its own. | Active |
| A-075 | 2026-08-03 | No assessment, scoring, or pass/fail | An enrollment records whether someone attended, never how they did (`docs/06-modules/training.md` §1, §15). The completion certificate is the pass signal, and gating it on attendance is the only gate this module has. Scoring needs a question model or an external score import, a passing threshold per course, a re-sit path, and a decision about what a failed attendance means for the cost already spent — four things, none of which the modules consuming training data (reports, dashboard-analytics) have asked for. Returns in §15 alongside per-day attendance, where a richer completion record has an actual reader. | Active |
| A-076 | 2026-08-03 | Announcements have no categories or types | No taxonomy table, no enum, no filter chip (`docs/06-modules/announcement.md` §1, §17). Every other module's category table **does work** — an expense category picks the approval route and the tax class, an asset category classifies the register, a training category groups spend — and an announcement's would drive no routing, no permission, no calculation, and no report anyone has asked for. The one member with a real job would be `urgent`, and `requires_acknowledgment` is already that axis, wired to a mandatory template and an inbox task rather than to a coloured label. `pinned` is the only prominence mechanism. If a filter is genuinely wanted later it is a column and not a table, and by then someone will be able to name the values, which nobody can today. | Active |
| A-077 | 2026-08-03 | Content is frozen at publish; no edit, no un-retract | `title`, `body`, targets, attachments, and the acknowledgment fields are immutable from `published` onward → `ANN_CONTENT_LOCKED`; only `pinned` and `expires_at` stay mutable, and `retracted` is terminal (`docs/06-modules/announcement.md` BR-ANN-005, BR-ANN-004). The acknowledgment case forces it — "I confirm I have read the leave policy" is worthless if the policy can be rewritten after the confirmations land, and that is a compliance artifact HR will eventually be asked to produce. The same reasoning holds for posts nobody acknowledged: an announcement is the **record of what the company told people**, and silently rewriting it after five hundred readers puts the record and the readers in permanent disagreement with an audit diff as the only trace. Cost, stated rather than softened: a typo in an all-hands post is lived with, or paid for with a retraction, a repost, and reset acknowledgments. The V2 that makes editing safe is not a UI change — it is the acknowledgment recording *which revision* was acknowledged (§15). | Active |
| A-078 | 2026-08-03 | A published audience is never recomputed; late joiners are never added | The recipient set is resolved once at publish and nothing recomputes it — no hire, transfer, promotion, or department move adds or removes a row (`docs/06-modules/announcement.md` BR-ANN-001, BR-ANN-015). The alternative, evaluating the targeting rules live on every read, has **no denominator that holds still**: "42 of 120 acknowledged" changes because someone transferred overnight, and last quarter's compliance number can never be reproduced. Performance-goals materialized participation at launch against the identical alternative. Cost: an employee hired the day after an all-hands post receives nothing, and HR reposts — a human act with a human's judgment about whether it is worth it. The honest V2 is an explicit "re-resolve and add newcomers" action that appends rows and records that it happened, which is the only shape in which a moving denominator is defensible. | Active |
| A-079 | 2026-08-03 | No read or view tracking, only acknowledgment | `announcement_recipients` carries one nullable `acknowledged_at` and nothing else; there is no `first_read_at`, no view count, no seen state owned here (`docs/06-modules/announcement.md` BR-ANN-006 line of §1). A read stamp costs a write on every detail open — ten thousand writes for one all-hands post — to buy a signal strictly weaker than the acknowledgment sitting beside it. If a tenant needs to know who saw a post, the flag exists and the answer it gives is stronger. The recipient row itself is still written for every announcement including those needing no acknowledgment, because splitting that would give the module two visibility paths and let a transferred employee see a different history depending on which kind each post happened to be. | Active |
| A-080 | 2026-08-03 | `employment_type` is not a targeting dimension | The four dimensions are branch, department, position, and job level — all organization's (`docs/06-modules/announcement.md` BR-ANN-003). "Everyone on PKWT" is a real case and it is declined because `employment_type` is an `employees` column, so admitting it drags a second module's port into a resolver that otherwise touches exactly one, for an audience nobody has yet asked to address as a group. Employee **status** is not a dimension either, but for the opposite reason: `active`/`on_leave` plus holding a user account is a hard filter under every rule, not a choice, because a terminated employee has no inbox to reach and a pre-onboarding hire has no identity to target. Adding the dimension later is one optional field on one port call. | Active |
| A-081 | 2026-08-03 | Retracting an announcement notifies nobody | No `announcement.retracted` template; the post disappears, inbox items close silently, and nothing is sent (`docs/06-modules/announcement.md` BR-ANN-010). Withdrawing a notice by sending a notice **re-delivers the thing being withdrawn** — the push preview would carry the same title — and it cannot carry the correction, because a retraction has no replacement text to say. The tenant's tool for "ignore Friday's message" is Saturday's message, which is the same position `docs/06-modules/organization.md` §13 takes on employee-facing communication being a deliberate act. The case this loses is a retracted "office closed Friday" that some people already acted on; the answer is the corrective post, which a human writes and this module cannot. | Active |
| A-082 | 2026-08-03 | No reminder cron for unacknowledged announcements | `acknowledge_by` sorts and styles the inbox item and filters the admin register; nothing sends a second notice (`docs/06-modules/announcement.md` BR-ANN-009, §13). Training and performance both shipped reminder crons, and neither had what this module has: `docs/05-platform/inbox.md` BR-INB-010 **never purges an `open` item**, so an unacknowledged post already nags permanently from the badge. A reminder push is a second nag for a fact the first has not stopped delivering, reaching only people who have already opened the app and skipped it. What it gives up: someone who never opens the app is never pinged again — and HR's real escalation for that person is their manager, which is what the outstanding-list export supplies. | Active |
| A-083 | 2026-08-03 | Announcement imports nothing and emits no domain events | No ImportDefinition and no outbox events (`docs/06-modules/announcement.md` §12, §13). **Import**: nobody holds a spreadsheet of announcements, and the one thing worth loading — an archive from a previous system — is a one-off migration script, so a permanent definition would mean a template version, a dry-run, a row-error report, and a rowHandler maintained forever for something that runs once. **Events**: nothing in V1 consumes one, channel-1 audit already captures every diff on both audited tables, and an event published for no subscriber is scaffolding — the position asset, expense, recruitment, performance, and training all reached, and adding `announcement.published` when dashboard-analytics wants it is additive. | Active |
| A-084 | 2026-08-03 | Reports produce xlsx only; no PDF in V1 | Every one of the ninety-three definitions outputs a spreadsheet through import-export's existing writer (`docs/06-modules/reports.md` §1, BR-RPT-013). **This leaves a line in an Accepted ADR unclaimed and says so**: ADR-0014 enumerates "report exports" among its five PDF consumers. The check that produced A-056 and A-064 negatively and training's certificate positively lands negatively here. All ninety-three promised reports are tables of numbers, and a table of numbers in a PDF is a table nobody can sort, filter, or sum — import-export.md's own premise is that Indonesian HR operations live in Excel. The two artifacts ADR-0014's retention argument was actually about, the payslip and the 1721-A1, already render in payroll and tax. Building it anyway would mean ninety-three React SSR templates for an output nobody asked for. Returns in §15 for the handful that get physically filed, behind the same `PdfService` and the same `reports` queue that already carries training's certificates. | Active |
| A-085 | 2026-08-03 | No caching and no stored report results | Every run is a live query, every result carries `generatedAt`, and nothing is persisted (`docs/06-modules/reports.md` BR-RPT-011, §4.1). The manifest already splits this: "aggregate caching" is on row 60, dashboard-analytics, not row 59. The split is right, because the two surfaces fail differently — a **dashboard** is glanced at, where a five-minute-stale headcount is fine and a slow load is the real failure, while a **report** is pulled to answer a question or file a figure, where "as of a moment we did not tell you" is the failure. Caching a report is how a payroll register and the payslips come to disagree. Materialized views were the worse alternative: ninety-three physical objects on a refresh schedule nobody tunes, in a module that deliberately owns zero tables. Cost, stated plainly: an expensive report is expensive every time. The controls are required params, the row and duration bounds, and the `reports` queue lane — and a specific report measured intolerable earns a cache individually, which is a smaller decision than pre-building a layer for ninety-three queries whose costs nobody has measured. | Active |
| A-086 | 2026-08-03 | No saved report views and no scheduled reports | Parameters are entered per run; nothing is named, stored, favourited, or fired on a cron (`docs/06-modules/reports.md` §1, §15). Saved views are the genuinely tempting one — HR does run the same five reports every month — and a params panel with sensible defaults covers most of the value, while a saved view costs a table (in the one module that owns none), a CRUD surface, permissions, and a migration whenever a definition's params change underneath it. Scheduled recurring runs are already reserved by `docs/05-platform/import-export.md` §15 as cron fan-out over definitions, and they belong there once, serving both registries, rather than here for one. Building either would also break the property that makes a ninety-three-row registry affordable: adding a report is a code change and nothing else. | Active |
| A-087 | 2026-08-03 | No report surface on mobile | Admin web only; no Drift mirror, no ADR-0003 sync class, nothing queue-reachable from a device (`docs/06-modules/reports.md` BR-RPT-015, §10). The decision needed making explicitly rather than defaulting, because the module's own `team` scope makes a manager-facing mobile report technically available — and it is still wrong. A report is fifteen columns of numbers, and fifteen columns on a 390pt screen is horizontal scroll, which `docs/03-standards/design-system.md` rejects outright. The underlying need is real and is answered one file over: a manager on a phone wants "who is out today" and "does my team have unapproved overtime", which are dashboard widgets and inbox items. Answering them with a report surface would build the wrong shape for the right requirement, one file before dashboard-analytics builds the right one. Same reasoning as A-074, pointed at a platform rather than a feature. | Active |
| A-088 | 2026-08-04 | No dashboard layout persistence — no reorder, pin, hide, or per-user default | Layouts are ordered widget lists declared in code (`docs/06-modules/dashboard-analytics.md` BR-DSH-004, §4.1). Persisting them costs a table, a CRUD surface, permission keys, an RLS policy, a migration path every time a widget key changes under a saved layout, and an orphan-row problem the first time a widget is retired — to reorder six cards. Settings could not have carried it either: the scope chain is platform → tenant → company → branch, with **no user level**. **Cost:** an executive who wants payroll cost first sees headcount first. Upgrade is additive — a code-declared order is exactly the default a stored order would override. | Active |
| A-089 | 2026-08-04 | Charts: shadcn/ui `chart` (Recharts) on admin web; **no chart library on Flutter** | The admin choice is inside the mandated stack, driven by design-system CSS variables so both themes come from the tokens rather than a second palette in a chart config (`docs/06-modules/dashboard-analytics.md` §6). Mobile renders the `manager` layout, which is eight scalars and one top-N and therefore chart-free; adding `fl_chart` would buy a sparkline nobody acts on and cost a dependency, a theming surface, an accessibility surface, and golden tests in both themes. **Cost:** a mobile trend card is not expressible until a renderer is added. | Active |
| A-090 | 2026-08-04 | Dashboard cache invalidation is TTL-only — no event subscription, no cron precompute | `ttlSeconds` ∈ {60, 300, 3600} per widget, single-flighted (`docs/06-modules/dashboard-analytics.md` BR-DSH-007, §12). Six event lines across attendance.md, leave.md, and overtime.md had named this module as a prospective consumer; **all six were amended on 2026-08-04 to record the refusal**, as were three `Ports served` consumer lists. `attendance.punch.synced` alone fires 20–40k times a morning at 10 000 employees, each spawning a handler job whose only effect is deleting a key that expires in 60 s. **Cost:** a card can be up to its TTL stale — disclosed on the card itself (BR-DSH-005). Per-widget subscription remains additive. | Active |
| A-091 | 2026-08-04 | Turnover counting convention fixed by us, and rendered wherever the number appears | `employee.turnover` (reports.md §4.3, registered 2026-08-04): leavers = employees entering a terminal status in the month, **all causes including PKWT expiry**; average headcount = (start + end) ÷ 2; rate = leavers ÷ average headcount. Not a regulatory value — a business convention with several defensible forms, which is exactly why it is pinned and shown as a caveat on the card and the report header rather than left to the reader. **Cost:** a tenant benchmarking against a differently-computed external figure sees a gap that is definitional, not real. | Active |
| A-092 | 2026-08-04 | No ESS dashboard layout | Mobile renders the `manager` layout only (`docs/06-modules/dashboard-analytics.md` BR-DSH-013, §4.4). reports.md holds **zero `self`-scoped definitions** — deliberately, per `announcement.per_employee` — so an ESS layout would first require self-scoped aggregates duplicating module screens that already answer the same questions better and offline (`GET /me/leave/balance`). A-087 punted **manager** needs here, and both are discharged as cards. **Cost:** an employee's home screen gains nothing from this module. | Active |
| A-093 | 2026-08-04 | No platform-user administration surface, and therefore no self-service TOTP recovery | Creating, disabling, or recovering a `platform_users` row is a deploy-time operations act, not a console form (`docs/06-modules/system-administration.md` BR-ADM-022, ADR-0017 decision 7). Three things follow and the third is the cost: every remaining console action has a tenant target, so audit-log §9's cross-tenant-platform-operations rule covers all of them with **no change to `audit_logs`** — the alternative would have meant a nullable `tenant_id` on the one table the compliance story rests on; a compromised platform session cannot mint itself a persistent second identity; and a platform user who loses their TOTP device has **no recovery path in the product** and needs an operations ticket. Correct at the operator count D13 implies (manual provisioning, no self-signup); wrong at fifty, which is when backup codes arrive. | Active |
| A-094 | 2026-08-04 | Every platform user holds every `sysadmin.*` key — no platform role tiers in V1 | ADR-0005 promises Super Admin holds `sysadmin.*` keys but the grant machinery is unusable: `roles`, `role_permissions`, and `user_roles` are all tenant-class. The nine keys are declared in a **code constant, deliberately not in the `permissions` table** — a `sysadmin.*` row there would be assignable to a tenant role by a Company Administrator (`docs/06-modules/system-administration.md` §2). `PlatformPermissionGuard` checks the route annotation; V1's grant rule is one line. Cost: no read-only `support` tier, so anyone who can view platform health can also provision and impersonate. Because the annotations are on the routes from day one, adding a tier later is a grant table rather than a re-plumb. | Active |
| A-095 | 2026-08-04 | No data-destruction endpoint anywhere in the product | security-standards §1 defines tenant offboarding as export + crypto-shred + storage purge and names no surface. That surface is deliberately **not** HTTP (`docs/06-modules/system-administration.md` BR-ADM-009): `archived` is a commercial state, statutory retention and its purge jobs continue per multi-tenancy §2, and destruction is a Phase 4 runbook executed against the database and KMS. Rationale: the only irreversible act in the system would otherwise sit in the same console that already grants impersonation into every tenant, one phished credential away. Cost: offboarding a departed customer's data needs an engineer and a scheduled window, and the runbook is unwritten until `docs/07-operations/` reaches it. | Active |
| A-096 | 2026-08-04 | The feature-flag registry ships with zero definitions | All eighteen finished business modules were searched: **not one registers a flag or says "behind a flag" anywhere.** The concept appears only as five promises — spec §10 item 19, product-overview twice, CONTEXT.md §1, settings.md §1, admin-nextjs's route tree. The mechanism ships (one platform table, one port, three endpoints) with an empty code registry and a live registration protocol (`docs/06-modules/system-administration.md` §4.5). Inventing plausible flags was rejected as fabricated domain content: a flag whose only consumer is the flag list invites somebody to toggle it, observe nothing, and stop trusting the page. Precedent for a live registry with no entries: `AUD_`, `DSH_`, and now `TEN_`, all owned with zero codes. | Active |
| A-097 | 2026-08-04 | No action deny-list during impersonation — a support operator can finalize a payroll run | BR-AUTHZ-013's ceiling (the impersonated user's own permission set) is the entire boundary; nothing sits below it (`docs/06-modules/system-administration.md` BR-ADM-017, ADR-0017 decision 6). A deny-list was rejected because every module would have to declare an impersonation-forbidden set complete only until the next module ships, and its real failure mode is the destructive action nobody listed. **The residual is real and stated rather than hidden:** an operator impersonating a Payroll Admin can finalize a run, and an operator impersonating an HR Admin can delete org structure. The controls are detective by design — dual-identity audit rows, a registered sensitive read on every request, a mandatory reason, a 30-minute ceiling, a non-dismissible banner, and a notification to the tenant's System Administrators. | Active |
| A-098 | 2026-08-04 | The tenant cannot terminate an impersonation session | A tenant's System Administrator is notified when a platform operator enters (BR-ADM-019) but has no button to eject them (`docs/06-modules/system-administration.md` §15). Building one means a new tenant permission key and endpoint inside authentication.md, which is `done`, for a control the 30-minute non-renewable ceiling and the start notification already largely provide. Cost: between the notice and the horizon, the tenant's only recourse is to contact the platform team. Reconsider alongside enterprise contract terms, where a customer-operated kill switch is a common ask. | Active |
| A-099 | 2026-08-04 | No mutation testing in V1 | `docs/07-operations/testing-strategy.md` §14.2. Stryker over a repo of this size is hours per run, and the defect it targets — assertion-free tests inflating a coverage number — is attacked more directly and far more cheaply by three gates this document already installs: the 446-rule BR traceability gate, the 172-code reachability gate, and golden vectors whose expected values a human reviews against the regulation. Adopt if coverage numbers ever start diverging from defect rates. | Active |
| A-100 | 2026-08-04 | No visual-regression service on the admin web | `docs/07-operations/testing-strategy.md` §14.2. No Chromatic, no Percy. `design-system.md` §13 assigns contrast to a token-file script and the remaining visual rules to a review checklist; a screenshot service adds a subscription, a baseline-approval workflow, and a new flake source for a surface whose visual language is already constrained to tokens and a fixed component kit. Flutter keeps its golden tests in both themes, which are in-repo and free. **Cost:** an unintended CSS regression on the admin web is caught by review or not at all. | Active |
| A-101 | 2026-08-04 | No device farm for the Flutter app in V1 | `docs/07-operations/testing-strategy.md` §14.2. No Firebase Test Lab. M1–M4 run on an emulator in CI; one physical device is checked by hand per release. The honest trigger to revisit is a device-specific defect actually shipping, since nothing today predicts which device or which OEM behavior would matter — and an emulator already exercises the parts most likely to break (offline queue, Drift, SQLCipher). **Cost:** OEM battery-optimization and background-execution differences, which are the classic sources of sync bugs, are untested. | Active |
| A-102 | 2026-08-04 | No manual QA gate, UAT phase, or release sign-off role | `docs/07-operations/testing-strategy.md` §1 and §14.2. The organization model in the handbook has no QA role and D13 describes a small operator population; defining a sign-off gate here would invent a person and then block releases on their absence. Quality gating is entirely automated (§13, G1–G20) plus code review. **Cost:** exploratory testing — the kind that finds what nobody thought to specify — happens only incidentally. Revisit when headcount makes the role real. | Active |
| A-103 | 2026-08-04 | Penetration testing is undefined (**narrowed 2026-08-04**) | Originally: dependency scanning, SAST, secret scanning, and penetration testing were all undefined — `security-standards.md` §13 pointed at `ci-cd.md` for scans without any definition existing. **The scanning half was closed the same week** by `docs/07-operations/ci-cd.md` §9: C1 advisories, C2 SAST, C3 gitleaks blocking merge; C4 Trivy blocking the staging deploy; C5 re-scanning deployed digests weekly; `security-exceptions.yml` mirroring `test-waivers.yml` with expiry. **What remains open is penetration testing only** — a commercial engagement, not a pipeline step, with none scheduled for V1. Still flagged for the file-72 audit so it is accepted explicitly rather than by silence. | Active |
| A-104 | 2026-08-04 | CI platform and runners | Spec §5.16 fixes **GitHub Actions**; the runner choice was open. `docs/07-operations/ci-cd.md` §3 fixes **GitHub-hosted runners** — `ubuntu-latest` everywhere, `macos-latest` only for iOS builds on `main` and release, since macOS bills at a 10× multiplier. **Self-hosted runners rejected**: a machine to patch, and a persistent runner leaks state and secrets between jobs. Revisit on billing pressure or a need for private network access. | Active |
| A-105 | 2026-08-04 | Deploy mechanism | `docs/07-operations/ci-cd.md` §8.1: **Helm `upgrade --install --wait --atomic` pushed from GitHub Actions** over Workload Identity Federation, chart in the app repo under `deploy/`. **No GitOps controller** (Argo CD, Flux) — a component to run, upgrade, secure, and debug, buying drift detection for three Deployments and one team while splitting "why is production on this version" across two systems. Not an ADR: swapping to a controller replaces one job and installs a chart, so the decision is reversible. `--atomic` is safe only because migrations are forward-only and database-conventions §10 rule 5 holds. Revisit on a second cluster or a second deploying team. | Active |
| A-106 | 2026-08-04 | Registry, architecture, and build attestation | `docs/07-operations/ci-cd.md` §6: **Artifact Registry** in the cluster region (WIF-native, in-region pulls, D5's Google ecosystem), **`amd64` only** (GKE nodes are amd64; multi-arch doubles every build for an Apple-Silicon convenience local Compose already covers), and **no SBOM or provenance attestation** — produced by one flag, consumed by nothing, with no policy for a bad result. D5's portability requirement is satisfied because the registry host is one environment value and the *manifests* stay portable. Revisit SBOM when a customer security questionnaire asks. | Active |
| A-107 | 2026-08-04 | No preview environments, canary, or automatic rollback | `docs/07-operations/ci-cd.md` §14.1. **Per-PR preview environments** cost three deployables, a database, and a seed per open pull request, and E2E is MSW-backed (testing-strategy §8.1) so UI review needs no live backend. **Canary and blue-green** need traffic splitting and metric-based analysis; rolling updates with readiness gates cover the failure GKE presents. **Automatic rollback on an error-rate spike** needs ADR-0011 alerting wired into a deploy controller; today the alert fires and a human runs the rollback workflow. **Cost:** a bad deploy that passes readiness is customer-visible until a person notices. | Active |
| A-108 | 2026-08-04 | SAST tooling, and no Dart equivalent | `docs/07-operations/ci-cd.md` §9: **Semgrep OSS CLI** for `hris-api` and `hris-admin`, blocking merge on `ERROR`, advisory on `WARNING`. **CodeQL rejected** — on private repositories it requires GitHub Advanced Security, a per-committer licence for the small team D13/A-102 describe. **Flutter gets strict `dart analyze` and no real SAST**, recorded rather than implied: no free tool covers Dart at a useful depth, and claiming three-stack parity would be a claim the pipeline cannot back. **Cost:** injection- and crypto-class defects in Dart are found by review or not at all. | Active |
| A-109 | 2026-08-04 | `smoke:reset` is an ops command, not product surface | `docs/07-operations/ci-cd.md` §12. testing-strategy §9 requires the smoke tenant to be reset and reseeded before each run; the mechanism is a **Kubernetes Job on the deployed image, never an HTTP route**, double-guarded to refuse a production target and to refuse any slug but the fixed smoke slug, reseeding through system-administration §5.3's real provisioning path. It has no permission key, no module, no error codes, and no `UC-*` — it is not a feature and must not be modelled as one. Reasoning: a route that erases a tenant's data is reachable by anything that can reach the API, in a product that already ships impersonation and platform users. | Active |
| A-110 | 2026-08-04 | Mobile store submission is manual; Renovate automerge is narrow | `docs/07-operations/ci-cd.md` §10 and §9. **CI uploads to the Play internal track and TestFlight internal and stops there** — store review is asynchronous, human, and rejectable, and phased rollout is a store-side control, so automating submission buys nothing and produces releases nobody expected. Build number = the Actions run number (monotonic, never reused). **Renovate automerges dev-dependency patch and minor updates only**, after every gate is green; runtime dependencies always get a human. Without automerge, dependency pull requests pile up and get bulk-merged unread, which is worse than the risk automerge carries. | Active |
| A-111 | 2026-08-04 | Secret delivery: Secret Manager + External Secrets Operator + Workload Identity | `docs/07-operations/environments.md` §5. security-standards §7 fixed the shape ("env vars injected from the cluster secret store") and deferred the wiring. Google Secret Manager is the source of truth, an `ExternalSecret` syncs to a Kubernetes Secret, pods consume it via `envFrom`; all GCP access is Workload Identity, so **no service-account JSON key exists anywhere** — including for `ADR-0009`'s V4 signed URLs, which sign through IAM Credentials `signBlob`, and for `ADR-0016`'s KMS. Boundary test is "would publishing this in the repository be a problem", not whether it feels sensitive. Rejected: the CSI driver (mounts files, contradicting security-standards §7 and backend-nestjs §11's boot validation), SOPS/Sealed Secrets (ciphertext in git is in git history forever, and the decryption key becomes the same problem one layer down), hand-made Secrets (no audit, no rotation). **Cost, and it is a rule not a footnote: rotation requires a rollout.** Env vars are fixed at pod start, so a new secret version changes nothing until an HPA scale-up mints a pod on it while the rest hold the old one — half the fleet failing on a rotated password. Procedure is write-then-`rollout restart`; no reloader controller. Not an ADR: swapping the store replaces a controller and a CRD. | Active |
| A-112 | 2026-08-04 | Local development stack | `docs/07-operations/environments.md` §4, specifying system-overview §3.3's sketch. Compose runs PostgreSQL 16, Redis 7, backend `both`, `fake-gcs-server`, and Mailpit; **`admin-web` and Flutter run on the host** (bind-mounted hot reload on macOS is slow and fails badly — the image exists to be deployed, not edited in); **FCM is the only real cloud dependency**, via a `hris-dev` Firebase project holding nothing else, because no FCM emulator exists. `fake-gcs-server` rather than the real bucket, because signing a V4 URL from a laptop needs either a JSON key — the artifact A-111 eliminates — or online ADC impersonation, and a fake keeps development working offline. `ADR-0016`'s KEK resolves to a local provider that refuses to load unless `APP_ENV=local`. **The load-bearing rule: Compose creates all three database roles and the app connects as `hris_app`.** As `postgres` the connection is the object owner, `FORCE` RLS does not bind it (database-conventions §9.3), and every tenant-isolation defect is invisible on a laptop until CI or production. | Active |
| A-113 | 2026-08-04 | Ingress, TLS, and DNS | `docs/07-operations/environments.md` §8: **`ingress-nginx` + `cert-manager`**, not GKE Ingress. D5 requires cloud-portable manifests and GKE Ingress means `ManagedCertificate` and `BackendConfig` CRDs that exist on one cloud — the manifests would stop being portable exactly at the edge, which is the first thing a migration touches. Two boring Helm charts buy standard `Ingress` and `Certificate` resources. **Four DNS records exist, total**, because `ADR-0004` resolves the tenant through a picker after login rather than through a hostname: no per-tenant subdomains, no wildcard certificate, no DNS provisioning inside tenant creation. The apex domain is a values-file variable — which domain the product ships under is commercial. NetworkPolicy is **default-deny inbound, permissive egress**: inbound is three rules that never change, while an egress allowlist is amended on every new dependency and fails as a timeout nobody attributes correctly. | Active |
| A-114 | 2026-08-04 | Workload shape and sizing | `docs/07-operations/environments.md` §7. **One `worker` Deployment**, as ci-cd §8.2 and system-overview §3.2 already imply; splitting is a `WORKER_QUEUES` values change on the same image, with named triggers (payroll CPU starving notification latency, or Chromium's footprint). **No HPA on `worker`** — CPU cannot distinguish a pod blocked on a Redis read from an idle one. **The handbook's crons are BullMQ repeatable jobs, not Kubernetes CronJobs**, stated because "cron" in a module doc otherwise produces a manifest each with no tenant context and no idempotency contract. *(Count corrected 2026-08-04 to **26** — see testing-strategy §14.1.)* CPU requests without CPU limits (throttling produces latency spikes indistinguishable from a defect, and D2 fixes p95 budgets); memory requests equal to limits (incompressible — a higher limit only buys a later, more confusing OOMKill); `--max-old-space-size` at ~75% of the memory limit, without which V8 outgrows the cgroup before the GC runs. Grace 30 s api / 300 s worker. Pool rule: `DATABASE_POOL_MAX` × max replicas + migrate Job + break-glass ≤ `max_connections`; **no PgBouncer** — the arithmetic fits and multi-tenancy §4 already made the code pooler-safe, so adding one later is a hostname change. Discharges backend-nestjs §8.1's deferral. | Active |
| A-115 | 2026-08-04 | Chromium sandbox mechanism | `docs/07-operations/environments.md` §7.5, discharging `ADR-0014`'s "worker image sizing + Chromium seccomp profile". The ADR prohibits `--no-sandbox`, and under `RuntimeDefault` the layer-1 sandbox cannot start — which is precisely why `--no-sandbox` is ubiquitous. Fixed as a **`Localhost` seccomp profile derived from `RuntimeDefault` adding exactly three syscalls — `clone` with `CLONE_NEWUSER`, `unshare`, `setns`** — placed by a small DaemonSet and referenced only by `worker`, so the widening is enumerable rather than vague. Rejected: `SYS_ADMIN` (near-root for a PDF) and a render-only Deployment (contradicts the one-worker decision for a blast radius of three syscalls). **Cost: the profile is a file on a node, not a manifest — the one place D5 portability leaks.** Exit named: pod user namespaces delete the DaemonSet and return the profile to a pod field. | Active |
| A-116 | 2026-08-04 | Redis configuration, and `noeviction` as a hard requirement | `docs/07-operations/environments.md` §9.1. **`maxmemory-policy` must be `noeviction`.** BullMQ stores job payloads as ordinary keys, so under `allkeys-lru` — the default anyone reaching for "it is a cache" would pick — memory pressure **silently evicts queued jobs**: not a delay or a retry, but a payroll run that was accepted and never happened, with no error and `ADR-0010`'s at-least-once guarantee quietly void. `noeviction` turns that into a loud, alertable write rejection. One instance serves queues, sessions, rate-limit counters, idempotency envelopes, and the dashboard cache under the `hris:` prefix; the trigger to split is cache growth pressuring queue writes, at which point the cache gets its own instance where `allkeys-lru` is correct. Standard HA + RDB in production. **Residual: Redis is the only durable home a *job* has** — the outbox covers events, not jobs — so a total loss strands an in-flight run, recoverable by re-trigger. | Active |
| A-117 | 2026-08-04 | Observability deployment and alert routing | `docs/07-operations/environments.md` §11, discharging `ADR-0011`'s "kube-prometheus-stack, Tempo, Collector deployment; alert routing". **Both environments run every component**, cut on retention rather than on components — the tempting production-only Grafana would make observability the single subsystem with no staging, while `ADR-0011` puts dashboards in the repository *to be reviewed like code*, and a dashboard nobody can render is reviewed by reading JSON. **Sentry: three projects, one per repo, environments as tags** — six would split every issue's history at the moment of promotion, which is when it is read. Routing: production `critical` → chat + email, `warning` → chat, **staging to a separate channel** (sharing one is how the channel becomes noise), Sentry → the production warning channel. **No PagerDuty** — paging needs a rotation and A-102's organization has no on-call role, so a pager with nobody assigned is a notification everyone ignores plus a bill. Grafana authenticates through Google OAuth, so no shared admin password exists to rotate. | Active |
| A-118 | 2026-08-04 | Infrastructure as code, and what stays out of it | `docs/07-operations/environments.md` §13. **Terraform, one root module with a variable file per environment, in `hris-api/infra/`, applied by a human.** IaC is what makes the parity rules real — two environments from one module either match or the plan says so, whereas parity maintained by clicking is parity maintained by memory. Not a fourth repository: A-006 fixed three and ci-cd already declined a fourth twice; the asymmetry (`hris-admin` deploys into a cluster declared elsewhere) is admitted, and the backend is the right holder since it already owns both Jobs and two of three workloads. **Not applied by CI** — the deploy identity must not be able to delete a database. **Secret values never enter Terraform**: a value passed through it is a value in the state file, a plaintext store with no audit trail, which is the most common way a well-run IaC setup leaks credentials. Consequence: **database roles and their passwords are created by a one-time bootstrap script writing straight to Secret Manager**, because `google_sql_user` and every `random_password` variant put the password in state. **Cloud SQL IAM database auth rejected** — it needs a GCP-specific connector in the application, contradicting D5 and the direct private-IP connection. | Active |
| A-119 | 2026-08-04 | Mobile flavors and which API a store build talks to | `docs/07-operations/environments.md` §10.4. ci-cd §10 says merge to `main` uploads to the Play internal track and TestFlight without saying which API the artifact points at — and that answer decides whether S5 means anything. Three flavors (`dev`, `staging`, `production`) with **distinct application ids**, so staging and production coexist on one device; consequence, spelled out because ci-cd did not: **two Play listings and two TestFlight apps**. **The production artifact is built by the promotion workflow, not by merge** — `ADR-0019`'s "promote a digest, never rebuild" governs container digests and mobile has none, and building at promotion is what prevents a console promote shipping an app pointed at staging. **Residual: the artifact reaching the store is not the artifact S5 smoke-tested** — unavoidable for mobile; the two differ only in `--dart-define` values and the Firebase config file, with no code path, flag, or conditional between them. Firebase config files are committed per flavor (they ship inside every installed APK); security-standards §12's rule that no secret rides `--dart-define` is untouched. | Active |
| A-120 | 2026-08-04 | Infrastructure excluded from V1 | `docs/07-operations/environments.md` §15.1. **Service mesh and in-cluster mTLS** — security-standards §4 deferred it here by name, so it is answered rather than left silent: V1 traffic is plaintext inside the private VPC, revisited on a compliance requirement or a shared cluster. Also excluded: **multi-region** (A-003 fixes one region), **CMEK** (`ADR-0009` defers it), **Cloud Armor/WAF** (rate limiting lives in the app per security-standards §3), **KEDA** (worker replicas are a values number), **dedicated per-tenant infrastructure** (`ADR-0002`'s escape hatch and multi-tenancy §7's runbook exist; nothing is provisioned), and **Redis backup beyond RDB**. **Cost:** cluster-internal traffic is unencrypted, a regional outage is a full outage, and a tenant demanding contractual isolation triggers a runbook rather than a switch. | Active |

| A-121 | 2026-08-04 | Availability SLO scope (reads D3) | `docs/adr/ADR-0021-availability-slo-service-window.md` (**Proposed**) and `docs/07-operations/observability.md` §3.2. D3's 99.9% monthly is **43 minutes 12 seconds**; A-102 has no on-call role and `environments.md` §11 has no PagerDuty, so an outage starting at 23:00 WIB and seen at 08:00 spends roughly **twelve months of budget in one night**. The SLO is scoped to a **service window — 08:00–20:00 WIB, Monday to Saturday** — best-effort outside it, with **both windowed and unqualified 24×7 availability computed and displayed** so the gap never becomes invisible. Detection is unscoped: rules fire at every hour. D3's RPO and RTO clauses are untouched. | Active — ADR **Proposed**, needs review |
| A-122 | 2026-08-04 | Metric cardinality | `docs/07-operations/observability.md` §2.1–§2.3. **`tenant_id` is not a label on any histogram or high-cardinality counter, without exception.** At D1 scale a RED histogram is ~600 route-method pairs × 3 status classes × 14 series ≈ 25,000 — multiplied by 500 tenants it is **over 12 million series from one metric**, which is where Prometheus stops rather than degrades. The `route` label is the **path template**, never the resolved path, which with `uuidv7` ids would be unbounded. Per-tenant questions go to logs and traces, which already carry `tenantId` and are built for it. Closed label vocabulary in §2.2; a 25-metric registry in §2.3; adding a metric or label edits the registry. **Cost:** "one tenant's payroll is slow" is a Tempo or Cloud Logging question, not a Prometheus one. | Active |
| A-123 | 2026-08-04 | Alert set and severity | `docs/07-operations/observability.md` §4. **27 rules in six groups** *(26 at first writing; **OB27**, backup and PITR freshness, added 2026-08-04 — `ADR-0011` had seeded it and observability.md had not built it)*, exactly **two severities** defined by response expectation rather than impact (`critical` = today; `warning` = the batched list), no third tier ever, and identical thresholds in both environments — only the route differs, since `environments.md` §12 permits retention to differ and says nothing about thresholds. **An alert nobody would act on is deleted, not downgraded**, because a `warning` nobody acts on is a `critical` nobody reads one channel over. Backlog alerts on **oldest job age, never depth** — a depth rule fires every morning on D1's attendance spike until someone silences it permanently, and that silence is still in place on the day it matters. | Active |
| A-124 | 2026-08-04 | Alert-to-runbook pairing | `docs/07-operations/observability.md` §5–§6. **No alert without a triage entry, no entry without an alert**, keyed by the same OB id in the same order, so the completeness check is a set difference rather than a matter of care. Entries are four fields, ~6 lines: `Means` · `First check` · `Likely causes` · `Do`. **`First check` holds a literal artifact — a dashboard and panel, a filter string, a console page — never a verb.** Rollback is referenced to `ci-cd.md` and never described, so one procedure exists rather than two that disagree after the first edit. An entry may say **do nothing**, and several do. | Active |
| A-125 | 2026-08-04 | Detecting a job that never ran | `docs/07-operations/observability.md` §4.4 (OB14). **26 registered crons**, ten of them destructive (testing-strategy §14.1). Every existing signal catches a job that *runs and fails*; none catches one that **never started** — repeatable registrations lost on a Redis restart, a wrong `WORKER_QUEUES`, a scheduler never wired. For `cron.leave.period-maintenance` that is invisible until the following January. **One gauge (`hris_job_last_success_timestamp_seconds`) and one rule over all 26**, with tolerance exported from the same registry that declares the schedule so the two cannot drift. Covers all 26 rather than a chosen subset, because the damage is longest-lived for the jobs that look boring. **The `absent()` arm is load-bearing:** a never-registered job exports no series and `time() - <empty vector>` is a silent pass. Free coverage of the outbox relay, which has the identical shape. | Active |
| A-126 | 2026-08-04 | Detecting that the alerting is dead | `docs/07-operations/observability.md` §4.6 (OB21). Everything that decides whether to alert lives **inside the cluster it watches** (`environments.md` §11), and with no PagerDuty there is no vendor heartbeat either. Ingress or load-balancer loss means the product is 100% down, every in-cluster scrape target is green, and the channel is **silent** — byte-identical to healthy. Two mechanisms: the kube-prometheus-stack **`Watchdog`** routed to a low-traffic channel where *absence* is the alert, and an **external Cloud Monitoring uptime check** on `/health` from outside GCP's network path, alerting through a channel sharing no failure domain with Alertmanager — the only signal in the handbook that survives total cluster loss. **Cost:** the uptime check is GCP-specific, a small debt against D5; it is a console check rather than a manifest. **Residual:** a human noticing an absence is unreliable, so the watchdog is checked in §13.4's weekly rhythm rather than continuously. | Active |
| A-127 | 2026-08-04 | Runtime security alerting | `docs/07-operations/observability.md` §4.7 (OB23–OB26). Until now, security detection stopped at CI and the audit log. Four `critical` rules: a `hris_migrator` **BYPASSRLS connection outside a deploy window**, **any break-glass credential retrieval** from Secret Manager, an **impersonation session start** (`ADR-0017`'s controls are otherwise purely detective — A-097's residual), and an **audit anchor mismatch** promoted from a Sentry event to a routed alert. **OB24 is a named exception to §15's exclusion of log-based alerting** — Secret Manager access exists only in Google's audit log and has no code path to instrument. **Rejected:** a per-request cross-tenant heuristic, since RLS already reduces a leaked query to zero rows and the detector would infer intent from empty result sets. **Named reliance:** there is no runtime alert for a cross-tenant read because under `FORCE` RLS it cannot exist — detection for a dropped policy is **G9, not production telemetry**. | Active |
| A-128 | 2026-08-04 | Telemetry PII enforcement | `docs/07-operations/observability.md` §12 and `testing-strategy.md` §5.6 / G22. `ADR-0011` declared three redaction routes and **none were verified**. This is the one handbook rule whose violation is a reportable UU PDP event rather than a bug, and it is **durable** — a salary logged today sits in Cloud Logging for 30 days with no way to un-log it. The realistic failure is `logger.info({ employee }, 'updated')`. **A merge gate** parameterized over security-standards §10 (coverage of all three SDKs plus a behavioural assertion through the real logger) **plus a quarterly human sample**, because the gate only catches keys somebody thought of and registry incompleteness is the likelier failure. **Rejected:** production log scanning — the storage *is* the breach, so detection-after-write buys nothing. Makes security-standards §10 load-bearing rather than advisory. | Active |
| A-129 | 2026-08-04 | Incidents and mobile observability | `docs/07-operations/observability.md` §13 and §18-equivalent (§2.3, §7). Incident severity is a **separate axis from alert severity**: S1/S2/S3, postmortem for **S1 only** as a GitHub issue with four headings, blameless, and **an action item with no owner and no issue number is a wish**. **A human declares an incident; an alert never does** — automating it fills the log with the 08:05 attendance spike and then nobody believes the log. **A cross-tenant leak is S1 by rule, not judgment.** Mobile keeps **Sentry only on-device** and is otherwise observed through three server-derived signals — sync lag as a **distribution** (a mean is dominated by the healthy majority), version spread, and failure rate by `ADR-0003` reason class. **No analytics SDK**, which would put a third-party collector in front of employee behavioural data under UU PDP. **Stated limit:** a phone that never reaches the network is permanently invisible. | Active |
| A-130 | 2026-08-04 | Observability excluded from V1 | `docs/07-operations/observability.md` §15. Nine exclusions, each with a named trigger: **log-based metrics** (OB24 the one exception), **tail sampling**, **Loki** (A-009's exit), **RUM**, **synthetic journey monitoring** (S1–S5 plus OB21 cover it, and a continuous authenticated synthetic needs a standing production credential), **continuous profiling**, **status page**, **ML/anomaly alerting**, and **per-tenant SLO reporting** (A-122 bans the label that would make it cheap). **Three of the nine share one trigger with A-121: the first contractual uptime SLA** — a commercial event, not a technical one, and the single event that changes this document most. | Active |

| A-131 | 2026-08-04 | Tenant-scoped recovery in a shared database | `docs/adr/ADR-0022-tenant-scoped-recovery.md` (**Proposed**) and `docs/07-operations/backup-restore.md` §5, §8. **Instance-wide PITR is never the instrument for one tenant's data loss.** `ADR-0002` puts 500 tenants in one database, so rewinding to Tuesday to repair tenant 47's bad import **discards two days of writes from the other 499** — converting a data-entry error into a company-wide incident with no undo, using the only procedure the handbook had documented. The instrument instead is a **manual extraction** in `ADR-0020`'s already-sanctioned shape: clone inside `hris-production`, extract that `tenant_id`, re-insert in FK order carrying original ids, verify, destroy the clone. Four existing mechanisms make it a procedure rather than a project — uniform `tenant_id`, **stable `uuidv7` ids with no sequences anywhere in the schema** so re-inserted rows keep every inbound FK, `ADR-0016`'s `v1:` ciphertext prefix so no historical key material is needed, and re-insertion as a *new* audited fact rather than a rewrite. **Not a product feature** — no endpoint, no button (BR-ADM-009, A-095 precedent). **Cost:** manual, hours to days, FK ordering validated by nobody, and it does not fit inside D3's four hours and does not claim to. Rejected: nightly per-tenant dumps (500 a night, a second retention system, standing cost against a rare event), universal soft delete (no help against a bad *update*), accepting the collateral, `ADR-0002`'s per-tenant-database hatch, and shipping tenant-level undo across 28 modules. | Active — ADR **Proposed**, needs review |
| A-132 | 2026-08-04 | What a backup document is scoped over | `docs/07-operations/backup-restore.md` §2. Scope is a **completeness table over every stateful store — eleven rows** — each carrying what it holds, what protects it, and whether anything recovers it, *including* the rows whose honest answer is nothing and the rows that are a vendor's problem. Written that way because a backup document listing only the database implies total coverage by omission, and the two stores whose loss is genuinely unrecoverable — the **KMS KEK** and **Terraform state** — were undocumented until this file. Four rows recover nothing. Seam with `environments.md`: that file decides which backup features are switched on, this one says what they can restore; backup configuration is quoted here and never re-decided, and release rollback is referenced to `ci-cd.md` and never described. | Active |
| A-133 | 2026-08-04 | Per-store recovery objectives, and which clause D3 binds | `docs/07-operations/backup-restore.md` §3. D3 reads *"**PostgreSQL** PITR with RPO ≤ 15 min; RTO ≤ 4 h"* — the RPO clause names one store, so **D3 binds the Cloud SQL row and objectives for the other ten are derived here**. Two consequences of tabulating it: Cloud SQL's real RPO is bounded by write-ahead-log archival and is seconds, so **D3's 15 minutes is a ceiling the mechanism beats and RPO is not the clause at risk**; and a database back inside four hours with every payslip PDF gone is not "service restored", which is what makes the bucket a first-class row. **Cloud SQL PITR does not restore in place** — it produces a new instance, so promotion is a `DATABASE_URL` secret version plus a rollout restart under A-111's existing rule. Budget line-itemed to **≈ 3 h 35 m of 4 h** with the 120-minute clone step **unmeasured**, which is what makes A-142's drill a requirement. **Two clocks, on `ADR-0021`'s precedent:** the budget runs from the *decision* to restore, detection lag sits before it, and customer-visible outage is reported as the sum. | Active |
| A-134 | 2026-08-04 | Backup and PITR retention, bounded from both ends | `docs/07-operations/backup-restore.md` §4 and `environments.md` §9. **PITR window 14 days, automated backups 35 days, daily and early WIB.** Both derived: 35 is the longest destructive-cron cadence plus notice margin — nine of testing-strategy §14.1's ten purge jobs are hourly or daily and reveal a defect within days, while **`cron.audit.archive` is monthly**, so a defect in it can be 31 days old before the next run exposes it; 14 is the useful life of an instance-wide rewind, since nobody discards three weeks of 500 tenants' writes and A-131 removes the single-tenant case entirely. **The only retention setting in the handbook squeezed from both directions:** backup retention is exactly how long a crypto-shred stays reversible by restore (A-138), so 90 or 365 days would triple or decuple a UU PDP exposure window while buying no capability the 35-day rule does not already deliver. Staging: PITR off, 7 days, and nothing depends on them. | Active |
| A-135 | 2026-08-04 | Instrument selection, and who may restore | `docs/07-operations/backup-restore.md` §5, §6.1, §6.4. A **symptom-to-instrument decision table in which one row out of ten reaches for instance-wide PITR**, because the commonest defect in a restore document is being used when something narrower would have worked. Two rules over it: **PITR is justified only when the damage is instance-wide**, and **take a forensic clone before any remedy that deletes or overwrites** — including the forward fix, which is the one people reach for fastest — while remedies that only write new rows need none. Crypto-shred appears nowhere on the table, deliberately: it is an erasure instrument, not a recovery one. Authorization: a restore is a **control-plane act**, so `environments.md` §13.3's break-glass path does not cover it; the capability is the human operator identity's alone, and **instance-wide production PITR needs a second person's acknowledgement with the target timestamp recorded** — the only operation in the system that destroys committed data for every tenant at once. Extractions and clones are **not** gated the same way, or the gate becomes a formality clicked through at 02:00. **The superseded instance is stopped and kept 30 days** — it holds the only copy of the writes the restore discarded, extractable by A-131's procedure. | Active |
| A-136 | 2026-08-04 | Post-restore reconciliation | `docs/07-operations/backup-restore.md` §7. Postgres lands at T; GCS, Redis, and KMS are still at *now*. Four skews: **dangling `files` rows** (BR-DOC-009 purges object-then-row so orphan objects never exist — a restore inverts the guarantee); **orphan objects** that `cron.document.purge` can never reach because it walks rows, leaving personal data with no retention path; **outbox re-dispatch**, which needs no action because `ADR-0010` requires idempotent consumers and G11 asserts it — recorded so the next reader does not spend an hour deciding; and **resurrected `tenant_keys`**, which undoes a completed erasure and is a UU PDP event. Plus **Redis flushed entirely before the fleet is repointed** — queued jobs reference discarded rows and, worse, **idempotency envelopes assert "already processed" for work whose effect is gone**, making a legitimate retry silently succeed while doing nothing; justified by A-116's existing "recovery is by re-trigger". **Sessions are a Postgres table, so the flush logs nobody out.** Also recorded: **no sequences and no minted running numbers exist anywhere in the schema**, so a rewind can never reissue an identifier — the property that makes A-131 feasible. | Active |
| A-137 | 2026-08-04 | Object recovery in Cloud Storage | `docs/07-operations/backup-restore.md` §10 and `environments.md` §10.2. Before this the production bucket had **no recovery of any kind** while holding payslip and tax PDFs under a ten-year floor, selfies, employee documents, CVs, and signed handover scans — and for most of those **the object is the only copy**, since the database keeps sha256 and metadata, which proves a file existed and reconstructs nothing. **Versioning + soft delete, noncurrent versions reaped at 30 days**, on the application bucket and the audit archive. A bucket flag beats a job because the failure mode being fixed is silence and a flag cannot stop running. Rejected: cross-bucket replication (doubles storage, lags or dies unobserved, needs its own freshness alert, protects nothing better against three of the four causes) and **bucket retention lock**, which would leave `cron.document.purge` and `cron.recruitment.candidate-purge` unable to delete and so make a UU PDP erasure obligation unsatisfiable. **Cost: versioning extends every erasure the purge crons perform by the noncurrent lifetime**, which makes 30 days a privacy parameter rather than a cost knob. | Active |
| A-138 | 2026-08-04 | Key material, and the crypto-shred that completes later | `docs/07-operations/backup-restore.md` §11, discharging `ADR-0016`'s *"backup-restore.md notes wrapped-DEK backup requirements"*. **The wrapped DEKs need no separate backup** — `tenant_keys` is an ordinary table and therefore inside PITR; stated explicitly because "back up the keys" invites an export, and an exported wrapped DEK plus KEK access is the entire plaintext. **A KEK version is never destroyed, only disabled** — destruction is irreversible and global across all 500 tenants; the destroy binding belongs to the human Terraform identity alone; and **the crypto-shred destroys a DEK, which is a database row, never a KMS version**, a distinction whose confusion shreds every tenant at once. The KEK is regional, so a restore in another region decrypts nothing — a structural second reason behind A-120's multi-region exclusion. **The finding: a crypto-shred is not complete when performed.** Every backup and every point inside the PITR window taken before it still contains the wrapped DEK, so the erasure completes only when the last of them expires; the procedure ends by recording that date. Whether an unreadable-but-restorable copy counts as erased carries a **⚠️ VERIFY** — a legal question, not an engineering one. | Active |
| A-139 | 2026-08-04 | The audit trail as evidence, and its two soft spots | `docs/07-operations/backup-restore.md` §12, amending `audit-log.md` BR-AUD-009, BR-AUD-010, UC-AUD-006. Both surface only when asked *"what must a restore be able to prove?"*, which is why Phase 2 missed them. **(a) The archive job deleted before it verified.** UC-AUD-006 exported a month to GCS and hard-deleted the hot partition with no read-back, so a truncated write was indistinguishable from a successful archive — and of testing-strategy §14.1's ten destructive crons this is the only one whose damage is reconstructible from nowhere. Now **verify-then-delete**: re-read, recompute, compare against both the manifest and that month's `audit_anchors`; on mismatch the partition stays hot and the job fails loudly. **(b) The independent witness expired before the evidence.** BR-AUD-009 rests on the daily digest being emitted to Cloud Logging, *outside DB reach* — but Cloud Logging keeps 30 days while the rows keep two years and the payroll rows behind them ten, so past a month the only digest left was `audit_anchors`, a table in the same database as the rows it attests to. Now a **Cloud Logging sink filtered to the anchor emit lands it in the audit archive bucket** under A-137's versioning: one Terraform resource, no new code, no new cron — rejected alternatives being wholesale retention extension (pays 10× for every log line to keep one per tenant per day) and a bespoke job (a second scheduled thing that can silently stop, when a sink cannot). A restore is also a legitimate cause of anchor mismatch: record it, and **never re-anchor**. | Active |
| A-140 | 2026-08-04 | Payroll and tax retention has no cold archive (corrects `database-conventions.md` §4.4) | `docs/07-operations/backup-restore.md` §1 session; `docs/04-database/database-conventions.md` §4.4. §4.4 read *"archive to cold storage per D4"* for payroll runs, payslips, 1721-A1, and BPJS reports. **D4's cold-archive clause attaches to the audit log only** — *"Payroll and tax records ≥ 10 years; audit log 2 years hot + cold archive"* — and no such job exists in payroll.md, tax-pph21.md, or bpjs.md, all three of which instead declare those rows exempt from every purge path. A dependent document was attributing a requirement to an authority that does not contain it, with no owner; same class as A-017 contradicting D7. **Decision: payroll and tax rows stay in the live database for the full ten years, and no cold archive is built.** They are *queried*, not merely retained — payslip reprint, the revisioned 1721-A1 reissue, retro worklists, and the multi-year form search tax-pph21 §15 names as a product need — so an archive you must restore to answer a routine HR request is worse than disk; volume is bounded beside attendance punches, which carry no ten-year floor; and an archive job would be an eleventh entry in the destructive class. Trigger: those tables reaching a size where vacuum time or disk cost binds. | Active |
| A-141 | 2026-08-04 | Reconstituting the system, not just the data | `docs/07-operations/backup-restore.md` §13 and `environments.md` §13.1. **The Terraform state backend was named nowhere** — §13.1 described one root module applied by a human and mentioned the state file only to forbid secrets in it, which read literally means `terraform init` with no backend block and therefore **local state on one laptop**; losing it costs `terraform import` across every resource, measured in days, with the plan lying throughout. Fixed: **versioned GCS bucket in `hris-production`, with state locking** — versioning is the load-bearing half, since a corrupt apply is repaired by rolling state back. **Secret Manager holds the only copy of the database role passwords by design (§13.2 step 2) and that is a re-bootstrap, not a data loss** — written down specifically so nobody answers this document with a secret escrow file, which would invert A-111. **Artifact Registry never reaps a tagged digest**, untagged at 30 days, because `ADR-0019`'s rollback depth of exactly one requires the previous digest to exist and a default age policy silently downgrades that guarantee to "usually". The per-release schema snapshot already exists (ci-cd §7.3) and is noted rather than rebuilt. | Active |
| A-142 | 2026-08-04 | Restore drills | `docs/07-operations/backup-restore.md` §14, discharging the assignment testing-strategy §1 and ci-cd §1 both made here by name in their seam tables. **Quarterly PITR drill** — restore to a **random** timestamp inside the window into a temporary instance, measure decision-to-verified, destroy it; random because a drill that always restores to "one hour ago" only ever exercises the newest transaction logs. Pass = A-133's budget met plus three checks: row counts, **a recomputed `audit_anchors` digest** (which exercises the integrity mechanism in the same action), and a live-key spot decrypt. Its primary output is the measured clone time, which replaces the estimate. **Semi-annual extraction drill** for A-131, into a scratch schema, never into live; its interval tracks release cadence because its rot is driven by schema change across 28 modules. **A forced Cloud SQL failover is not drilled** — it exercises Google's mechanism, carries the one vendor SLA in the stack, and forcing it in production is a self-inflicted outage inside `ADR-0021`'s window. Rule: **a procedure not executed within its interval is presumed broken**, with the last-executed date kept in the document. **Stated limit:** nothing automated enforces a drill at this organization's size, an alert would be wrong (a calendar fact is not telemetry, and observability §15 excludes that class), so a missed drill is an issue with an owner. And **a staging restore is never a rehearsal** — no PITR there, wrong volume, and clone time is the line that scales. | Active |
| A-143 | 2026-08-04 | Backup and restore exclusions | `docs/07-operations/backup-restore.md` §15, §16. Two lists. **What cannot be recovered — seven rows, each with a disposition**, including telemetry beyond retention (*by design* — `ADR-0011` makes the audit log the historian, which is exactly why observability §14 scopes a breach from it), unsynced punches on a destroyed phone (no mechanism can; mitigation is sync cadence and OB17), a destroyed KEK, and a shredded tenant past backup expiry. **None of them has an RTO, because none has a restore** — D3's four hours describe one row of A-133's table. **Nine V1 exclusions, each with a trigger:** cross-region backup copies, a warm standby, one-click restore tooling, automated reconciliation, per-tenant logical backups, Redis persistence beyond RDB, CMEK, backup of Prometheus/Tempo/Grafana state (**the one exclusion with no trigger — never**), and continuous backup validation. Two are singled out: **cross-region is a data-residency question before it is a cost one**, since `asia-southeast2` is Indonesia's only region so any second copy leaves the country and A-003 does not answer that; and **continuous validation is the only exclusion here that is not a real trade-off** — it is simply larger than a quarterly drill, and it is where this document goes next. | Active |
| A-144 | 2026-08-04 | What a performance document derives rather than restates | `docs/07-operations/performance.md` §1. Seam: **`observability.md` says a budget was missed; this document says what the budget is made of, what must exist for it to be met, and how it is proven at volume.** Every other Phase 4 file *reacts to* a number; this one *derives* them, and five siblings deferred a figure here by name with nowhere left to push it. The MANIFEST row named five topics of which **three already have owners** — index rules (database-conventions §7), pooling (environments §7.4), virtual scrolling (admin-nextjs §9) — so all three are **quoted and re-decided nowhere**; a fourth document restating them would drift on the first edit. Also fixed in §1: **a number showing its arithmetic is derived and checkable; a number without one is a labelled guess that names the measurement replacing it.** The `⚠️ VERIFY` marker is *not* used for engineering guesses — it stays reserved for Indonesian regulatory values, which in this work means exactly one, in `ADR-0023`. | Active |
| A-145 | 2026-08-04 | D1 read at fleet scale, and what the morning actually costs | `docs/07-operations/performance.md` §2. **D1 is written per tenant and every document read it that way** — attendance.md §14's spike test is 600 punches over 900 seconds, 0.7 writes/s. But every tenant is Indonesian (A-003) and offices open at the same hour, so the spike is **one event, not 500**: 300,000 clock-ins over 15 minutes = **333 app opens/s**. The correction that matters is that an employee does not issue a punch, they open an app — token refresh (**a write**, since `ADR-0004`'s 15-minute access horizon guarantees overnight expiry), permission resolution, `/me`, `/settings/effective`, ~3 delta pulls, a signed-URL mint, the home surface, then the punch. **≈ 2,700 requests/s, ≈ 670 HTTP writes/s, ≈ 1,300 database write statements/s.** So **the punch is one of two writes and one of eight requests**, token rotation is exactly as large a write source as the feature the spike is named after, and anything sized against "333 writes/s" understates the morning roughly eightfold. Month-end is the other synchronized peak: ~1M payslips concentrated on Indonesian payroll dates ≈ **12 employees/s sustained for three days** against a per-run budget of 5.5/s. §2.4 bounds the lens deliberately — **only those two workloads are clock-synchronized**; admin traffic, reports, imports, and requests are not. | Active |
| A-146 | 2026-08-04 | D2 is server-side, decomposed, and has one closed exemption | `docs/07-operations/performance.md` §3. observability §3 already measured D2 as a prom-client histogram inside `api`, making it **server time** — never stated as a decision until now, and it is the source of every *"the app feels slow and the dashboard is green"* conversation. Decomposed: guards 2–5 cost **three Redis round trips and zero queries**, plus `BEGIN` and `set_config`, so **fixed overhead is ~10 ms of 300 and about 97% of the read budget is the query**. Therefore **a route missing D2 has one structurally wrong query, not accumulated overhead** — worth publishing because the instinct sends investigations into a middleware chain where nothing is wrong. **One exempt route: `GET /reports/{key}/result`** — synchronous, interactive, 94 definitions, no cache by design (BR-RPT-011, A-085), and **OB1 excludes it by label**, because an alert firing on correct behaviour gets muted and muting OB1 costs the whole read SLO. The list is **closed** in environments §12.1's shape. | Active |
| A-147 | 2026-08-04 | PostgreSQL timeouts, and the pool's right-hand side | `docs/07-operations/performance.md` §4.1–§4.4. **No `statement_timeout`, `lock_timeout`, or `idle_in_transaction_session_timeout` existed anywhere**, while database-conventions §10 rule 6 routed backfills to `maintenance` jobs for exceeding *"lock/statement tolerances"* and reports.md named *"the statement timeout"* five times without a value. Set per role — 5 s api / 300 s worker statement, 3 s lock and 5 s on migrations, 30 s idle-in-transaction — with **reports' 15 s the one documented override**, supplying BR-RPT-010's `limitMs`. environments §7.4's inequality gains its missing side: 10×6 + 10×2 + 5 + 5 = **90**, and OB6's 80% means **`max_connections` ≥ 200**. **The reframing that matters is Little's Law:** 80 connections carry the peak only while mean hold time stays near **30 ms**, so **OB5 means hold time crossed the line, not that the pool is small — and raising `DATABASE_POOL_MAX` is the wrong fix and the first thing anyone will try.** Floors set (8 vCPU / 32 GB, chosen) with environments §12's "cost, not behaviour" preserved *above* the floor. | Active |
| A-148 | 2026-08-04 | The cache registry and its admission rules | `docs/07-operations/performance.md` §5.1, §5.3. **Seven read caches existed, invented independently in seven documents**, with five TTL values and three invalidation strategies, and **no rule anywhere for when a cache is permitted at all**. §5.1 is the registry — key, TTL, invalidation, access pattern, owner — appended by the document that introduces a cache, the third use of a protocol the error catalog and pagination registry already run. Three admission rules: a measured or bounded hot path; **the key is tenant-scoped *and* the RLS transaction still opens on every miss** (BR-DSH-006 generalized, so a cache never becomes the reason isolation is checked once); disclosed staleness. Default invalidation is **post-commit bust with TTL as backstop**, which five of seven already do; A-090's TTL-only stays the documented exception. **Finding: three of the seven are keyed per employee** — permissions 60 s, org placement 5 min, shift verdicts 15 min — against a user who appears twice a day, so on the ESS path each is a guaranteed miss. They are admin-web instruments; the fix was A-152, not a TTL change. | Active |
| A-149 | 2026-08-04 | The idempotency envelope was the largest thing in Redis | `docs/07-operations/performance.md` §5.2, §5.4, amending `ADR-0007`. `ADR-0007` stored the **full terminal `(status, body)`** for **7 days**. At the design point that is ~14M response bodies at 1–2 KB — **14 to 28 GB** — in an instance where `maxmemory-policy` is `noeviction`, so filling up **rejects writes** and voids `ADR-0010`'s at-least-once guarantee. And offline-sync §5 already states the envelope is not load-bearing: a unique violation on `(tenant_id, op_id)` yields the same replay, so **duplicates are impossible at any TTL, by construction**. **TTL cut to 24 hours** — a value change, not a supersession, since `ADR-0007` declared it settings-tunable. Residual: a device offline past the window pays one extra database round trip. **`maxmemory` floor 16 GB**, derived per consumer (~9 GB steady state); at the original TTL the floor would have been an instance class higher, which is why the two decisions sit in one section. | Active |
| A-150 | 2026-08-04 | Capacity is proven manually, at production size, or not at all | `docs/07-operations/performance.md` §11. Two questions get conflated: **capacity** needs production-sized infrastructure and D1-scale data; **regression** needs consistency. Only the first is built. **A manual capacity rehearsal** in backup-restore §14's shape — ephemeral production-sized environment from the existing Terraform root module, destroyed after; **not staging** (zonal, Basic Redis, one replica, two small tenants) and **not production** (forcing peak load inside `ADR-0021`'s window is a self-inflicted outage, the argument backup-restore used to refuse a failover drill). **Tool k6** — JS so no fourth language, containerized, Prometheus output into the existing Grafana; rejected Gatling and Locust (a language each), Artillery (weak above a few hundred RPS), JMeter (GUI/XML). **A D1-scale synthetic tenant generator is a named deliverable**, because security-standards §3 caps punches at 12/min per user so 333/s needs thousands of seeded users, and raising staging's limit is the parity destruction environments §12.2 refused. Its second consumer is backup-restore §3.2's unmeasured clone estimate — two procedures, one fixture. **Five pass criteria, three of which replace chosen numbers in this document; a rehearsal that changes no number has not been read.** No per-merge gate (testing-strategy §13's zero-retry rule makes a flaky gate muted or fatal); trigger is a regression reaching production twice. | Active |
| A-151 | 2026-08-04 | Payroll's two clocks, and a concurrency bound on a shared queue | `docs/07-operations/performance.md` §3.4, §7.1–§7.2. `ADR-0010` keeps `payroll` concurrency low deliberately, and **one queue serves 500 tenants**, so month-end runs wait behind each other with nothing bounding the depth. **OB16 could not tell the two apart** — if it measured processing, a run that waited three hours reported 25 minutes and looked healthy; if enqueue-to-done, it was a queue-depth alarm wearing a calculation runbook. **Two clocks on `ADR-0021`'s and backup-restore §3.3's precedent:** D1's 30 minutes is processing, `hris_queue_oldest_job_age_seconds{queue="payroll"}` is the wait, customer-visible time is the sum, and OB16's triage now establishes which clock ran long **first**, because the remedies are unrelated. **Concurrent-run admission bound 4**, over a **chosen 50 ms per-employee CPU cost** on which the entire 30-minute budget rests and which nobody has measured. **The first move against month-end contention is per-queue concurrency** — `ADR-0010` already owns it as in-process config, one values change — with splitting `worker` as the named escalation environments §7.2 asked for and could not threshold. | Active |
| A-152 | 2026-08-04 | Permission resolution is lazy | `docs/07-operations/performance.md` §6.2, amending `ADR-0005`. BR-AUTHZ-002 gives every route `@RequirePermission`, `@Public()`, or `@AuthenticatedOnly()`, and BR-AUTHZ-009 resolves data scope in repositories — so ESS routes have **no permission key to check**. But multi-tenancy §1 had `RequestContext` carrying the effective permission set unconditionally, and **whether it was built eagerly was never stated**: the difference between the hottest path in the system paying for a permission resolution and skipping it. **A route declaring `@AuthenticatedOnly()` resolves nothing and touches no cache**; the set materializes on first access, which never comes. **Does not reopen `ADR-0005`** — model, key grammar, 60-second TTL, and post-commit bust all unchanged; it decides *when*, which no document had. Removes one guaranteed-miss Redis round trip and one query from 333 requests/s. Minor gap, decided per best practice. | Active |
| A-153 | 2026-08-04 | The admin web gets one budget, not a web-vitals program | `docs/07-operations/performance.md` §10.2. Scoping argument stated rather than assumed: no SEO surface, no marketing page, no anonymous traffic, desktop broadband, auth-gated — **LCP and CLS measure things that do not decide whether this product is pleasant to use; INP does**, because the product is grids, forms, and approval clicks. **Initial JS per route ≤ 300 KB compressed, gated** on the number Next.js already prints at build (deterministic, therefore gateable); **interaction-to-render measured, not gated**, since a Lighthouse-in-CI gate is the flaky merge gate testing-strategy §13's zero-retry rule cannot absorb — the same reasoning that excluded the per-merge load gate. Recorded as **already correct so nobody optimizes it away**: `Cache-Control: no-store` plus no ETags means React Query's client cache is the only read cache on this path, and that is a security decision — payroll data must never land in a shared cache. | Active |
| A-154 | 2026-08-04 | Migration lock behaviour at volume | `docs/07-operations/performance.md` §9, discharging the *"migration lock duration"* both testing-strategy §1 and ci-cd §1 assigned here by name. **A DDL cost table** — metadata-only, full rewrite, full scan — as the reference an author checks *before* writing the migration. **Index creation on a large table is `CONCURRENTLY`, which cannot ride a transactional migration**, so it goes in its own migration or a `maintenance` job under §10 rule 4's `-- manual:` convention; the default generated path is the blocking one. **The severity property: a waiting `ACCESS EXCLUSIVE` request blocks every query behind it**, so a migration stuck behind one long report takes the table down. And **ci-cd §8.2's drain gate protects the worker restart, not the schema change** — the `migrate` Job runs first, ungated, so a merge on the 25th applies DDL to a database a payroll run is writing to. **It stays ungated and `lock_timeout` makes that safe**: the failure degrades to *"the deploy failed, retry"*, and since migrate is step one the abort leaves the old app on the old schema, the cleanest failure state in the pipeline. Moving the gate earlier would make every deploy wait 35 minutes including the majority with no migration. The failure message must name the blocking relation. | Active |
| A-155 | 2026-08-04 | The morning autoscaling blip is accepted, and OB1's window stops being luck | `docs/07-operations/performance.md` §8. environments §7.3 configured an HPA and **stated no target utilization**; set here at **70% — chosen** — because environments §1 deferred *"why that number"*. Reaction time is scrape lag plus Node start and readiness, so **capacity lands 60–90 seconds after a spike that happens at the same minute every workday**. Accepted reactively: `minReplicas` is **derived from the trough, not the peak**, since raising it pays for six replicas overnight to serve nobody. **OB1 fires only after 10 minutes**, a clause chosen to stop a p95 over four requests paging on a quiet Sunday — and it is also what makes the daily lag non-paging. **Recorded so it is deliberate rather than lucky**, giving any future edit to that clause a second reason to weigh. Scheduled WIB pre-scaling is the escalation (environments §9 already reasons in WIB for this class of coincidence), triggered by the blip exceeding OB1's window. | Active |
| A-156 | 2026-08-04 | Month-end convergence, and a render budget that does not fit | `docs/07-operations/performance.md` §2.3, §7.3, amending `ADR-0014`. A completed payroll run produces payslip PDFs **and** payslip-published notifications, so calculation, Chromium, and fan-out are **one causal chain on one `worker` Deployment with 2 replicas and no HPA**. `ADR-0014` placed rendering *"outside the 30-minute calculation budget"* — and outside every other budget too. **The arithmetic does not fit:** Chromium's 100–200 MB of RSS shares a cgroup with a V8 heap set to ~75% of a 1Gi limit, giving **one concurrent render per pod with no margin** — ~2 renders/s against a month-end fleet need near 12/s. `pdf` concurrency is **1 per pod** until environments' values file resolves it (more memory, or the render-queue split §7.2 already named), because the alternative is an OOMKill mid-run and §7.3 set memory limit equal to request precisely so that kill is early and legible. Fan-out itself is already well bounded and is **cited, not re-decided**: BR-NTF-009's ≤500-recipient chunks, BR-IMP-007's three bounds, BR-IMP-004's 200-row batches. | Active |
| A-157 | 2026-08-04 | Reports stay on the primary; the read replica is named, not built | `docs/07-operations/performance.md` §12, §13.2. **Reports are the only user-triggered unbounded-cost query class in the system and they run on the primary** — 94 definitions, no cache by design, competing with the morning spike for the same database. **The control is A-147's 15-second statement bound**, which is mechanism reports.md already built and was waiting on a number for; that is far cheaper protection than an instance. **The seam is already cheap and saying so is the point:** `ADR-0002`'s `ConnectionProvider` keyed by `TenantContext` makes a read-only variant the same shape, and RLS with transaction-local `set_config` works unchanged on a replica — the same deferral property environments §7.4 used for PgBouncer. Trigger: **report query duration correlating with OB1 across unrelated routes**, not "reports feel slow", since A-146 exempted them. **BR-RPT-011's tension recorded honestly**: it refuses stored results, not physical replication; sub-second lag is not the failure it was written against, and `generatedAt` already discloses the staleness that module cares about. | Active |
| A-158 | 2026-08-04 | Nothing guarantees tenants cannot starve each other | `docs/07-operations/performance.md` §13.1, amending `ADR-0002` and `multi-tenancy.md` §5. **`ADR-0002` guarantees tenants cannot see each other's data. No document anywhere guarantees they cannot starve each other.** security-standards §3's limits are per user and per IP; **there is no per-tenant limit, quota, or resource class in the system**, and a tenant at D1's 10,000-employee ceiling generates five times a typical tenant's load against one shared database, one `noeviction` Redis, and one `api` fleet. Verified absent: zero occurrences of CDN, noisy-neighbour, per-tenant quota, resource isolation, or QoS across all 71 files. Excluded from V1 with a trigger — one tenant's activity measurably degrading another's p95 — but **called out at length rather than left as a table row, because a reader of multi-tenancy.md will believe it is covered**: "isolation" means one thing there and a reader hears two. It is also the exclusion most likely to become a commercial commitment before an engineering one, exactly as A-121 and A-130 predicted for availability. Enforcing it would additionally need the per-tenant metric labels A-122 currently forbids on cardinality grounds. | Active |
| A-159 | 2026-08-04 | Performance exclusions | `docs/07-operations/performance.md` §12, §13. Two lists, on the sibling pattern. **§12 "deliberately not optimized"** — load paths that exist, are understood, and are left alone: reports on the primary, the three per-employee caches, the morning blip, no partitioning, and `worker` without an HPA (environments §7.1's reason still holds — a pod blocked on a Redis read looks exactly like a pod with nothing to do). **§13.1 six V1 exclusions, each with a trigger:** per-tenant QoS (A-158), read replica, partitioning, scheduled pre-scaling, per-merge load gate, and a CDN — *"a single-region product serving one country from Jakarta"*, whose trigger is users outside Indonesia, a commercial event before a technical one. **Six more inherited and cited rather than re-argued:** PgBouncer, KEDA and the `worker` split, report caching, a materialized report layer, ETags, and automated metric analysis at promotion — the last meaning a performance regression is caught by OB1 after deployment and answered by a rollback of depth one. | Active |
| A-160 | 2026-08-04 | Attendance retention is a gap, and no number was invented for it | `docs/adr/ADR-0023-table-growth-and-partitioning.md` (**Proposed**) and `docs/04-database/database-conventions.md` §4.4. **`attendance_punches` was absent from §4.4's retention table entirely** — the largest table in the system, ~500M rows a year at D1, with `attendance_days` at 250M. `attendance.selfie_retention_months` governs the image files; nothing governed the rows. The tempting close was *"punches purge after 24 months"*, and it is refused: **attendance records are evidence in an Indonesian labour dispute**, so the horizon is a regulatory fact and CLAUDE.md forbids inventing one — the same failure A-017 and A-140 already caught. The row is added marked **undefined**, carrying the ⚠️ VERIFY marker, which is this work's only one. Two consequences are fixed regardless of the answer: the table **cannot be range-partitioned by date** (its `op_id` unique carries no date column and cannot), so deletion will always be a batched `DELETE` under §10 rule 6 rather than a `DROP PARTITION`; and archiving was rejected for A-140's reason — those rows are *queried* by payroll recalculation, corrections, and reports, not merely retained. | Active — ADR **Proposed**, needs review |
| A-161 | 2026-08-04 | Row volume at the design point, and the partitioning deferral discharged | `docs/adr/ADR-0023-table-growth-and-partitioning.md` (**Proposed**) and `docs/07-operations/performance.md` §4.5–§4.6. **`ADR-0002` and `ADR-0013` both deferred declarative partitioning to this document by name** — *"may arrive via the performance doc without superseding this ADR"* — and neither could be answered, because **no document had ever projected row counts**. §4.5 supplies them: `attendance_punches` ~500M/yr, `attendance_days` ~250M, payslip lines ~240M, all at 500 × 2,000; **two exceed the audit log, the only table anyone thought to partition**. That projection is also what finally makes database-conventions §7 rule 4's *"no speculative indexes"* checkable, since nobody could previously say what "large" meant. **Decision: no partitioning in V1**, trigger numeric — a table crossing 500M rows, or autovacuum on `attendance_days` failing to keep up (it takes an upsert per punch, ~333/s in the morning window, making it the highest-churn large table). **The fact neither ADR could have known: on `attendance_punches`, hash-by-`tenant_id` is the only key still available** — PostgreSQL requires the partition key to be a subset of every unique constraint, and `uq_attendance_punches_tenant_id_op_id` carries no date column and cannot, since an `op_id` is minted on a device with no knowledge of when the row lands. So the *useful* key, range-by-date, is foreclosed by the constraint that makes offline dedup correct, and hash buys vacuum and index size while buying **nothing** for deletion. **`attendance_days` keeps both doors** and is therefore the first candidate if the trigger ever fires. Recorded now because PG16 cannot convert a plain table to a partitioned one in place — a decide-now-or-pay-100×-later item, and the only kind worth raising in a product with no customers. | Active — ADR **Proposed**, needs review |
| A-162 | 2026-08-04 · **revised 2026-08-05** | Delivery capacity, and why the plan carries no dates | **Revision (issue #6): capacity is one engineer plus an AI agent.** The original figure below was three to five engineers plus a part-time statutory advisor, and this entry exists precisely so that reversing it is one edit. What reverses: §3's three workstreams no longer run concurrently across people, so **track 2's independence now means schedulable at any moment rather than running beside track 1** — the ordering fact survives, the simultaneity does not. What does **not** reverse: §4's sequence, which is ordered by dependency rather than by headcount; A-006's three repositories, because `ADR-0019` rejected the monorepo on the mobile client's version skew rather than on team size, and that ground does not weaken as a team shrinks; and A-102, which is now more true rather than less. **Track 3 gains the owner A-163 lacked — the maintainer, personally** — and that makes §8's release gate the schedule's binding constraint, since the only person who can discharge a `⚠️ VERIFY` marker is also the only person writing code. Original entry follows. Capacity was never stated anywhere, yet four documents already assumed it: A-102 refuses a manual UAT gate because *"the org model has no QA role; inventing a gate here would invent a person"*, `ci-cd.md` §11 sets one approving review because *"on a team of this size a two-reviewer rule manufactures rubber stamps"*, `ADR-0019` rejects a release train partly on coordination cost that grows with team size, and A-006's three repositories put a floor under it. **Recorded as an input: roughly three to five engineers, no QA role, plus a part-time statutory advisor.** The number that matters is not headcount but **how many independent tracks can run at once** — the whole day-one parallelism of §4.4 is false at one track. Consequence: the roadmap expresses tracks and dependencies and **carries no calendar dates, no per-module estimates, and no Gantt**, because a date in a document nobody reads weekly is wrong within a month while a dependency stays true for years. Reversing this reverses every parallelism claim in the file, from one place. | Active |
| A-163 | 2026-08-04 | Statutory verification is a third workstream with its own owner, not engineering capacity | `docs/00-overview/implementation-roadmap.md` §3 and §4.4, and `ADR-0018` Consequences. **119 `⚠️ VERIFY` markers across twenty-seven files** — bpjs 18, tax-pph21 16, settings 13, overtime 11, payroll 6, leave 5 — **none discharged, and the count had never been made**. Discharging one is a person reading PMK 168/2023, PP 58/2023, PP 35/2021, UU 13/2003 jo. Cipta Kerja and the BPJS regulations and signing a number; it **produces no commits**, which is exactly why it is absent from every engineering plan, since a plan made of commits cannot see work that makes none. `ADR-0018` already designed its artifact — a vector file *"a domain expert reviews without reading TypeScript"*, whose git history becomes the audit trail of every statutory reinterpretation — but named no owner and no start. **Decision: it starts on day one alongside tracks 1 and 2, and belongs to the advisor in A-162, never to whichever engineer holds `bpjs.md` when a sprint is short** — which is precisely how an unverified number becomes a hardcoded constant, the failure `ADR-0018` exists to prevent. **Owner named 2026-08-05 (issue #6): the maintainer, personally.** A-162's advisor role does not exist at the revised capacity, and the role cannot be delegated to the AI agent — `CLAUDE.md` prohibits inventing a regulatory value, which is the whole of the job, and the `⚠️ VERIFY` marker exists because that prohibition needed somewhere to point. The separation this entry defends therefore survives in the only form left to it: **not a separate person, but a separate act.** Reading a regulation and signing a number is not a coding task that happens to touch `bpjs.md`, and it is done as its own work with its own artifact — the vector file — never inline while implementing a calculator. The hazard the original wording named is now sharper rather than milder, because the engineer under sprint pressure and the advisor are the same person. | Active |
| A-164 | 2026-08-04 | The `pending-verification` gate is scoped to reachable calculators | `ADR-0018` decision 5 (amended in place — a narrowing, not a reversal, so no supersession; `ADR-0012` precedent), `testing-strategy.md` §13, `ci-cd.md` §8, `docs/00-overview/implementation-roadmap.md` §4.4. As written, G20 printed a global count and blocked promotion on any non-zero value. With A-163's 119 markers open that holds **every** build at staging indefinitely — including a pilot exposing only attendance and leave, whose BPJS code no path reaches because no payroll run exists to invoke it. That was a side effect of a counter written before anyone counted the markers, not an intent. **Scoped: G20 blocks promotion for the calculators a release actually exposes.** The rule it defends is undiluted — a calculator a user can invoke does not reach production unverified. | Active — `ADR-0018` still **Proposed** |
| A-165 | 2026-08-04 | A tenant at the D1 typical size could not be onboarded, and one import closes it | `docs/adr/ADR-0024-tenant-onboarding-as-a-v1-capability.md` (**Proposed**), `docs/06-modules/payroll.md` §13/§15/§2, `docs/05-platform/import-export.md` §4.3/§4.4, `docs/00-overview/implementation-roadmap.md` §7.1. Nine modules refuse an import and every refusal is right on its own terms; together they leave a step with no mechanism. **`employee.master` carries no salary column** and payroll writes **"No import."** in bold, so a two-thousand-employee tenant types two thousand multi-line effective-dated pay packages by hand — and without a package there is no run. **No module could see this; only a document whose input is every module at once.** Resolution reverses nothing: payroll's argument is an argument against **supersede**, and an onboarding package supersedes nothing, so `create_only` — already used by `employee.master` (A-019) and described in those words by `leave.balance_adjustment` — carries the property the excluded dry-run was there to buy. `payroll.salary_opening` ships in V1; bulk salary **revision** stays in payroll §15 behind the dry-run. | Active — ADR **Proposed**, needs review |
| A-166 | 2026-08-04 | The roadmap indexes the V1 cut and the post-GA backlog rather than restating them | `docs/00-overview/implementation-roadmap.md` §1 and §9. `MANIFEST.md` names four topics for file 69; two already have owners — the V1 cut across spec §3.2/§6, product-overview §9, every module §1, and three ops §14 sections; the post-GA backlog across **twenty-eight module §15 sections and every ADR's Future considerations**, roughly two hundred items. Copying either produces a second source of truth that drifts on the first edit, the same reasoning that refused three of the five topics `MANIFEST.md` named for `performance.md` (A-144). **The two topics with no owner anywhere — build order and milestones — are owned in full.** `HANDBOOK_SPEC.md` §7's Phase 3 ordering is explicitly a *documentation* order, holiday leading because it is *"small, nearly standalone"* and makes a good template, which is an argument about writing. | Active |
| A-167 | 2026-08-04 | A milestone is a capability demonstrable in staging, never a coordinated release | `docs/00-overview/implementation-roadmap.md` §5 and §6.3. `ADR-0019` decision 4 refuses a pinned pair and a release train, so a milestone defined as "all three repositories ship together" would be a release train under another name and would contradict an anchor. Defined instead as a capability the tier-4 smoke suite demonstrates end to end — **checked rather than declared**, which is the only shape available in an organization A-102 leaves with no QA role and no sign-off gate. Two consequences kept deliberately: production exposure stays an independent per-repository decision, so a tenant can use attendance in production while payroll sits behind the release gate; and **an app-store queue attaches to a milestone without gating it**, since `ADR-0019` states plainly that mobile ships *"when a store lets it"*. | Active — `ADR-0019` still **Proposed** |
| A-168 | 2026-08-04 | R1/R2 is a release boundary and does not move the V1 scope boundary | `docs/00-overview/implementation-roadmap.md` §6.1 and §6.2. `HANDBOOK_SPEC.md` §3.2 places all nineteen business modules in V1 and nothing here changes that; the roadmap states arrival order only. **The cut is forced by the port graph, not chosen**: payroll consumes `AttendanceQueryPort`, `LeaveQueryPort`, `OvertimeQueryPort`, `Pph21CalculatorPort` and `BpjsCalculatorPort`, so the nine backbone modules interlock — remove one and the run is wrong rather than smaller. The periphery is a pure consumer set and **`payroll.md` consumes no expense port at all**, with asset's payroll charge still deferred at A-052, so not one port breaks by deferring it. Reports and dashboard defer too, because the statutorily mandatory outputs are ExportDefinitions registered by tax-pph21 and bpjs and travel with their own modules. | Active |
| A-169 | 2026-08-04 | The tenant onboarding sequence and the first-production-release gate are collected here | `docs/00-overview/implementation-roadmap.md` §7 and §8. Neither existed anywhere: provisioning is owned by system-administration (the tenant row, one company, one branch, the admin user), the nine import definitions by import-export, **and the order by nobody** — deriving it is what surfaced A-165. Both are dependency orderings, which is this file's subject on a different axis, and both fit in one table each; a new ops file for them would add a file to the handbook for a section's worth of content. Two facts the derivation exposed: **step 9 is one-way** — `TAX_OPENING_YTD_LOCKED` refuses an opening YTD seed after a run closes, already stated by BR-TAX-015 and quoted rather than claimed — and **`ci-cd.md`'s `migrate:forward` bootstrap skip means testing-strategy §14.1 rule 2 is inactive for the entire build**, so the riskiest migration in the project's history is the first one that check guards rather than the last. | Active |
| A-170 | 2026-08-04 | The roadmap carries no status column, and marks its own shelf life | `docs/00-overview/implementation-roadmap.md` §10 and §10.1. **`PROGRESS.md` tracks handbook generation and the implementation repositories track implementation; this file states order, never state** — the moment it carries status it becomes a second `PROGRESS.md` and rots, which is the likeliest failure mode of any document called a roadmap. The second likeliest is being discarded whole once half of it is stale, so §10.1 marks shelf life per section: §4, §5, §6 and §8 are spent once executed, while **§7's onboarding sequence and §9.2's dependency clusters never go stale** — the sequence is as true for the five-hundredth tenant as the first, and two deferred items that are one build stay one build. §9.2 is the reading no single module could make: nine clusters where separately-scheduled post-GA items would build the same capability twice, several of them saying so in their own words. | Active |
| A-171 | 2026-08-04 | ERD domain grouping is presentation, not architecture | `docs/04-database/erd-overview.md` §4 partitions all 115 tables into ten domains so that no diagram exceeds the `HANDBOOK_SPEC.md` §13 ceiling of ~40 nodes. The partition is a **legibility device with no architectural meaning**: it is not a module boundary, not a schema namespace, not a deployment unit, and nothing may be derived from two tables sharing a domain. ADR-0001's module ownership is the only boundary that binds, and §6 of that document is the section that respects it. Recorded because a ten-way grouping of the whole schema is exactly the kind of artifact a later reader mistakes for a decision. | Active |
| A-172 | 2026-08-04 | FK counts exclude the RLS discriminator | `docs/04-database/erd-overview.md` §6.1 reports two bases and uses the **net** one throughout: `database-conventions.md` §3's `...tenantId` spread emits a real `.references(() => tenants.id)` on each of the 96 RLS-class tables, and those 88+ edges are excluded from every count. The reason is that they are present by class definition, identical across every module, and removed by no extraction — counting them puts `tenants` at the top of the fan-in table by 2.4× and buries the extraction-cost signal the inventory exists to carry. **Both numbers are stated** rather than one silently chosen, because the original document reported only the net figure while its maintenance rule instructed a future reader to treat any differing recount as a defect — guaranteeing that a correct inclusive recount would be reported as a bug. | Active |
| A-173 | 2026-08-04 | `created_by` / `updated_by` / `deleted_by` carry no foreign key | ~250 instances across ~96 tables, the largest deliberate non-FK set in the schema, now `docs/04-database/erd-overview.md` §7. Cost is secondary — a constraint check on the system's hottest write path × 250. The deciding reason is that the only defensible policy for an optional actor column is `ON DELETE SET NULL`, which means removing a user **erases their attribution across every row they ever touched**; §7's `audit_logs` row already refuses exactly that failure. Consequence: `database-conventions.md` §8's clause *"`SET NULL` only for optional actor/reference columns (`created_by`)"* named a column class with zero instances, and `leave.md`'s lone `.references()` override was a defect rather than a precedent. **Both discharged 2026-08-05 (MANIFEST row 70)**, and the rule generalised past the audit triple in the same sweep: `audit_logs.actor_user_id`, `approval_actions.actor_user_id` and `impersonation_sessions.target_user_id` are now `erd-overview.md` §7 rows on the same reasoning — an actor column in a trail must outlive the account it names, and `NULL` already means *the system did it*, so a nulled row is indistinguishable from one that was never attributed. | Active |
| A-174 | 2026-08-04 · **amended 2026-08-05** | The handbook carries executable artifacts — now two | **Amendment:** `scripts/guide-check.mjs` joins `scripts/erd-check.mjs` (`ai-development-guide.md` grilling). Same shape — Node stdlib, no `package.json` — and the same justification generalised: **a document that claims to be derived must be checkable, or the claim decays into a memory.** The guide asserts its pair set comes from the ADR corpus and its §2/§6 are live pointers; `G1`–`G7` recompute both and fail on disagreement. It earned itself immediately — writing `ADR-0025` broke the build until the ADR had a pair, which is the behaviour the file describes. The class recorded here is therefore no longer "one script" but **"a check per derived document"**, and the next such document is expected to carry one. Original entry follows. `scripts/erd-check.mjs` — Node stdlib only, no `package.json`, no dependency tree — derives `docs/04-database/erd-overview.md`'s census and FK graph from the Drizzle blocks and fails on any disagreement (`C1`–`C9`). A documentation repository holding runnable code is a new artifact class and is recorded as such. It was accepted because that file's value **is** its counts, seven of them were wrong, and every one is a fact a script settles in a second — while its own maintenance rule already told readers to recount, an instruction that was unactionable by hand. Mermaid *syntax* validation deliberately stays an uninstalled `npx` one-liner: vendoring it would buy a failure class that has never occurred here, and the three reversed-crow's-foot defects regrilling found all parse cleanly. | Active |
| A-175 | 2026-08-05 | The reading protocol resolves contracts by symbol, not by section | `docs/08-ai-guide/ai-development-guide.md` §2.3. An agent implementing a feature reads the entry module document in full and its dependencies **not at all** — it greps the named port, event, or code. The alternative, citing the section that declares it, is unimplementable today: port declarations appear under five different headings (`4.2 Ports served`, `4.3 Ports consumed`, `4.4 Ports and reads consumed`, `4.5 Ports consumed`, `4.2 Ports (the consumer contracts)`), `employee.md` declares `EmployeeStatusPort` in §13, and `leave.md`, `payroll.md` and `holiday.md` have no Ports heading at all. Standardising 28 documents to make a citation work is a large change to buy what one grep already does. Consequence: the inconsistency is **not** a defect to fix — the protocol is deliberately indifferent to it. Recorded so a later tidy-up does not mistake the grep for a workaround and "fix" it into a fragile section reference. | Active |
| A-176 | 2026-08-05 | The reading budget is ~3,200 lines before the module document | `docs/08-ai-guide/ai-development-guide.md` §2.1 and §10. Measured, not chosen: `CONTEXT.md` 360 + the whole 24-file ADR corpus 1,900 + the stack's architecture and coding-standards pair + `naming-conventions.md` 238 + the stack's cross-cutting anchors. The ADR corpus is read **whole** because 1,900 lines is about 1.4 module documents and no safe subset exists — the most-cited ADR appears in 24 of 28 module documents and the rest is a flat tail, so any "top N" rule silently drops a decision some module depends on. Registries (`error-catalog.md` 457, the naming-conventions key registries, `environments.md` §6) are **grepped**, which is what keeps the number at 3,200 rather than 3,700. Consequence: the budget is a live constraint on the handbook, not a description of it — if the anchors grow past ~3,500 the answer is to split an anchor, because the alternative is agents quietly skipping one. | Active |
| A-177 | 2026-08-05 | A worked schema example in a non-owning document is a second table definition | `docs/03-standards/naming-conventions.md` §2.1, MANIFEST row 70. The `pgTable(` example there declared `payroll_runs` — a real table owned by `payroll.md` — and because that file sorts first, any tool resolving ownership by scanning attributed the table to it and corrupted `erd-overview.md` §3's census to 20/68/27 (check `C9`). **Renaming the example to a fictitious table does not fix it**: tried during the sweep, and the fictitious table was immediately counted as a 116th real table with no diagram node (check `C1`). The fix is to make the example **unparseable** — angle-bracket placeholders — which teaches the same camelCase↔snake_case mapping and matches no parser. Generalised: a document that owns no tables must not contain a syntactically valid `pgTable(` call, and the same applies to any future worked example of a parseable declaration form. The correction note in that file deliberately does not restate the old line, because spelling it out re-creates it — which happened once during the sweep. | Active |
| A-178 | 2026-08-05 | Every implementation repository holds two `CLAUDE.md` files, and the handbook's is the wrong one | `docs/08-ai-guide/implementation-claude-md-template.md` §1, `ai-development-guide.md` §2. `ADR-0025` mounts the whole handbook repository at `docs/handbook/`, which brings `docs/handbook/CLAUDE.md` with it — a file that instructs a *handbook author* to read `PROGRESS.md`, generate one large document per task, and never bulk-generate. An implementer following it does something entirely unrelated to writing product code, and nothing about the filename warns them. **Found because `ai-development-guide.md` cited `CLAUDE.md` as the authority in six §7 pair rows**, written one session earlier, before the submodule decision existed to make the citation ambiguous. All six now cite the architecture document or ADR that owns the rule. Consequence: the hard constraints are stated **once each, in the layer that owns them** — `mobile-flutter.md` §1 and `admin-nextjs.md` §1 already did; `backend-nestjs.md` §1 gained the Prisma/TypeORM/MikroORM prohibition, which had existed only in `ADR-0013`'s rejected-alternatives list and in the handbook project's own `CLAUDE.md`. Each template block states the two-file hazard inline, because a reader who notices two files with one name looks in the one they already have open. | Active |
| A-179 | 2026-08-05 | The reading protocol is loaded by `@`-import, not requested by prose | `docs/08-ai-guide/implementation-claude-md-template.md` §2.1 and §3–§5. Each `CLAUDE.md` opens with `@docs/handbook/docs/08-ai-guide/ai-development-guide.md`, which in Claude Code loads the guide at session start — turning the mandatory read from an instruction an agent may skip into a fact it already holds. 355 lines resident against the guide's own 3,300-line pre-work budget, and §7's fifty pairs are the part that earns it: pairs are needed *while writing the line*, which is exactly when a read-once-at-startup document has been forgotten. **Two limits, both stated rather than assumed.** The import is a Claude Code feature; an agent without it gets only the prose line beneath, which is therefore written to stand alone. And the import's failure mode is **silence** — an uninitialised submodule resolves it to nothing and the session looks normal — so the same fact is carried by three independent signals: the import, the prose instruction to stop if `docs/handbook/` is empty, and ci-cd gate `C11`. | Active |
| A-180 | 2026-08-05 | The handbook repository's branch protection, and why two of its settings are forced | `docs/07-operations/ci-cd.md` §13, `ADR-0025` §2. That ADR requires `hris-handbook` to carry protection and names no numbers; these are them, applied 2026-08-05 and **verified by attempting a rejected push rather than by reading the API back**. `main`: pull request required, force-push and deletion blocked, linear history required, `enforce_admins` on, **zero** required approvals, and the **`handbook` status check required** *(amended 2026-08-05, A-181 — this entry originally read "no required status checks", which stopped being true about an hour after it was written, and **nothing detected that**: it is a claim about GitHub settings rather than about files, so neither check script can see it. `ci-cd.md` §435's C10 exists for exactly this and does not exist here.)*. `develop` carried the same force-push and deletion block until it was deleted (below). One setting still deviates from §13 and it is not a choice. **Zero required approvals** — there is one collaborator, and GitHub forbids approving one's own pull request, so §13's *"One approving review"* would lock the repository rather than review it; it becomes `1` the day a second engineer joins, and that is the trigger. **Required status checks were empty until the workflow existed** — a required check that no workflow reports blocks every pull request forever, so the ordering was forced: build `.github/workflows/checks.yml`, let it report once, then require it (A-181, discharged same day). **`enforce_admins` on** is not a deviation but is the load-bearing setting: it is the only one that closes the threat `ADR-0025` §2 names, because the agent that ADR warns about holds the maintainer's admin token — with the admin bypass on, the protection is decorative. Consequence: `main` is unreachable except by pull request, by the maintainer and by every agent alike. **Recorded here because getting to it took a wrong turn worth not repeating** (issue #1): the repository was briefly put on gitflow, which `naming-conventions.md` §12 forbids by omission — its branch types are `feat`, `fix`, `refactor`, `docs`, `test`, `chore`, `perf`, `ci`, with no `develop`, no `release/`, no `hotfix/` — and which `ADR-0019` §1 had already decided against by name. The conflict surfaced first as `ci-cd.md` §430, and §430 was amended to accommodate gitflow before either anchor was read. **A dependent document needing an edit to stop disagreeing with a decision is the signal to re-read the anchors, not to edit them** (`CLAUDE.md` rule 4). | Active |
| A-181 | 2026-08-05 | The handbook repository has its own CI, and `ci-cd.md` does not govern it | `.github/workflows/checks.yml`, issue #3. **`ci-cd.md` specifies no pipeline for this repository** — every gate in it, C1–C13 and G1–G22, is scoped to *"all three"* or to a named implementation repository, and the one paragraph mentioning `hris-handbook` (§13) was written the same day and covers branching, not builds. This workflow is therefore a new decision recorded here, not an appeal to that document. It runs `erd-check` and `guide-check` on pull requests into `main` and on pushes to `main`. Three choices are load-bearing. **No path filter**, which looks like an obvious optimisation and is a trap: a required status check that is skipped never *reports*, and GitHub reads "never reported" as "not yet passing", so the merge blocks forever — the job takes seconds, and running it on changes it cannot affect is far cheaper than that outage. **`actions/checkout` pinned by commit SHA**, per `security-standards.md` §137 and `ci-cd.md` §375; both clauses are unscoped, so they bind here, and the version is named in a comment so bumping it is a deliberate commit. **No `actions/setup-node`** — both scripts are Node stdlib ESM with no dependencies and no `package.json`, and the runner ships Node, so this is one fewer third-party action to pin, audit, and keep current. Adding the `handbook` job as a required status check discharges the gap A-180 records; **the job's `name:` is that check's identity**, so renaming it silently unbinds the requirement, which is the failure `ci-cd.md` §435's C10 exists to catch and which nothing catches here yet. **Stated limit:** this gates mechanical drift only. Nothing in it would have caught the gitflow excursion in A-180, because no script detects an anchor that went unread — the value is bounded to what the two scripts already compute. `naming-conventions.md` §12's rule that a squash-merged **pull-request title** must be a Conventional Commit binds this repository too and is **not** gated; a bad title becomes a permanent commit message, and force-push is now blocked. | Active |
| A-182 | 2026-08-05 | The three implementation repositories have **no** branch protection, and the handbook assumed a paid plan without ever saying so | `docs/07-operations/ci-cd.md` §13, `ADR-0025` §2, issue #8. **GitHub Free offers neither branch protection nor rulesets on a *private* repository** — both are free only on public ones. `hris-api`, `hris-admin`, and `hris-mobile` are private (product source of a commercial SaaS), so §13's *"Protection applies to administrators"*, its one approving review, and **C10**'s branch-protection drift check are all currently unimplementable there. `ADR-0025` §2's *"like the other three"* reads as though the other three already had protection; none of them did, because none of them existed. **What still works, and it is more than it sounds:** squash-only merge with rebase and merge-commit disabled, and auto-delete on merge, are *repository* settings rather than protection and applied cleanly; and CI runs normally on private repositories, so gate **C11 reports on every pull request — it simply cannot block one.** The gap is the enforcement, not the signal. **Accepted for now rather than paid for or worked around**, on a bounded argument: today each repository holds six files and no code worth protecting, and the threat `ADR-0025` §2 actually names is an agent rewriting *the anchors* — which live in `hris-handbook`, public, protected, and verified (A-180). A force-push to `hris-api` is recoverable from any local clone; a force-push to the handbook was not. **Trigger, explicit: install protection before the walking skeleton lands** (`implementation-roadmap.md` §4.1), because that is the commit where these repositories stop being empty. Three ways out, in order of preference: GitHub Pro (~$4/month, restores every setting the handbook assumes), an organization on a plan that includes it, or public repositories. Recorded because a handbook that specifies C10 while its own repositories cannot run it is stating a gate it does not have.

**Closed 2026-08-05 by the third route: all three repositories are public.** The maintainer had declined it hours earlier and reversed on the trigger firing, which is what a trigger is for. Protection now matches `hris-handbook` exactly — pull request required, zero approvals, `enforce_admins`, `gates` as the required check, linear history, no force-push, no deletion — and was **verified by attempting a direct push and having it rejected** (`protected branch hook declined`, naming the missing check), not by reading the API back. The reversal was preceded by a history scan of all three repositories for key material, provider tokens, and committed `.env` files: none, and the only credentials present are the local-development literals in `docker-compose.yml` and `docker/postgres/init-roles.sql`, which pass environments.md §5's own test — *"would publishing this in the repository be a problem"*.

Two gates become implementable that were not, and both are enabled: **C3** (secret scan) as GitHub secret scanning with **push protection**, which is the control that actually matters now that the source is public — it refuses the commit rather than reporting it afterwards; and **C10** (branch-protection drift), which now has a protection to drift from. Two consequences worth naming: Actions minutes are no longer metered, and the `ADR-0025` submodule URL stays HTTPS for a simpler reason than before — it needed no credential when a public submodule sat inside a private repository, and it needs none now that neither is private. | Resolved 2026-08-05 |
| A-183 | 2026-08-05 | The RLS policy template's unset-variable promise was false on every pooled connection after the first | `docs/04-database/database-conventions.md` §9.2, `ADR-0002`, multi-tenancy §5 **L2**. The template compared `tenant_id` to `current_setting('app.tenant_id', true)::uuid` and three documents promised that an unset variable yields **zero rows**. It does — once. `set_config(..., true)` is transaction-local, so at commit the GUC reverts to its *reset value*, which for a custom setting that has been set at least once on that backend is the **empty string**, not `NULL`; `''::uuid` then raises `invalid input syntax for type uuid`. Behind a connection pool every connection has served a tenant, so in the steady state a query that forgets `set_config` **errors** instead of reading nothing. Fail-closed either way — no data crosses — but a `SYS_INTERNAL` where the leak matrix asserts an empty result, which is why L2 failed against the specified policy the first time it was executed. Corrected in place to `NULLIF(current_setting('app.tenant_id', true), '')::uuid`, which yields `NULL` for both the never-set and the reset-to-empty case. Found by running it (`implementation-roadmap.md` §4.1's stated purpose), not by reading it. | Resolved 2026-08-05 |
| A-184 | 2026-08-05 | core-schema §10's bootstrap migration order could not be applied | `docs/04-database/core-schema.md` §10. `0004_rbac` created `user_roles`, whose `company_id` references `companies.id`, one migration before `0005_org_core` created `companies`. The pair does not commute and the dependency runs one way only, so the fix is to swap them; nothing in `org_core` references an RBAC table. Undetectable by reading — `drizzle-kit` reported it the moment the migrations were generated against the list. | Resolved 2026-08-05 |
| A-185 | 2026-08-05 | The walking skeleton implements a *subset* of `authentication.md`, and the omissions are named rather than implied | `hris-api`, `implementation-roadmap.md` §4.1 item 2. Built: login with the tenant picker, the pre-tenant `hris_auth` lookup path, lockout, rate limiting, the Ed25519 access token, the full guard chain, and `GET /auth/me`. **Not built, deliberately**: refresh and rotation (BR-AUTH-004/005), logout, session and device management, password reset and change, invite accept, and device registration — every session the skeleton mints is a web session with `device_id NULL`. A refresh token *is* issued, because `sessions.refresh_token_hash` is `NOT NULL`; there is simply no endpoint that consumes it yet. `/auth/me` omits the `name` and `employeeId` its §7 contract declares: both live on `employees`, which the employee module owns, and reaching into another module's table is the `ADR-0001` violation the architecture exists to prevent. They arrive with that module's port. Recorded so that the gap between §7 and the running surface is a list rather than a discovery. | Resolved 2026-08-06 — the omission list is closed by A-191 (auth remainder built, `hris-api` PR #7); only the `/auth/me` `name`/`employeeId` gap remains, and it waits for the employee module by design |
| A-186 | 2026-08-05 | A host service on a default port silently shadowed the Compose one | `hris-api/docker-compose.yml`. A Homebrew Redis on `127.0.0.1:6379` coexisted with Docker's `0.0.0.0:6379` publish, and the application connected to whichever answered loopback — the host one. Everything worked, against the wrong store, and the only symptom was that `docker compose exec redis redis-cli KEYS '*'` came back empty. Both services now publish to `127.0.0.1` explicitly, which turns the collision into `docker compose up` failing with *address already in use*. A loud failure at startup beats a silent one that survives an entire test session. | Resolved 2026-08-05 |
| A-187 | 2026-08-06 | Three implementation decisions in the overtime pricing calculator the module document leaves open | `hris-api/src/modules/overtime/domain/overtime-pricing.ts`, `overtime.md` §4.5, ADR-0018. Track 2's first calculator, and the handbook is silent on three points the code cannot be. **(1) `multiplierHours` rounds once, at the end**: the exact tier sum `Σ minutes × factor` divides by 60 and rounds to the column's `numeric(8,4)` — half-up — rather than rounding each tier term before summing. §4.5 writes the formula as a sum and stores one figure; the `tiers` trace carries integer minutes, not per-tier hours, so there is no stored intermediate a per-term rounding would have to reconcile with. Both §4.5 worked examples yield identical results either way. **(2) TOIL days derive from the pinned 4dp figure**, not the exact rational: `multiplierHours × 60 / H` to `numeric(6,2)` half-up (leave.md's ledger scale), so an auditor can reproduce the ledger entry from the occurrence row alone. Half-up on both because the document fixes no direction and §1 names rounding *down* as the legally exposed one. **(3) A malformed rate row set** (gap, overlap, bounded top tier, fractional-minute bound, non-contiguous indexes) fails as `OVT_RATE_RULES_MISSING` with the defect named in `details.reason`, not as a new code: the catalog defines the code as "no effective multiplier row set", a set the walk cannot trust is that condition one notch later, and only a broken migration can produce the state. Structural vectors pin all three (ADR-0018 §3: "rounding direction and unit" is exactly what they exist to pin). | Active |
| A-188 | 2026-08-06 | Six implementation decisions in the BPJS contribution calculator the module document leaves open | `hris-api/src/modules/bpjs/domain/bpjs-calculator.ts`, `bpjs.md` §4.3/§4.4, ADR-0018. **(1) The month credit applies at the (program, payer) bucket, allocating the main line first and the dependent surcharge after.** BR-BPJS-010's `mtd` carries one charged figure per program and payer, and a prior run's surcharge is inside it — crediting only the main line re-charges a settlement's surcharge in full. **This one is recorded as a found defect, not a preference:** the first implementation credited the main line only, no vector covered dependents-plus-credit, and writing this assumption down is what surfaced it — fixed and pinned by two vectors (`settlement-credits-the-dependent-surcharge`, `partial-credit-allocates-the-main-line-first`) before anything merged. **(2) Rounding to `contribution_rounding_unit` is half-up** — BR-BPJS-014 parameterizes the unit but not the direction; the ⚠️ VERIFY on that rule covers both, and the structural vector pins the choice so the advisor can flag it. **(3) The JP ceiling is attained on the payment-date birthday itself** (`paymentDate ≥ birthDate + ceiling years`), with a Feb-29 birth clamping to Feb 28 in a non-leap year — BR-BPJS-006 says "passes the statutory age" without fixing the boundary day; vectors assert both sides of it. **(4) Zero-amount lines are omitted** — a fully-credited or zero-rated line would be noise in `payroll_run_lines`; the trace records why it is absent. **(5) Calculator warnings are lowercase snake codes** (`company_not_registered`, `jkk_risk_class_missing`) — they are not catalog errors by §11's explicit choice, and the casing keeps them out of the error-code namespace. An unconfigured wage floor warns at preflight only, not once per employee. **(6) An incomplete parameter slice fails as `PAY_PARAMETER_MISSING` from inside the calculator too** (missing rate row, risk row, or scalar), naming the missing row in `details.parameter` — the same reuse §11 sanctions for the adapter, applied one layer down so no path computes on a partial version. | Active |
| A-189 | 2026-08-06 | Seven implementation decisions in the PPh 21 calculator the module document leaves open | `hris-api/src/modules/tax/domain/pph21-calculator.ts`, `tax-pph21.md` §4.4/§4.6, ADR-0018. **(1) The monthly figure floors at zero** — `max(0, rate × cumulative − MTD withheld)`: refunds are the annual path's job, and BR-TAX-009 already names the double-refund hazard of recovering monthly. **(2) PPh 21 rounds half-up to its unit** — BR-TAX-016 fixes only PKP's direction (down); the withholding's direction is pinned by vector so the advisor can flag it. **(3) The monthly gross-up closed form walks the TER bands until the solution lands in the band that priced it** — the allowance is taxable bruto, so `A = e(r·cum − mtdW)/(1 − e·r)` under one band can push the cumulative into the next; the walk terminates because bands ascend. The rounded allowance IS the withholding line, so the pair stays exactly equal and the payslip foots. **(4) The annual gross-up solves `A = W(A)` by fixed-point iteration** rather than a per-layer closed form — PKP's floor-rounding makes the closed form messier than it is worth, the marginal rate is under 1 so the map contracts, and the fictional vector's fixed point (6,300,000 reproducing itself through the floor) pins the behaviour. Against a refund the allowance clamps to zero (§9). **(5) The annual credit is `ytd.pph21Withheld + mtd.pph21Withheld`**, and annual gross likewise adds the MTD bruto — a same-month sibling run is approved but not yet in the ledger, and counting it only on one side of the equation would double- or under-count December. **(6) Biaya jabatan composes as `min(pct × gross, monthly_cap × monthsEmployedInYear, annual_cap)`** — the two caps and the months field all exist in the contract and this is the composition that uses all three; the values and the true composition carry BR-TAX-008's ⚠️ VERIFY. **(7) The severance tariff is cumulative over the year's final income** (`bands(ytd.finalIncome + this run) − ytd.pph21Final`, floored at zero, no NPWP surcharge) — the `finalIncome`/`pph21Final` YTD fields exist in the contract precisely for this, mirroring the TER credit pattern; cumulativity across tranches carries BR-TAX-011's ⚠️ VERIFY. The non-NPWP surcharge multiplies a positive withholding only, never a refund. | Active |
| A-190 | 2026-08-06 | Five implementation decisions in the payroll pipeline calculator the module document leaves open | `hris-api/src/modules/payroll/domain/payroll-calculator.ts`, `payroll.md` §4.5, ADR-0012. **(1) The pipeline composes the sibling calculators in-process through their facades** — stage 4 calls `computeBpjs`, stage 6 calls `computePph21`, and the tax path derives inside (`final_settlement` or a December payment date, BR-TAX-008). The two siblings never call each other; pipeline order is the whole of the coupling, exactly as tax-pph21.md §13 words it — which makes ADR-0012's "fixed snapshot in, exact payslip out" golden class vectorable end to end before any run machinery exists. **(2) Stage rounding is half-up at a single `line_rounding_unit`** — BR-PAY-012 says each stage rounds by its own effective-dated rule and names no rules; one parameterized unit is the smallest structure that upholds the invariant that matters (the net is a sum of already-rounded lines), and the per-stage registry arrives with the module. **(3) The overtime line classifies `non_wage` + `regular`** — non_wage is forced (overtime pay inside `upah sebulan` would feed its own basis, the same cycle A-034 kills for the tax allowance); `regular` is the tax reading pinned by vector, ⚠️ VERIFY with the classification rules. **(4) The proration factor clamps to [0, 1]** and multiplies proratable lines only, never the bases — BR-PAY-027's wire is pinned by a vector that pays half a month while charging a full month of contributions. **(5) Deliberately absent from the track-2 slice, named rather than implied**: sick-leave wage scaling (BR-PAY-014), retro deltas, `rate_of_base` resolution (the adapter prices components), and run-level aggregates — each arrives with the module in the business backbone. | Active |
| A-191 | 2026-08-06 | Implementation decisions in the authentication remainder (refresh, sessions, devices, credential flows) the module document leaves open | `hris-api/src/modules/auth`, `authentication.md`, ADR-0004, A-185. **(1) Settings absent → the `auth.*` registry values ship as named constants** (`AUTH_DEFAULTS`, each carrying its settings key): max devices 1, replacement policy `self_service`, sliding 30 d mobile / 7 d web, absolute 90 d / 30 d / 12 h — the swap to tenant-tunable is a mechanical find when settings lands. BR-AUTH-006's sliding half, which the skeleton could not express, is now enforced in the refresh path. **(2) The §4 Redis structures as written:** the grace cache stores the raw successor pair for 10 s keyed by the *old* hash (BR-AUTH-005's idempotent replay); the used-token history stores only `{sessionId, tenantId}` for 7 d and is what family-revoke detection reads. Both platform-level keys — a refresh arrives before any tenant is proven. The concurrent-rotation sliver (hash in *neither* store because history is written post-commit) answers `AUTH_REFRESH_INVALID`, never a family revoke: fail-safe, and the client's retry lands in the grace cache. **(3) One non-committal code on refresh:** user status ≠ active and tenant `missing` both answer `AUTH_REFRESH_INVALID` (§7 registers no other fitting code, and distinguishing them is an oracle); a *revoked* session presented with its current hash answers by its reason — `device_revoked` → `AUTH_DEVICE_REVOKED` (the terminal client flow needs the distinction, offline-sync §9), anything else → `AUTH_REFRESH_INVALID`. §3's per-session refresh budget is keyed by token SHA-256 (1:1 proxy). **(4) Email delivery is a named omission:** reset/invite token rows are created with full mechanics, but the links ride with the notification module (spine order 6) — the raw token never enters the outbox (`token` is on the redaction registry; payloads are pointers), so delivery will take the raw value in-process when its consumer exists. The `admin`-policy blocked-replacement notification and the §12 purge crons (worker machinery, ADR-0010) defer the same way. **(5) Password policy floor:** the breached-list ships as an embedded top-slice of ≥10-char corpus entries and the derived-string denial covers the email local part and tenant name (≥4 chars); the full corpus arrives when the policy becomes tenant-tunable. Policy entries are field-level codes — `AUTH_PASSWORD_BREACHED`, `AUTH_PASSWORD_DERIVED`, registered in the catalog this session; length reuses `VAL_TOO_SHORT`/`VAL_TOO_LONG`. **(6) §7's admin rows (`users/{id}/unlock`, `users/{id}/reset`) answer a missing key with 404**, so they check the permission imperatively under `@AuthenticatedOnly()` — `@RequirePermission`'s refusal is a 403 that would distinguish the probes; the four session/device keys resolve lazily (a thunk) so own-scope calls never pay resolution (ADR-0005). Unlock clears **both** locks (BR-AUTH-013 status + the timed Redis counter), and a successful reset clears the timed lockout ("unlock via reset", error-catalog §5). **(7) Web cookie:** `hris_rt`, `HttpOnly` `Secure` `SameSite=Strict`, path-scoped to `/api/v1/auth/refresh`, persistent (`Max-Age` 30 d) only when remembered; the §5 server-side `Origin` allow-list check waits for the CORS wiring that the admin web's first deployment forces — until an allow-list exists there is nothing to check against. Reset-request returns its 200 *before* any lookup starts (fire-and-forget), which is the BR-AUTH-010 timing-equality mechanism. Invite-accept reuses `password_reset` as its session-revoke reason — §4's vocabulary has no invite entry and a fresh invitee has no sessions to revoke. E2E supertest per §14 stays at the skeleton's test-tier scope; the unit suite (43 new tests) was mutation-proven on the grace window, the family revoke, the replacement ownership check, and the change-spares-acting rule. | Active |
| A-192 | 2026-08-06 | Implementation decisions in the audit-log store, sensitive-read channel and read API the module document leaves open | `hris-api/src/modules/audit`, `audit-log.md`, `ADR-0007`, `ADR-0010`, `ADR-0011`. **(1) Scope is the store, channel 3 and the read surface; channels 1 and 2 are named omissions with their blockers stated.** Channel 1's hook lives on `TenantScopedRepository` (§4.2, backend-nestjs §8.2) — a class that does not exist, whose other four invariants have no implementations yet, and whose first real consumers are organization's seven audited tables; **not one of §4.2's registered tables exists in the API today**, so building the hook now would be designing a base class against zero callers and reshaping it at the first one. Channel 2 needs the outbox relay, which is BullMQ machinery that has not been wired (the `domain_events` writer exists; nothing dispatches). `cron.audit.anchor` and `cron.audit.archive` defer with the worker, GCS and the Cloud Logging sink — **the anchor computation ships without its emit**, and half of BR-AUD-009's amended witness (a row in the same database as the evidence) is worse than a visible gap. `AuditQueryPort` (§4.4) is not built: it was added on first-caller discipline and reports.md is its first caller. **(2) `occurred_at` is left to the column default on every insert** — §9 makes it one clock for every pod, and a `Date` minted in-process would put pod skew straight into the column the cursor orders by. `NewAuditLog.occurredAt` exists only so channel 2 can carry fact time. **(3) The anchor day is a uuidv7 id range, UTC** (ADR-0010 makes crons platform-level UTC): the boundary is the smallest legal v7 for a millisecond — version nibble 7, variant nibble 8, zero randomness — so `[lo, hi)` is exact and one day's upper bound *is* the next day's lower bound. The digest folds in **every column and the row count** (so deleting a row and inserting a replacement does not restore it) and canonicalises jsonb key order (a dump-and-restore reading as tampering costs an investigation). `verify` chains against the digest **stored on the anchor**, not a re-resolved predecessor — the question is whether this day's rows still hash to what was certified, and re-deriving the link smears a neighbouring day into the verdict. Back-fill chains to the newest anchor *strictly before* the day, never to "yesterday", which is what keeps §9's skipped-day chain continuous. **(4) UC-AUD-004 runs read → audit → respond.** Auditing first would let an unfiltered first page return its own access record; fail-closed is unaffected because both statements share the request's transaction, so UC-AUD-003's "the insert precedes the response" still holds. Nothing is caught in `sensitiveRead` — a `try/catch` would turn the module's central promise into a best effort silently. **(5) Cursor pagination arrives with this module**, the first cursor feed in the codebase (api-standards §6): opaque base64url `[occurredAt, id]`, row-value keyset `(occurred_at, id) < (…)` driving `idx_audit_logs_cursor` backwards, `limit + 1` for `hasMore`, `meta` = `{ nextCursor, hasMore }` and **never a total** (§5.4). Every malformed cursor — bad base64, wrong arity, unparseable date, non-uuid — is `VAL_INVALID_CURSOR` at decode, so no client typo reaches the database as a cast and comes back as the server's 500. **(6) `@RequirePermission` here, unlike auth's two admin rows:** §2 makes audit a *tenant-level duty* with no per-row data scope, so there is no existence to hide and 403 is the honest refusal. **(7) Two cross-field rules sit in the use cases, not the DTO** (backend-nestjs §6): `from < to` needs a second field, `day ≤ yesterday` needs the clock. The `day` **format** check is load-bearing rather than decorative — the range test is a string comparison, correct for `YYYY-MM-DD` and quietly wrong for anything else, so without it `'banana'` answers `VAL_OUT_OF_RANGE`. **(8) Found here, resolved 2026-08-06 (issue #21): BR-AUD-005 contradicted §4.2 on money columns** — §10's registry masks `/amount|salary|wage/i` while `salary_histories`, `asset_incidents`, `job_offers` and four others are registered "full diffs" precisely so those figures appear. §10 is a telemetry list (Pino/Sentry/span lint); the audit masker needed a different reading. **Settled in `audit-log.md` BR-AUD-005 as a three-layer rule** — ADR-0016 column type, a credential floor, then the table's own §4.2 note — before the organization module writes the diff builder, as required. **(9)** `AUD_` still registers zero codes (§11); the module's failures are `VAL_`, `AUTHZ_` and 404 throughout. Unit suite 52 tests, mutation-proven on the row-hash column set, the read-before-audit order, the fail-closed insert and the back-fill chain; eight Testcontainers assertions cover what only a real database can answer — the BR-AUD-001 revokes, RLS, BR-AUD-003's dedup index, and §9's same-millisecond keyset tie-break — each also mutation-proven. | Active |
| A-193 | 2026-08-06 | Implementation decisions in the settings module the module document leaves open | `hris-api/src/modules/settings`, `settings.md`, `ADR-0010`, `ADR-0012`, `ai-development-guide.md` §5. **(1) Fourteen of §4.2's fifty-five rows are deliberately not registered, cut by one mechanical rule: a default carrying `⚠️ VERIFY` states a regulation-dependent number, and §5 forbids an assistant typing one anywhere — a fixture asserts, a *seed runs*, so a placeholder here would configure real tenants against a figure nobody signed.** The absentees are the four `overtime.*` hour limits, the four `payroll.*` proration and overtime-basis keys, `bpjs.wage_floor`, `leave.carry_over_expiry_months`, `attendance.selfie_retention_months`, `holiday.cuti_bersama_deducts_leave`, `recruitment.candidate_retention_days` and `announcement.acknowledgment_retention_days`; each is registered by the session that builds its module, once track 3 has signed the number. The other forty-one ship now, including keys whose module does not exist — that is the registry working, not speculation: definitions are seeded at release so a module can arrive *reading* a key admins have already been able to set. **(2) `setting_values.branch_id` carries no foreign key.** `branches` belongs to organization; the column and the scope CHECK ship because §4.1 declares them, and the FK is one line in the migration that creates the table. **(3) BR-SET-008's `direction` is carried on the definition and enforced through `min`/`max`.** Every registered direction-constrained key states its constraint as a floor or a ceiling — `auth.password_min_length` may only rise from 10, `import-export.max_rows` may only fall from 10 000, `auth.lockout_attempts` lives in `[3, 5]` — so the bound already *is* the rule, and §6 renders the direction ("minimum 10 — platform floor"). Two enforcement paths for one sentence is two things to keep in agreement. **(4) A same-day supersede drops the earlier row instead of closing it.** BR-SET-005 says correcting a live value is a supersede from today, but a row that *began* today would close to `[today, today)` — an empty interval the exclusion constraint ignores and the history timeline shows as a phantom. History granularity is a day, so the second write of a day is that day's row, which is what §14's "idempotent day granularity" asks for. **(5) An immediate write under an existing schedule closes at the schedule's start** rather than running open-ended: the alternatives are overlapping the constraint (a 409 with nothing useful to say) or silently cancelling a change someone scheduled for next month. **(6) BR-SET-009's bust runs inside the request transaction, not post-commit** — a named narrowing. Nothing in this codebase has a post-commit hook (the whole handler runs inside `TransactionInterceptor`'s transaction), the exposure is the remainder of one handler, and §9 already answers a lost bust with the five-minute TTL. For the same reason a failed bust is **logged, not raised**: raising it would roll the write back, so a Redis outage would stop every settings write to avoid staleness the rule accepts. **(7) The definition-level override is an additional key, not a replacement.** §2 says "overriding", but the route's `@RequirePermission('settings.setting.configure')` is what lets the request reach a handler that can see which key was sent. The matrix comes out identical either way — System Administrator holds both rows, HR Admin holds only the base and is correctly refused `tax.method`. **(8) `/settings/effective` resolves at tenant scope for every caller** until the employee module supplies placement, which is the answer UC-SET-005 already gives for a caller without one. **(9) `settings.sync-definitions` is a script on the migrator credential, not a BullMQ job** — §12's trigger is the release pipeline, so it runs beside `migrate`. Its idempotence had to mean **no writes at all**, not "no net change", or every release would read as a configuration change: the first implementation rewrote 31 of 41 rows on every run because **jsonb does not round-trip key order**, and the integration test is what caught it. `stableStringify` in `shared/` fixes it and is shared with the audit anchor, which needs the identical property for the identical reason. **(10) ADR-0010's outbox writer moved from `auth/infrastructure/` to `src/database/`** — one writer beside the unit-of-work it rides, with each module keeping its own typed event-name port. 76 unit tests plus 12 Testcontainers assertions, mutation-proven on the resolution order, the same-day drop, the validation short-circuit, the as-of cache bypass, the stale-map fallback, the key sort and the scope CHECK. | Active |
| A-194 | 2026-08-06 | Implementation decisions in the organization module the module document leaves open | `hris-api/src/modules/organization`, `hris-api/src/database/tenant-scoped.repository.ts`, `organization.md`, `audit-log.md` §4.2, `ADR-0001`, `ADR-0016`, `implementation-roadmap.md` §4.2. **(1) `TenantScopedRepository` arrives here, and the §4.2 registry arrives as code.** audit-log §4.2 names the class; this module ships the first audited tables in the system, so the base is written against six real callers rather than zero. Modules register their own tables with `registerAuditedTables`, the `registerErrorStatuses` shape, and BR-AUD-005's 2026-08-06 fail-loud gate is a **constructor assertion** — an audited table with no §4.2 entry stops the process at module init, which is the only moment early enough to matter. **(2) A diff omits eight columns the audit row already carries, more precisely than the column does.** `id` is the row's `entity_id` and `tenant_id` is what it is filed under; `occurred_at` is one clock for every pod where `updated_at` is the writer's, and `actor_user_id` + `impersonator_id` are two identities where `updated_by` is one. Repeating any of them lengthens the diff without adding evidence. Everything else diffs in full, per BR-AUD-005's default. **(3) An update whose changed-column set is empty files no row.** A save that moved nothing is not an act, and one row per no-op would bury the trail this log exists to make legible. **(4) BR-AUD-005 layer 1 is implemented as `columnType === 'PgCustomColumn'`.** ADR-0016 decision 2 makes `encryptedText` a Drizzle `customType`, and a custom type is the only thing in this schema that reports it — so layer 1 is exact today with no list to maintain, and tightens to a named check if a second custom type ever arrives. **(5) `employees` and `user_roles` are read directly, and this is the module's one recorded architectural deviation.** ADR-0001 rule 6 makes the owner's port the default path and rule 2 forbids reaching into another module's tables, but employee.md is spine order 3 and its `employee_directory` view — rule 6's own published-view amendment, written for exactly this shape of read — does not exist yet. Every such read carries an `A-194:` comment: three joins across `assignment.repository.ts` and `position.repository.ts`, two counts in `company.repository.ts` (the second being authorization-rbac §9's `user_roles.company_id` archive promise), and `EmployeeLookupRepository`, whose three-column result is already the subset employee.md's facade will expose. The marker greps to the complete swap list; the binding is one provider line. **The holder joins could not have been ports even if employee.md existed** — the employment filter is a predicate inside an aggregate, not a page of rows, which is the case ADR-0001 rule 6's amendment cites when it says a port cannot serve a filter that must run before the page boundary. **(6) The `PeriodLockPort` stub lives in `shared/`.** `implementation-roadmap.md` §4.2 sanctions the fake once for three consumers rather than three times in three modules; attendance.md §4.2 owns the real port, and moving the declaration to that facade later is an import change in three files. Until then every date is open, and organization.md §14's locked-month scenario drives the port rather than the stub. **(7) A backdated correction inherits its predecessor's `effective_to`.** BR-ORG-008 says a move closes the row covering the new date and inserts the successor; it does not say where the successor ends, and open-ended is wrong — a `correction` landing inside a closed interval would then overlap every later placement and the exclusion constraint would refuse the whole act. Inheriting keeps the timeline gapless and leaves the constraint nothing to refuse, which is what makes UC-ORG-003's `correction` kind usable at all. **(8) `ORG_ASSIGNMENT_OVERLAP` is mapped from the constraint violation and never pre-checked, while duplicate codes are pre-checked and never mapped.** Because (7) has the planner close the interval it is about to fill, a `23P01` here means the history moved under the read — two admins moving one employee in the same instant — and no pre-check wins that race. A duplicate code is the opposite: §7 wants a `VAL_DUPLICATE` entry naming the field, and a constraint name cannot say which field it was, so the partial unique index stays the backstop rather than the reporter. **(9) A refusal for want of a tenant-wide assignment raises `AUTHZ_PERMISSION_DENIED`.** §2 requires one to create or archive a company and to configure a job level. The condition is "your assignment does not reach this", which is what the guard's own code already means to a client, so no `ORG_` code was minted for it (error-catalog §1's one-condition-one-code rule). **(10) The org chart does not filter holders on having a user account.** BR-ORG-003's account filter exists for approval resolution — a manager who cannot log in cannot approve a step — while a holder with no login still occupies the seat, and hiding them would render a vacancy that is not one. `holderCount` and `?vacant=` follow the chart's rule, not the resolver's; `directManagers` and `positionHolders` keep the resolver's. **(11) `fk_holidays_branches` is not in this migration.** §4.1 lists both deferred foreign keys, but `holidays` does not exist yet, so holiday's own migration adds it at creation like any ordinary forward reference — fulfilling a deferral needs a table to alter. `fk_setting_values_branches` did land, closing A-193 decision 2. **(12) `VAL_DUPLICATE` joined `shared/shared.errors.ts`'s field codes** — error-catalog §4's platform-level uniqueness code, spelled once now that a module raises it. **(13) §8's coordinate pairing is enforced in the service, against the merged row.** The `ck_branches_coordinates` CHECK says the same thing, but a constraint violation surfaces as `SYS_INTERNAL` and §8 asks for a field entry the form can point at — so the rule is application code and the CHECK is the backstop. Merged rather than patch-only, because a `PATCH` sending one coordinate is editing a point and whether it leaves a valid one depends on what was already stored. **(14) The `Promise.all`-in-a-transaction ban was swept, not just followed.** coding-standards-nestjs §4 bans parallel repository calls inside a unit-of-work — the transaction rides one `pg` socket — and this module was written with ten of them before review caught it. The sweep also found three pre-existing violations, in `settings/setting-value.repository.ts` and auth's session and device repositories, all list-plus-count pairs; they are fixed in the same PR because the rule is CI-invisible and a module following it beside three that do not is how it stops being a rule. **(15) UC-ORG-006's "others: forced own" reads the caller's employment, not their role assignments.** An ordinary employee holds no `user_roles` row at all, so `companyScope` is empty for them — and the first implementation derived the chart's company from that scope, which 404'd the majority of the endpoint's intended callers on the one surface in this module with no permission key. The company now comes from the caller's `employees` row (A-194 decision 5's read, one more column), and an employee naming someone else's company is silently forced back to their own rather than refused. A tenant-wide admin naming no company still gets 404: tenant-wide means every company, so there is nothing to fall back to and the scope bar has to say which. 88 unit tests plus 38 Testcontainers assertions, covering the timezone and coordinate CHECKs, the exclusion constraint in all four of §14's directions, RLS on the five new tables, the vacancy subquery, the as-of boundary and every archive guard against real SQL, and channel 1 end to end including the rollback case no queue-based hook could promise. | Active |
| A-195 | 2026-08-07 | Implementation decisions in the employee module the module document leaves open, plus three contract gaps its arrival exposed | `hris-api/src/modules/employee`, `hris-api/src/shared/crypto`, `hris-api/src/shared/data-scope.ts`, `employee.md`, `organization.md` §4.2, `authentication.md` §13, `ADR-0001` §4, `ADR-0016`, `environments.md` §6. **(1) The ADR-0016 crypto layer arrives here, and `tenant_keys` arrives with it.** system-administration.md §4.1 owns that table and will be its only writer once provisioning ships; the `shared/` crypto helper is and stays its only reader, so the table lands ahead of its module for the same reason `TenantScopedRepository` did — employee holds the first encrypted column in the system. Until provisioning exists, `seed-dev` and the integration harness mint the row. **(2) `encryptedText` reads the DEK synchronously from a process cache, because a Drizzle `customType`'s hooks are synchronous and take no context.** That is the mechanism ADR-0016 decision 2 chose, so the key must be resolved *before* the statement: `TenantKeyService.ensureLoaded()` is the first line of every repository method touching an encrypted column, on reads as well as writes, since `fromDriver` is what turns a `v1:` string back into a NIK. A cache miss **throws** — the alternative is a column written in the clear or ciphertext handed to a domain object as though it were an identifier. **(3) `LocalKeyProvider` loads under `local` **and** `test`.** environments.md §6 says *"`local` only"*, written when `api` was the only listed consumer; ADR-0020 decision 1 puts `test` in the closed `APP_ENV` set for CI-ephemeral containers, and an integration test proving that ciphertext reaches the column has to actually encrypt. What the row protects is unchanged: `staging` and `production` cannot construct the provider at all. **(4) The employee grid never selects an encrypted column.** ADR-0016's tradeoffs promise the fields are *"read on profile/payroll-setup paths, never in hot list queries"*; the first implementation selected the whole row and dropped the set in a mapper, which paid a DEK unwrap and twenty crypto hops per page to produce a payload that never carries them. The projection is now the boundary, which is stronger than a mapper — a forgotten mapper leaks, a column absent from the `SELECT` cannot. **(5) The data-scope helpers moved into `shared/`, and `ADR-0001` rule 4's whitelist is amended in place to name them.** ADR-0005 §Decision-2 already directed modules to resolve row visibility *"through shared ownership helpers"*; organization had them as module-local code and employee would have been the second copy. No business logic, no schema, and no honest owner — every module after these two resolves the same company scope from the same assignment set. Same class as the rate-limit guard's 2026-08-05 addition: a name added to the list, not a change to what the list is for, so no supersession. **(6) `employee_directory` carries `join_date`.** The entry's own rule — *"additions require an edit here and a consumer that needs them"* — is discharged by organization: a placement may not precede the employment it moves, and `EmployeeLookupRepository` had been reading the base table under an `A-194:` marker for exactly that column. **A-194 decision 5 is retired in full**: every direct `employees` read in organization is now a `employee_directory` join, and only the `user_roles` count remains direct, because authorization-rbac publishes no view. **(7) `/auth/me` returns `name` and `employeeId` again, through the view.** authentication.md §7 has declared both since the module was written and the walking skeleton left them out, correctly, as an ADR-0001 violation waiting to happen. The view is rule 6's sanctioned channel — no encrypted column, no masked column, `security_invoker = true` — so a platform module reading it creates no import edge and no cycle, and the extraction cost is the one rule 6 constraint (d) already prices. `null` for a user with no employee row, which is what makes `employeeId` optional in the contract. **(8) `Placement` gains six display names, and `OrgQueryPort` gains `directReports`.** Both are organization contract changes that employee.md §7 made unimplementable to omit: the grid renders `{ positionTitle, branchName, departmentName }` and `/me/profile` renders six names, with no sanctioned channel to turn an id into a name; and §13 has listed a *"team inverse"* among the ports employee consumes since 2026-08-02 while §4.2 never wrote the method. Both are additive. `directReports` answers in **employee** ids rather than user ids and does not filter on having an account — BR-ORG-003's account filter exists so the approval engine cannot assign a step to somebody unable to act on it, which is a different question from who reports to whom. **(9) `/me/profile.manager` is null when the manager has no login, and that is a narrowing.** It resolves through `directManagers`, which answers in user ids for the resolver's reasons. Closing it needs an employee-shaped projection on that port — organization's contract to change, and out of scope for the session that found it. **(10) `AccountLifecyclePort` splits two ways from its §13 sentence.** *"Links `employees.user_id`"* happens in the employee module: `employees` is employee.md's table and ADR-0001 rule 5 says only the owner writes it. Same transaction, same outcome, right side of the boundary. *"Assigns the Employee role template"* is **not done**, because there is no template to assign — authorization-rbac seeds the ten templates and has not shipped, so `roles` holds one row per tenant. Benign: every employee self-service route is `@AuthenticatedOnly()`, so an invited employee can use the entire employee surface with no role. **(11) `PayrollEmployeeFacts.status` lists `'suspended'`, which the §4.1 enum cannot produce.** `employee_status` is `active | on_leave | resigned | terminated`; `suspended` is a *tenant* status. Implemented with the four real values and corrected in the document. **(12) §8's working-age floor is not implemented.** It reads *"today − 15 years"* and §8 carries a `⚠️ VERIFY` on the fifteen. `ai-development-guide.md` §5 forbids an assistant typing a regulatory number in code, and a validator is code — so the format is checked and the age rule waits for a human, exactly as a statutory vector does. **(13) The exit sweep of pending requests is absent, and so are three of §4.1's six tables.** `employee_documents` needs `files` (document-storage, spine order 5); `employee_data_change_requests` and `employee_resignations` need `approval_instances` (approval engine, order 4). BR-EMP-009, BR-EMP-010, BR-EMP-012 and the BR-EMP-006 clause that cancels pending requests all land with their dependency rather than being half-built against tables that do not exist. `employee_contracts.file_id` ships FK-less on the `setting_values.branch_id` precedent; document-storage's migration adds `fk_employee_contracts_files`. `EmployeePayrollPort` is specified and unbuilt on a different rule: its `rosterFor` decrypts bank details, writes one sensitive-read row per run, assembles placement and contract facts from two more sources, and carries an `includeExited` mode whose only definition of correct is what a THR run needs — a port that needs machinery only its caller can shape cannot be written correctly without one. **(14) `/me/profile.leaveSummary` is `null` for a second reason the contract already allows.** §7 fills it from `LeaveQueryPort.balanceFor`; leave.md is further down the spine. **(15) The contract exclusion uses inclusive-end `'[]'` ranges**, unlike every other effective-dated table in the system, because BR-EMP-007 says a contract ends *on* `end_date` — so a renewal starts the day after its predecessor, and a same-day successor is refused. The integration suite asserts both directions, because an off-by-one here pays an employee twice for one day. **(16) UC-EMP-007 claims before it acts.** `markApplied` is guarded by `applied_at IS NULL`, making it an atomic claim rather than a stamp: two runners serialize on the row lock and the loser does nothing. Ordering the claim last would leave a crashed run's effects to be repeated, and the outbox emit is not idempotent — a second emit is a second `eventId`, which is what every consumer's dedup guard keys on. Asserted against a real database with two concurrent transactions. **(17) `jose` is transformed rather than stubbed in the test configs.** It is ESM-only and ts-jest emits CommonJS, so any spec whose import graph reaches auth's access-token service died on `Unexpected token 'export'` the moment employee imported auth's facade. A stub would let a signing test pass without signing. 73 unit tests plus 31 Testcontainers assertions, covering both partial uniques across every status and the soft-delete case, the contract CHECK and the exclusion in both directions including a two-writer race, RLS on the three new tables, the view's `security_invoker` isolation, ciphertext at rest with cross-tenant digests, the fail-closed missing-key path, and BR-AUD-005's three masking layers on one row. | Active |
| A-196 | 2026-08-07 | Implementation decisions in the approval engine the module document leaves open, plus the three contract gaps its arrival exposed | `hris-api/src/modules/approval`, `hris-api/src/modules/authz`, `hris-api/src/database/tenant-scoped.repository.ts`, `approval-engine.md` §4/§13, `authorization-rbac.md` §4.1, `organization.md` §4.2, `audit-log.md` §4.2. **(1) The §13 request-type registry lives in the engine, not in the modules.** §13 says so — *"a new request type = module §13 declaration + registry entry here"* — and a `registerRequestType()` call shaped like `registerAuditedTables` would not work anyway: chain configuration and BR-APRV-003's provisioning seed both need the full list, and nine of the ten modules do not exist. The settings precedent is the closer one. **Field names only, no types**: five of the ten declarations state types and five state only names, the fields exist to answer one §8 question (*is this condition field declared for this type*), and inventing types for half the registry to fill a column nothing reads would be worse than not having it. The chain editor's typed field list is admin-web work with no §7 endpoint behind it. **(2) `RoleHolderPort` is declared in authorization-rbac, which had declared no port at all.** `role_holders` and BR-APRV-006's fallback rung both need `user_roles`, which ADR-0001 rule 2 reserves to its owner. Declaring it in the engine would have been the deviation-by-comment §3 rule 1 rejects, so it is authz's port with authz's implementation, additive, in the same PR. It answers **by id and by key** because the resolver names a role by id and `approval.fallback_role` names one by key. **(3) `OrgQueryPort.positionExists` is §8's reference check and nothing else.** `positionHolders` answering empty is a *vacancy*, which is what the ladder exists for; a deleted position is a chain that can never resolve, and the difference is a field entry in the editor versus a stuck instance nobody finds until a request is late. **(4) `approval_chains` and `approval_delegations` are audited; the four execution tables are not.** BR-AUD-004 keeps the trail out and says nothing about the configuration that produced it, and audit-log §4.2 carried no `approval_*` row at all. Configuration that decides who approves a payroll run is the class of act §4.2 exists for — the same class as a role grant, which rides channel 2. **(5) `TenantScopedRepository.live` degrades to `true` on a table with no `deleted_at`.** `approval_delegations` ends through `revoked_at`, which is an update; the alternatives were inventing a column §4 does not declare or writing `recordChange` by hand, and the port's own doc comment says the repository base is its only caller. **(6) A `specific_user` resolver is validated against `employee_directory`, not against `users`.** Every other resolver in §4 answers in the user ids of live employees — BR-ORG-003's holder rule filters on exactly that — so a named approver who is not one of those people could not have been produced by any other rung either. Stronger than §8's *"exists and live in tenant"*, and coherent with what an approver is in an HRIS. **(7) BR-APRV-007's reroute goes up exactly one level, then falls into the vacancy ladder.** ADR-0008 and §14 both say *next* level, singular. A walk that kept climbing would need an invented depth cap (reporting lines have none — organization.md §4 caps departments only), and "nobody up there either" is the vacancy BR-APRV-006 already answers. The base is the deepest `direct_manager` the step asked for, so a step configured at level 2 reroutes to 3. **(8) The self-approval guard runs before the delegation redirect and again after it.** A delegation pointing back at the requester would otherwise hand them their own request; the guard's decision was "not this person" and a delegation is not an override of it. **(9) A stuck step's signal is `approval.step.activated` with an empty `assigneeUserIds`.** §12 registers no stuck event and §13 asks for a stuck notification; the empty list is the fact, `is_stuck` is its queryable half, and inventing an event name would be adding to a contract section. **(10) The `any`-quorum loser can lose in two places, and both are 409.** The step version guard is §9's mechanism, but under READ COMMITTED the loser may instead read its own seat one statement after the winner's `closeRemaining` — so a seat that is no longer `active` raises `APRV_STEP_ALREADY_DECIDED` rather than `APRV_NOT_AN_APPROVER`. Having no seat and having a spent one are different facts. **(11) Gate two answers 403 on the act path and 404 on the read path.** Reaching `ApprovalPort.approve` means the module's static `@RequirePermission` already passed, so a caller with no seat holds the permission by construction — BR-APRV-012's `APRV_NOT_AN_APPROVER`. Reaching `GET /approval/instances/{id}` proves no such thing, so outside the visibility set it is 404. **(12) A `return` claims the acting seat `skipped`.** `approval_step_status` has three outcomes and none of them is "returned"; the authoritative record of who returned it is the action row (BR-APRV-015, BR-AUD-004), and leaving the seat `active` would put a live inbox item on a terminal instance. **(13) BR-APRV-005's live-uniqueness is a constraint, never a pre-check**, and a violation surfaces as `VAL_DUPLICATE` on `requestId` — §14 says the module surfaces the conflict, and that is what a module which did not pre-check gets. **(14) Delegation overlap takes a transaction advisory lock on the delegator.** The rule reads rows that do not exist yet, so no row lock covers it, and no constraint expresses *these two `text[]` scopes intersect over these two dates* — `gist` has no operator class for `text[] &&`. The lock is what makes UC-APRV-006's 409 true rather than likely; the redirect lookup is also ordered newest-first so an ambiguous state has a defined answer. **(15) The SLA scan ships without its schedule.** §12 asks for every 15 minutes and ADR-0010 puts crons on BullMQ, which has no worker in this repository — the audit anchor and the employee effectuate job are in the same position. Nothing in it writes a step status, which is BR-APRV-010's *no auto-decide, ever*, asserted by counting calls rather than by assuming. **(16) A chain edit is refused only when it would leave conditional siblings with no catch-all.** Removing the last chain of a type entirely is allowed: that fails loudly at submit with `APRV_NO_CHAIN_CONFIGURED`, while a conditional set with no catch-all is the silent version, and only the silent one is worth blocking. **(17) The engine ships with no consumer and a complete port.** Nine module documents declare `ApprovalPort` and none exists; unlike `EmployeePayrollPort` (A-195) every method here is defined entirely by §5 and the `BR-APRV-*` rules, so there is nothing a caller could shape. `approval.fallback_role` resolves to `hr_admin` and no tenant has such a role yet — authorization-rbac seeds no templates (A-195 item 10) — so the last ladder rung is empty until it does, which surfaces as a flagged stuck instance rather than as silence. 107 unit tests plus 18 Testcontainers assertions, covering the partial unique across resubmission and across tenants, the append-only revoke, both CHECKs, the one-seat-per-step unique, RLS on all six tables, the same-transaction rollback, the snapshot surviving a chain edit, the `any`-quorum race, and channel 1 on a chain write. | Active |
| A-197 | 2026-08-07 | Implementation decisions in document storage the module document leaves open, plus the registry-entry field nothing declared | `hris-api/src/modules/document`, `hris-api/src/modules/employee/application/document-owner.service.ts`, `hris-api/src/database/schema/document.schema.ts`, `document-storage.md` §4.2. **(1) The `ownershipResolver` had no shape and nine documents depended on it** — §4.2 named the field `(module port)` and the modules said *"this module's ownership resolver"*; the `FileOwner` interface is now declared in §4.2 as a consumer contract this module publishes, the `registerAuditedTables` relationship rather than the `EmployeePayrollPort` one. **(2) `writePermission` / `readPermission` are answered inside the resolver rather than declared beside it**, because half the seed table's rows state a gate no static key can express — *"self, or `expense.claim.create`"*, *"or a live approver of its instance"*, *"while the claim is `draft` or `returned`"* — and keeping the expressible half as registry data would have left the rest unenforced. **(3) A category with no registered owner is not live**: 404, not an unguarded upload, which is what makes registration the gate. Nine of the ten §4.2 categories ship policy-complete and ownerless. **(4) `generated_document.maxSizeBytes` is `null`, not a number** — §4.2's *"— (worker-only)"* means there is no client upload path, and a ceiling invented to fill the dash would be a contract value nobody decided. **(5) BR-DOC-004 says the object's size must *match* the declared size; §7 says `DOC_SIZE_EXCEEDED` is *"actual object over cap/declared"*.** Implemented as §7 — over the declaration fails, under it is accepted and the row stores the verified bytes — because BR-DOC-010 has the client compress a selfie before it uploads, so promising a bound and delivering less is the normal case. **(6) Delete checks `canRead` → `clientDeletable` → `canDelete`,** in that order, so both answers stay reachable: a caller who can already see the file learns it is retained (`DOC_DELETE_FORBIDDEN`), and one who cannot learns nothing, including that. **(7) `files` is not channel-1 audited and `FileRepository` therefore does not extend `TenantScopedRepository`** — audit-log §4.2 has no row for it and §12's two events are already on audit-log's consumed list, so a diff would file the same two facts twice and add a row for every `expiry_reminded_at` stamp. **(8) §9's *"row flagged"* on a missing object is a log line**, because `commit_failure_code` is documented staged-rows-only and writing it would put a commit failure on a file that committed fine. **(9) No `DocumentPort` / `DocumentStoragePort`.** Four module documents name one, none declares its shape, and none of the four exists — the `EmployeePayrollPort` line of A-195, drawn again. `StorageUsagePort` ships because §13 declares it in full. **(10) UC-DOC-004's worker write path is not built** for the same reason; the `generated_document` mint, its 120 s TTL and its fail-closed `document.download.generated_document` row are tested against a seeded row instead. **(11) Idempotency: §7 marks both queue-reachable endpoints *accepted* / *idempotent by design* while api-standards §7 rule 1 says queue-reachable is *required*.** The module document wins — a duplicate slot is a disposable staged row the sweep collects, and confirm is idempotent by BR-DOC-006 — so no `IdempotencyInterceptor` is built here; the first endpoint that genuinely requires one (payroll execute, disbursement) is where that subsystem lands. **(12) `document.expiry_reminder_days` is resolved at tenant scope though it allows `company`** — `files` carries no company and the owning entity is where one would come from. A company-level override is invisible to the scan; if that matters, `FileOwner` grows a `companyOf(ref)`. **(13) The expiry scan stamps and emits nothing.** §12 registers no reminder event and notification does not exist yet, so the stamp is the record its template will read — the approval SLA reminder settled the same question the same way. **(14) Retention months are 30 days.** A retention horizon is not a calendar obligation, and a purge landing either side of a month boundary is the same purge. **(15) Retention setting keys are resolved lazily, per category, only when rows of it exist** — `attendance.selfie_retention_months` and `recruitment.candidate_retention_days` are deliberately absent from the settings registry, and the owner check that gates the scan is also what keeps the job from asking for a key that throws. **(16) OOXML sniffs to two candidates until a zip entry name decides.** xlsx and docx share `PK\x03\x04`; the read window is 4 KB, which reaches past `[Content_Types].xml` to the entry that names `xl/` or `word/`, and two candidates is the honest answer when it does not. **(17) `employee_contracts.file_id`'s deferral is closed by the schema, not by hand** — adding `.references(() => files.id)` lets drizzle generate the constraint, so the name is its default `employee_contracts_file_id_files_id_fk` rather than the `fk_employee_contracts_files` the 0011 comment guessed. Two CHECKs ride the same migration: `ck_files_size_bytes` (§8's *int ≥ 1*) and `ck_files_sha256_when_committed` (BR-DOC-004 — a committed row without a digest is a file nobody checked wearing the status that says somebody did). **(18) `employee_document` is bound and `employee_documents` is not built.** employee.md §2 assigns the binding to this module's arrival, and the resolver needs nothing but the directory view; the table, UC-EMP-010 and the §7 endpoints stay employee's own task (A-195). | Active |
| A-198 | 2026-08-10 | Implementation decisions in the notification module the module document leaves open, plus the three contract gaps its arrival exposed | `hris-api/src/modules/notification`, `hris-api/src/database/schema/notification.schema.ts`, `notification.md`, `approval-engine.md` §12. **(1) The template registry ships whole, and most of it is inert.** §13 calls §4.2 *"the platform seed"* and this module owns it, so all forty-five rows are registered — including the thirty-odd whose modules have not been built. A template nobody sends costs a row in the preference matrix and nothing else, and a module arriving later calls the port with a key that is already there. The mirror image of A-197's line: there a *policy* was complete and its gate undefined; here the *definition* is complete and only its sender is missing. **(2) The message text is this module's, not the handbook's.** BR-NTF-001 says templates carry i18n keys and no document says what they say; both locales ship per D12, and a translation pair whose placeholders drift apart fails a test rather than rendering a sentence missing its value. **(3) BR-NTF-006's locale chain has two empty rungs, so every V1 render is `id`.** `users` (core-schema §4) has no locale column, settings §4.2 registers no tenant-default locale key, and api-standards §3 scopes `Accept-Language` to server-generated documents — which a notification is not, and which an async send could not read anyway. Recorded rather than invented: adding a column to `users` is core-schema's decision and no consumer of it exists. **(4) Push and email dispatch are not built.** The pipeline records their delivery rows `pending`, which is exactly the state `dispatch.push` and `dispatch.email` consume; there is no FCM or email provider wired, no credentials to wire one with, and ADR-0010 puts their scheduling on a BullMQ worker this repository does not have. **BR-NTF-007's device targeting and the invalid-token clear ride with them** — both need a port on auth whose only caller would be the job that does not exist. `in_app` is complete: `sent` at row creation per UC-NTF-003, which is the channel BR-NTF-008 calls the durable one. **(5) `approval.instance.*` gained `requesterUserId`** (hris-handbook PR #32) — §4.2 sends `approval.instance_decided` to the requester and the terminal payload was the only recipient-bearing event in §12's set that did not name its recipient, while `approval_instances` is the engine's table under ADR-0001 rule 2 and the payload carried no employee id to bridge through `employee_directory` either. **(6) `approval.instance.cancelled` maps to no template.** §4.2's audience is the requester on approved, rejected and returned; a cancellation is the requester's own act, and telling somebody what they just did is not a notification. **(7) The feed's population is a delivery semi-join, not a list of template keys** — a feed item is a notification with a live `in_app` delivery. An email-only template still writes its row (BR-NTF-008: the row is the parent of its deliveries) and stays out of the feed; a channel a preference suppressed is `skipped` per §4.1 and stays out too, so the list and the badge answer the same question about the rows that exist rather than about the registry as it reads today. **(8) Neither the outcome nor the decision comment is a template variable.** An outcome word passed as a parameter renders untranslated into a snapshot BR-NTF-006 freezes forever, and a comment is free text with no length discipline; the frame is localized and the deep link carries both — the shape announcement.md settled on for the same reason. **(9) The event handlers set no deep link.** BR-NTF-011 wants a go_router path on every row and cites mobile-flutter §7, which is the sync-engine seam — **no route grammar exists anywhere in the handbook**, and a path invented server-side is a client contract nobody agreed to. `deep_link` is nullable in §4.1, mobile owns the dead-link degradation BR-NTF-011 specifies, and the port carries `deepLink?` so a caller that knows its own route supplies it (inbox.md's `InboxPort.createAckItems` already takes one for this reason). **(10) BR-NTF-012 is enforced at the source rather than by three security-standards §10 entries.** That registry matches key names case-insensitively and recursively, so `title`, `body` and `params` would redact `positions.title`, every HTTP body, and every `VAL_` detail's interpolation values across the whole system. Nothing in this module logs content; the unresolved-variable breadcrumb §9 asks for logs variable **names**, which are not values. **(11) §9's `users.status = 'active'` recipient filter is not implemented.** It needs a port on auth with exactly one caller, and its only observable effect today is a feed row an inactive user cannot log in to read; it arrives with push dispatch, where it prevents something — a push to a departed employee's phone. **(12) None of the three tables is channel-1 audited**, so no repository extends `TenantScopedRepository`: audit-log §4.2 has no row for any of them, a notification is a derived fact whose cause was audited where it happened, and the preference rows are a user's own switches over their own traffic. **(13) `NTF_TEMPLATE_MANDATORY` is the module's only refusal.** Another user's row is `SYS_NOT_FOUND` (existence hiding), and an unknown template key or channel is §8's `VAL_INVALID_ENUM` — the pair check *"is this channel declared by **that** template"* sits in the use case because it needs the other field to answer. **(14) A mandatory template ignores stored opt-out rows entirely** rather than trusting that none exist: `toggle` refuses to write one, and a row that arrived some other way must not read back as an honoured opt-out on a security notice. **(15) `ck_notification_deliveries_sent_at`** — §4.1's `pending → sent` is the transition that stamps the time, written as a constraint before its only writer exists, because the dispatch job is the one writer this module does not yet contain. **(16) `findTemplate` uses `Object.hasOwn`**: a plain object literal inherits `Object.prototype`, so `'constructor'` was a registered template that returned a function. Found by a test, on the path the port and the handlers reach with a caller-chosen string. **(17) A send is synchronous inside the caller's transaction, and that does not contradict BR-NTF-003.** The rule's stated reason is that a request path must never wait on SMTP or FCM — *"never inline SMTP/FCM in a request"*, protecting BR-AUTH-002/010's response-timing promise — and nothing here touches a provider: two bounded inserts, which is what makes the feed row durable at the moment the cause happened. **(18) `NotificationEventHandlers` is not exported from the facade.** It is the seam the outbox relay will call, the relay does not exist, and backend-nestjs §4 has a facade export port tokens with interfaces rather than concrete classes. | Active |

## §6 defaults outcome (D1–D14)

Presented 2026-08-01; recommendation **keep all fourteen** (D5 extended by A-003).

| Default | Outcome | Note |
|---|---|---|
| D1–D14 | ✅ Confirmed as proposed ("keep all 14"), 2026-08-01 | Now binding. D5 includes A-003 (Jakarta region). Architectural entries become Accepted ADRs in Phase 1: D6 → ADR-0011, D8 → ADR-0014, D9 → ADR-0015, D11 → ADR-0002. |
| A-199 | 2026-08-10 | Implementation decisions in the inbox module the module document leaves open, plus the three contract gaps its arrival exposed | `hris-api/src/modules/inbox`, `hris-api/src/database/schema/inbox.schema.ts`, `inbox.md`, `approval-engine.md` §7/§13. **(1) `approval.step.activated` cannot materialize an inbox item, so the engine gained a read port** — `ApprovalTaskPort.stepTasks` (approval-engine §7, this session). The payload is `{ instanceId, stepId, assigneeUserIds }` and BR-INB-004 fixes `dedupe_key` as the **assignee row id**, §4's `source_ref` holds `requestType` and `requestId`, BR-INB-009 sorts on `activated_at + sla_hours`, UC-INB-001 badges the delegate and renders a title from the requester and the context snapshot — five facts, none of them in the payload, all of them on tables ADR-0001 rule 2 puts behind the engine's port. A port rather than a wider payload because a task snapshot is not a recipient: A-198 widened the *terminal* payload precisely because that event family already names its recipients and the consumer needs nothing else, whereas here ADR-0010's *"consumers re-read state by id"* is the stated channel and the other two consumers of this event want none of it. approval-engine §13's inbox bullet said items *"derive from live `approval_assignees` rows"*, which read as a licence for a cross-module table read; it is corrected in place to name the port. **(2) BR-INB-006 filters on two keys that §4 stores inside `source_ref` jsonb, which database-conventions §1.8 forbids** — *"never for fields that business rules filter or join on"*. The column stays, because it is the §4 contract and `inbox.item.acknowledged` carries it whole; the closure queries get one **partial expression index** on `(tenant_id, (source_ref->>'instanceId'), (source_ref->>'stepId')) WHERE status = 'open'` instead. One index serves both closure paths because `approval.step.decided` carries the instance id as well as the step id, and partial on `open` because closure never touches anything else. Additive to the schema block; database-conventions §7 rule 4 is satisfied by UC-INB-002 documenting the query. **(3) The item titles are this module's, on notification's precedent.** There is no §4.2-style registry here and BR-INB-005 renders once at creation in the recipient's locale, so a per-request-type title and subtitle ship in both locales over the context fields `REQUEST_TYPE_CONTEXT_FIELDS` already declares. The locale is `id` for every V1 render, for A-198's reasons unchanged — no `users.locale` column, no tenant-default locale key, and `Accept-Language` scoped by api-standards §3 to server-generated documents. Acknowledgment titles render from the `titleParams` the caller passes, so no announcement copy is invented here. **(4) `deep_link` is `NOT NULL` and no route grammar exists anywhere in the handbook.** BR-NTF-011 cites mobile-flutter §7, which is the sync-engine seam; §7's router line describes a resolver, not a vocabulary. A nullable column could record the absence and this one cannot, so approval tasks store `<requestType>/<requestId>` — a *reference* built entirely from the two ids `source_ref` already fixes, carrying no invented screen or route name — and acknowledgment items store whatever `createAckItems` is given. **(5) The two ack-item methods ship with no caller.** `createAckItems` and `closeAckItems` are inbox §5's port for announcement.md, which is Phase 3; `dedupe_key` = the announcement id and `closed_reason = 'retracted'` are the values inbox §13 says held without amendment, and shipping them now is what makes that true rather than aspirational. **(6) No relay and no scheduler**, for the reason every job and handler in this repository has neither: ADR-0010 dispatches the outbox and runs crons from a BullMQ worker that does not exist. `InboxEventHandlers.handle` is the seam the relay will call and `cron.inbox.purge` is a body; `inbox.item.acknowledged` is written to `domain_events` and nothing dispatches it yet. **(7) Acknowledge is idempotent by state, not by an idempotency store.** §7 marks the endpoint queue-reachable with `opId` as the key and no shared `IdempotencyInterceptor` exists in this repository yet; offline-sync §5 says state-transition ops beyond the Redis window rely on the module mapping an already-in-target-state call to replay-success, which BR-INB-008 already specifies as a 200 no-op returning the existing `doneAt`. **(8) The shared `IdempotencyInterceptor` still does not exist, and this is the endpoint whose arrival was supposed to build it.** `app.module.ts` deferred it on the ground that *"nothing here is queue-reachable"*, which stopped being true with `POST /inbox/{id}/acknowledge`. It stays deferred, on two grounds recorded rather than assumed: inbox.md §7 declares that endpoint `Idempotency: accepted` and not required, so nothing in this module's contract is unmet; and BR-INB-008's 200 no-op is **durable state** where a stored response is a cached one, which is exactly the distinction offline-sync §5 draws for a state-transition op replayed past the window. The trigger is restated in place: the first endpoint whose §7 block says **required** — attendance's punch, or a payment-affecting payroll action — where `op_id` uniqueness and a stored response are the only things between a retry and a duplicate. **(9) None of the three §4 statuses is trusted to application code alone** — one CHECK enforces §4's stated invariant that `done_at` and `closed_reason` are mutually exclusive with each other's status, and that an `open` item carries neither. | Active |
| A-200 | 2026-08-10 | Implementation decisions in the import-export framework the module document leaves open, plus the two contract gaps its arrival exposed | `hris-api/src/modules/import-export`, `hris-api/src/modules/document`, `hris-api/src/database/schema/import-export.schema.ts`, `hris-api/src/database/connection.provider.ts`, `document-storage.md` §13. **(1) The framework ships with the protocol and zero definitions, and that is the decision rather than an omission.** BR-IMP-001 is the platform law — *"modules register `ImportDefinition`/`ExportDefinition` in code + their doc §13 + this doc's §4.3 table"* — and §4.3's thirty rows belong to twenty modules, each carrying a permission key, a natural key, and a `rowHandler` or `queryPort` only its owner can write. Registering them from here would be this module deciding what a row of somebody else's data means. It is document-storage's shape exactly (A-197 item 3): nine of its ten categories ship policy-complete and ownerless, and an unowned category is **not live**. A definition key with nothing registered is likewise not live — `POST /imports` for it is `VAL_INVALID_ENUM` and `GET /definitions` does not list it, which makes registration the gate rather than a decoration. `employee.master` is the one §4.3 row whose owning module exists, and it is *not* registered: its import needs an organization port resolving `company_code`/`branch_code`/`position_code` that `OrgQueryPort` does not have, and its export needs an employee query port that decrypts ADR-0016 columns and writes a sensitive read — both are those modules' contracts to grow, in the session that holds their documents. **(2) `DocumentPort` is declared, because UC-DOC-004's worker write path finally has a worker.** A-197 item 10 recorded that path as unbuilt for want of a consumer; import-export writes an error workbook and an export output and reads back an uploaded one. The five methods are defined entirely by UC-DOC-004 and UC-IMP-001/002 rather than guessed, and **no download minting crosses it** — every client read still goes through `GET /documents/{fileId}/download-url`, where the gate, the TTL and the sensitive-read trail live. `softDelete` is deliberately not gated by `clientDeletable`: that flag answers *"may a user delete this"* on UC-DOC-005's endpoint, while §12's purge is the owning module retiring its own artifact. **(3) The reader is buffered, which contradicts ADR-0015's named mechanism, and the reason is a library defect.** The ADR fixes *"exceljs **streaming** both directions (WorkbookReader / streaming writer)"*; the writer half holds and is used. In exceljs 4.4 `stream.xlsx.WorkbookReader` **emits only the first worksheet of a multi-sheet workbook**, leaves every `name` at its `Sheet<n>` fallback, and — depending on how the source stream chunks — throws `Cannot read properties of undefined (reading 'sheets')` from its own `_parseWorksheet`; its worksheets are deferred to temp files until `xl/workbook.xml` is parsed, and that file is written *last* by its own streaming writer. BR-IMP-006 puts the marker in a hidden `_meta` sheet, so **every** workbook this module touches is multi-sheet, and the choice was between the rule and the reader. What replaces the ADR's memory bound is BR-IMP-007's own two: `.xlsx` at **20 MB** through the `import_file` category cap and **10 000 rows** through `import-export.max_rows`, *"three independent bounds, all cheap, all early"*. What is genuinely lost is the abort *mid-file* — the cap is checked after the parse rather than during it. **(4) The definition-permission routes are `@AuthenticatedOnly()`,** document-storage §2's documented deviation for the identical structural reason: the required key is a property of the `type` in the request, so `POST /imports` needs `employee.master.import` for one body and `holiday.calendar.import` for the next and no decorator can be both. The three job-list routes carry the static `import-export.job.read`. **An unrunnable definition answers `VAL_INVALID_ENUM` on `type`, never 403** — §7 filters `GET /definitions` by permission, so a `POST` answering 403 would hand back the fact that list was built to withhold; §7's own error line already says an unknown type is that code, and the two are now indistinguishable by design. The refusal carries no `allowed` list for the same reason. **(5) Columns map by position, not by header text.** The `_meta` marker already asserts the file came from this template at this version, the headers are localized so matching on them would mean accepting two spellings of each, and a reordered column is a file the marker cannot vouch for. **(6) BR-IMP-008's undecided vocabularies.** `DD/MM/YYYY` is **refused rather than guessed**, on the same reasoning the rule states for the id-ID decimal comma — it is indistinguishable from `MM/DD/YYYY` and a guess turns 03/04 into either March or April depending on who typed it. Booleans accept `true/false/1/0/yes/no/**ya/tidak**`, both languages, because refusing "Ya" in an Indonesian product is a worse failure than accepting it. Enum values match case-insensitively and return the canonical spelling. **(7) The error workbook carries errored rows only, and localized messages only for the codes this module raises.** *"Mirror the input file"* is about the layout and the original row numbers rather than about reproducing nine thousand rows that were fine. ADR-0006 puts user-facing text in the client's bundle and no i18n bundle exists server-side, so a module business code arriving from a `rowHandler` gets its **code** written and no invented sentence — the code is what BR-IMP-009 makes the contract. **(8) BR-IMP-005's guard is a partial unique index in the migration's `-- manual:` block,** since drizzle-kit cannot express `uniqueIndex().where()`, and the repository inserts with `ON CONFLICT (tenant_id, type) WHERE status IN (…) DO NOTHING` — §9's *"the partial unique index decides at insert"* is only race-free if the check and the write are one statement. **(9) `ConnectionProvider.savepoint` is added to `src/database`, and BR-IMP-003 is why.** *"A bad row is skipped inside its batch, never a batch rollback"* is unimplementable in PostgreSQL without a savepoint: a `rowHandler` refusing through a constraint — which is exactly how `payroll.salary_opening` refuses per ADR-0024 — aborts the enclosing transaction, and the first such row would otherwise take every row after it. Per-row in partial mode; strict mode never reaches the loop with verdicts outstanding, so *"nothing written"* is the absence of the loop rather than its rollback. **(10) Commit runs the full validation pass once and does not re-check the `_meta` marker.** §9 says commit *"revalidates against the current definition; a column contract change surfaces as row errors (or strict abort) — never a crash against a stale shape"*, and re-raising `IMP_TEMPLATE_STALE` there would be the job-level refusal that rule rules out; BR-IMP-006's marker gate is an upload-time gate, where UC-IMP-002 puts it. Cross-row validators need the whole file, so the pass is per file rather than per batch; the batches are the *apply* unit, which is what `last_committed_batch` resumes. **(11) A partial-mode commit lands `partially_completed` whenever any row errored, including when none applied.** §4.2's arrow says *"applied + error rows both nonzero"*, which describes the ordinary case; a run where every row was refused is not `completed` and nothing about it `failed` — `failed` is the state §4.1 pairs with a `failure_code`, and such a job has none. **(12) The frozen column entitlement lives inside `export_jobs.params` under `_columns` and `_gated`,** which is where UC-IMP-006 says to put it (*"frozen into params"*), and it goes out on §7's row whole: the wizard needs to know which columns the file it is waiting for will carry. `_gated` is what the `import_file` owner reads to decide whether a mint is `document.download.gated_export` — a **per-file** fact, which is why the hook is on `FileOwner` and not on the category. **(13) §4.3's definition-resolved amendment is implemented and unused.** `ExportDefinition.resolve` is ten lines and it is what makes reports.md registrable at all, its ninety-three permissions and column sets being a function of `reportKey`; without it that module could not register one row. **(14) No relay and no scheduler**, for the reason every job in this repository has neither: ADR-0010 dispatches from a BullMQ worker that does not exist. `validate`, `commit` and `generate` are processor bodies, the two crons are bodies, and the two §12 events are written to `domain_events` that nothing dispatches yet. **(15) BR-IMP-011's window is measured from `created_at`,** because an `awaiting_confirmation` row has no other stamp — `confirmed_at` is null by definition on exactly the rows the sweep collects — and validation is minutes against a day. **(16) Neither table is channel-1 audited**, so neither repository extends `TenantScopedRepository`: audit-log §4.2 has no row for either, and §12's two events are already on audit's consumed list. A job row is machinery; the writes a commit made are audited in the modules whose `rowHandler` made them. **(17) Every server-rendered artifact is `id`,** for A-198's two empty rungs unchanged — no `users.locale` column, no tenant-default locale key, and a job has no request headers to read `Accept-Language` from. **(18) `exceljs` is added to `package.json`**, which ADR-0015 and D9 both name; nothing else was added. 159 unit tests plus 25 Testcontainers assertions, covering the partial index across all four active statuses and all four terminal ones, RLS on both tables including the `WITH CHECK` half, the savepoint rollback proved by rows that survive beside rows that do not, the injection guard asserted on the written bytes, and a golden fixture written by this module's own writer and read by its own parser. | Active |
| A-201 | 2026-08-11 | Implementation decisions in the holiday module the module document leaves open, plus the two contract gaps its arrival exposed | `hris-api/src/modules/holiday`, `hris-api/src/database/schema/holiday.schema.ts`, `hris-api/src/shared/period-lock.port.ts`, `organization.md` §4.2, `holiday.md` §13, `docs/07-operations/test-vectors/holiday-resolution.json`. **(1) Holiday is built before shift rather than beside it**, which is what implementation-roadmap §4.3 asks for (*"holiday.md is built with them and is small"*) read against BR-SHF-004: shift's suppression asks `HolidayQueryPort.dayType`, and the only alternative was a `shared/` stub for a port whose owning module was one file away from existing. The roadmap's sanctioned stub covers `PeriodLockPort` for a reason that does not extend here — attendance is genuinely later in the forced order, holiday is not. **(2) `PeriodLockPort`'s signature is corrected to attendance.md §4.2's.** The stub shipped with organization read `lockAt(employeeId, date)`, which no document declares; a period is company-scoped and date-ranged (BR-ATT-014), and keying it by employee made organization's own caller look natural while making every other consumer wrong — a holiday row and a shift definition are company facts that name no employee. `isLocked` and `firstLockedDate` are now verbatim, and organization's one call site moved to the company it already had in hand. **(3) The golden vectors land in the handbook, which is ADR-0018 decision 7's rule firing for the first time.** Holiday resolution is the one rule the handbook states is implemented twice, so its file lives at `docs/07-operations/test-vectors/holiday-resolution.json` and `hris-api` reads it **in place from the submodule** — not vendored, not hashed, per ADR-0025's amendment. Fourteen vectors, each naming the rule it came from; nothing in the file is statutory and it declares no rate set. **(4) BR-HOL-003's unique index is `COALESCE`-based and it is the reason a unit test cannot own that rule.** Two of the three scope columns are nullable and `NULL` never equals `NULL`, so the plain form would let one tenant-wide row be inserted a hundred times; the index coalesces both to the nil UUID. A fake repository comparing `row.companyId === input.companyId` in JavaScript agrees with any index at all. **(5) The resolution cache is keyed by tenant-month, not by `(company, branch, date)`.** UC-HOL-001 names the finer key; the shape here holds the **reducer's input** for a month and factors the scope out, because a calendar change knows its dates and cannot know who will read them — scope-keyed entries would have to be found by scanning — and a month of rows for one tenant is a few dozen small objects, so per-branch copies would multiply the memory without removing a single database read. One entry serves every company and branch in the tenant, which the specs assert directly. **(6) The delta cursor carries the row id alone, and the position is re-read inside the query.** `updated_at` is `timestamptz` at microsecond precision and `node-pg` hands it over as a millisecond `Date`, so a cursor built from that value is *earlier* than the row it names and `WHERE updated_at > cursor` re-serves the same page forever — which is exactly what five rows written in one transaction (one `now()`, five identical stamps) produce. Comparing `(updated_at, id)` against a subquery naming the cursor row removes the round trip through JavaScript, where the precision was being lost. api-standards §8's `(updated_at, id)` ordering is unchanged. **(7) `holiday.cuti_bersama_deducts_leave` is *not* registered in the code settings registry**, on the rule that registry already states: a key whose default carries `⚠️ VERIFY` waits for the session in which a human has verified the figure, and BR-HOL-006's marker covers the deduction basis itself. Nothing here reads it — leave.md owns the arithmetic — so the deferral costs no behaviour. §4.2 of settings.md still carries the row; the registry's fourteen deliberate absentees become thirteen when leave lands with a verified figure. **(8) The import emits one event per row rather than per commit batch.** §12 says one event per mutation batch, and the framework commits in batches of two hundred without telling a `rowHandler` where a batch ends, so per-batch is not expressible from inside the handler. Per row is safe rather than merely available: consumers recompute by date and the handler is idempotent, so a thousand-row calendar costs a thousand outbox rows and one recompute per date — what a hand-entered calendar would have cost anyway. **(9) `/resolved` decides admin-versus-self on the read key, not on a second route.** §7 says non-admin callers get their own scope and their parameters are *ignored rather than refused*, so one `@AuthenticatedOnly()` route serves the admin calendar and the employee list; the permission set resolves lazily, which on the employee path is the miss ADR-0005's 2026-08-04 amendment exists to avoid. `/sync` is self-scoped for **every** caller including admins — sync is not a privileged channel (api-standards §8). **(10) The self-scope read goes through `employee_directory`,** ADR-0001 rule 6's published view, and organization.md §4.2 grows `branchCompanyId` and `companyIds` rather than this module joining `branches` or `companies`. Both were added with their first caller, on the `positionExists` precedent, and both are recorded in organization.md in the same session. 57 unit tests plus 18 Testcontainers assertions, covering the fourteen handbook vectors against the reducer, the `COALESCE` unique across all three scope levels and the soft-delete release, the scope-pair CHECK, RLS including the `WITH CHECK` half, the channel-1 trail rolling back with its change, and the sync predicate proved against four scope shapes rather than against a fake's array filter. | Active |

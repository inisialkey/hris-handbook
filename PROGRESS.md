# PROGRESS.md — Status Tracker

Seeded from `MANIFEST.md` (Phase 0, 2026-08-01). Statuses: `pending` / `in-progress` / `done` / `approved`. Update at the end of every task — no exceptions (`HANDBOOK_SPEC.md` §9.3, §12.9).

## Phase 0 — Planning

| Path | Phase | Status | Updated | Notes |
|---|---|---|---|---|
| `ASSUMPTIONS.md` | 0 | approved | 2026-08-01 | A-001–A-004 active; D1–D14 confirmed as proposed ("keep all 14"), A-003 confirmed |
| `MANIFEST.md` | 0 | approved | 2026-08-01 | 71 files + 2 task rows (schema-declaration sweep, final audit) = 73 numbered rows, plus 25 ADRs listed unnumbered; erd-overview moved to Phase 4 (A-002). *Count corrected 2026-08-05 (row 73 audit) from "71 files + final audit task" — the sweep row was inserted 2026-08-04 and this note was not updated with it.* |
| `PROGRESS.md` | 0 | approved | 2026-08-01 | — |

**✅ Phase 0 approved 2026-08-01: D1–D14 kept (D5 incl. A-003 Jakarta region). Phase 1 unlocked.**

**✅ Phase 1 complete 2026-08-02 (21/21: anchors + ADR-0001–0015 all Accepted + core-schema + error-catalog seed).**

**✅ Grilling checkpoint passed 2026-08-02: 17 rounds over ADR set + assumptions — all decisions confirmed as written; A-005–A-010 → Confirmed; one amendment: ADR-0003 durable `op_id` unique column on sync-class rows (+ `opId` glossary entry). ADR-0001–0015 ratified Accepted; changes now require supersession. **✅ design-system.md approved 2026-08-02 (§5.12 checkpoint passed) — module UI Flow work unblocked. ADR-0016 still awaits review (Proposed). Settings pulled ahead (reorder recorded on its row).

**✅ Phase 2 complete 2026-08-02 (21/21 files: 6 architecture + 5 standards + ADR-0016 [Proposed] + design-system [approved] + 9 platform modules). Catalog: 12 owned prefixes (incl. AUD_ at zero), 58 registered codes. Settings registry: 22 keys. Notification registry: 16 templates.**

**✅ holiday.md approved 2026-08-02 (spec §7 checkpoint passed). Locked as structural template for all 19 business modules (already anchor #5 in CLAUDE.md). Phase 3 running: organization.md done 2026-08-02. Catalog: 19 owned prefixes, 116 codes. ✅ ADR-0016 Accepted 2026-08-02 (grilled, 4 amendments — see its row). ADR-0001–0016 now all Accepted; changes require supersession. ✅ Grilling cluster A passed 2026-08-02 (organization + approval-engine + document-storage + core-schema, 5 rounds): multi-holder positions, mandatory hire seed (BR-ORG-002 amended), open org chart, single `employee.data_change` type + `fieldGroup` context, reminders split by fact type — engine/doc-storage/core-schema confirmed as written. ✅ Grilling cluster B passed 2026-08-02 (inbox + notification, 5 rounds): new engine event `approval.assignee.acted` (per-decision actor-item completion — closes the `all`-quorum stale-badge hole; approval-engine §12 + inbox BR-INB-006/UC-INB-002 rewired), cosmetic replay lane sanctioned in offline-sync §10 (notification read + inbox seen — never queued, loss harmless), hybrid FCM message (notification block + data block; UC-NTF-003 + mobile-flutter §11), returned-request inbox items deferred to §15 Future, admin-web badge = focus + 60 s visible-tab interval (no web push V1). ✅ Grilling cluster C passed 2026-08-02 (settings + audit-log + import-export + error-catalog, 5 rounds): `SYS_NOT_FOUND` registered (default entity-404 + existence-hiding code — catalog §2 amended, module `*_NOT_FOUND` only when clients branch), export outputs requester-only + gated-column mints = audited sensitive reads (`document.download.gated_export` — BR-IMP-010 / doc-storage §4.2 / audit §4.3), import upload user-parented → re-parented at job creation (UC-IMP-001 chicken-egg closed), `settings.value.changed` gains `action` field + cancel emits the dead value (traceless-cancel hole closed), sensitive-read auditing fail-closed (UC-AUD-003). ✅ Grilling cluster D passed 2026-08-02 (database-conventions + multi-tenancy, 3 rounds): `hris_migrator` gains BYPASSRLS (FORCE RLS binds the owner — in-migration DML on tenant rows silently no-opped; ADR-0002 amended, move-runbook restore fixed), pre-tenant auth lookup path defined (`SET LOCAL ROLE hris_auth`, SELECT-only on users/sessions/devices/auth_tokens + `auth_lookup` policies — closes the core-schema §9 promise authentication.md never fulfilled; leak test L7 added), retention keys aligned (no `maintenance.*` namespace — module-owned keys; `import-export.retention_days` registered + purge cron added), stale `*_NOT_FOUND` refs → `SYS_NOT_FOUND` (cluster-C alignment), out-of-scope write references = `SYS_NOT_FOUND`, job contexts default companyScope=all. ✅ Grilling cluster E passed 2026-08-02 (offline-sync + mobile-flutter + authentication, 3 rounds): mobile auth-loss disposition split — **session-lost** (`AUTH_REFRESH_INVALID`/`AUTH_REFRESH_REUSED`/`AUTH_SESSION_REVOKED` reason ≠ device_revoked/`AUTH_TENANT_SUSPENDED` → tokens cleared, queue + DB preserved, identity-checked re-login via new DB-owner marker; password reset / 30-day-dark no longer destroy pending punches) vs **device-terminal** (grilled wipe rule unchanged), refresh request gains optional `fcmToken` (rotation propagates ≤15 min while online — was login-only, weeks-dead push), offline logout = best-effort revoke, op_id dedup covers creates (>7 d transition replays converge via business rejection; modules may map to replay-success). ✅ Grilling cluster F passed 2026-08-02 (api-standards + naming + security-standards + holiday retro, 3 rounds): login/reset rate limits split per dimension — per-email strict (5/min + 20/h) carries anti-stuffing, per-IP NAT-tolerant backstop (30/min + 300/h; onboarding-day office wifi no longer locks a tenant out); rate limits = platform config, not settings keys; existence-hiding wording → `SYS_NOT_FOUND` (api-standards §11, cluster-C alignment); holiday Drift mirror renamed `local_holidays` → `holidays` (naming §2.6 — `local_` = never-drop bookkeeping only); naming §5 gains action-extension clause; holiday §7 = canonical instantiation of api-standards §13; reset/invite TTL + PIN threshold marked platform-fixed; argon2-cap mislabel fixed. Grill backlog: cluster G low-risk rest (coding ×3, admin-nextjs, backend-nestjs, system-overview, product-overview) — optional. ✅ employee.md done 2026-08-02 (all ADR-0016 / grill-A / org duties discharged — see its row; org ports extended, `AccountLifecyclePort` added to authentication §13, verb `terminate` registered). ✅ shift.md done 2026-08-02 — **grilled before generation (6 rounds, `/grill-with-docs`)**: resolve-on-read roster model (config only, zero materialization, no crons), employee + company-default assignment ladder (departments dropped from resolution → CONTEXT `Roster` rewritten), arrangement-level holiday suppression with a per-day escape, start-date ownership of cross-midnight shifts + write-time punch-window overlap guard, ESS = read-only + `shift.roster_changed`, `shift.roster` import in / swap + publish + export + rest guards out. Verb `assign` registered (naming §3 + api-standards §1.2); api-standards §10 admits the natural-key bulk shape; naming §2.4's `ck_shifts_end_after_start` example renamed (cross-midnight shifts have `end < start`). ✅ attendance.md done 2026-08-02 — **grilled before generation (8 rounds, `/grill-with-docs`)**: **attendance owns the period lock** (`attendance_periods` + `PeriodLockPort` — the "payroll period port" stub in holiday BR-HOL-008 / shift BR-SHF-009 / organization BR-ORG-008 is retired and all three repointed here), materialized derived day (punch upsert + hourly branch-local close job, pinned schedule snapshot, frozen at lock), status enum rewritten to `present|incomplete|absent|on_leave|off|off_worked` with lateness demoted to raw measured minutes (CONTEXT.md corrected), geofence = record + flag with `strict` opt-in, printed signed branch QR additive to geofence, late-arriving punches always stored (recompute if open, quarantine + review queue if locked), corrections write `source='correction'` punches through the same derivation path, and exports + missing-clock-out reminder + live board ride along while bulk punch import goes to §15. ✅ leave.md done 2026-08-02 — **ledger-is-truth balance model** (`leave_ledger_entries` append-only + `leave_balances` as the running total and the `FOR UPDATE` serialization point carrying `pending_days`, the hold that stops two overlapping requests spending one day), net days resolved through `ShiftQueryPort` and **pinned on the request at approval** (`covered_dates`), three quota modes (`balance` / `per_request` / `unlimited`) covering every seeded statutory Indonesian type, cuti bersama charged exactly once as a ledger entry, one daily `cron.leave.period-maintenance` doing seed→accrue→carry→expire→reconcile idempotently, cancellation split into a self-service window (pending, or approved before start) and an admin-only window after it. Three forward duties discharged: `LeaveQueryPort.coverageFor` (attendance's empty stub retired), the `leave.request.decided` event, and the `attendance_days.leave_request_id` FK — plus `EmployeeStatusPort` added to employee.md §13 so BR-EMP-005's `active ↔ on_leave` finally has a writer. **Half-day leave excluded from V1 against spec §10 item 6 (A-026 — needs attendance day fractions; flagged for user review).** Verb `return` registered. ✅ overtime.md done 2026-08-02 — **grilled before generation (11 rounds + wrap, `/grill-with-docs`)**: request-first strict model (measured minutes with no approved order are never payable — only reported), **labor-law/wage-law boundary** (overtime publishes `multiplierHours` + a per-occurrence tier trace; payroll owns the 1/173 divisor and the wage basis), the five-row collapsed tier table with `H` = that date's normal hours (the statute's three rest-day variants are one rule), `min(approved, actual)` actualization on this module's own daily cron, blocking statutory caps whose numbers live in settings so a sector exemption is an audited settings raise, TOIL routed into **leave's** ledger rather than a second balance, job-level exemptions owned here so organization.md is untouched, request header + per-date occurrence rows, and consent captured by `acknowledge` on ordered overtime. Four anchor amendments: `ScheduledDay.standardMinutes` (shift.md), `overtime_toil` kind + `toil` seeded type + `LeaveBalancePort` (leave.md), ADR-0012's pull-at-snapshot clarification, attendance's `overtimeCandidateMinutes` consumer flipped forward → live. **Catalog: 18 prefixes, 104 codes** — the `SYS_`/`VAL_` blocks had drifted 3 ahead of the stated total, now counted explicitly in the ledger. ✅ **payroll.md done 2026-08-02 — grilled before generation (15 rounds).** Load-bearing decisions: statutory bases are **code formulas over one `wage_category` per component** rather than per-base inclusion flags (ADR-0012 amended), period lock is a **precondition** of `calculating` and payroll never writes attendance state (ADR-0012 amended a second time), a run declares its own period and pins its roster at snapshot, `paid` is a deliberate act with bounces tracked per employee on an independent state machine, retro is a dirty-flag worklist a human selects from, the payslip renders from the trace with the PDF as an on-demand download, and **no `team` scope exists in the module at all**. One boundary contradiction found and corrected: employee.md §13 claimed payroll reads employee tables "via repositories per the ADR-0001 whitelist" — no such clause exists, and ADR-0001 §2 permits only ports and events, so `EmployeePayrollPort` was added to employee.md. **Catalog: 19 prefixes, 116 codes.** ✅ **tax-pph21.md done 2026-08-03 — grilled before generation (15 rounds).** Load-bearing decisions: statutory tax tables are **platform data** on the `overtime_rate_rules` precedent rather than settings values (settings §4.2's forward promise corrected); the module owns exactly one tenant table, `employee_tax_profiles`, keyed per employee per **tax year**; **PTKP is pinned** at the year's first snapshot because the employee record is not effective-dated, and a live change never rewrites a pinned year; the **tax year is the calendar year of `payment_date`** — cash basis, which also discharges A-032's open marker; a **month-to-date slice** joins the YTD slice so a THR run and a regular run sharing a payment month price on the month's combined base instead of both landing in a lower TER band; **`final` becomes a fourth income class** so severance is priced on its own tariff instead of being wrong under every available classification; **opening YTD seeds payroll's ledger** through a new port rather than shadowing it, so a mid-year onboarding does not produce a wrong first-year 1721-A1; and **1721-A1 issuance is an explicit, revisioned act**, because a December close is not the year's last tax event. **Catalog: 20 prefixes, 122 codes.** ✅ **bpjs.md done 2026-08-03 — grilled before generation (13 rounds).** Load-bearing decisions: three platform tables rather than five, because unlike tax's four genuinely different rate shapes the five BPJS programs share one — program × payer → rate — with JKK's risk axis the single exception that earns its own table; the company registration is **one effective-dated correlated record**, not seven settings keys, because deregistering a program and clearing its number is a single act with a single date; the **wage floor splits** — its amount is tenant-entered branch-scoped settings because regional minimums come from hundreds of local decrees we cannot resolve a branch to, while *which programs honour it* is a versioned statutory flag; participation is **default-on with sparse exclusions** on the `overtime_exempt_job_levels` precedent, and the JP age ceiling is **derived from birth date rather than stored**, because a birthday is not a data-entry event; the base is the **stage-1 unprorated `upah sebulan`**, with a month of unpaid leave as the reductio against proration; **one contribution per employee per month** enforced two ways, a run-type rule plus a per-program MTD credit, because the rule alone misses the exiter who appears in both a regular run and a final settlement; and **additional dependents are an explicit count**, never derived from `employee_family_members`, because enrolling a parent is an election and deriving it over-deducts from every large family. **Two defects found and fixed, both in payroll:** `payroll_component_kind` had no value able to hold the employer-cost lines stage 4 already promised, and three of the five employer premiums are taxable income to the employee ⚠️VERIFY — invisible to `Pph21Input.lines`, so every 1721-A1 would have understated gross permanently → **ADR-0012 amendment 4** (`employer_cost` kind, taxable assembly sums it, net excludes it); and `bpjs_employee_ytd` mixed deductible JHT/JP with non-deductible Kesehatan while `PayrollYtdSeedPort.seedOpening` carried no contribution fields at all, so December could not read a correct figure and a mid-year tenant's first year over-taxed → two named ledger columns and two seed fields. **Catalog: 21 prefixes, 125 codes.** ✅ **expense-reimbursement.md done 2026-08-03 — grilled before generation (14 rounds).** Load-bearing decisions: a claim is a **header with lines** because a business trip spans four categories and a single-category claim forces the employee to file four; approval is **all or nothing** with return-for-revision as the only partial path, because chain selection is by amount and an approver who cuts a 12,000,000 claim to 4,000,000 is running a chain snapshotted for a number that no longer exists; policy limits **advise rather than block** — only a missing required receipt refuses a submission — which also dissolves the concurrency leave needed `pending_days` for, since nothing is being reserved; **`claimForRun` returns eligible claims and pins them to the run in the snapshot transaction**, with `releaseForRun` on revoke, because without the pin a THR run created after a regular run reaches `approved` pays the same claim twice, invisibly, with both runs internally consistent; route and tax class are **category defaults pinned on the claim at submit**, so a category edit can never re-route a claim already approved or re-classify one already taxed; the finance route is **per-claim payment state plus an export, no batch entity**, because payroll settled the same question at 10,000 employees; the lifecycle is **two independent axes** so a bounce never runs the approval status backwards; and eligibility is **approval time with no retro flag ever**, because a newly approved claim is a new fact and `closed` is terminal. **Catalog: 22 prefixes, 130 codes.** ✅ **asset.md done 2026-08-03 — grilled before generation (15 rounds).** Load-bearing decisions: **one row per physical object with no quantity column anywhere**, because a stock model needs movements, balances, and a serialization point yet cannot answer who holds laptop SN-1234; the **open assignment is the holder** with a partial unique index as the invariant and `assets.status` as the guarded serialization point, so no denormalized holder column exists to drift; **custody is employee-only**, because a handover needs a signatory who can acknowledge, be exited, be notified, and be a respondent; **no approval chains at all** — holiday, organization, shift, tax, and bpjs also register none, but all five are configuration or computation surfaces and asset is the first with employee-facing transactional records to make the call, since assignment records a fact that already happened and a chain would make the record lag the object; a **single-axis status** with condition as a separate column, because unlike expense's bounce there is no pair that runs backwards, and `assigned + damaged` is the ordinary state; **condition captured at both ends of custody**, since the damage question is never "what shape is it in" but "was it already scratched when he got it"; **two handover artifacts** — the generated form ADR-0014 already declared and the wet-signed scan — neither gating anything; and **money is recorded, never moved** (BR-AST-009). **The find, systemic rather than local:** attendance, leave, overtime, and expense all render `employee: { employeeNumber, fullName }` and offer a `q=` search over it, and **none had a sanctioned channel** — `OrgQueryPort` returns IDs only and `EmployeePayrollPort` decrypts NIK/NPWP/bank and writes a sensitive-read audit row per batch, while ADR-0001 §6 named only reports and dashboard-analytics as read-model consumers. A batch enrichment port cannot close it either: a filter or sort on a name must run **before** the page boundary and a port returns rows after it. → **ADR-0001 §6 amended in place** to sanction owner-published read-model views under four constraints (declared and owned · no encrypted or masked column · `security_invoker = true` mandatory, since a default view bypasses RLS and turns a join into a cross-tenant read · counted as extraction cost like a cross-module FK), **employee.md publishes `employee_directory`**, and all four modules were repointed in their §4.4 the same session. **Catalog: 23 prefixes, 136 codes.** ✅ **recruitment-candidate.md done 2026-08-03 — grilled before generation (19 rounds).** Load-bearing decisions: **no public careers surface of any kind** (A-054) — the whole platform resolves a tenant from an authenticated identity and RLS binds on it, so an unauthenticated write has neither, and spec §10.13's "publishing metadata" is satisfied by recording *where a vacancy was advertised*; **person and pipeline are separate tables**, so one human applying twice is one candidate and two applications and a re-applicant is a match rather than a merge migration; **two axes** — `stage` moves forward only and may skip, `status` is terminal — because a single enum overwriting `screening` with `rejected` destroys the only question recruiting reports ask; **offers are versioned rows with one live per application**, since a renegotiated figure under an `approved` instance is the failure expense was built to prevent; **chain approval is what extends the offer**, no separate send step, because with no candidate-facing channel the extending happens offline and approval is the authority; **conversion is dual-gated and transactional** — `EmployeeHirePort.hire` runs `UC-EMP-001` in full inside recruitment's transaction, requiring `employee.master.create` on top of a recruitment key, so a recruiter alone cannot mint a row carrying an encrypted NIK and a bank account; **no internal-applicant model** (A-055), because BR-EMP-001's NIK uniqueness already prevents two employee rows for one human and buying a nicer error message costs a cross-module FK, a code, and a field someone must remember to set; **money is recorded and never moved *in*** (A-059), the mirror of asset's A-052; and **erasure is anonymization in place with a `[redacted]` audit diff**, because a full diff would leave a permanent queryable copy of exactly what the purge deleted. **Two forward duties discharged** — employee.md UC-EMP-001's "recruitment conversion" actor via the new `EmployeeHirePort`, and organization.md BR-ORG-002's third `assignOnHire` caller, which reaches the port *through* employee so there is one hire path rather than two. **Two anchor gaps found and fixed:** URL verb `export` was in live use by payroll, tax, and asset and had never been registered in naming §3 (registered retroactively — precisely the drift that list exists to prevent), and A-050 still carried the superlative corrected everywhere else last session. **Catalog: 24 prefixes, 147 codes.** ✅ **performance-goals.md done 2026-08-03 — grilled before generation (19 rounds).** Load-bearing decisions: **the cycle is the spine** and nothing exists outside one, because date-overlap collection recomputes the reviewed goal set on every render; **participation is materialized at launch** and is the row the state machine lives on; **one goal table, one level of nesting, same participant** — a KPI is a measured leaf and an OKR objective an unmeasured parent, since the two models differ in arity and not in kind (A-060 excludes cross-employee cascade); **weights block at exactly 100** rather than advising like expense's limits, because a weight is an arithmetic input and 85 yields a score silently 15% low; **the rating is judgment weighted by goals, never achievement arithmetic** — the manager picks a level per goal, `calculated_score` only pre-fills the overall, and achievement is display-only, which dissolves lower-is-better inversion, `binary` goals with no percentage, and the 300%-of-target outlier in one move with no cap to configure; **rating scales are FK'd tenant tables frozen once a cycle leaves draft** rather than jsonb-snapshotted, because ratings are the most-joined rows here and a snapshot degrades every distribution grid; **two review seats keyed by `kind`, not by reviewer** — deliberately unlike `interview_scorecards`, since keying by reviewer would permit two people filing a manager review; **calibration writes beside the rating and never over it**, with the manager seeing both and the employee only the final; **release is a cohort act**, because staggering leaks which ratings calibration did not touch; and **development items are keyed by employee, sourced from a participant**, which is what makes "carried forward" true rather than aspirational. **No approval chains — a conflict avoided rather than a feature declined:** BR-APRV-006 resolves approvers at step activation while BR-PRF-003 pins the reviewer at launch, so a chain would route goal agreement to a different person than the one holding the review form. Second Phase 3 module after asset with employee-facing records and zero engine interaction, for a different stated reason. **No rating→pay path** (A-063), third in a row after asset's A-052 and recruitment's A-059. **ADR-0014 checked and left untouched** — no appraisal PDF (A-064), same check that produced A-056. `PRF_NOT_THE_REVIEWER` is the **second** 403 exception to existence-hiding, and catalog §27 promotes the pair into a stated rule so a third module follows it. `performance.goal` is the first ImportDefinition whose atomic unit is the **employee, not the row**. **Catalog: 25 prefixes, 158 codes** — the `VAL_` block was found to have been under-counted by one since the Phase 1 seed (the `VAL_TOO_SHORT / VAL_TOO_LONG` row holds two codes and every tally counted it once); ledger corrected and the hazard named in §29. ✅ **training.md done 2026-08-03 — grilled before generation (19 rounds).** Load-bearing decisions: **the catalog is separate from the thing you attend** — a course is browsed and a session is scheduled, because collapsing them turns "how many people did we put through Excel, at what cost" into a string match over copied rows and makes a course with no upcoming date invisible to the employee who wanted to ask for it; **capacity binds when a seat is taken and never when one is requested**, under a session row lock against a **live count with no counter column anywhere**, deliberately unlike leave's `pending_days` because a leave day is money-equivalent and unrecoverable while a chair is recoverable, and a forgotten request silently holding a physical seat for three weeks is the worse failure; **one status axis** on the enrollment on asset's argument, since nothing here runs backwards and **a no-show never frees its seat retroactively**; **one attendance verdict per enrollment with no `partial` and no per-day roster** (A-071); **closing a session blocks while any seat is unmarked** — the tempting auto-marks are both wrong in opposite directions, `attended` issues certificates to absentees and `no_show` writes HR's forgetfulness onto an employee's permanent record; **cost is one column on the enrollment**, pre-filled by the session and never totalled there, which is what makes a no-show's wasted fee representable at all (A-072, fourth consecutive module recording money it never moves after A-052, A-059, A-063); **a certification is a state and not an event** — keyed by employee with nullable enrollment provenance, on performance's `development_items` shape, because credentials arrive with new hires, the compliance query runs against a row, and renewal is a **new row** rather than an edited date; **the credential row owns the expiry, not the file** (BR-TRN-013), which moved a Phase 2 promise rather than deleting it. **ADR-0014 checked and this time the answer was yes** — training certificates are one of its five enumerated consumers, so the completion certificate is generated on demand from an `attended` seat on a `completed` session; the same check produced A-056 and A-064 the other way. The obstacle was branding and it was verified rather than assumed: **no tenant logo column exists anywhere in the schema**, so the document carries `companies.legal_name` and no logo (A-073) — acceptable here and not for an offer letter, because a completion certificate asserts facts the system holds and is not a legal instrument. **One approval chain, routing on money**: `training.enrollment` with `costAmount` in the context playing expense's `overPolicyLineCount` role, and **assignment skips the engine entirely** because BR-APRV-003's seeded `direct_manager(1)` would otherwise route an HR-budgeted nomination through the nominee's own line manager. **The seat is allocated inside the transaction that records the completing approval**, not in the terminal event handler — which is what makes the concurrent-last-seat case serialize cleanly instead of needing an answer for "approved, but the room filled". **A-070 flagged**: training writes nothing to `attendance_days`, so a 20-person 3-day off-site costs 60 attendance corrections; the alternatives were worse (a status value would make this the second external writer into a lock-frozen derived record and would assert attendance from a *registration*, and a seeded leave type would route one absence through two chains). **A-074**: no catalog targeting, decided on timing — targeting is `announcement.md`'s scope and announcement is the very next file, so building it here ships two implementations one file apart. Forward duty discharged: **performance-goals §4.3 gains `DevelopmentItemPort.openItemsFor`** on first real caller, with the FK on training's table — the employee.md/`EmployeeHirePort` and leave.md/`LeaveBalancePort` pattern. Five tables, 20 permission keys, 31 endpoints, 6 `TRN_` codes, 1 cron, 1 approval request type, 4 event handlers, 4 notifications, 1 import, 2 exports, 2 settings keys, 1 port served, **0 events emitted, 0 new URL verbs, 0 new permission actions, 0 gated export columns, 0 retention jobs, 0 403 exceptions**. **Catalog: 26 prefixes, 164 codes.** ✅ **announcement.md done 2026-08-03 — grilled before generation (19 rounds).** Load-bearing decisions: **the audience is resolved once at publish into rows** and never recomputed, because the acknowledgment rate is this module's product and a rate whose denominator moves when someone transfers departments can never be reproduced — criteria in, a frozen recipient set out, serving as the fan-out roster, the visibility check, and the denominator all at once (A-078 names the cost: a hire the next day gets nothing and HR reposts); **department rules descend the subtree while positions and job levels stay exact**, decided on failure asymmetry rather than taste, since over-inclusion is visible and self-correcting while under-inclusion is invisible and nothing in the system will ever surface it; **the subtree walk belongs to the tree's owner**, so targeting resolves through a new `OrgQueryPort.audienceEmployeeIds` rather than a published view — a flat view cannot express ancestry, and resolving it here would mean joining `departments`; **acknowledgment is inbox's action and this module's fact**, with no acknowledge endpoint registered here at all, because inbox already owns the platform's only queueable offline write and the same-transaction alternative would have a Phase 2 platform module calling into a Phase 3 business one; **content freezes at publish** (A-077), since an acknowledgment attesting to text later rewritten is worthless and an announcement is the record of what the company told people; **`recipient_count` NULL is the completion marker** and there is no `publishing` status, because a status is a claim someone has to retract when the job dies — the same device training used to avoid a certification status column; **publish refuses a zero-resolution audience** on both the endpoint and the scheduler path, because a post delivered to nobody is otherwise indistinguishable from one still delivering; and **the body is markdown with raw HTML disabled**, forced by security-standards §6's "API never emits HTML" rather than chosen, since a WYSIWYG storing HTML makes a tenant admin a stored-XSS author against every employee in their tenant. **`retract` registered as the fifth new URL verb** rather than reusing the reserved `revoke`, because inbox wrote `closed_reason = 'retracted'` into its schema, its rules, and the catalog before this module existed, and one act spelled two ways across a boundary is the drift the verb list exists to catch. **No approval chain, as a refusal rather than an absence**: a chain would sit between "the Jakarta office is closed, flooding" and the people driving to it, and unlike every module that does route through the engine this one's effect is not reversible by a later decision — retracting does not unread. **Two retention keys** because the acknowledgment register is the module's only compliance artifact, its default carrying a ⚠️ VERIFY instead of an invented number; **`announcement_recipients` is deliberately unaudited** on `leave_ledger_entries`' reasoning. Three anchor amendments, all first-caller: `OrgQueryPort.audienceEmployeeIds`, **`employee_directory` gains `user_id`** — the addition that view's own text invited, and the first consumer to read it for something other than painting a grid — and an optional `dueAt` on `InboxPort.createAckItems`. Two forward promises discharged: inbox §13's "ack-rate reporting belongs to announcement.md", and A-074's deferral of targeting out of training. One drift repair in passing: **api-standards §1's verb mirror had never received `export`**, which naming §3 registered retroactively during the recruitment session — mirror now level with the source. **Catalog: 27 prefixes, 166 codes.** ✅ **reports.md done 2026-08-03 — grilled before generation (19 rounds).** Load-bearing decisions: **one definition, two surfaces** — a paged screen result and an xlsx file off the same query, because a report you can only download is a report nobody runs twice, and a screen number that comes from a second query is reporting's most common and most embarrassing bug; **the registry ships all 92 promised reports** rather than a V1 subset, affordable only because the module owns **zero tables and zero permission keys**, which makes a registry row the whole specification of its report and adding row 94 a code change with no migration, no endpoint, no key, and no screen; **derived versus owned** splits the 93 rows by *channel* rather than by convenience — a derived report reads through the owning module's declared `queryPort`, ADR-0001's default path, and the §6 read-model exception carries only the 85 questions no port answers; **a report never grants access the owning module would refuse**, so every gate is an existing key in the owning namespace and a blanket `report.run` never exists; **scope is declared, applied, and stamped**, with a runner narrower than the definition **refused rather than silently filtered**, because a narrowed aggregate is a wrong number wearing a right number's label — "Headcount 12" reads identically for a manager's team and a 400-person company; **no encrypted or masked column reaches any report**, via an ADR-0001 §6(b) extension from published views to the designated consumers that also binds dashboard-analytics before its file exists, with the consequence that no report output can carry a gated column set at all; **owner predicates travel with the rows**, discharging payroll's BR-PAY-026 duty against an unfiltered `SUM(amount)` that would report employer statutory cost as employee gross; **nine sensitive-flagged** definitions on a stated boundary, with expense's unconditional-trail precedent confronted and distinguished on the ground that its flag would have been tenant data nobody reviews while this one is code in a published registry; and **no caching and no stored results anywhere**, because the manifest already put aggregate caching on dashboard's row and the two surfaces fail differently — a stale dashboard is fine, a stale report is how a payroll register and the payslips come to disagree. **Import-export §13's Phase 2 promise discharged on inspection**: reports added no writer, no injection defence, no job row, no polling contract, no notification, no purge cron, and no endpoint — one §4.3 row (`report.result`, **the first definition-resolved definition**), one §12 routing line, nothing else. **`report.result` runs on the `reports` queue**, the workload ADR-0010 named it for. **Three anchor amendments:** ADR-0001 §6(b); **`AuditQueryPort`** added to audit-log §4.4 on first caller, finally giving "reports consume the query API, never the table" a mechanism — and firing `audit.log.queried` *inside* the port, so a consumer cannot read the audit log without leaving a trace; api-standards §5.3 report-results family row. **`RPT_SCOPE_INSUFFICIENT` is the third 403 exception**, the first to arrive under the rule catalog §27 promoted, and the first code in the catalog to distinguish ADR-0005's two axes in its message — `AUTHZ_FORBIDDEN` would be factually wrong, since the caller holds the permission and lacks only scope. A-084–A-087. **Catalog: 28 prefixes, 168 codes** — `RPT` off the reserved-empty list; **three remain** (`TEN`, `ADM` → system-administration.md, `DSH` → dashboard-analytics.md), all belonging to unwritten files.** **✅ dashboard-analytics.md done 2026-08-04 — grilled before generation (19 rounds, `/grill-with-docs`), all 19 recommendations taken.** 24 widgets over 4 code-declared layouts (`hr`, `payroll`, `manager`, `executive`), and **zero tables, zero SQL, zero permission keys, zero migrations, zero jobs, zero crons, zero events, zero exports, zero imports, zero settings keys, zero audit keys, zero error codes, zero ADR changes**. **A widget is a projection over a `ReportDefinition`** — `scalar` / `series` / `breakdown` / `topN` — so this module issues no query of its own and holds **no ADR-0001 §6 seam at all**: after both designated read-model consumers exist, the exception has exactly one exerciser. Discharges UC-RPT-008's pre-commitment that a widget never re-derives an aggregate a report owns, which is what stops a card reading 412 against a report reading 408. **Access is transitive and cannot widen:** BR-RPT-002 says a report never grants what the owning module refuses, this module mints no key, therefore a widget never grants what the report refuses. **Scope insufficiency is absence** — no refusal tile, never narrowed — because a refusal the *layout* asked for is a dead tile nobody can clear. **Cache keyed `hris:dashboard:{tenantId}:{widgetKey}:{scopeDigest}`**, digest built from the resolved scope predicate in the same code path as the query, never role or user id: two managers with disjoint subtrees can never share an entry, two HR admins at one company always do. **TTL-only invalidation** (60/300/3600, single-flighted) — six event subscriptions across attendance/leave/overtime **declined and amended in their own docs**, plus three `Ports served` consumer lists corrected; `attendance.punch.synced` alone would be 20–40k handler jobs a morning to delete keys expiring in 60 s. **Cache sits above the port**, so reports' BR-RPT-011 (no cache, ever) holds unamended. **`sensitiveRead` definitions are ineligible and the port refuses them** — a cache hit serves rows without running the query, so a flagged widget would audit on miss and not on hit, and a trail with holes reads as complete. Every card renders `appliedScope` in words naming the anchor, `computedAt` as the value's real computation time, and the report's caveats. **`ReportQueryPort` added to reports §4.5 on first caller** (gate inside the port, no scope override, refuses flagged definitions, returns the whole envelope). Registry cost back to reports: **one definition** (`employee.turnover`, UC-RPT-007's first live exercise, serving four widgets off one row) and **one param** (`payroll.component_cost` gains a `month` grain) — reports now **94 rows / 93 promises, 8 derived / 86 owned**. import-export's expected-arrival note discharged: nothing registered. **Mobile: `manager` layout only, online-only, no Drift mirror, no sync class** — discharges A-087's two named needs as cards 3 and 4. **`DSH` owned with zero codes** beside `AUD` (catalog unchanged at 28 prefixes / 168 codes; **two** reserved-empty remain, both system-administration.md). A-088–A-092. **No ADR touched** — ADR-0001 §6(b) bound this module in advance, one file early.**

**✅ Phase 3 complete 2026-08-04 — `docs/06-modules/system-administration.md` done, 19/19 business modules, 61/61 handbook files before Phase 4.** Grilled 23 rounds (`/grill-with-docs`). Four platform tables (`platform_sessions`, `tenant_keys`, `tenant_feature_flags`, `impersonation_sessions`) plus two TOTP columns on `platform_users`; BR-ADM-001–024, UC-ADM-001–010, 23 endpoints under `/api/v1/platform`, 9 `sysadmin.*` keys, 4 `ADM_` codes, 1 notification template — and **zero** jobs, crons, queues, events, imports, exports, approvals, settings keys, mobile surface, and ADR-0001 §6 seams. **A sixth capability was found missing and added:** platform authentication had never been specified anywhere, leaving the one credential that reaches every tenant with no documented login. **`ADR-0017` written (Proposed, needs review)** — platform identity and impersonation; `ADR-0004`'s MFA line scoped to tenant users and **A-007 narrowed** rather than reversed, since its stated evidence was spec §5.8's tenant login list. **TOTP mandatory for platform users only.** Provisioning is **three phases, one transaction** — KMS before, invite after — so a tenant is whole or absent; it seeds through **facades**, so the widest-privileged module holds no cross-module SQL and the §6 exception still has two exercisers. **UC-ORG-007 discharged.** **No destroy button** (A-095) — crypto-shred stays a runbook. **No platform-user CRUD** (A-093), which is what makes every console action tenant-targeted and audit-log §9's cross-tenant rule sufficient with no `audit_logs` change. Impersonation: named user, mandatory reason ≥20 chars, **30 min no refresh**, one per operator, **cascades dead** when the parent platform session is revoked, bypasses `TenantStatusGuard` for suspended/archived, **no deny-list** (A-097, residual stated). Flags: platform-class table, **zero definitions** (A-096 — no finished module asks for one). Health: failed set read direct, `job.retry()`, no retry-all, **never renders the payload**. Counts via `employee_directory` plus two new ports (`StorageUsagePort`, `NotificationStatsPort`), discharging document-storage's and notification's promises. **`ADM_` registers 4 codes as catalog §31** (Deprecated → §32, ledger → §33) and **`TEN_` joins the owned-with-zero note**: catalog now **29 prefixes / 172 codes**, and the **reserved-empty list is closed, two to zero** — every prefix in naming §4 now has a written owner. Leak-test matrix gains **L8 and L9**. A-093–A-098. Next: Phase 4 — `docs/07-operations/testing-strategy.md`.**

**🚧 Phase 4 running — `docs/07-operations/testing-strategy.md` done 2026-08-04 (62/72), grilled 23 rounds.** The file is a **contract, not a manual**: ~480 scenario rows already live in 28 module §14 tables and the three coding-standards §9 sections already fix tiers and tooling, so it restates none of it. What it adds: a **generated** traceability matrix behind one gate over all 446 `BR-*` with a mechanism-naming waiver file; a **172-code reachability gate** replacing any `UC-*` gate, since exception flows terminate in codes and a code tag cannot be faked by a test title; differentiated coverage floors where **three layers deliberately carry none**, with diff coverage doing the binding; **five one-line kits** including a **registry-integrity** suite nobody had promised; **golden vectors split structural vs statutory** with a deliberately fictional rate set, behind **`ADR-0018` (Proposed)**; a **closed E2E list** E1–E6/M1–M4 enforced by filename; a **fourth tier** — post-deploy staging smoke — because A-011's hand-written wire types meant *nothing anywhere proved the three repos agree*; an **OpenAPI breaking-change gate** encoding ADR-0007's open enums so it does not cry wolf; **zero retries on merge-gating tiers** with a capped, expiry-dated quarantine; and a **destructive-operation class**, since **10 of 26 crons delete data** and idempotency says nothing about whether a purge predicate is right. **Two defects fixed:** security-standards §5 said L1–**L6** against multi-tenancy's L1–L9, and error-catalog §1 item 2 still pointed at §31 for Deprecated after last session's renumber to §32. Four anchor promises discharged (ADR-0002/0005/0006/0010). `CONTEXT.md` deliberately untouched — testing vocabulary is engineering, not domain. A-099–A-103. Next: `docs/07-operations/ci-cd.md`, which consumes §13's G1–G20 gate table and inherits A-103's scanning duty.**

**🚧 `docs/07-operations/ci-cd.md` done 2026-08-04 (63/72), grilled 21 rounds.** Seam mirrors testing-strategy's: **this file owns the pipeline, `environments.md` owns the target; a secret's name appears here, its value and store never do.** Spine + three delta tables + a gate-to-job matrix mapping **G1–G21 and ten checks C1–C10**; no full workflow YAML. **`ADR-0019` (Proposed)** records the release model: **digest as identity, semver tag as a human label**, automatic staging and human-promoted production, **independent per-repo promotion with compatibility from ADR-0007's contract rather than co-testing** — pinning web-to-api buys a guarantee an installed mobile app already makes impossible. **Rollback depth is exactly one release, derived from database-conventions §10 rule 5**, which turns out to also be what makes Helm `--atomic` correct. Migrations run as an **in-cluster Job on the exact deployed digest**, because the alternative exposes the production database to GitHub's address ranges; **§7.3 finally produces the production schema snapshot two documents depended on** — schema only, never data, bootstrap exemption self-clearing on the first `v*` tag. **A payroll drain gate** because D1 allows a 30-minute run and no grace period covers that. **§8.5 closes ADR-0010's gap:** events carry `version`, jobs do not, so job payloads are additive-only across a release pair. **A-103 narrowed to penetration testing only** — C1–C5 define the scanning half, including a **weekly re-scan of deployed digests**, the only check that finds CVEs in code nobody touched, while **Dart's lack of real SAST is written down** rather than papered over. **Smoke reset is a Job, never a route**, double-guarded; **production data is never restored into staging**. **ADR-0014's golden-render relocated to testing-strategy G21 + §6.6** with pixel diffing rejected, honoring this file's own seam. `CONTEXT.md` untouched, zero error codes. A-104–A-110. **ADR-0019 and ADR-0018 both await review; period-lock ADR is now 0020.** Next: `docs/07-operations/environments.md`, which inherits every secret name, deploy target, and values file this document referenced without defining.**

**🚧 `docs/07-operations/environments.md` done 2026-08-04 (64/72), grilled 20 rounds.** Seam completes ci-cd's in the mirror: **this file answers *what exists and how it is configured*; its siblings answer *what you do when it misbehaves*.** Three environments, with CI-ephemeral infrastructure and the GitHub `release` secret scope named explicitly as **not** environments. **Two GCP projects, forced before chosen** — ADR-0009 puts the bucket in the FCM project, so two Firebase and therefore two GCP projects exist regardless; given that, the clusters follow, which **turns an isolation boundary ci-cd §11 asserts into one Google enforces**. Derived, not decided: GKE Standard over Autopilot, **no public IP on either database** — which makes ci-cd §7.1's in-cluster migration Job structural — and one registry so promotion re-tags one digest. **§6 is the complete environment-variable registry**, the only place the handbook answers "what does this process need in order to start". Secrets: Secret Manager → External Secrets Operator → `envFrom`, Workload Identity throughout, **no service-account key file anywhere**, and **rotation requires a rollout** as a rule rather than a footnote. **`ADR-0014`'s Chromium duty discharged with a `Localhost` seccomp profile adding exactly three named syscalls**; residual admitted — a node-level file is the one place D5 portability leaks. **`maxmemory-policy = noeviction`**, because BullMQ job payloads are ordinary keys and `allkeys-lru` silently evicts a payroll run that was accepted and then never happened. Parity is enforced by Terraform and by a **closed four-item list of permitted `APP_ENV` branches**. **`ADR-0020` (Proposed)** records environment isolation and the production data boundary, whose load-bearing half is a **refusal**: testing-strategy §7 rule 4's anonymization path is **not built**, since ADR-0016's per-tenant DEKs make a scrubber useless or catastrophic and an allowlist fails open — replaced by forensic PITR inside the production project. **Three defects found and fixed: A-017 contradicted D7** (it claimed the spec named no email provider; D7 names Resend and prohibits SES in bold — corrected, absorbed entirely by `EmailPort`), **ci-cd §11 gave S5 no credential at all** and would have tripped the per-email login limit, and **multi-tenancy §4's "the migrator credential exists in CI"** was stale in a way that reads as permission to hand CI a `DATABASE_URL`. Four forward promises discharged (ADR-0011, ADR-0014, backend-nestjs §8.1, security-standards §4). `CONTEXT.md` untouched, zero error codes, zero ⚠️ VERIFY. A-111–A-120. **ADR-0020, ADR-0019, ADR-0018, and ADR-0017 all await review; period-lock ADR is now 0021.** Next: `docs/07-operations/observability.md`, which inherits every alert route, retention window, and deployed component this document placed without thresholds.**

**🚧 `docs/07-operations/observability.md` done 2026-08-04 (65/72), grilled 20 rounds.** Completes the Phase 4 seam trio: **`environments.md` says a channel exists; this document says what arrives in it, what it means, and what you do next** — the rule and the response. **The alert is the atomic unit**: OB1–OB26 appear in §4 and §6 as one set in one order, so "no alert without a triage entry, no entry without an alert" is a **set difference rather than a matter of care**, and an alert nobody would act on is **deleted, not downgraded**. **`tenant_id` is banned as a metric label without exception** — `ADR-0011` warned about cardinality and nobody costed it: ~600 route-method pairs × 3 status classes × 14 series ≈ 25,000, × 500 tenants = **over 12 million series from one metric**. Per-tenant questions belong to logs and traces, which already carry `tenantId`. **Two severities, defined by response expectation**, identical thresholds in both environments — only the route differs. **Backlog alerts on oldest job age, never depth**, or the rule is silenced every morning by D1's attendance spike and the silence is still there on the day it matters. **Three gaps closed that no document had asked about:** a cron that **never ran** (OB14 — 26 registered crons, 10 destructive, and `time() - <empty vector>` is a silent pass, so the `absent()` arm is load-bearing); the **alerting itself being dead** (OB21 — everything that decides whether to alert lives inside the cluster it watches, so total ingress loss is byte-identical to healthy: a watchdog whose *absence* is the signal, plus an external uptime check sharing no failure domain with Alertmanager); and **D3 versus the operation that has to deliver it** — 43 minutes a month against no on-call role, where one unattended overnight spends a year of budget → **`ADR-0021` (Proposed)** scopes the SLO to an **08:00–20:00 WIB service window** while computing both numbers and leaving **detection unscoped**. **§10 walks the correlation contract for the first time** — declared in five documents, executed in none — with literal query strings, the honest note that 10% sampling makes an absent trace *correct*, and a fourth step every runbook omits: no log line means the punch is still in Drift on a phone. **§12 turns three declared redaction routes into a merge gate** (G22 + testing-strategy §5.6) plus a quarterly human sample. **§14 discharges security-standards §9** — the handbook's only forward promise carrying a legal duty — mechanism-heavy and number-free, with the window and addressees behind ⚠️ VERIFY and the clock starting **at detection, not confirmation**. **One defect fixed:** testing-strategy §14.1 counted 27 crons against 26 registered, the extra being an `ADR-0010` illustration predating employee.md. `CONTEXT.md` untouched, zero error codes, one ⚠️ VERIFY. A-121–A-130. **ADR-0021, ADR-0020, ADR-0019, ADR-0018, and ADR-0017 all await review; the period-lock ADR is now 0023** *(0022 taken by tenant-scoped recovery, 2026-08-04)*. *(Amended 2026-08-04, backup-restore session: **OB27** added — `ADR-0011` had seeded a PITR/backup-freshness rule that this file never built — so the set is now **OB1–OB27, 27 rules in six groups**.)* Next: `docs/07-operations/backup-restore.md`, which inherits D3's untouched RPO and RTO clauses, `ADR-0020`'s forensic-PITR-inside-the-boundary replacement for the anonymization path, and §14's dependency on the audit log as the evidence of record.**

**🚧 `docs/07-operations/backup-restore.md` done 2026-08-04 (66/72), grilled 20 rounds.** Seam: **`environments.md` says a backup exists; this document says what it can restore, what it cannot, what you do to use it, and the proof it still works.** Scope is a **completeness table over eleven stateful stores**, four of which recover nothing — and the two whose loss is *unrecoverable*, the **KMS KEK and Terraform state**, were undocumented anywhere until this file. **D3 binds one row**: its RPO clause names PostgreSQL, WAL archival beats 15 minutes without effort, and **RTO is the clause actually at risk** — budgeted line by line to **≈ 3 h 35 m of 4 h with the clone step unmeasured**, on the fact that breaks most restore runbooks at step 1: **Cloud SQL PITR does not restore in place**, it produces a new instance, so promotion is a secret version plus a rollout restart under A-111's existing rule. **Retention is the handbook's only setting bounded from both ends** — 35 days is the monthly `cron.audit.archive` cadence plus margin, and it is *also* exactly how long a completed crypto-shred stays reversible by restore. **`ADR-0022` (Proposed)** names the incident nothing had named: not "the database died", which regional HA already answers, but one tenant's bad import — where instance-wide PITR **discards two days of writes from the other 499**. Manual extraction instead, cheap only because uniform `tenant_id`, **stable `uuidv7` ids with no sequences anywhere**, `ADR-0016`'s `v1:` prefix, and new-fact re-insertion all already existed. **§7 reconciles three stores sitting at three different times**, including **Redis flushed before the fleet is repointed** — its idempotency envelopes otherwise claim "already processed" for work whose effect was just discarded. **Four defects in existing documents, all found by asking what a restore must be able to prove:** `ADR-0011` seeded a backup-freshness alert that was never built → **OB27**; the production bucket had **no recovery of any kind** while holding the only copy of every payslip PDF → versioning and soft delete, with a retention *lock* rejected because it would make a UU PDP erasure unsatisfiable; the audit archive job **deleted before it verified**, the one destructive cron whose damage is reconstructible from nowhere → verify-then-delete; and the tamper-evidence witness **expired 119 months before the evidence** it attests to → a log sink into the archive bucket. Plus **the Terraform state backend was named nowhere**, which read literally means local state on one laptop. One correction: database-conventions §4.4 attributed a payroll cold-archive requirement to D4, whose cold-archive clause covers the audit log alone → payroll and tax stay live for the full ten years. `CONTEXT.md` untouched, zero error codes, one ⚠️ VERIFY. A-131–A-143. **ADR-0022, ADR-0021, ADR-0020, ADR-0019, ADR-0018, and ADR-0017 all await review; the period-lock ADR is now 0023.** *(Superseded 2026-08-04: `ADR-0023` was taken by performance.md's table-growth decision, so the period-lock ADR is now **0024**.)* Next: `docs/07-operations/performance.md`, which inherits D1's scale ceilings, D2's p95 budgets, environments §7.3–§7.4's sizing and pool arithmetic, and the volumes that decide whether §3.2's 120-minute clone estimate survives.**

**🚧 `docs/07-operations/performance.md` done 2026-08-04 (67/72), grilled 19 rounds.** Closes `docs/07-operations/` entirely. Seam: **`observability.md` says a budget was missed; this document says what the budget is made of, what must exist for it to be met, and how it is proven at volume before a customer proves it for us.** Every other Phase 4 file **reacts to** a number; this one **derives** them — five siblings deferred a figure here by name and there was nowhere left to push them. The MANIFEST row named five topics of which **three already had owners**, so index rules, pooling, and virtual scrolling are quoted and re-decided nowhere. **The central correction: D1 is written per tenant and every document read it that way.** Every tenant is Indonesian and offices open at the same hour, so the spike is one event, not 500 — **333 app opens/s**, and an employee does not issue a punch, they open an app: **the punch is one of two writes and one of ~8 requests**, with **token rotation exactly as large a write source** as the feature the spike is named after. Anything sized against "333 writes/s" understated the morning **eightfold**. **Findings that changed other files:** `ADR-0007` held **14–28 GB of idempotency envelopes** — full response bodies for 7 days in a `noeviction` Redis shared with BullMQ — while offline-sync §5 already guaranteed *"duplicates are impossible at any TTL, by construction"*, so the TTL is cut to 24 h and that alone is what makes a 16 GB `maxmemory` floor sufficient; **three of seven caches are keyed per employee** against a user who appears twice a day, fixed by making permission resolution **lazy** for `@AuthenticatedOnly()` rather than touching `ADR-0005`'s TTL; **ci-cd §8.2's drain gate protects the worker restart, not the schema change**, since the `migrate` Job runs first and ungated — `lock_timeout` degrades that from *"the payslips table is locked"* to *"the deploy failed, retry"*; **OB5, OB6, OB7, and OB10 all thresholded against numbers the handbook never set**, now floored; **raising `DATABASE_POOL_MAX` is the wrong fix for OB5** and is the first thing anyone will try, because Little's Law makes it hold time and not pool size; **`ADR-0014`'s render budget does not fit** — 1Gi against Chromium's 100–200 MB leaves one concurrent render per pod, ~2/s against a month-end need near 12/s, which turns environments §7.2's split trigger from a hypothesis into the 25th of the month; and **`ADR-0002` guarantees data isolation, not performance isolation** — no per-tenant quota exists anywhere, and multi-tenancy §5 now says so, because a reader hears two meanings in one word. **OB1's 10-minute window turned out to be what makes the daily autoscaling blip non-paging** — by accident until now, by design from here. **`ADR-0023` (Proposed)** discharges the partitioning question `ADR-0002` and `ADR-0013` both deferred here by name: no partitioning in V1 with a numeric trigger, against the first row-volume projection anyone has made — and the fact neither ADR could have known, that on `attendance_punches` **range-by-date is already foreclosed** by the `op_id` unique that makes offline dedup correct, so the key that would make retention a `DROP PARTITION` is gone and deletion will always be a `DELETE`. **Attendance retention was absent from database-conventions §4.4 entirely** — the largest table in the system — and no number is invented for it: attendance records are labour-dispute evidence, so it carries the ⚠️ VERIFY marker, this work's only one. One correction of my own during grilling: I stated refresh-to-punch as 3:1 from bad arithmetic; it is **1:1**, and the eightfold request finding is what survives. `CONTEXT.md` untouched, **zero error codes**. A-144–A-161. **ADR-0023, ADR-0022, ADR-0021, ADR-0020, ADR-0019, ADR-0018, and ADR-0017 all await review; the period-lock ADR is now 0024.** Next: `docs/04-database/erd-overview.md` (68/72), the first file since Phase 1 whose input is every module at once.**

**🚧 `docs/00-overview/implementation-roadmap.md` done 2026-08-04 (68/72), grilled 17 rounds.** Generated out of MANIFEST order at user request — `erd-overview.md` (row 68) remains pending. Seam: **the only file in the handbook nothing defers to** — seventy-five documents cross-reference constantly and not one routes a question here — so its value is not collecting what is written but being **the only document whose input is every module at once**. It owns build order, milestones, release gates, and the tenant onboarding sequence; for the V1 cut and the post-GA backlog it **indexes and never copies** (A-166), the same refusal `performance.md` made of three of its five MANIFEST topics. Two of MANIFEST's four topics had no owner anywhere: `HANDBOOK_SPEC.md` §7's Phase 3 order is explicitly a *documentation* order, and **no date, team size, or milestone appears anywhere in the handbook**. Structure is dependency-ordered with a **reading rule** — every one of ~40 ordering claims marked **forced** (the constraint named inline) or **judgment** (the reason named), so a reader knows what may be moved. One Mermaid: three tracks converging on R1, deliberately not a Gantt. **Findings that changed other files.** (1) **A tenant at the D1 typical size could not be onboarded** — nine modules each refuse a bulk import for individually correct reasons; together, `employee.master` carries no salary column and payroll writes **"No import."** in bold, so two thousand multi-line effective-dated pay packages are typed by hand and until they are there is no payroll run. No module could see it. Resolved reversing nothing: payroll's argument is against **supersede**, an onboarding package supersedes nothing, and `create_only` already carries the property the excluded dry-run existed to buy → **`ADR-0024` (Proposed)**, `payroll.salary_opening` in V1, revision import still deferred (A-165). (2) **119 `⚠️ VERIFY` markers across 27 files, counted for the first time** — discharging them produces no commits, which is why the work is invisible to every plan made of commits; made the third workstream with a named owner starting day one (A-163), and `ADR-0018`'s G20 narrowed from a global count to **the calculators a release exposes**, since read globally it holds every build at staging including a pilot whose BPJS code no path reaches (A-164). (3) **`ADR-0019` guarantees rollback depth one and nothing ever executes it** — first execution during an incident is a guarantee nobody has; rehearsal added to its Consequences. (4) **`ci-cd.md`'s `migrate:forward` bootstrap skip leaves testing-strategy §14.1 rule 2 inactive for the entire build**, so the riskiest migration in the project's history is the first one it guards. (5) `ADR-0012` + `ADR-0018` make the four statutory calculators **pure functions with zero runtime dependency**, so the product's largest correctness risk can be attacked on day one instead of last. (6) Nine **post-GA dependency clusters** where separately-scheduled deferred items are the same build — several saying so in their own words. No dates, no estimates, no Gantt, **no status column**, plus a shelf-life table marking §7 and §9.2 as never stale. **7 amendments across 6 files** + ADR-0024; A-162–A-170; zero new terms, zero new error codes.

**🚧 `docs/04-database/erd-overview.md` done 2026-08-04 (69/72).** Closes `docs/04-database/`. Seam, stated in §1 and pointed both ways: **module docs own column-level truth, this file owns the map** — a column appears only when it is an edge, and where the two disagree about a *column* the module doc wins, while a disagreement about whether an *edge exists* is a defect to be logged. That direction is what makes it safe for the file to be derived rather than authored. It is the only document drawn from all 115 tables at once, so it answers what no module doc can: the full census by class and owner, and **the complete cross-module foreign key inventory ADR-0001 §5 requires but had never been totalled** — every module doc lists its own outbound FKs, nobody held the sum. **277 FKs inclusive / 189 net of the RLS discriminator, 126 crossing a module boundary, 103 of those pointing at `core-schema.md`** (shared kernel, not extraction cost) — **leaving 39 genuine module-to-module FKs, and three tables absorb 30 of them**: `files` 15, `branches` 8, `approval_instances` 7. All seven approval edges are nullable, because every approvable module supports a direct administrative act, so **the engine can be extracted without a single NOT NULL violation**. Past the organizational references, exactly **four business-to-business constraints exist in the whole system** — `attendance_days.shift_id`, `attendance_days.leave_request_id`, `expense_claims.payroll_run_id`, `training_enrollments.development_item_id` — which is the strongest evidence available that ADR-0001's boundaries are real and not aspirational. Two things the graph made visible that no module doc could: **RLS class is not inferable from the columns** (§5 — six tables carry `tenant_id` or tenant payloads while sitting outside RLS, so any linter or generator testing for the column gets them wrong), and **16 of the 39 cross-module FKs were *deferred*** (§8 — the constraint real but added by a later migration for stated module load-order reasons, carrying no `.references()`, and therefore **invisible to the `drizzle-kit check` drift gate**). *(That section was deleted on regrilling: no such load-order constraint exists — see the entry below.)* Diagrams: 10 `erDiagram` blocks partitioning all 115 tables exactly once, no attributes (attributes here would be a drifting second copy of each module's Drizzle block), max 25 nodes against spec §13's ~40 ceiling; **all 10 validated with the Mermaid 11 parser, not by eye** — which regrilling showed is necessary and not sufficient: three edges were drawn with the crow's foot reversed and every one of them parses. Grouping is legibility only, recorded as A-171 so no later reader mistakes it for a boundary. **No `⚠️ VERIFY` marker: the file states no rate, cap, bracket, or multiplier** — the eight statutory parameter tables appear as structure, their values stay in `tax-pph21.md` and `bpjs.md`. **§11 logged four defects found by drawing the graph, none fixed here** (one file per task, and three of them are other modules' files) — renumbered §10 and grown to fifteen on regrilling: (1) `overtime.md` §schema says *"ADR-0001 forbids the constraint"* for `toil_ledger_entry_id`, but ADR-0001 §5 says cross-module FKs are **allowed** — the decision is sound, the citation inverts an Accepted ADR, so the fix is one sentence in `overtime.md`, not a supersession; (2) three composition cascades `database-conventions.md` §8 names by example exist in `payroll.md` only as `// FK cascade` **comments**, so a generator emits `RESTRICT`; (3) `payroll.md` declares **zero** `.references()` across its eight tables, the one module whose schema cannot be derived mechanically and had to be rebuilt by hand for §4.7; (4) `naming-conventions.md` carries a full `pgTable('payroll_runs', …)` example — a second definition of a real table name in a non-owning document, which defeats ownership resolution by scan. All four carried into the final audit row below.

**🚧 `docs/04-database/erd-overview.md` regrilled 2026-08-04 (10 rounds, `/grill-with-docs`), all 10 recommendations taken.** The file was generated but never grilled; this closes that. **The load-bearing move: a document that calls itself derived was verified against an actual derivation, and `scripts/erd-check.mjs` now *is* the derivation** — 115 tables, both FK bases, nine checks `C1`–`C9`, exit 1 on any failure, zero dependencies in a repository that has none. Seven of the file's own figures were wrong and every one of them is a fact a script settles in a second. **The missing row is the whole story:** `training_enrollments.development_item_id → development_items` was absent from §6, and with it the headline claim *"exactly three genuine business couplings between business modules — the strongest evidence ADR-0001's boundaries are real."* There are **four**. `training.md` had declared the edge correctly in a block titled *"Outbound cross-module FKs (ADR-0001 §5 extraction inventory)"*; the inventory dropped it because it was assembled by reading 26 files, and that edge is the only one whose `.references()` wraps onto a second line. Everything downstream moved with it — **37 → 39** module-to-module, **`files` 13 → 15**, **28-of-37 → 30-of-39** — while §6.1's *"the remaining nine"* was right all along, which is the tell that the derivation had 39 and the summary garbled it. **Three structural findings the arithmetic uncovered.** (1) **`ADR-0001` rule 5 was decorative.** It requires *"module docs list their outbound FKs"* and **24 of 26 never did** — only `training.md` and `announcement.md` ever carried the block — while `erd-overview.md` §1 asserted they all had, which is precisely what made the omission invisible for three phases. **Rule 5 amended in place** (relocation of a bookkeeping duty, not a change to what is permitted, so no supersession): the inventory lives in §6, derived. The two docs that maintained the block correctly are the proof that copying was never the unreliable step — adding up 26 files was. (2) **The `215` headline silently excluded ~88 real foreign keys.** `database-conventions.md` §3's `...tenantId` spread emits a genuine `tenants` constraint on all 96 RLS tables; the document's own §12 pre-committed a future reader to recounting and calling any mismatch a defect, so it had guaranteed that a *correct* recount would be reported as a bug. §6.1 now states **both bases** — 277 inclusive, 189 net — and every figure declares which one it is on. (3) **§8's deferred-FK pattern rested on a constraint that does not exist.** It claimed module load order made `.references()` impossible; the schema-file dependency graph built from every cross-file reference has **no cycles anywhere**, and every module owning a deferred target imports core only. The pattern bought nothing and cost the drift gate — its own rule 4 conceded `drizzle-kit check` could not see the constraints. **Section deleted**, the 16 columns becoming ordinary FKs in the sweep; 12 of the 16 never carried the migration-naming comment §8 required, which reads less like a pattern than like the section retroactively rationalising twelve forgotten calls. **`created_by`/`updated_by`/`deleted_by` reached §7** — ~250 unconstrained pointers, larger than all eight existing rows combined, undocumented in the file that owns *"which relations are deliberately not foreign keys, and why"*. Ratified unconstrained on a reason stronger than cost: the only sane policy for an actor column is `ON DELETE SET NULL`, and that means deleting a user **erases their attribution everywhere they ever acted** — the exact failure §7's `audit_logs` row already refuses. `database-conventions.md` §8's `SET NULL` clause therefore names a column class with zero instances (A-173). §7's closing rule reworded from `*_id` to **"unconstrained pointer"**, since suffix spelling was deciding whether the rule applied: `created_by` is a pointer with no `_id`, `op_id` and `install_id` are the reverse. **Three diagram defects and one undrawn table**, all caught by the script and none by the Mermaid parser this file had already passed — `domain_events`→`processed_events` and `audit_anchors`→`audit_logs` had the crow's foot reversed, `job_applications |o--o| employees` put the FK holder on the parent side, and **`overtime_rate_rules` appeared in no diagram at all** against §4's *"none omitted"*, because it holds no FK and no relation line reached it. Now drawn through `rate_version` — **the one relation in the schema where a foreign key is not declined but unavailable**, since the pin addresses a row *set* (`effective_from`, non-unique alone) and there is no key to reference. **`naming-conventions.md`'s `pgTable('payroll_runs', …)` example is not the cosmetic issue §11 called it:** it sorts ahead of `payroll.md`, so the derivation reads it first and classes `payroll_runs` as platform, corrupting the census to **20/68/27**. **`MANIFEST` gains row 70, a schema-declaration sweep, and the denominator moves 72 → 73** — placed *before* `ai-development-guide.md` deliberately, because that file teaches Drizzle conventions by example and would otherwise teach a deferral pattern that no longer exists. Its exit condition is objective: `node scripts/erd-check.mjs` exits 0. Today it exits 1 on 92 checks, all of them the sweep's list. **§11 → §10 and rebuilt as the file's full defect record, 15 entries split open/fixed** — against my recommendation, and correctly: §11 tells a reader to recount, so a reader who trusted `215` or `files 13` has to be able to learn otherwise *in the document where they read it*, not in a status tracker. `CONTEXT.md` untouched, zero error codes, zero ⚠️ VERIFY. A-172–A-174. Next: `MANIFEST` row 70, the sweep.

**🚧 `docs/08-ai-guide/ai-development-guide.md` done 2026-08-05 (70/73), grilled 11 rounds (`/grill-with-docs`), all 11 recommendations taken.** Opens `docs/08-ai-guide/`. **Written out of MANIFEST order** — row 70, the schema-declaration sweep, is still pending; taken deliberately after checking that the sweep touches nothing this file cites (the pairs derive from ADRs and coding-standards; the sweep's targets are 16 module documents' Drizzle blocks, which the guide never quotes), with the single leak — `database-conventions.md` §8's `SET NULL` clause naming a column class A-173 proved has zero instances — corrected here rather than deferred. **The seam is a subtraction.** `HANDBOOK_SPEC.md` §14 names ten rule areas; **nine already have an imperative home** in `coding-standards-{nestjs,nextjs,flutter}.md`, `naming-conventions.md`, `database-conventions.md`, `testing-strategy.md` and the six architecture documents. Written literally this file becomes the fourth copy of every rule in the handbook, and the copy is always the one a reader trusts. It restates none of them: §6 is a pointer index, and §10 makes deleting an accidental restatement a maintenance rule. **The tenth area — "documentation" — turned out to be nearly empty and to conceal the real question.** Swagger is `api-standards.md` §12, commits are `naming-conventions.md` §12, ADRs are the deviation protocol; what nothing anywhere decided is **what happens when code and the handbook disagree**, which matters precisely because `ADR-0025` pins the handbook while the code moves. Answered by splitting authority: **the handbook owns contracts** — module-document §§2, 3, 4, 7, 8, 11, 12 — **and is silent on implementation**, so a contract divergence is a defect taking the upstream-PR path and an internal difference is not drift and is recorded nowhere. Without that split, either every refactor owes an upstream PR and nobody complies, or the handbook rots while §2 still instructs agents to trust it. **Three questions had to be decided before §14 could be written at all, and became `ADR-0025` (Proposed).** How an implementation repository reaches the handbook — a **pinned git submodule at `docs/handbook/`**, where git *is* the drift mechanism, no sync script and no hash list, and the pin bump is the reviewable commit that says which revision the code was written against. This retires ci-cd **C8** and amends `ADR-0018` decision 7: vendoring the holiday vector plus a `sha256` check was a hand-built substitute for what a pin already guarantees — and that clause was the only part of the handbook that assumed a handbook-access mechanism **nothing had ever defined**. Where a deviation is recorded when the handbook is read-only — **one ADR namespace**, no local `docs/adr/` and no local `CONTEXT.md`, the submodule being a full clone so the agent writes the ADR inside it, opens a PR, **implements without waiting**, and marks every dependent line `// ADR-nnnn (Proposed, PR #n)` so a rejected proposal has a grep instead of an archaeology exercise. **The pair set is derived, not curated.** `## Alternatives considered` is the derivation nobody had noticed: by construction it enumerates what a reasonable engineer would otherwise have done, which is the violation example, already written — **135 of them across 24 ADRs**, plus **78 ban phrases** in the coding-standards documents. The filter is two conditions, both required: **a model's prior fights the rule, and no automated check catches the violation.** A lint error teaches faster than prose and never drifts, so `console.*`, `parseFloat` on money and `riverpod` in `pubspec.yaml` get index rows, not pairs; what earns a pair is the violation that **passes CI green** — `Promise.all` inside one transaction, a use case injecting a sibling, a repository joining another module's table. Seven ADRs are declared to have no code-shaped violation, so an absence is deliberate rather than an omission. **The reading protocol is a retrieval strategy with a measured budget.** All 53 non-ADR documents carry a line-3 `Status:` header and all 28 module and platform documents name both `Related ADRs:` and `Depends on:`, so the graph already existed. Following `Depends on:` at depth 1 costs another 3,645 lines from `leave.md`, almost all of it prose the feature never touches. Instead: **the ADR corpus whole** (1,900 lines, about 1.4 module documents — no safe subset exists, since the most-cited ADR is 24/28 and the rest is a flat tail), **registries grepped not read**, the entry module in full, and **dependency modules never read whole** — contracts resolved by grepping the port *name*, because port declarations sit under five different headings, `employee.md` declares `EmployeeStatusPort` in §13, and three module documents have no Ports heading at all (A-175 records that this inconsistency is deliberately tolerated, not a defect awaiting tidy-up). **~3,200 lines before the module document** (A-176), and §10 makes that a live constraint on the handbook rather than a description of it. **The `⚠️ VERIFY` rule is the highest-stakes line in the file:** an assistant never types a regulatory number, anywhere. Recounted at **121 markers across 28 documents** — `ADR-0018` §5 said 119/27 and is corrected, the original count having predated markers added later the same day. `ADR-0018` §4 is amended to extend **`structural-fiction-v1` from fixtures to the seed migration**, on its own argument one layer down: a fixture asserts, **a seed runs**, and an engine seeded with plausible rates is indistinguishable from a production-ready one. **`scripts/guide-check.mjs`** is the handbook's second executable artifact (A-174 amended from *"one"*), `G1`–`G7`, and it earned itself inside the session — writing `ADR-0025` broke the build until the ADR had a pair, which is exactly what §8 claims it does. Three CI gates registered in ci-cd §5 as **C11/C12/C13** with **C8 retired rather than renumbered**; C13 finally discharges `ADR-0006`'s Phase-1 claim that the three vendored `Result` copies *"are kept identical by this ADR + the AI development guide"* — a promise that had no mechanism and named a file that did not yet exist. Files amended: `ADR-0006`, `ADR-0018` (×3), `ci-cd.md`, `testing-strategy.md`, `database-conventions.md`. `CONTEXT.md` untouched — nothing here is a domain term; zero error codes; zero new ⚠️ VERIFY. Next: `MANIFEST` row 70, the sweep, then row 72.

**🚧 `MANIFEST` row 70 — schema-declaration sweep done 2026-08-05 (71/73).** Mechanical, no grill: the worklist was `erd-overview.md` §10.1's eight findings and the exit condition was objective — **`node scripts/erd-check.mjs` exits `0`**, from 92 failing checks (43 `C3`, 47 `C6`, 1 `C5`, 1 `C9`). **44 comment-only foreign keys became real `.references()`** across twelve documents. `payroll.md` was the bulk of it: the one module doc using no `.references()` anywhere now adopts the `...id, ...tenantId` spreads like every other and declares all of its edges, including the **three `onDelete: 'cascade'`** compositions that `database-conventions.md` §8 names *as its own worked example* while `payroll.md` left them as `// FK cascade` comments — an implementer generating from that block got `RESTRICT` on the payslip aggregate. **The discriminator count is the proof the sweep is complete: 88 → 96, which is exactly the number of RLS-class tables**, because payroll's eight tables had been spelling `tenant_id` by hand. **Six relations were declared unconstrained rather than constrained, and the reasoning generalised a rule that had been stated too narrowly.** `erd-overview.md` §7's audit-column row covered `created_by`/`updated_by`/`deleted_by`; the same argument governs `audit_logs.actor_user_id` and `approval_actions.actor_user_id` — **an actor column in a trail must outlive the account it names**, and since `NULL` already means *the system did it*, a nulled row is indistinguishable from one that was never attributed. `audit_logs.event_id` joins `processed_events.event_id` on the purge-schedule argument with a wider gap: the outbox purges at 30 days, an audit row lives for years. `impersonation_sessions.target_user_id` and `setting_values.key` are class crossings — the first already documented as *"no FK: cross-class reference"* in `system-administration.md` and simply never recorded where the rule lives. **The six statutory parameter sets turned out to be one pattern, not six coincidences:** `tax_*` and `bpjs_*` pin their parent by `effective_from`, whose parent unique key is `(effective_from, key)` — so it addresses a *row set*, not a row, which is the `overtime_rate_rules.rate_version` case exactly. `erd-overview.md` §7 grew 10 → 16 classes and ten diagram edges became dotted. **The `naming-conventions.md` fix took three attempts and the failures are the finding** (A-177). Renaming the `pgTable('payroll_runs')` example to a fictitious table — the fix §10.1 finding 4 actually prescribed — made the script count a **116th table** with no diagram node. Writing the correction note then re-created the defect a third time, because the note spelled the old declaration verbatim and the parser reads backticks. What works is making the example **unparseable**: angle-bracket placeholders teach the same mapping and match nothing. **Every figure in `erd-overview.md` moved**, and §§3, 6.1, 8, 9, 10 and 11 were rewritten to the derived values: **322 inclusive / 226 net / 149 crossing a boundary / 8 declared cascades / 16 non-FK classes**, `employees` 39, `users` 28, `payroll_runs` 6. **Module-to-module stayed at 39 with the same four business couplings** — which is the strongest confirmation available that last session's hand-derived inventory was right, since it was assembled by reading comments the script could not then see. §6.1's *"a floor, not a total"* becomes a total; §10 has nothing open. One finding carried out rather than fixed: **check `C6` flags only `*_id` columns**, while §7 rule 2 says the suffix decides nothing — so `paid_by`-class actor pointers escape it. Widening the check mid-sweep would have generated a fresh worklist; it goes to the final audit. Zero new terms, zero error codes, zero ⚠️ VERIFY. Next: `MANIFEST` row 72.

**🚧 `docs/08-ai-guide/implementation-claude-md-template.md` done 2026-08-05 (72/73), grilled 7 rounds (`/grill-with-docs`), all 7 recommendations taken.** Closes `docs/08-ai-guide/` and, with it, every content file in the handbook — only row 73's audit remains. **The seam is a subtraction, again.** `ADR-0025` put every protocol in a submodule the agent can read from inside its own repository, so the template's job shrank to whatever must be true *before* any handbook read happens: which repository this is, the mandatory read, and the stack prohibitions an agent can violate in its first file. Everything else — decision hierarchy, registries, Definition of Done — applies *while* working, by which time the guide is open. A `CLAUDE.md` that restated any of it would be a mutable copy in three places of what the submodule holds immutably in one. **The gate stopped being behavioural.** R8 of the guide's grilling accepted one residual risk — an agent might skip a mandated read — and `@`-import removes it: each block opens with `@docs/handbook/docs/08-ai-guide/ai-development-guide.md`, so in Claude Code the 355-line guide is resident before the first token. The size argument runs backwards from how it looks: §7's fifty pairs are needed **while writing the line**, which is exactly when a read-once-at-startup document has already been forgotten. Two limits stated rather than assumed — the import is a Claude Code feature, so the prose line beneath it is written to stand alone; and its failure mode is *silence*, an uninitialised submodule resolving it to nothing while the session looks normal, so **three independent signals carry the same fact**: import, prose, and gate `C11` (A-179). **Three verbatim blocks rather than one templated file**, because §14 says *ready-to-use* and anything with markers to delete or deltas to apply is ready-to-edit — with a silent failure mode at exactly the step nobody reviews. Duplication moves from three repositories, where nobody can see it, into one document, where `G8`–`G10` can: every block present, gating on §2 by name, carrying the import and the marker, and **no line appearing in exactly two of three blocks**, which is the signature of an edit that landed incompletely. **The grill found a defect in the previous session's own work.** `ai-development-guide.md` cited `CLAUDE.md` as authority in six §7 pair rows — written before `ADR-0025` existed to make that citation ambiguous. Under the submodule it resolves to `docs/handbook/CLAUDE.md`, the **handbook-generation** instructions: read `PROGRESS.md`, one large document per task, never bulk-generate. An implementer following it does something unrelated to writing product code, and the filename gives no warning. Every implementation repository will hold **two files named `CLAUDE.md`, and the one the guide cited was the other one** (A-178). All six repointed at the architecture document or ADR that owns each rule, which exposed a second gap: `mobile-flutter.md` §1 and `admin-nextjs.md` §1 state their prohibitions, **`backend-nestjs.md` never stated Prisma's** — it lived only in `ADR-0013`'s rejected-alternatives list and in the handbook project's `CLAUDE.md`. Now stated where an implementer reads the backend's fixed frame. Each template block repeats the two-file hazard inline, because a reader who notices two files with one name looks in the one already open. **`C13` generalised rather than a `C14` added**: it already diffed a marked region against a handbook source for `Result`, so it becomes a manifest of (local path, handbook source) pairs covering `CLAUDE.md` and `docs/agents/domain.md` too — one mechanism instead of two invented a session apart, with local additions still permitted below each marker so the file is never frozen. Below-the-marker scope is `ADR-0025` §3's split one layer up: **repo-local tooling belongs there, anything about the product goes upstream**, because a `CLAUDE.md` rule about the product is contract-class and an agent reading both copies cannot tell which wins. Ships `docs/agents/domain.md` verbatim — `ADR-0025` created that consequence and nothing owned it, and a template pointing at a file that does not exist is broken on arrival. CI workflow YAML deliberately excluded: `ci-cd.md` owns pipelines and ships none anywhere in 83 documents. `CONTEXT.md` untouched, zero error codes, zero ⚠️ VERIFY. A-178, A-179. Next: `MANIFEST` row 73, the final consistency audit — the last row.

**🚧 `MANIFEST` row 73 — final consistency audit done 2026-08-05 (73/73). The handbook is complete.** Run as **checks, not as reading**: cross-reference resolution, ADR reference resolution, MANIFEST-versus-filesystem in both directions, MANIFEST-versus-`PROGRESS`, error codes used versus registered, cross-file `§` citations, Mermaid validity, glossary coverage. **Most of it was already clean** — 0 broken paths across 83 documents, 0 dangling ADR references, 0 MANIFEST ghosts, 0 failing diagrams in the 57 documents that carry one, 20 glossary sections covering every module namespace with all six required Indonesian terms. That is the result the three grilling sessions earned; an audit that reads instead of checking would have found none of what follows. **Five defects, and their shape is the finding.** Four of the five were introduced in the last three sessions, and every one is a fact a script settles in a second: an invented error code (`training.md` raising `APRV_INSTANCE_NOT_IN_PROGRESS` where the catalog registers `APRV_INSTANCE_NOT_ACTIONABLE`), a **line number written as a section** (`backend-nestjs.md` §105 in the guide — and `ADR-0025` citing §11 for the same claim, both meaning §5), an ADR created mid-task and never registered in `MANIFEST`, a row-10 note stale since the sweep was inserted, and `payroll_runs.paid_by` unconstrained while **twelve of thirteen** sibling actor columns were not. One broken section citation in 83 documents, and it was in the newest file, twice — which is the honest measure of how fast a fresh document accumulates them. **The most useful finding was that a carried finding was wrong.** Two sessions carried a note that `attendance.md`'s `PeriodLockPort` served-list omitted four consumers. It omitted nothing: `asset.md`, `bpjs.md`, `expense-reimbursement.md` and `tax-pph21.md` each state **No `PeriodLockPort`** in bold, with reasoning. The note came from a `grep -l` that cannot tell consuming a port from declining one — and `ai-development-guide.md` §2.3 instructs an implementer to resolve contracts by exactly that grep, so the caveat now sits in the protocol that needs it: **read the hit, do not count it.** A false finding survived two sessions because nobody re-derived it, which is the same lesson `erd-overview.md`'s regrilling produced from the opposite direction. **The `C6` suffix gap closed instead of carrying forward.** `erd-check` inspected only `*_id` while `erd-overview.md` §7 rule 2 asserted the suffix decides nothing — a check claiming an enforcement it did not have, which is the defect class this handbook has spent three sessions removing. Widened to `*_by`; twelve of thirteen columns already complied, so it cost one constraint. Final derived counts: **115 tables · 323 foreign keys inclusive / 227 net · 150 crossing a module boundary · 39 module-to-module with four business couplings · 16 non-FK pointer classes · 8 cascades.** `erd-check` and `guide-check` both exit 0. **73 of 73 rows. 83 documents, 25 ADRs, 179 assumptions, 2 executable artifacts, 0 open findings.**

## Phase 1 — Anchor documents

| Path | Phase | Status | Updated | Notes |
|---|---|---|---|---|
| `docs/00-overview/product-overview.md` | 1 | done | 2026-08-01 | A-005 added (System Administrator tenant-scoped). New terms registered in `CONTEXT.md` seed (next file, per manifest dependency) |
| `CONTEXT.md` | 1 | done | 2026-08-01 | Seed: 7 domain groups, ~60 terms; VERIFY blanket for statutory values in §6. **2026-08-03 (asset.md): new §13 group "Company property (asset)"** — 8 asset terms (Asset, Asset code, Asset category, Custody, Asset condition, Handover document, Asset incident, Retirement) plus **Read-model view**, the third cross-module channel introduced by the ADR-0001 §6 amendment. Appended as a new group rather than renumbering, on the ADR-0016 Security-group precedent |
| `docs/03-standards/naming-conventions.md` | 1 | done | 2026-08-01 | A-006 added (three repos). Module prefix/namespace registry (§4) is the shared registry for error codes, permissions, events, jobs, settings, i18n. **Grilled 2026-08-02 (cluster F)**: §5 gains action-extension clause (new permission actions register first — parity with §3 verbs / §4 prefixes). **2026-08-02**: verb `terminate` added to §3 set (+ api-standards §1 mirror) — employee.md. **2026-08-02 (shift.md)**: verb `assign` added to §3 (+ mirror); §2.4 check example renamed `ck_shifts_end_after_start` → `ck_shifts_times_differ` (cross-midnight shifts legitimately have `end < start` — the old example invited a constraint that rejects them) |
| `docs/04-database/database-conventions.md` | 1 | done | 2026-08-02 | UUIDv7 app-generated PKs, numeric(15,2) money, set_config RLS wiring, forward-only migrations, D4 purge table (VERIFY). **Grilled 2026-08-02 (cluster D)**: `hris_migrator` BYPASSRLS (§9.3, §10.6 — FORCE binds owner, in-migration DML silently no-opped), §4.4 retention = module-owned keys (stale `maintenance.*` ref killed) |
| `docs/adr/ADR-0001-modular-monolith-module-boundaries.md` | 1 | done | 2026-08-01 | Accepted (records §5.3). Facade-only imports, table ownership, FK inventory, extraction criteria. **Amended in place 2026-08-03 (asset.md) — extension of §6's exception list, not a reversal of rule 2, so no supersession (ADR-0002 cluster-D and ADR-0012 precedents).** §6 now sanctions **published read-model views**: a module may publish a named, read-only view over a narrow subset of its own tables that any module's repositories may JOIN. Four constraints, all load-bearing — (a) declared, owned, and versioned by the owner; (b) **no ADR-0016 encrypted column and no masked column**, which is what preserves rule 2's actual purpose since a joiner then cannot bypass masking, decrypt, or skip the sensitive-read trail; (c) **`security_invoker = true` is mandatory**, because a Postgres view otherwise executes with its owner's rights and silently bypasses the underlying RLS policy — a leak, not a style choice; (d) counted as extraction cost exactly like a cross-module FK under rule 5. Motivated by a real defect rather than a wish: attendance, leave, overtime, and expense had all shipped grids returning `fullName`/`employeeNumber` with a `q=` search over them and **no sanctioned channel** — `OrgQueryPort` carries IDs only, `EmployeePayrollPort` decrypts statutory identifiers and audits per batch. A batch enrichment port cannot fix it because a filter or sort on a name must run before the page boundary. Enforcement stays mechanical: dependency-lint allows the view name, keeps rejecting the table. Consequences bullet extended. First and only published view: `employee_directory` |
| `docs/adr/ADR-0002-multi-tenancy-rls.md` | 1 | done | 2026-08-02 | Accepted (D11 confirmed). No-BYPASSRLS rule (runtime); platform aggregates via per-tenant jobs; ConnectionProvider escape hatch; 2-tenant leak tests mandatory. **Amended 2026-08-02 (cluster D grill)**: `hris_migrator` carries BYPASSRLS (CI-only; FORCE binds owner), `hris_auth` narrow lookup role noted |
| `docs/adr/ADR-0003-offline-sync-conflict-resolution.md` | 1 | done | 2026-08-01 | Accepted. Server-authoritative, 4 sync classes, opId=Idempotency-Key, SQLCipher local DB, MSS approvals online-only V1 (confirmed in grilling). "Sync class" added to CONTEXT.md. Amended 2026-08-02: durable `op_id` unique column on sync-class rows |
| `docs/adr/ADR-0004-auth-sessions-device-management.md` | 1 | done | 2026-08-01 | Accepted. Email unique per tenant + picker; opaque rotating refresh w/ family revoke; no permissions in JWT; 1-device default; local-only PIN/biometric; A-007 (no MFA V1); CONTEXT §8 added |
| `docs/adr/ADR-0005-rbac-permission-model.md` | 1 | done | 2026-08-01 | Accepted. Two-axis (permission + data scope); immutable templates + custom roles; assignment-level company scoping; additive-only, no deny/hierarchy/wildcards; deny-by-default route lint; 5 terms → CONTEXT §8 |
| `docs/adr/ADR-0006-result-pattern-error-handling.md` | 1 | done | 2026-08-01 | Accepted. Vendored canonical Result (TS + Dart sealed), throw/return boundary table, immortal codes, no HttpException from use cases |
| `docs/adr/ADR-0007-api-versioning-response-envelope.md` | 1 | done | 2026-08-01 | Accepted. Whole-surface URI versioning; success/error envelope + X-Request-Id; money as decimal string; offset vs cursor split; Idempotency-Key 7d Redis replay. Catalog seed owes: VAL_ idem-conflict code |
| `docs/adr/ADR-0008-approval-workflow-engine.md` | 1 | done | 2026-08-01 | Accepted. Chain snapshot; quorum all/any; 4 resolvers + fallback; no transitive delegation; no auto-approve on SLA; restart-on-resubmit; 5 terms → CONTEXT §7 |
| `docs/adr/ADR-0009-file-storage-strategy.md` | 1 | done | 2026-08-01 | Accepted. Signed-URL-only (no Firebase client SDK); metadata-as-authority; staged uploads; no AV in V1 (hook reserved, flagged); A-008 selfie retention; CONTEXT §9 added |
| `docs/adr/ADR-0010-background-jobs-events.md` | 1 | done | 2026-08-01 | Accepted. 8-queue registry + retry classes; transactional outbox for events; scan+fan-out crons; idempotent processors mandatory. Edited naming §7 (`on.` prefix, `events` queue); CONTEXT §10 added. core-schema owes `domain_events` |
| `docs/adr/ADR-0011-observability-stack.md` | 1 | done | 2026-08-01 | Accepted (D6). A-009 (Cloud Logging + Tempo); 10% head sampling + force-sample hooks; requestId correlation chain; PII-free telemetry rule; business failures ≠ Sentry events |
| `docs/adr/ADR-0012-payroll-calculation-engine.md` | 1 | done | 2026-08-01 | Accepted. Snapshot-deterministic engine + calc trace; closed=terminal, retro via delta components; YTD ledger; no formula DSL; run approval via ADR-0008; 4 terms → CONTEXT §6. **Amended in place 2026-08-02 (overtime.md — clarification, not a reversal, so no supersession; ADR-0002 cluster-D and ADR-0016 grill precedents):** the `run_input` bullet now states that `run_input` names the **component class, not the transport** — overtime and leave effects are **pulled** from their owning modules' ports at snapshot time, reimbursements and ad-hoc adjustments are entered directly, and no module pushes rows into a run. The ADR previously said both "approved overtime hours enter as `run_input`" and "run creation snapshots approved overtime effects"; payroll, tax-pph21, and bpjs would each have re-derived a reading from that **Amended twice 2026-08-02 (payroll.md grill), in place — substance holds, no supersession (ADR-0002 cluster-D precedent):** (1) component statutory attributes are now **two orthogonal axes** — `wage_category` for statutory wage-base membership, `income_class` for tax treatment — with every base a code-owned formula over categories; the earlier "inclusion flags per BPJS contribution base" is replaced, because flags make a silent underpayment valid config and cannot express the 75% floor, which compares two bases. (2) "period locking binds at `calculating`" → **precondition of `calculating`**; attendance owns the period, payroll verifies and never writes it, `PayrollRunGuardPort.runsOver` guards release. This is the second such wording ambiguity found in this ADR (the first was `run_input` push-vs-pull, clarified at overtime.md) — both were resolved before a dependent file had to guess. **Amended a third time 2026-08-03 (tax-pph21.md grill), in place — extension, not reversal, so no supersession:** (1) `income_class` gains **`final`** — PPh 21 on severance is a final tax on its own tariff, outside both the monthly base and the annual recalculation, and with only three values every classification a tenant could pick for it was wrong; same defect shape amendment 1 removed from the wage axis. (2) the calculator input carries a **month-to-date slice** beside the year-to-date one, because TER is a rate on a month's *total* bruto and this ADR's own run model permits two runs to share a payment month; determinism additionally requires that a run not begin calculating while another run for the same company and month is in `calculating` or `review`. **Amended a fourth time 2026-08-03 (bpjs.md grill), in place — extension, not reversal:** component `kind` gains **`employer_cost`**. The employer's share of a statutory contribution is neither an earning nor a deduction, and before this value the whole employer side had no line representation at all while this ADR's own pipeline already promised "BPJS employee/employer parts" as a stage output. Load-bearing rather than cosmetic: **some employer-paid premiums are taxable income to the employee** ⚠️VERIFY, and the tax calculator's only view of income is its `lines` argument — a premium that is not a line is a premium missing from bruto, permanently and invisibly, on every payslip and every 1721-A1. Two invariants written into the amendment because they are what the value must not break: net stays `Σ earning − Σ deduction` with employer_cost outside both, and taxable assembly sums earning **and** employer_cost lines by income class. Also clarified there: the BPJS stage sitting before taxable assembly is load-bearing ordering, and it reads the **stage-1 unprorated** wage rather than proration's output. Four amendments in four sessions, each from a downstream module finding real detail — a consolidated supersession belongs at the Phase 4 audit, not before. **2026-08-03 (expense-reimbursement.md): stale example corrected in place, no amendment.** The amendment-1 clause listed "reimbursements and ad-hoc adjustments are entered directly" against overtime and leave being pulled. payroll.md §4.4 has declared `ExpenseQueryPort` since 2026-08-02 and expense now serves it, so reimbursements move to the pulled list and ad-hoc adjustments stay directly entered. No decision changed — this is the same class of repair as the settings §2 and §4.2 forward-promise corrections, not a fifth amendment. |
| `docs/adr/ADR-0013-database-conventions-drizzle.md` | 1 | done | 2026-08-01 | Accepted. Rationale companion to database-conventions.md (9 load-bearing choices justified); no new rules introduced |
| `docs/adr/ADR-0014-pdf-generation.md` | 1 | done | 2026-08-02 | Accepted (D8; pdfmake question settled → Puppeteer). Generate-once-store-forever; worker-only rendering, no-network Chromium, sandbox mandatory; React SSR templates |
| `docs/adr/ADR-0015-import-export-framework.md` | 1 | done | 2026-08-02 | Accepted (D9). Definition registry; mandatory dry-run + commit revalidation; partial/strict commit modes; xlsx-only, formula-injection defense; 4 terms → CONTEXT §11 |
| `docs/04-database/core-schema.md` | 1 | done | 2026-08-02 | 14 tables + 6 enums, full Drizzle + ERD; RLS application table (outbox exception documented); no branch_id on employees (org assignments own placement); A-010 (PG16+). Debts cleared: domain_events, sessions/devices, RBAC tables |
| `docs/03-standards/error-catalog.md` | 1 | done | 2026-08-02 | Seed: SYS(5) VAL(3+7 field-level) AUTH(11) AUTHZ(1); existence-hiding 404 rule; ADR-0007 idempotency debts cleared; all other prefixes reserved-empty. **Grilled 2026-08-02 (cluster C)**: `SYS_NOT_FOUND` added (default entity-404/existence-hiding — no module code existed for the 404s docs emit), §2 amended (module `*_NOT_FOUND` only when clients branch), stale `SYS_RATE_LIMITED` source fixed **2026-08-02 (payroll.md):** `PAY_` block added as §21 (12 codes), Deprecated → §22, ledger → §23. **19 owned prefixes, 116 codes.** `PAY_PERIOD_NOT_LOCKED` is the deliberate inverse of the §18–§20 lock family: those fire on a write into a locked period, this fires on a calculation over a period not yet locked. **2026-08-03 (tax-pph21.md):** `TAX_` block added as §22 (5 codes), Deprecated → §23, ledger → §24, and `PAY_MONTH_RUN_IN_FLIGHT` appended to §21. **20 owned prefixes, 122 codes**; `TAX` removed from the reserved-empty list. Two conditions deliberately declined a `TAX_` code: an absent statutory parameter version reuses `PAY_PARAMETER_MISSING` (payroll already raises it at run creation with the same `{ parameter, asOf }` shape, and a second code would split the branch clients write), and every unknown identifier stays `SYS_NOT_FOUND` per §2. `PAY_MONTH_RUN_IN_FLIGHT` sits in the `PAY_` block although tax motivated it — codes belong to the module that raises them. **2026-08-03 (bpjs.md):** `BPJS_` block added as §23 (3 codes: registration overlap, coverage overlap, risk class required), Deprecated → §24, ledger → §25. **21 owned prefixes, 125 codes**; `BPJS` removed from the reserved-empty list, which now holds ten. One code covers both employee-level tables — an exclusion and a dependent count fail the same way, and `details.program` distinguishes them. Three conditions deliberately took **no code at all**: an unregistered company, an unconfigured wage floor, and an employee excluded from every program are preflight **warnings**, and giving a warning a code invites a client to branch on it as a failure. **2026-08-03 (expense-reimbursement.md):** `EXP_` block added as §24 (5 codes), Deprecated → §25, ledger → §26. **22 owned prefixes, 130 codes**; `EXP` removed from the reserved-empty list, which now holds nine. `EXP_CLAIM_IN_RUN` is noted as resembling the §18–§20 lock family in shape but not in cause — those fire on a write into a frozen period, this one on a write to a row a live run has claimed, and releasing it is an act on the run. Three conditions deliberately took no code: an over-policy line is a flag, since a code invites clients to branch on it as a failure and reintroduces the blocking limit the module rejected; archiving a category with live claims raises nothing because every claim pins its own policy; unknown ids stay `SYS_NOT_FOUND`. **2026-08-03 (asset.md):** `AST_` block added as §25 (6 codes), Deprecated → §26, ledger → §27. **23 owned prefixes, 136 codes**; `AST` removed from the reserved-empty list, which now holds eight. `AST_ALREADY_ASSIGNED` and `AST_NOT_ASSIGNABLE` share a status and stay separate because the next action differs — get it back from its holder versus wait for the repair shop. Four conditions deliberately took no code: a duplicate asset code or serial is `VAL_DUPLICATE`, archiving a category with live assets breaks nothing, a worse return condition is a response flag rather than a failure (a code would turn BR-AST-007's prompt into the refusal it rejects), and unknown ids stay `SYS_NOT_FOUND`. Asset registers neither a lock-family nor an `APRV_` code — no period-lock and no approval-engine interaction at all |

## Phase 2 — Architecture, standards, platform modules

| Path | Phase | Status | Updated | Notes |
|---|---|---|---|---|
| `docs/02-architecture/system-overview.md` | 2 | done | 2026-08-02 | C4 context + container, GKE topology, request spine, sync/outbox wire flows, invariants + capacity tables. No new rules/terms/codes — pointer doc by design |
| `docs/02-architecture/backend-nestjs.md` | 2 | done | 2026-08-02 | Folder anatomy, layer import rules, facade mechanics, request lifecycle → Nest primitives (tx-before-validation tradeoff documented), unwrap/filters, UnitOfWork + TenantScopedRepository invariants, worker bootstrap fail-closed, Swagger, CI enforcement table. A-011 added (class-validator backend; Zod stays admin-web) |
| `docs/02-architecture/mobile-flutter.md` | 2 | done | 2026-08-02 | Feature anatomy, layer rules, Cubit-default + 2 standing Bloc justifications (sync engine, clock-in), local-first read path, Drift+SQLCipher (key-loss = device-replacement consequence class; local_* never destructively migrated), sync registry seam, dio interceptor chain, unlock flow, bootstrap order, CI enforcement. A-012 added (get_it/go_router/dio/Equatable). **Grilled 2026-08-02 (cluster E)**: §8 interceptor row 2 = two-class auth-loss disposition (session-lost preserves DB+queue / device-terminal wipes), §9 gains DB-owner identity marker row + pre-render identity check |
| `docs/02-architecture/admin-nextjs.md` | 2 | done | 2026-08-02 | Anatomy, RSC-never-calls-API rule (token model consequence), Server Actions = locale/theme cookies only, silent-refresh bootstrap + multi-tab, Axios chain, React Query conventions (no optimistic on money surfaces), RHF+Zod server-error mapping, DataTable wrapper, permission UI + existence-hiding rendering, next-intl (A-013), CI enforcement |
| `docs/02-architecture/offline-sync.md` | 2 | done | 2026-08-02 | Full Drift queue/cache-meta schema, single-flight drain + backoff, no-batch-endpoint rule, outcome table, durable `op_id` server pattern (ADR-0003 amendment applied), per-class conflict mechanics, 6 crash invariants, pending-ops UI states, logout/revocation flows, module §10 checklist. Registered `SYNC_VERSION_CONFLICT` + `SYNC_OFFLINE`; Tombstone → CONTEXT.md. Settings keys `sync.*` named — settings.md registers definitions. **Grilled 2026-08-02 (cluster E)**: §4 terminal-auth row split (session-lost vs device-terminal), §9 session-lost re-login flow (identity check; wipe only on identity change) + offline-logout best-effort note, §8 paused-banner split (suspended notice), §5 note: op_id dedup covers creates — >7 d transition replays converge |
| `docs/02-architecture/multi-tenancy.md` | 2 | done | 2026-08-02 | TenantContext/RequestContext contracts, resolution matrix per entry point (suspended semantics: in-flight jobs finish, retention continues), company-scope 2nd axis (repo-enforced, 404 on miss), RLS ops (roles SQL, no session-SET rule, policy-coverage CI gate), leak-test matrix (→ testing-strategy), non-Postgres hygiene table, per-tenant-DB move runbook, provisioning invariant (no setup mode). **Grilled 2026-08-02 (cluster D)**: §4 roles SQL gains migrator BYPASSRLS + `hris_auth` (SET LOCAL ROLE, 4-table SELECT-only + `auth_lookup` policies), L7 added (matrix now L1–L7), scope-miss codes → `SYS_NOT_FOUND` incl. write references, job companyScope=all, runbook restore note |
| `docs/03-standards/api-standards.md` | 2 | done | 2026-08-02 | Resource/method rules (no PUT, no 204 — envelope everywhere), request conventions (unknown fields rejected, no multipart), deterministic-sort rule, pagination registry seeded (8 families; audit log = cursor; offset depth cap 10k), idempotency working rules (raw-body sha256 hash, replays skip re-auth), delta-sync endpoint shape, body-version concurrency, bulk per-item result shape, Swagger review gate, module §7 spec template. **Grilled 2026-08-02 (cluster F)**: §1.4 admits `— (authenticated)` own-scope endpoints, §11 existence-hiding → `SYS_NOT_FOUND` (cluster-C alignment), §13 holiday §7 declared canonical instantiation. **2026-08-02 (shift.md)**: §1.2 verb list + `assign`; §10 admits the natural-key bulk shape (`{ items }` for create/upsert batches — a bulk create cannot reference ids it is about to mint; first use `roster-days/bulk-assign`) |
| `docs/03-standards/security-standards.md` | 2 | done | 2026-08-02 | ASVS L2 map with honest gaps (no MFA/AV/pinning), auth hardening defaults, rate-limit tiers (throttle before argon2), CSP/CORS/CSRF (SameSite+Origin, no token machinery), injection table, EdDSA JWT (A-014), UU PDP map (VERIFY on breach window/selfie basis/retention), redaction registry (ADR-0011 debt cleared), supply chain, mobile posture (no pinning V1 — reasoned; FLAG_SECURE payslip/selfie). **Grilled 2026-08-02 (cluster F)**: §3 login/reset limits split per dimension (per-email strict / per-IP NAT backstop 30/min + 300/h) + rate limits = platform config not settings keys; §2 reset/invite TTL + PIN threshold platform-fixed, argon2-cap mislabel fixed |
| `docs/adr/ADR-0016-field-level-encryption.md` | 2 | approved | 2026-08-02 | **Accepted 2026-08-02 — grilled (4 rounds), ratified with 4 amendments:** (1) scope confirmed — NIK/NPWP/BPJS×2/bank+holder encrypted; PTKP/contacts/amounts explicitly plaintext (core-schema §1/§7 comments aligned); (2) individual UU PDP erasure = purge-based after statutory horizon (VERIFY), crypto-shredding tenant-offboarding only, per-employee DEKs rejected in Alternatives; (3) blind indexes NIK+NPWP only, digits-only normalization, BPJS/bank index-less; (4) self-view = masked always + online-only memory-only reveal, registered as sensitive read (employee.md duty). Status refs flipped in security-standards (header, §1 V6, §8 table), document-storage §0, audit-log §0. 3 glossary terms → CONTEXT §11 (new Security group; Import & export → §12) |
| `docs/adr/ADR-0017-platform-identity-impersonation.md` | 3 | done | 2026-08-04 | **Proposed — needs user review.** Written during system-administration.md's grilling, because platform authentication turned out to be specified nowhere: `platform_users` carried `email` + `password_hash` since core-schema §4 with no login, no session, and no impersonation mechanics. Seven decisions: (1) `platform_sessions` as a disjoint identity class applying ADR-0004's law unchanged, minus the rotation grace window and minus an `mfa_verified` column; (2) `typ` audience separation enforced both directions; (3) **TOTP mandatory for platform users only** — A-007 narrowed rather than reversed, since its stated evidence is spec §5.8's tenant login list and no platform surface existed when it was written; (4) impersonation as a 30-minute non-renewable single-occupancy token against a named user with a mandatory reason, cascade-ending with its parent session; (5) the only `TenantStatusGuard` bypass in the system; (6) **no action deny-list** — visibility over an incomplete prohibition, residual stated (A-097); (7) platform-user administration off the API, which is what keeps every console act tenant-targeted. Alternatives recorded: in-place ADR-0004 amendment, no-MFA, IP allowlist, staff SSO, nullable-`tenant_id` session reuse, stateless JWT, read-only impersonation, tenant-scoped impersonation, deny-list, two tiers. Consequences applied same session to ADR-0004, A-007, security-standards §1, error-catalog, notification §4.2, audit-log §4.2/§4.3, multi-tenancy §5. |
| `docs/adr/ADR-0018-statutory-calculation-test-strategy.md` | 4 | done | 2026-08-04 | **Proposed — needs user review.** Written during testing-strategy.md's grilling. The problem: `CLAUDE.md` forbids inventing regulatory numbers, so every statutory rate carries `⚠️ VERIFY`; BR-TAX-001, BR-BPJS-001, and BR-OVT-009 make those rates **platform data seeded by migration**, so a Testcontainers database holds exactly the unconfirmed values — and any golden vector computed against them produces a number that **looks like compliance evidence and is not**, passing forever after the real rate turns out to differ, because code and expectation moved together. Seven decisions: (1) golden vectors as a defined artifact with `input`/`expected`/`source` per vector, reviewable by a domain expert without reading TypeScript; (2) **a vector is never edited to make a test pass** — a failure is a code defect until a human re-verifies against the regulation and records why, which makes the file's git history the audit trail of every statutory reinterpretation the product has made; (3) **structural and statutory vectors split into separate files with different lifecycles**; (4) structural vectors run against a **deliberately fictional rate set** — PTKP 100,000,000, JKK 1%, every overtime factor exactly 2.0 — because a fixture using *approximately* real rates is the version that quietly becomes the compliance suite, and an engineer "correcting" it to a plausible value destroys the property rather than fixing a typo; (5) pending statutory verification **blocks release promotion, never a merge**, since no pull request can supply a verified Indonesian regulation — and the count becomes the concrete definition of "the calculators are ready", replacing a prose intention with a number a pipeline enforces; (6) **no integration or e2e test asserts a money amount** anywhere, because without it the other six leak straight back in through a file nobody searches when a rate is corrected; (7) a vector file lives in the handbook only when more than one implementation consumes it — today exactly one, holiday resolution, since holiday §4 states it is implemented twice while shift §4 says once. Alternatives recorded: best-guess rates marked VERIFY, structural-only, approximately-real fixtures, snapshot baselines, inline expected values, one file per module, all vectors in the handbook, property tests as a replacement. No schema change, no error codes, no module-doc change. |
| `docs/03-standards/design-system.md` | 2 | approved | 2026-08-02 | **User-approved 2026-08-02 — binding for all module UI Flow sections.** Both skills in loop, reconciliation recorded (§1); shared brand blue + slate, status vocabulary (never color-alone), Inter 400/500/600 tabular-nums money, admin border-first flat / mobile M3, Lucide vs Material Symbols split, AA checklist, DataTable + Flutter kit inventory, microcopy rules, signature devices: admin scope bar + mobile sync truth line |
| `docs/03-standards/coding-standards-flutter.md` | 2 | done | 2026-08-02 | Strict analyzer rules (no dynamic/bang/default-on-sealed), two sanctioned state shapes, isClosed guards, no-ceremony-forwarding use-case rule, repo conversion boundary code, Drift money = TEXT+Decimal converter (A-012 appended), hand-written JSON mapping (unknown enums → `unknown`, never throw), test table per layer (fakes>mocks, bloc_test, both-theme goldens, sync crash-invariant tests), CI table |
| `docs/03-standards/coding-standards-nextjs.md` | 2 | done | 2026-08-02 | TS rules (strict+noUncheckedIndexedAccess, no any/enum/bang, open-wire-enum fallback vs assertNever), 'use client' at feature root, derive-don't-sync effect rule, RQ hook idioms (invalidation in mutation hooks), lib/zod.ts field primitives, no client money arithmetic, no runtime response validation, context whitelist, error surface order (field>panel>toast), token-only styling, test stack Vitest/RTL/MSW/Playwright (A-015), CI table |
| `docs/03-standards/coding-standards-nestjs.md` | 2 | done | 2026-08-02 | TS rules (verbatimModuleSyntax OFF — emitDecoratorMetadata divergence documented), error-factory shape + status map beside factories, async discipline (sequential awaits inside tx — single pg connection; Promise.all only outside tx), Drizzle idioms (row types never cross repo boundary, N+1 rule), money = decimal.js explicit rounding (A-016), Clock port (no new Date() in domain/application), business dates as date strings, thin processors, pino structured logging, test table (Jest/supertest/Testcontainers, leak tests L1–L6, double-delivery idempotency proof), CI table |
| `docs/05-platform/authentication.md` | 2 | done | 2026-08-02 | Full §9.1 template. BR-AUTH-001–014; rotation grace window 10s (BR-AUTH-005, glossary added); `auth_tokens` table (reset+invite, single-use); session/device stateDiagrams; 15 endpoints (operation-style paths = sanctioned §1 deviation, documented); registered `AUTH_RESET_TOKEN_INVALID` + `AUTH_INVITE_TOKEN_INVALID`, AUTH_ owner flipped from pending; device replacement `admin` policy = manual revoke, NO approval-engine dep (V1); purge crons + 3 outbox events. New settings key for settings.md: `auth.device_replacement_policy`. **Grilled 2026-08-02 (cluster D)**: §5 gains the pre-tenant lookup path (`SET LOCAL ROLE hris_auth` — login scan, refresh hash, reset/invite tokens, device check; writes under resolved tenant context) — the core-schema §9 wrinkle now actually documented here. **Grilled 2026-08-02 (cluster E)**: refresh request gains optional `fcmToken` (§7 — device-row upsert; §9 edge reworded), UC-AUTH-003 offline logout = best-effort revoke, §10 rewritten to two-class disposition, §14 +password-reset-preserves-queue scenario. **2026-08-02 (employee.md arrival)**: §13 gains `AccountLifecyclePort` (createUserForEmployee — user + link + Employee template + invite same-tx; deactivateUser — inactive + session revoke same-tx) |
| `docs/05-platform/authorization-rbac.md` | 2 | done | 2026-08-02 | Full §9.1 template. BR-AUTHZ-001–013; grilling note delivered: clone drift = live set-diff vs current template (no timestamps — `cloned_from_role_id` column added to roles), drift banner + explicit adopt (never silent sync); catalog registration protocol = union of module §2 matrices (mirrors error catalog); last-admin guard; permission keys immortal + `deprecated_at`; template re-sync = release job, idempotent, clones untouched; registered `AUTHZ_SYSTEM_ROLE_IMMUTABLE`/`AUTHZ_ROLE_IN_USE`/`AUTHZ_LAST_ADMIN`, owner flipped; "Clone drift" → glossary; /me/permissions satisfied by /auth/me (no separate endpoint) |
| `docs/05-platform/approval-engine.md` | 2 | done | 2026-08-02 | Full §9.1 template. BR-APRV-001–015; 6 Drizzle tables (chains w/ steps-jsonb, instances w/ live-partial-unique, steps, assignees, immutable actions [UPDATE/DELETE revoked], delegations); instance+step stateDiagrams; **design decision: action endpoints are module-owned HTTP routes → engine port in-process (no public action API — keeps @RequirePermission static, two-gate at port)**; provisioning seeds default chain per type (BR-APRV-003); stuck-instance flag (glossary added); SLA = reminder@SLA + escalate@2× platform-fixed; 7 APRV_ codes registered (catalog renumbered: Deprecated→§9, ledger→§10, no external refs broken); request-type registry seeded (8 types). Settings keys → settings.md: `approval.max_chain_depth`, `approval.fallback_role`. **Grilled 2026-08-02 (clusters A+B)**: cluster A confirmed as written; cluster B added §12 event `approval.assignee.acted` (per recorded decision, incl. partial `all`-quorum — inbox actor-item completion)  **2026-08-03 (expense-reimbursement.md arrival):** `expense.claim`'s §13 context declared in the consumer registry — including `overPolicyLineCount`, the field that turns an advisory policy flag into a real control by letting a chain route an over-budget claim to a stricter approver instead of a validator refusing it. |
| `docs/05-platform/notification.md` | 2 | done | 2026-08-02 | Full §9.1 template. BR-NTF-001–012; template registry code-owned, §4.2 seeded 13 templates (auth 7, approval 5, authz 1); two entry paths (event handlers + NotificationPort direct) → one pipeline; async-always (BR-NTF-003 protects auth timing defense); dedupe `(tenant, dedupeKey, user)` unique; render-once locale snapshot; revoked devices never pushed (offline-sync §8 promise kept); in-app = durable channel; fanout chunks ≤500; feed cursor + unread-count endpoints; A-017 (EmailPort + SES ap-southeast-1); `notification.retention_days` → settings §4.2; `NTF_TEMPLATE_MANDATORY` registered (catalog: NTF_=§10, Deprecated→§11, ledger→§12); glossary +1. Forward: environments.md owns SPF/DKIM/sender domain. **Grilled 2026-08-02 (cluster B)**: hybrid FCM message (UC-NTF-003 — data-only doesn't render on killed iOS), mark-read → cosmetic replay lane (§10), admin-web badge focus + 60 s visible-tab interval (§7 unread-count; inbox shares posture). Cluster E touch: §9 FCM-rotation edge now cites refresh-carried `fcmToken` (authentication §7). **2026-08-03 (tax-pph21.md):** +`tax.form_issued` (in-app + push, mandatory, to the employee, carrying tax year and **revision** — the payload that matters when a form they already filed against has been superseded) and `tax.issuance_finished` (in-app, to the initiating admin, mirroring `payroll.calculation_finished`). Both two-segment per the registry convention  **2026-08-03 (expense-reimbursement.md):** +`expense.claim_paid` (in_app + push, opt-out, to the claim's employee, carrying `disburseVia` and `paymentReference`) — one template fired from **both** disbursement routes, so the employee is told which one paid them. Opt-out rather than mandatory because the payslip and the claim's own history both carry the fact durably. **2026-08-03 (asset.md): 2 templates added** — `asset.assigned` (in_app + push, opt-out, to the issued employee) and `asset.clearance_pending` (in_app, **mandatory**, role audience of `asset.item.read` holders). The second is mandatory because it fires exactly once, at the last moment anyone can reach a departing employee, and asset owns no cron to send a second |
| `docs/05-platform/inbox.md` | 2 | done | 2026-08-02 | Full §9.1 template. BR-INB-001–010; **inbox = navigation layer, never state truth** (BR-INB-001 — target screen authoritative); materialized `inbox_items` (dedupe unique per assignee/announcement); badge = open count, seen ≠ done; **acknowledge = the one queue-reachable action** (append-only fact class, no-op replay); approval tasks never dismissible; open items never purge; closure via new engine event `approval.step.decided` — **appended to approval-engine §12 same session**; `inbox.retention_days` → settings §4.2; 2 INB_ codes (catalog: INB_=§11, Deprecated→§12, ledger→§13); emits `inbox.item.acknowledged` (announcement.md consumes). **Grilled 2026-08-02 (cluster B)**: BR-INB-006/UC-INB-002 rewired to new `approval.assignee.acted` (actor item `done` per decision — `all`-quorum stale-badge hole closed), seen marks → cosmetic replay lane (§10), returned-request items → §15 Future (V1: push + my-requests state suffice) |
| `docs/05-platform/document-storage.md` | 2 | done | 2026-08-02 | Full §9.1 template. BR-DOC-001–012; `files` schema (staged→committed, quarantined reserved, commitFailureCode, documentExpiresAt vs staging TTL split); category registry code-owned (8 categories seeded; per-category permissions/resolver/TTL/deletable — payslip URL TTL 120s); **generic endpoints = @AuthenticatedOnly + registry-driven gates (documented BR-AUTHZ-002 deviation)**; confirm idempotent (drain safety); worker-generated files skip staging; slot+confirm queue-reachable (selfie drain order: upload before punch op, aggregate-blocked); 5 DOC_ codes (catalog: DOC_=§12, Deprecated→§13, ledger→§14); 3 `document.*` settings + `document.expiring` template registered same session. Grilled 2026-08-02 (cluster C): `import_file` gates split (write = any import definition perm via user-parented slot; read = source/error definition perm vs export outputs requester-only), gated-export mints = fail-closed sensitive reads  **2026-08-03 (expense-reimbursement.md arrival):** the `receipt` row's forward blanks filled — write gate, read gate, ownership resolver, and the sensitive-read note — and client-delete widened from "while claim draft" to `draft` **or** `returned`, because a returned claim is being edited and a receipt that cannot be replaced makes the return useless. **2026-08-03 (asset.md): the `asset_document` row's forward blanks filled** — write = the assignment and incident endpoints' own gates, read = `asset.item.read` or the assignment's employee, resolver = asset's, covering both `asset_assignment` and `asset_incident` entities; client-deletable, no expiry reminders. **Deliberately not a sensitive read**, unlike `receipt`: a handover form and a photo of a cracked screen carry no health, identity, or financial data |
| `docs/05-platform/audit-log.md` | 2 | done | 2026-08-02 | Full §9.1 template. BR-AUD-001–011; **two-channel capture**: same-tx repository hook for diff-bearing mutations (audited-table registry §4.2, seeded empty — Phase 3 arrivals) + outbox `on.` consumption for evented facts (earlier docs' "audit consumes" promises honored verbatim, deduped by event_id); **diff masking reuses security-standards §10 registry** (no second list) *— superseded 2026-08-06, see the amendment note at the end of this row*; approval trail NOT duplicated (BR-AUD-004); ids-not-names (UU PDP erasure-safe); tamper evidence = revoked grants + daily anchor digest to Cloud Logging (per-row chain rejected — write contention, tradeoff documented); sensitive-read registry §4.3 (payslip mints — document-storage §12 amended same session; audit queries self-audit); AUD_ = owned, zero codes (ledger noted); `audit.hot_retention_months` → settings §4.2; 2 glossary terms (fixed a table-separator slip in CONTEXT §10 during registration). **Grilled 2026-08-02 (cluster C)**: sensitive reads fail-closed (insert precedes response, failure refuses the read), §4.3 + `document.download.gated_export`, anchor day-bucket = insert day (uuidv7 range) made explicit. **2026-08-03 (tax-pph21.md arrival):** `employee_tax_profiles` added to §4.2 (the pinned PTKP is the only historical record of what basis a tax year was computed on — the employee record is not effective-dated — so its reasoned correction is the row that explains a year's withholding); §4.2's "expected arrivals: tax and BPJS parameter tables" resolved the other way — the five tax parameter tables are **excluded** on the `overtime_rate_rules` reasoning, being platform tables with no tenant write path whose history is their effective-dating. No new sensitive-read event: 1721-A1 downloads reuse `document.download.generated_document`, which §4.3 already described as covering "payslip/1721-A1 class files". **2026-08-03 (bpjs.md arrival):** all three BPJS tenant tables added to §4.2 — a risk-class revision re-prices the employer for a whole company, an exclusion removes a statutory contribution from one person on a judgement nothing in the employee record implies, and a dependent count is a direct multiplier on a monthly deduction. The three BPJS parameter tables are **excluded** on the same reasoning as tax's five, which lets the registry state the rule once and close the question: a statutory number nobody in the tenant can change needs no diff, a tenant judgement about how it applies to a person does. No parameter table is expected here from any module still to come  **2026-08-03 (expense-reimbursement.md arrival):** all three expense tables added to §4.2 — the claim tables registered despite being a request aggregate, because the admin file-on-behalf path lands a claim at `approved` with no approval instance and the payment state moves outside any chain, so the diff trail is the only control on both. Plus `expense.receipt.viewed` in §4.3, registered **unconditionally across every category** rather than behind a per-category sensitive flag: a tenant that forgets to tick the flag on its medical category loses the trail on exactly the receipts that needed one, and nothing detects the omission. **2026-08-03 (asset.md): 4 tables added to §4.2** — `asset_categories`, `assets`, `asset_assignments`, `asset_incidents`, with **no exclusions**. With no approval engine anywhere in it there is no `approval_actions` trail running beside these rows, so the channel-1 diff is the sole control on every act. No sensitive-read entry. **2026-08-06 (issue #21 — BR-AUD-005 amended):** the rule read "diff masking reuses the security-standards §10 redaction registry", and §4.2 had since been populated with notes that contradict it in both directions — seven tables registered "full diffs" precisely so money figures appear, against §10's `/amount|salary|wage/i`; `leave_requests.reason` and three `candidates` columns masked though §10 lists none of them; `company_bpjs_registrations` numbers diffed in full though §10 lists them. §10 is a telemetry registry (Pino, Sentry, span lint) and was never the operative list here. **Rewritten as three layers**: an ADR-0016 `encryptedText` column masks by *type*, which is automatic and gets `employees.npwp` and `companies.npwp` right without a list; a credential-and-token **floor** never diffs on any table; everything else is that table's own §4.2 note. Two decisions taken while writing it, both beyond the issue as filed: **one marker** — `{ masked: true }` for all three layers, since §4.2's prose `[encrypted]` and `[redacted]` read like two more wire formats and an implementer would have built three; and the registry is **fail-loud** — an audited table with no §4.2 entry throws at module init rather than defaulting to a full diff. No ADR: this narrows an existing BR, and §3 already carries "Amended" notes on BR-AUD-009 and BR-AUD-010, so a second mechanism would be the drift the amendment is about. Settled *before* organization writes the diff builder, as A-192 item 8 required |
| `docs/05-platform/settings.md` | 2 | done | 2026-08-02 | **Pulled ahead of rows 37–40 (manifest note: 37 depends on 41; reorder recorded here).** Full §9.1 template. BR-SET-001–010; definitions = code registry (platform table) + values = tenant RLS effective-dated `[from,to)` w/ exclusion constraint + scope CHECK (hand-written SQL); one mechanism for history/scheduling/as-of; most-specific-wins branch→company→tenant→default; §4.2 key registry seeded — backlog cleared: sync.* (3), auth.* (10), approval.* (2); **rate-limit tiers deliberately NOT settings keys (decision recorded §4.2 — platform config, YAGNI)**; tighten_only direction validation; no activation cron (dates self-activate); 4 SET_ codes registered (catalog: SET_=§9, Deprecated→§10, ledger→§11); 2 glossary terms. **Grilled 2026-08-02 (cluster C)**: `settings.value.changed` gains `action` set\|scheduled\|cancelled (emitted on all three mutations; cancel carries the dead value — hard-deleted schedules now leave an audit fact), non-employee `/effective` callers resolve tenant scope. Cluster D: +`import-export.retention_days` (registry now 24 keys). **2026-08-03 (tax-pph21.md):** +`tax.method` (enum, effective-dated, company default with a per-employee override on the tax profile) — and §4.2's standing promise that `tax.ter_table_version` "plus statutory tables" would land here is **corrected in place**: a TER table is a matrix rather than a scalar, and `setting_values` is tenant-RLS'd, so storing it here would make a tenant technically able to edit the law computing its own withholding. Statutory tax data lives in platform tables (tax-pph21.md §4.1), on the `overtime_rate_rules` precedent; only tenant *policy* stays a key. Registry now 47 keys. **2026-08-03 (bpjs.md):** +`bpjs.wage_floor` (decimal, **branch**-scoped, effective-dated, unset by default) — 48 keys. The reason it is a key at all is the interesting part and it is written into §4.2: BPJS statutory data is platform tables like tax's, but a wage floor is *regional* rather than national, set by hundreds of separate local decrees, and branches carry an IANA timezone and coordinates rather than an administrative code — so the amount is tenant-entered and the applicability per program stays a versioned statutory flag. A **second stale forward promise corrected in place**: §2's permission table still offered a definition-level override for "regulatory parameter groups (tax/BPJS tables)", and no statutory table lives in this module at all; the override now guards the small set of tenant policy keys with statutory-sized blast radius, `tax.method` and `bpjs.wage_floor`. **2026-08-03 (expense-reimbursement.md):** +`expense.max_backdate_days` (integer, tenant + company, default 90, A-044) — **49 keys**. Category policy is columns, not keys, and the reason is structural rather than stylistic: settings definitions are a code registry, so a tenant creating a category could never mint a key for it. |
| `docs/05-platform/import-export.md` | 2 | done | 2026-08-02 | Full §9.1 template. BR-IMP-001–012; import/export job schemas + 8-state import machine (uploaded→validating→awaiting_confirmation→committing→terminals; 24h auto-cancel); definition TS contracts spelled (ImportDefinition/ExportDefinition — rowHandler port keeps domain logic in modules); commit resumes via last_committed_batch; concurrency = partial unique index; template `_meta` version marker; export column sets frozen at enqueue from requester perms; injection defense; row-level errors = VAL_/module codes in workbooks, IMP_ = job-level only (5 codes; catalog: IMP_=§13, Deprecated→§14, ledger→§15); `import-export.max_rows` → settings; 2 templates → notification §4.2; 2 events → audit-log §12. Definition seed empty — employee.master + holiday.calendar arrive Phase 3. **Grilled 2026-08-02 (cluster C)**: export downloads requester-only + gated mints audited (BR-IMP-010; job.read leak closed), upload slot user-parented → re-parented at `POST /imports` (files NOT-NULL chicken-egg closed), notification audiences pinned (export link → requester only). Cluster D: `cron.import-export.purge` added (job rows + artifacts, `import-export.retention_days` default 365 — BR-IMP-009 "operational retention" made concrete). **2026-08-03 (tax-pph21.md):** §4.3 gains `tax.opening_ytd` (import, `create_only`, rowHandler calls payroll's `PayrollYtdSeedPort` so the accumulators land in payroll's own ledger — tax owns the concept, payroll owns the write) plus `tax.monthly_withholding` and `tax.annual_1721a1` exports with gated identifier columns. Government file formats deliberately not produced. **Pre-existing markdown defect fixed:** the definition registry's module groups had been separated by blank lines since shift.md, which terminates a GFM table — every row from `shift.roster` onwards was rendering as literal text. Rows are now contiguous under one header and the three "no import in V1" notes are consolidated below it. **2026-08-03 (bpjs.md):** §4.3 gains `bpjs.monthly_contribution` and `bpjs.membership_mutation` (both exports, gated identifier columns, permission `bpjs.report.export`); `tax.opening_ytd`'s template gains `jht_employee` and `jp_employee`. **No BPJS import** (A-041) — both tenant tables are sparse by design, roughly fifty rows each for ten thousand employees, so a bulk loader is scaffolding. The "expected arrivals: BPJS statutory exports" line is now discharged and the remaining arrival is the reports registry  **2026-08-03 (expense-reimbursement.md):** §4.3 gains `expense.claim` and `expense.disbursement` (both exports, bank columns as a gated set on the `payroll.bank_file` precedent, permission `expense.claim.export`). **No import** — a claim requires a receipt and a spreadsheet row cannot carry a JPEG, so an import would exist only by skipping the module's single blocking rule; legacy claims are opening figures and belong in payroll's. **2026-08-03 (asset.md): 3 definitions added** — `asset.registry` import (`create_only`, naturalKey `[asset_code]`) plus `asset.registry` and `asset.assignment` exports, the **first export pair in Phase 3 with no gated column set** because nothing in the module is encrypted or masked. Asset is also the first module to **split** the import question: the registry imports, custody does not — an assignment asserts that a named person took possession on a date, which is the evidence the module exists to hold |

## Phase 3 — Business modules

| Path | Phase | Status | Updated | Notes |
|---|---|---|---|---|
| `docs/06-modules/holiday.md` | 3 | approved | 2026-08-02 | **Approved 2026-08-02 — locked structural template for all business modules.** Reference module, full §9.1. BR-HOL-001–010; add/negate row model (`observed=false`), most-specific-wins per (date,kind); partial unique w/ COALESCE scope; branchId FK deferred to organization.md (settings precedent); HolidayQueryPort (dayType/nonWorkingDays — attendance/leave/overtime/shift consume); resolution = pure fn, golden vectors shared server + Dart local reducer; no lifecycle diagram (explicitly justified — template note); yearly import via framework (custom kind rejected in files); BR-HOL-008 period-lock port = forward contract to payroll.md; delta-sync mirror + branch-transfer cursor reset. Registered same session: 2 HOL_ codes (catalog §14, ledger→§16), `holiday.cuti_bersama_deducts_leave` (company, **effective-dated**, VERIFY) → settings, `holiday.calendar` ImportDefinition → import-export §4.3 (first entry), `holidays` → audit-log §4.2 (first channel-1 entry), "Holiday negation" → glossary. **Retro-grilled 2026-08-02 (cluster F)**: §10 Drift mirror renamed `local_holidays` → `holidays` (naming §2.6); §7 format = canonical instantiation of api-standards §13. **2026-08-02 (attendance.md arrival)**: BR-HOL-008's "payroll period port" → `PeriodLockPort` (attendance §4.2, stub retired); UC-HOL-001 corrected — attendance reaches `dayType` **through** `ShiftQueryPort`, which applies suppression once. **2026-08-02 (leave.md arrival)**: BR-HOL-006's deduction arithmetic implemented (leave.md BR-LVE-011 — one ledger entry per employee per cuti bersama date, negated days deduct nothing, a leave request spanning the date is not charged twice); UC-HOL-006's "leave revalidates pending requests" and §9's "already-approved leave is not silently recomputed — surfacing is leave.md's rule" both discharged in leave.md UC-LVE-006 / §9. `HolidayQueryPort.nonWorkingDays` gains a second legitimate consumer (leave needs the holiday `kind`, which `ScheduledDay` does not carry) |
| `docs/06-modules/organization.md` | 3 | done | 2026-08-02 | Full §9.1 per holiday template. BR-ORG-001–010; 5 new tables (branches w/ 3-zone IANA CHECK + coordinates, departments hierarchy ≤6, job_levels tenant-wide ranked, positions w/ reports_to, org_assignments effective-dated + gist exclusion) + companies extension (legal_name/npwp/address/phone); **approval-engine §13 forward requirement fulfilled**: OrgQueryPort.directManagers(n)/positionHolders (position-walk reporting line, subject excluded, active/on_leave+user filter) + placement as-of + OrgPlacementPort.assignOnHire; **deferred FKs fulfilled**: settings.setting_values.branch_id + holidays.branch_id → branches; archive guards ORG_IN_USE incl. user_roles.company_id blocker (authz §9 promise); moves = supersede, ORG_PERIOD_LOCKED forward contract (BR-HOL-008 pattern); no lifecycle diagrams (justified — reference-data + date-axis rows); mobile org chart excluded V1. Registered: 5 ORG_ codes (catalog §15, Deprecated→§16, ledger→§17), 6 audited tables → audit-log §4.2, 2 glossary terms (Org assignment, Reporting line). No settings keys/templates/imports (each stated). Forward duties recorded on: system-administration (provisioning seeds company+branch), employee (hire assignment via port; termination closes assignments; unplaced chip; import uses org codes), attendance (consumes organization.branch.updated), shift (consumes organization.assignment.changed). **Grilled 2026-08-02 (cluster A)**: multi-holder + open chart ratified as written; BR-ORG-002 amended — placement mandatory at creation (hire seed same-tx). **2026-08-02 (employee.md arrival)**: §4.2 ports extended — `OrgQueryPort.placements` (batch, grid enrichment) + `OrgPlacementPort.closeOnExit` (terminal-status assignment closure, BR-EMP-006). **2026-08-02 (shift.md arrival)**: §12 forward duty on shift discharged — the `organization.assignment.changed` handler is a schedule-cache bust (roster assignments are personal, so a department move never rewrites working hours; a branch move only changes the timezone the same wall clock resolves in). **2026-08-02 (attendance.md arrival)**: BR-ORG-008's "payroll period port" → `PeriodLockPort` (attendance §4.2, stub retired); BR-ORG-007's forward duty discharged — `on.organization.branch.updated` recomputes unlocked days for the branch, past derived days stay pinned |
| `docs/06-modules/employee.md` | 3 | done | 2026-08-02 | **Done 2026-08-02 — full §9.1 per holiday template.** BR-EMP-001–013; employees extension (ADR-0016 encrypted set + nik/npwp bidx, demographics, PTKP plaintext) + 6 satellites (contracts w/ kind↔end CHECK + gist no-overlap, status_history w/ effectuate scheduling, family, documents, data_change_requests w/ encrypted payload + op_id, resignations); one-live-employment partial unique (rehire = new row); status machine + request lifecycle stateDiagrams + hire sequence; masking matrix (last-4, A-018) + audited fail-closed reveals; 2 request types declared (data_change fieldGroup identity \| contact \| bank \| family — bank chain stricter recommended; resignation w/ retract); contract reminder ladder (`employee.contract_reminder_days` csv 60,30); 2 crons (status-effectuate, contract-scan); `employee.status.changed` event (payroll/shift/attendance/leave forward). Registered same session: 5 EMP_ codes (catalog §16, Deprecated→§17, ledger→§18), settings key, `employee.contract_expiring` template, `employee.master` import (create_only, A-019) + export (gated columns) → import-export §4.3, 4 audited tables + `employee.sensitive.revealed` → audit-log §4.2/4.3, 2 glossary terms (Field group, Rehire), A-018/019/020, verb `terminate` → naming §3 + api-standards §1. Cross-doc: OrgQueryPort.placements + OrgPlacementPort.closeOnExit (organization §4.2), AccountLifecyclePort (authentication §13). Forward duties recorded on: leave (on_leave transitions via status port, leaveSummary in /me/profile), payroll (final settlement off `employee.status.changed`; validates bank + religion presence), shift/attendance (derivation stop on terminal status), recruitment (conversion calls hire UC), system-administration (provisioning seeds — UC-ORG-007 chain). Original duty stack (discharged): ADR-0016 duties: encrypted columns + nik/npwp bidx + partial uniques, per-role masking matrix, reveal endpoints (self+admin) registered as sensitive reads (audit-log §4.3). Grill-A duties (2026-08-02): creation (form/import/conversion) carries position+branch — hire assignment same-tx (BR-ORG-002); §13 declares `employee.data_change` context field `fieldGroup` (identity\|contact\|bank\|family) + documents recommended stricter bank chain; contract-end reminder ladder off PKWT end date — contract files carry `document_expires_at = NULL` (file expiry reserved for KTP/cert validity). Org duties (organization §5/§9/§13): hire creates initial placement via OrgPlacementPort.assignOnHire; termination closes org_assignments; grid flags "unplaced" employees; employee.master import references org codes (branch/department/position/job-level). **2026-08-02 (leave.md arrival)**: §13 gains `EmployeeStatusPort` (`scheduleLeaveStatus` / `cancelLeaveStatus`) — BR-EMP-005's `active ↔ on_leave` arm had no writer and the enum values `employee_status_source = 'leave'` + `source_id` were already waiting for one; BR-EMP-005 reworded (short leave never moves the status — only types flagged `sets_employee_on_leave`); `/me/profile.leaveSummary` filled from `LeaveQueryPort.balanceFor` **2026-08-02 (payroll.md arrival):** §13's claim that payroll/tax/bpjs read employee tables "via repositories within the modular monolith per ADR-0001 whitelist" was a **drafting error corrected this session** — that whitelist governs `shared/` (ADR-0001 §4), and §2 permits exactly two cross-module channels, an exported port or a domain event. `EmployeePayrollPort` (`rosterFor`, `exitedBetween`) now serves the payroll roster and settlement worklist. The boundary is a security control, not bookkeeping: `bank_account_number`/`npwp` are ADR-0016 encrypted, BR-EMP-003 masks them, and reveal is audited — a direct table read would have bypassed all three from inside a module that never agreed to them. One sensitive-read audit row per run, not per employee (a run legitimately reads 10k accounts). **2026-08-03 (tax-pph21.md arrival):** `EmployeePayrollPort` gains **`taxIdentitiesFor`** — decrypted NIK/NPWP for Form 1721-A1 rendering and the filing exports, one audit row per batch. Kept as a separate method rather than two more fields on `PayrollEmployeeFacts`: a monthly payroll run needs only the *presence* of an NPWP to price the surcharge, and folding the numbers into `rosterFor` would decrypt identifiers on every run for the sake of one annual document. **2026-08-03 (bpjs.md arrival) ×2:** `PayrollEmployeeFacts` gains **`birthDate`** — JP contributions stop at a statutory age and that boundary is a date comparison the BPJS calculator must own, since a pre-computed age forces the caller to guess which date it is measured at and how it rounds; plaintext class, absent from the redaction registry, travelling the port `ptkp_status` already does. And **`bpjsIdentitiesFor`** joins `taxIdentitiesFor` as a separate method rather than both being generalized into a `statutoryIdentitiesFor`: generalizing would change a live contract for no gain and make every 1721-A1 decrypt two membership numbers it never prints. **2026-08-03 (asset.md): publishes `employee_directory`**, the first read-model view under the ADR-0001 §6 amendment made the same session — `employee_id, tenant_id, company_id, employee_number, full_name, status` over live rows, `WITH (security_invoker = true)`. No `branch_id` (there is none on `employees`; org assignments own placement), nothing encrypted, nothing masked. Consumers listed on the row: asset.md plus the four modules repointed retroactively |
| `docs/06-modules/shift.md` | 3 | done | 2026-08-02 | **Done 2026-08-02 — grilled before generation (6 rounds), full §9.1 per holiday template.** BR-SHF-001–014; 5 tables (`shifts` w/ times + break + tolerances + punch window, `shift_patterns` + `shift_pattern_days` cycle, `roster_assignments` employee-or-company-default w/ combined gist exclusion `COALESCE(employee_id, company_id)`, `roster_days` explicit per-date rows); **resolve-on-read model — nothing materialized, zero crons owned**; `ShiftQueryPort.scheduleFor/scheduleRange/scheduleForMany` returns the verdict with holiday suppression applied (`kind`/`source`/`offReason`); resolution-ladder flowchart + punch-resolution sequence diagram; no lifecycle diagrams (justified — reference data + date-axis rows + per-date facts); time arithmetic table w/ cross-midnight worked examples; 17 endpoints incl. two `bulk-assign` shapes and `/me/schedule` window read. Registered same session: 4 SHF_ codes (catalog §17, Deprecated→§18, ledger→§19 — 15 prefixes, 72 codes), `shift.roster_changed` template → notification §4.2, `shift.roster` import (upsert, `OFF` sentinel) → import-export §4.3, 5 audited tables → audit-log §4.2, 3 glossary terms (Day off, Punch window, Roster day) + `Roster` and `Cross-midnight shift` rewritten, A-021 (mobile window ±30/60 platform-fixed). **No settings keys** (per-shift columns instead — stated). Anchor amendments: verb `assign` (naming §3 + api-standards §1.2), api-standards §10 natural-key bulk shape, naming §2.4 check example renamed `ck_shifts_times_differ`. Forward duties recorded on: attendance (owns punch→shift matching against the port's window; its derived day snapshot **is** the schedule history per BR-SHF-010; recomputes on `shift.roster.changed` / `shift.definition.changed`), overtime (baseline = `endAt`), leave (working-day test = `kind='work'`), payroll (period-lock port answers "open" until it lands). Organization §12 forward duty (`organization.assignment.changed` roster re-check) discharged as a cache bust — personal assignments mean a department move never rewrites a schedule. **2026-08-02 (attendance.md arrival)**: BR-SHF-009 + §13 "payroll period port" → `PeriodLockPort` (attendance §4.2, stub retired). **2026-08-02 (leave.md arrival)**: §4.3's forward duty "leave's working-day test is `kind = 'work'`" discharged (leave.md BR-LVE-003 — `scheduleRange` is the sole net-day source, so weekends, rostered days off, holidays, and cuti bersama are free by construction); leave also consumes `shift.roster.changed` to recompute **pending** requests only — approved ones keep their pinned cost. **2026-08-02 (overtime.md arrival)**: §4.3's forward duty "overtime's baseline is `endAt`" discharged (overtime.md BR-OVT-005 — a window starting inside the shift is a shift change, not overtime). `ScheduledDay` gains **`standardMinutes`** — pre-suppression paid minutes for the date, `0` on a real day off — the fact overtime's rest-day multiplier boundary is defined against; purely additive, existing consumers unaffected, one §14 vector added. Pricing a suppressed short Friday at 7 h instead of its actual 5 h underpays four multiplier-hours, which is why the fact is published rather than reconstructed from an adjacent week |
| `docs/06-modules/attendance.md` | 3 | done | 2026-08-02 | **Done 2026-08-02 — grilled before generation (8 rounds), full §9.1 per holiday template.** BR-ATT-001–018; 4 tables (`attendance_punches` append-only w/ no soft delete + `op_id` unique + geo/QR/integrity columns, `attendance_days` derived w/ pinned schedule snapshot + anomalies jsonb + partial review index, `attendance_corrections` w/ pending partial unique, `attendance_periods` date-range w/ gist exclusion); 2 stateDiagrams (period, correction) + erDiagram + punch sequence + UI flowchart; **`PeriodLockPort` served here** (holiday/shift/organization repointed, stub retired; payroll consumes) plus `AttendanceQueryPort.dayFor/daysFor/summaryFor` (ADR-0012 snapshot input carries `unresolvedAnomalies`/`quarantinedPunches`); 18 endpoints; gate rule = online refuse / queued quarantine, settings read as-of `punched_at`. Registered same session: 8 ATT_ codes (catalog §18, Deprecated→§19, ledger→§20 — 16 prefixes, 80 codes), 6 settings keys (`geofence_radius_m`/`geofence_policy`/`selfie_required`/`selfie_retention_months` [A-008 VERIFY]/`qr_required`/`qr_key_version`) → settings §4.2 (backlog line cleared), 2 templates (`attendance.missing_clock_out`, `attendance.punch_quarantined`) → notification §4.2, 2 ExportDefinitions (`attendance.daily`, `attendance.recap`, no import) → import-export §4.3, 3 audited tables + `attendance.selfie.viewed` sensitive read → audit-log §4.2/4.3 (`attendance_days` excluded by design, reason recorded there), 6 glossary terms rewritten/added, A-022 (close grace 2 h after the last window, platform-fixed) + A-023 (geofence default 100 m). **No new URL verbs or permission actions** (all from the reserved sets). 3 crons (`close-days` hourly, `punch-reminder` 15 min, `period.stamp` on demand) + 7 event handlers; emits `attendance.punch.synced` (makes naming §6's own example real), `attendance.correction.applied`, `attendance.period.locked`/`unlocked`. Duties discharged: shift.md's punch→shift matching + derived-day-is-history + recompute contract; organization BR-ORG-007's timezone recompute; document-storage §4.2's `punch_selfie` permission binding; approval-engine §13's `attendance.correction` declaration. Forward duties recorded on: payroll (`PayrollRunGuardPort.runsOver` gates unlock; snapshots `summaryFor`), overtime (`overtimeCandidateMinutes`). **2026-08-02 (leave.md arrival)**: all three leave duties discharged — `LeaveQueryPort.coverageFor` flipped forward→live in §4.3, `on.leave.request.decided` flipped forward→live in §12 (the event carries `coveredDates`, so the handler recomputes exactly those), and `fk_attendance_days_leave_requests` was added in leave.md's migration (§4.1 note updated). **2026-08-02 (overtime.md arrival)**: the `overtimeCandidateMinutes` forward duty discharged — overtime reads it through `AttendanceQueryPort.daysFor` and clamps to `min(approved, actual)`; `attendance.correction.applied` and `attendance.period.unlocked` each gain an overtime handler (re-actualization while open). No column, event, or job added here. **§15's pre-approval interlock item stands** — overtime reconciles on its own daily pass, not at day close **2026-08-02 (payroll.md arrival):** `PayrollRunGuardPort.runsOver` is **live** — the stub is retired and unlocking a period under a non-draft run is now genuinely blocked. `closed` runs are included in the result deliberately (a closed run's period can never reopen). ADR-0012's lock wording was amended to match this module's grilled rule: lock is a **precondition** of `calculating`, never an effect of it, and payroll never writes attendance state. **2026-08-03 (asset.md): `employee_directory` added to §4.3** — the grid, live board, and team views already returned `fullName`/`employeeNumber` and offered `q=` over them with no declared channel; now declared under the ADR-0001 §6 amendment |
| `docs/06-modules/leave.md` | 3 | done | 2026-08-02 | **Done 2026-08-02 — full §9.1 per holiday template.** BR-LVE-001–018; 4 tables (`leave_types` policy config w/ 3 quota modes + per-scope cuti-bersama-target partial unique, `leave_balances` materialized running total + `pending_days` hold, `leave_ledger_entries` append-only w/ `(employee,type,kind,source_date)` idempotency key, `leave_requests` w/ pinned `covered_dates` jsonb + gist no-overlap on live rows); 1 stateDiagram (request; three absences justified) + erDiagram + net-day flowchart + submit→approve→recompute sequence + UI flowchart; **`LeaveQueryPort` served** (`coverageFor`/`summaryFor`/`balanceFor` — attendance's empty stub retired, payroll + reports consume); 22 endpoints incl. `GET /leave-requests/preview` (the cost preview — a pure read, so no verb needed) and `bulk-approve` (api-standards §10's illustration made real); 11 seeded statutory types under one ⚠️ VERIFY. Registered same session: 11 LVE_ codes (catalog §19 — **17 prefixes, 91 codes** at the time; overtime.md later pushed Deprecated and the ledger to §21/§22), 3 settings keys (`leave.annual_period_basis` A-024, `leave.carry_over_expiry_months` A-025 ⚠️VERIFY, `leave.balance_expiry_notice_days`) → settings §4.2 (backlog line cleared), 2 templates (`leave.balance_expiring`, `leave.request_cancelled`) → notification §4.2, 1 import + 2 exports (`leave.balance_adjustment` create_only, `leave.balance`, `leave.request`) → import-export §4.3, 2 audited tables + `leave.attachment.viewed` sensitive read → audit-log §4.2/4.3 (ledger + balances excluded by design, reason recorded there), `leave_attachment` category → document-storage §4.2, 12 glossary terms rewritten/added, A-024/025/026. Verb `return` → naming §3 + api-standards §1.2 (ADR-0008's return-for-revision action; overtime/expense/data-change will reuse it). No new permission actions. 1 cron + 3 event handlers; emits `leave.request.decided` + `leave.balance.expiring`. Duties discharged: attendance's `LeaveQueryPort` / `leave.request.decided` / `attendance_days.leave_request_id` FK; shift's working-day test; holiday BR-HOL-006 deduction arithmetic + UC-HOL-006 pending-request revalidation + §9's "already-approved leave is flagged, never silently recomputed"; employee BR-EMP-005's `on_leave` writer (`EmployeeStatusPort` added to employee.md §13; `/me/profile.leaveSummary` filled). **⚠️ A-026 for review: half-day leave excluded from V1 against spec §10 item 6** — attendance carries one whole-day verdict, so enabling it means amending attendance.md. Forward duties recorded on: payroll (`summaryFor` snapshot, `balanceFor` encashment + `settlement_payout` entry, sick-leave wage scaling). **2026-08-02 (overtime.md arrival)**: ledger kind `overtime_toil` added (enum + BR-LVE-005), 12th seeded type `toil` / *Cuti pengganti lembur* added to §4.2 (`annual_quota_days = 0` + `accrual_method = upfront` = "never accrues on its own"; `carry_over_max_days = 0` → A-028 period-boundary expiry), and **`LeaveBalancePort` served — this module's first write port**, one method, `overtime_toil` only, idempotent on the occurrence id, mirroring the `EmployeeStatusPort` direction leave already uses. No arithmetic changed: a TOIL credit is a signed ledger entry and redemption is an ordinary `balance`-mode request, so hold, net-day counting, coverage, and cancellation windows all apply unmodified. §15 gains per-entry `expires_on` **2026-08-02 (payroll.md arrival):** `summaryFor` and `balanceFor` both have live consumers — run snapshot input and final-settlement encashment, the latter posting the `settlement_payout` ledger entry at run close. Sick-leave wage scaling stays out of this module by design (leave publishes type/paid/days/dates, payroll prices them). The §15 daily-rate port remains **unbuilt** — payroll computes the rate internally, and it becomes a port when leave-liability valuation has a live consumer. **2026-08-03 (asset.md): `employee_directory` added to §4.4** — requests grid, balances grid, team calendar, and `teamOverlap` were already returning employee names with no declared channel; now declared under the ADR-0001 §6 amendment |
| `docs/06-modules/overtime.md` | 3 | done | 2026-08-02 | **Done 2026-08-02 — grilled before generation (11 rounds + wrap), full §9.1 per holiday template.** BR-OVT-001–018; 4 tables (`overtime_rate_rules` **platform table — no `tenant_id`, no RLS, no runtime write path** per ADR-0002; `overtime_exempt_job_levels` sparse, default eligible; `overtime_requests` header w/ `ordered_by` + `acknowledged_at` + `op_id`; `overtime_occurrences` per-date w/ pinned day class / `H` / tiers / `rate_version` and a **tstzrange** gist exclusion — timestamp not date, because two occurrences on one date at different hours are legitimate); 2 stateDiagrams (request, occurrence) + erDiagram + submit→approve→actualize sequence + baseline/`H` flowchart; **`OvertimeQueryPort` served** (`summaryFor`/`occurrencesFor` — `multiplierHours` + `paidHours` + `toilHours` + per-occurrence tier trace + `unactualizedOccurrences`, the attendance `unresolvedAnomalies` mirror); 21 endpoints incl. `GET /overtime-requests/preview` (leave precedent), `bulk-assign` (the team order), `acknowledge` (worker consent); 5 seeded multiplier tiers under ⚠️ VERIFY. Registered same session: 10 OVT_ codes (catalog §20, Deprecated→§21, ledger→§22 — **18 prefixes, 104 codes**), 6 settings keys (`standard_daily_hours`, `max_hours_per_day` 4 ⚠️, `max_hours_per_week` 18 ⚠️, `compensation_mode`, `meal_threshold_hours` ⚠️, `max_backdate_days` A-027) → settings §4.2, 2 templates (`overtime.acknowledgment_required` mandatory, `overtime.occurrence_actualized` opt-out) → notification §4.2, **2 exports, no import** (an order that must clear caps and eligibility has no spreadsheet path) → import-export §4.3, 2 audited tables → audit-log §4.2 (occurrences excluded on the `attendance_days` reasoning; rate rules have no tenant write path), 8 glossary terms → CONTEXT §4 (renamed "Time, shift, attendance & overtime"; `Overtime (lembur)` moved out of §6 and rewritten), A-027/028/029. **No new URL verbs, no new permission actions.** 1 cron + 4 event handlers; emits `overtime.request.decided` + `overtime.occurrence.actualized` (payroll does **not** subscribe — it pulls at snapshot). Anchor amendments: `ScheduledDay.standardMinutes` (shift.md §4.2/§4.3 + a §14 vector), `overtime_toil` ledger kind + `toil` seeded type + `LeaveBalancePort` — leave's first write port (leave.md), ADR-0012 §"payroll component" clarified (`run_input` = component class, not transport; overtime/leave effects are **pulled**), attendance.md §13 forward duty flipped live. Duties discharged: attendance's `overtimeCandidateMinutes`, shift's `endAt` baseline, leave's `coverageFor` no-baseline check, approval-engine's `overtime.request` §13 declaration. Forward duties recorded on: payroll (`summaryFor` snapshot + the 1/173 divisor + the 75% wage-basis floor + a possible meal component + the retro path for locked-period corrections) **2026-08-02 (payroll.md arrival):** every money-shaped forward duty discharged — `payroll.overtime_divisor` (173) and `payroll.overtime_basis_floor_pct` (75) are effective-dated settings owned by payroll, the hourly basis is `max(upah sebulan, floor × total wage) / divisor`, and the tier trace feeds the payslip explain-view. `toil` occurrences never reach payroll. `unactualizedOccurrences` surfaces as a run-creation warning; a locked-period correction raises a payroll retro flag. **2026-08-03 (asset.md): `employee_directory` added to §4.4** — the requests grid and `me/team/overtime` were already returning employee names with no declared channel; now declared under the ADR-0001 §6 amendment |
| `docs/06-modules/payroll.md` | 3 | done | 2026-08-02 | **Done 2026-08-02 — grilled before generation (15 rounds), full §9.1 per holiday template.** BR-PAY-001–024; 8 tables (`payroll_components` with the two-axis classification, `salary_histories` + `salary_history_lines` header/lines package, `payroll_runs`, `payroll_run_employees` carrying snapshot + trace + payment state, `payroll_run_lines` as real rows so aggregates survive, `payroll_ytd_ledger`, `payroll_retro_flags`); run lifecycle + independent payment state machines; `PayrollRunGuardPort` served (attendance's stub retired); 27 endpoints; 12 `PAY_` codes; 6 jobs incl. one cron; 6 settings keys; 3 notification templates; 2 exports and an explicit no-import. **Q3 → ADR-0012 amendment 1:** statutory bases are code formulas over `wage_category` (`basic`/`fixed_allowance`/`variable_allowance`/`non_wage`) crossed with `income_class` (`regular`/`irregular`/`non_taxable`) — per-BPJS-base inclusion flags replaced, because they make a silent underpayment expressible as valid config and cannot represent the 75% floor, which compares two bases. **Q13 → ADR-0012 amendment 2:** "period locking binds at `calculating`" → **precondition**; attendance owns the period, payroll verifies and never writes, `PayrollRunGuardPort.runsOver` guards the reverse direction, both read-only across the boundary. **Q7 → employee.md correction:** its §13 claimed payroll/tax/bpjs read employee tables "via repositories per the ADR-0001 whitelist"; that whitelist governs `shared/` (ADR-0001 §4) and §2 permits only ports and events — `EmployeePayrollPort` (`rosterFor`, `exitedBetween`) added, decryption and the sensitive-read trail stay inside employee where BR-EMP-003 binds them. Other decisions: run declares its own period and pins its roster (Q1); package as header + lines with basic as a seeded component (Q2); named calculator ports taking a pure input struct **including the YTD slice** so tax never calls back mid-run (Q4); explicit mark-paid + per-employee `payment_state`, bank file as an export definition filtered `all`/`unpaid`, bounce never blocks close (Q5, A-032); THR targeted by holiday date + religion set, uniform policy = the all-religions case (Q6); settlement via notification + derived worklist (Q8); **no `team` scope anywhere in payroll** (Q9, A-033); retro as dirty flag + on-demand shadow diff + human selection (Q10, A-031); proration basis as effective-dated config with `1/173` kept separate (Q11, A-030); payslip rendered from the trace, PDF on demand, visible at `paid` (Q12); `ExpenseQueryPort` forward contract with `disburse_via` owned by expense.md (Q14); rounding per statutory stage with **the payslip foots** as a BR and a test (Q15). Registered same session: `PAY_` catalog §21 (Deprecated→§22, ledger→§23, **19 prefixes / 116 codes**), 6 settings keys, 3 notification templates, `payroll.bank_file` + `payroll.run_recap` exports with bank columns as a gated set, 4 audit tables + a derived-table exclusion paragraph, **`payroll_run` into approval-engine §13's request-type registry**, 9 glossary terms in CONTEXT §6 plus a corrected `Payroll component` row, A-030–A-033. Arrival notes written into attendance.md (port live), leave.md, overtime.md. **2026-08-03 (tax-pph21.md arrival) — seven touches:** `payroll_income_class` gains `final` and BR-PAY-002 rewritten to classify it (payroll classifies, tax prices); `payroll_ytd_ledger` gains `final_income_ytd` + `pph21_final_ytd`, placed here rather than on tax's own year row because the ledger is written inside the close transaction UC-PAY-010 refuses to complete on failure; §4.4 gains the **MTD slice** and both `Pph21CalculatorPort` rows flip forward → live, `preflight` included; **new BR-PAY-025 + `PAY_MONTH_RUN_IN_FLIGHT`** serializing runs that share a company and payment month; **`PayrollYtdSeedPort` served** for mid-year opening balances (§4.3); §12 consumes `tax.profile.corrected` → retro flags, a re-pinned PTKP being the same class of fact as a late attendance correction; §7's run-creation warning strip gains the tax preflight, which matters most on a December run because a prior-employer record entered after that close makes the whole year wrong. **2026-08-03 (bpjs.md arrival) — thirteen touches, and two of them are corrections to this module rather than additions:** `payroll_component_kind` gains **`employer_cost`** with new **BR-PAY-026** stating its three consequences (net excludes it, taxable assembly includes it, every aggregate must filter by kind) — before it, §4.5 stage 4 promised employer lines the enum could not express and the taxable employer premiums were invisible to tax; new **BR-PAY-027** pins stage 4 to the **stage-1 unprorated** wage, written as a rule because the pipeline's own ordering implies the opposite and would apply proration by accident. Plus: BR-PAY-012 gains the explicit `net = Σ earning − Σ deduction` statement, BR-PAY-002 records that `income_class` is inert on deduction lines, `payroll_ytd_ledger` gains `jht_employee_ytd` + `jp_employee_ytd`, `PayrollYtdSeedPort.seedOpening` gains the matching pair, the MTD slice gains per-program contributions-charged, both `BpjsCalculatorPort` rows flip forward → live, UC-PAY-001's seeded set names the nine BPJS component refs, §7's warning strip gains the BPJS preflight (which matters most on a **first** run for a new company, where the failure is total and silent), §12 consumes `bpjs.registration.changed` + `bpjs.coverage.changed` with two new retro reasons, `payroll.run_recap` gains an explicit kind filter, the two employer-cost scalars are redefined as derived, and §14 gains five scenarios. **2026-08-03 (expense-reimbursement.md arrival) — six touches:** §4.4's `ExpenseQueryPort` row flips forward → **live** and is **renamed** `approvedForPeriod` → `claimForRun`, joined by `releaseForRun`, because the method stamps `payroll_run_id` on every claim it returns and a writer must not be named like a read; a paragraph beside the MTD prose explains why it is the one consumed port that writes and what the pin prevents; §4.5 stage 1 names expense claims as a `run_input` source; UC-PAY-001's seeded set gains the two `non_wage` expense components; the `revoke` spec block now calls `releaseForRun` in the same transaction, without which a revoked run strands its claims approved and unpayable forever; and §12 records that `payroll.run.closed` has its first module consumer. |
| `docs/06-modules/tax-pph21.md` | 3 | done | 2026-08-03 | **Done 2026-08-03 — grilled before generation (15 rounds), full §9.1 per holiday template.** BR-TAX-001–022; 6 tables — 1 tenant (`employee_tax_profiles`: pinned PTKP + correction reason, method override, prior-employer block, form pointer/issued-at/revision) and 5 **platform** (`tax_ptkp_amounts` carrying the TER category mapping, `tax_ter_rates`, `tax_brackets`, `tax_severance_brackets`, `tax_parameters`) with no `tenant_id`, no RLS, no write path anywhere in the API; tax-year stateDiagram + erDiagram + withholding-path flowchart + calculation sequence; **`Pph21CalculatorPort` served** (`compute` + `preflight`) — payroll.md §4.4's forward row flips live; 10 endpoints; 5 `TAX_` codes; 4 jobs incl. one cron; 1 settings key; 2 templates; 1 import + 2 exports. **Q1:** statutory tables are platform data, not settings values — a TER table is a matrix, not a scalar, and `setting_values` is tenant-RLS'd, which would let a tenant edit the law computing its own withholding; settings §4.2's forward promise of `tax.ter_table_version` corrected in place. **Q2/Q14:** the purity contract is **adapter reads tables, pure calculator takes arguments** — without that split the profile table would have contradicted ADR-0012 on day one. **Q3:** PTKP pinned per year; a June marriage and a June typo correction are the same edit to the system and only a human can say which, so the pin never auto-refreshes. **Q4:** gross + gross-up, allowance solved closed-form and returned as a **line pair** so the pipeline stays single-pass; classified `non_wage` (A-034) because `variable_allowance` creates a genuine cycle through the 75% overtime floor. **Q7 → ADR-0012 amendment 3:** `income_class` gains **`final`**; before it, every classification available for severance was wrong. **Q9:** tax year = year of `payment_date` (A-035), which **discharges A-032's open marker** — a bounce never moves the tax point. **Q13 → ADR-0012 amendment 3 (second half) + payroll BR-PAY-025:** the **MTD slice**, because TER applies to a month's total bruto and payroll deliberately permits two runs in one payment month; without it every tenant paying THR separately under-withholds all year. **Q14:** opening YTD seeds `payroll_ytd_ledger` via **`PayrollYtdSeedPort`** rather than a parallel table — two sources for one number fail silently in every downstream reader. Registered same session: `TAX_` catalog §22 (Deprecated→§23, ledger→§24, **20 prefixes / 122 codes**) + `PAY_MONTH_RUN_IN_FLIGHT` in §21, `tax.method` settings key, 2 templates, 3 import-export definitions, `employee_tax_profiles` → audit-log §4.2 plus a parameter-table exclusion paragraph, 11 glossary terms in CONTEXT §6 with `Income class` and `YTD ledger` rewritten, A-034–A-038. **Reused rather than minted:** the `generated_document` category and `document.download.generated_document` sensitive read (document-storage §4.2 already read "payslip/1721-A1 class files" — a `tax_form` category would have been a duplicate), `PAY_PARAMETER_MISSING`, and `SYS_NOT_FOUND`. **No approval touchpoint** — stated explicitly; nothing here is a request for a decision. **2026-08-03 (bpjs.md arrival) ×3:** BR-TAX-008's "less the employee's own JHT and JP" now names its supplier and its accumulators (`payroll_ytd_ledger.jht_employee_ytd` + `.jp_employee_ytd`, separate because the combined employee total carries a non-deductible Kesehatan part); BR-TAX-015's opening seed gains the same two figures, without which a July onboarding's December path over-taxes the year; and §13 records the seam precisely — the two calculators never call each other, BPJS's deductibles reach `Pph21Input` through payroll and its taxable employer premiums reach `lines` as `employer_cost` rows, so pipeline order is the whole of the coupling. |
| `docs/06-modules/bpjs.md` | 3 | done | 2026-08-03 | **Done 2026-08-03 — grilled before generation (13 rounds), full §9.1 per holiday template.** BR-BPJS-001–021; 6 tables — 3 **platform** (`bpjs_program_rates` with a nullable rate the CHECK permits only on the JKK employer row, `bpjs_jkk_risk_rates`, `bpjs_parameters`) and 3 tenant (`company_bpjs_registrations` effective-dated with gist no-overlap, `employee_bpjs_exclusions` sparse per employee-program-interval, `employee_bpjs_dependents` effective-dated count); **no lifecycle diagram, explicitly justified** — every entity is a date-axis row, the holiday/organization/shift/overtime precedent — replaced by a coverage-resolution flowchart; **`BpjsCalculatorPort` served** (`compute` + `preflight`), returning **classified contribution rows** rather than bare amounts, which is the one deliberate divergence from `Pph21CalculatorPort`: nine amounts each needing the `income_class` that decides taxability, and that classification is a BPJS fact; 9 endpoints; 3 `BPJS_` codes; 1 job, **no cron** (a remittance-deadline reminder would fire against a fact V1 does not track); 1 settings key; 2 exports, no import; **zero notification templates and zero approval touchpoints**, both stated rather than left blank. **Q5 → ADR-0012 amendment 4:** `payroll_component_kind` gains `employer_cost` — two defects with one fix, since stage 4 already promised employer lines the enum could not hold, and employer Kesehatan/JKK/JKM are taxable income to the employee ⚠️VERIFY that `Pph21Input.lines` could never have seen. Net stays `Σ earning − Σ deduction`; taxable assembly sums earning **and** employer_cost; every aggregate now needs a kind filter. **Q9:** `payroll_ytd_ledger` gains `jht_employee_ytd` + `jp_employee_ytd` and `seedOpening` gains the matching pair — the annual path deducts exactly those two and the old lump carried a non-deductible Kesehatan part. **Q6:** run-type rule + per-program MTD credit; worked example in §4.6 puts the THR-month double-charge at 260,000 per employee, 130,000,000 for a 500-person company, in one month. **Q3:** the floor's amount is a branch settings key and its applicability a platform-row flag. **Q4:** JP's age ceiling is derived from a new `PayrollEmployeeFacts.birthDate`; non-derivable exclusions stay rows because `employees` carries no nationality — a gap named in §9 rather than papered over. Registered same session: `BPJS_` catalog §23 (Deprecated→§24, ledger→§25, **21 prefixes / 125 codes**), `bpjs.wage_floor` settings key (48), 2 export definitions + 2 columns added to `tax.opening_ytd`, 3 audited tables + a parameter-exclusion paragraph that closes the registry's "expected arrivals" line for good, 8 glossary terms + 3 rewritten, A-039–A-042. **Reused rather than minted:** `PAY_PARAMETER_MISSING`, `SYS_NOT_FOUND`, `VAL_*`, and the existing MTD-slice assembly and `PAY_MONTH_RUN_IN_FLIGHT` serialization. Three conditions deliberately took no code at all — unregistered company, unset floor, fully-excluded employee are **warnings**, and a code invites a client to treat a warning as a failure. **Prior forward duties, all discharged — the original text is kept below so the promises can be checked against what arrived.** **From tax-pph21.md (2026-08-03):** the sibling calculator port follows the **same two-layer purity contract** — a port adapter that reads this module's own tables, wrapping a pure calculator that takes every parameter as an argument (tax-pph21.md BR-TAX-017); ADR-0012's golden-file class targets the inner function. Its rate/cap tables are expected to be **platform tables** on the `overtime_rate_rules` / `tax_*` precedent (audit-log §4.2's exclusion paragraph already anticipates them), with JKK risk class the one genuinely per-company fact and therefore the one candidate for a tenant table or a settings key. The employee-side JHT and JP contributions it returns are the deductions tax subtracts on the annual path (BR-TAX-008), so their sign and rounding must be agreed on arrival. Employer-part accumulators already exist in `payroll_ytd_ledger`. **From payroll.md (2026-08-02):** implements `BpjsCalculatorPort.compute` with the same purity contract as the tax port. The contribution **base** is `upah sebulan` = `Σ basic + Σ fixed_allowance` per `wage_category` (payroll.md BR-PAY-003) — this module owns the per-program caps, splits, and JKK risk classes, not the base composition. Employee parts return as `deduction` lines, employer parts as employer-cost lines, both entering `payroll_ytd_ledger` at close. Reads employee registration data via `EmployeePayrollPort`. Monthly report exports register in import-export §4.3 on arrival. |
| `docs/06-modules/expense-reimbursement.md` | 3 | done | 2026-08-03 | **Done 2026-08-03 — grilled before generation (14 rounds), full §9.1 per holiday template.** BR-EXP-001–018; 3 tables (`expense_categories` policy config with tenant/company scoping, `expense_claims` header carrying both axes and the run pin, `expense_claim_lines` with the pinned category code, pinned income class, and the `policy_notes` evidence behind each over-policy flag); **2 stateDiagrams** — approval axis and money axis, independent — plus an erDiagram and a submit-time evaluation flowchart; **`ExpenseQueryPort` served** with `claimForRun` + `releaseForRun`; 19 endpoints; 5 `EXP_` codes; **0 crons, 0 owned jobs, 0 emitted events**; 1 settings key; 1 template; 2 exports and an explicit no-import. **Q4 — the find:** payroll's forward contract was a pure read (`approvedForPeriod`), and a pure read cannot stop a THR run created after a regular run reaches `approved` from pulling the same claim — `PAY_MONTH_RUN_IN_FLIGHT` serializes only `calculating` and `review`, both runs foot internally, both bank files are correct against their own run, and the duplicate exists solely in the claim's history where nothing reads it. Fixed by making the method **take rather than read**: it stamps `payroll_run_id` inside the caller's snapshot transaction, eligibility is `approved AND unpinned` (a set that only shrinks), and `revoke` calls `releaseForRun` or strands every claim it touched. Renamed on arrival because a method that stamps rows must not be named like a read; payroll.md §4.4 updated. **Q8:** `income_class` is per category, ⚠️VERIFY against the natura/kenikmatan regime — the second instance in two sessions of taxable money that `Pph21Input.lines` would never have seen, after bpjs.md's employer premiums, which is why the rule is now stated generally: any money entering a run carries its class explicitly, never by assumption. Two seeded `non_wage` components, `expense_reimbursement` and `expense_taxable_benefit`. **Q3:** limits advise and only receipts block, which killed the ledger, the hold, and the `FOR UPDATE` — the period cap is a query over pending + approved lines. `overPolicyLineCount` in the approval context is what keeps the flags from being decoration. **Q10:** cash advance, per-diem/mileage, and cost-center all excluded (A-045–A-047), the advance sketched in §15 as the likeliest first ask. Registered same session: `EXP_` catalog §24 (Deprecated→§25, ledger→§26, **22 prefixes / 130 codes**), `expense.max_backdate_days` (49 keys), `expense.claim_paid` template, `expense.claim` + `expense.disbursement` exports with gated bank columns, 3 audited tables + the `expense.receipt.viewed` sensitive read, the `receipt` category's write/read/resolver blanks filled in document-storage §4.2 and its client-delete widened to `returned`, `expense.claim` context declared in approval-engine §13, 7 glossary terms + a rewritten `Income class` row, A-043–A-047. **Reused rather than minted:** `SYS_NOT_FOUND`, `VAL_*`, `APRV_*`, the `receipt` category, `document.receipt_max_size_mb`, payroll's `payments` sub-resource shape, and the engine's SLA scan instead of an aging cron. **One stale ADR example corrected in place:** ADR-0012's amendment-1 clause listed reimbursements as directly entered against overtime and leave being pulled; payroll.md §4.4 had declared `ExpenseQueryPort` since 2026-08-02, so the example was stale — corrected, **not** amended, since no decision moved. **Deviation from the grilled plan, stated:** Q12 listed 6 permission keys; the file carries **7** — `expense.claim.update` was added for the admin cancel/amend/re-route path, exactly as leave.md carries `leave.request.update` beside `leave.request.create`. **Prior forward duty, discharged — original text kept so the promise can be checked against what arrived.** **From payroll.md (2026-08-02):** must serve **`ExpenseQueryPort.approvedForPeriod`** — payroll pulls at snapshot, expense never pushes into a run (ADR-0012). The payroll-vs-finance disbursement split (spec §10 item 11) lives **here** as a `disburse_via` flag on the category or claim; payroll's port asks only for claims already flagged for payroll, so both paths work with zero payroll configuration. Claims land as `non_wage` components; whether `income_class` is `non_taxable` is tax-pph21.md's ruling, not this module's. **2026-08-03 (asset.md): `employee_directory` added to §4.4** — the claims grid and disbursement view were already returning employee names with no declared channel; now declared under the ADR-0001 §6 amendment. The gated bank columns are untouched: the view carries nothing gated |
| `docs/06-modules/asset.md` | 3 | done | 2026-08-03 | **Grilled before generation (15 rounds, `/grill-with-docs`).** 4 tenant tables (`asset_categories`, `assets`, `asset_assignments`, `asset_incidents`), BR-AST-001–018, UC-AST-001–011, 20 endpoints, 10 permission keys, 6 `AST_` codes, 1 import + 2 exports, 1 job, 2 notification templates. **One row per physical object, no quantity** — a stock model has movements and balances but cannot say who holds SN-1234, and consumables are never returned, handed over, or damaged. **Open assignment is the holder**, partial unique `(tenant_id, asset_id) WHERE returned_at IS NULL` as the invariant and `assets.status` as the guarded custody lock — two defences, no denormalized column to drift. `close_reason ∈ returned, lost, written_off` because closing a lost item as returned lies in the record disputes are read from. **Single-axis status** (`available, assigned, in_repair, lost, retired`) with condition its own column: expense's split was earned by a bounce that runs backwards, and asset has no such pair — `assigned + damaged` is ordinary, `lost → available` is legal because things turn up, `retired` is terminal with a reason collapsing sold/scrapped/donated/written-off. **Condition at both ends of custody**, since the real question is whether it was already scratched at handover. Worse-on-return **prompts** an incident, never creates one silently and never refuses the return. **Zero approval chains** — five earlier modules register none too, but all are config/computation surfaces; asset is the first with employee-facing transactional records, and the permission key becomes the only second signature, which is why `asset.item.delete` splits from `.update` and `asset.incident.create` from `.update`. **Money recorded, never moved** (BR-AST-009): no `AssetChargePort`, deliberately not the `ExpenseQueryPort` mirror — with no chain a pushed deduction cuts pay on one admin's unilateral act, statutory deduction ceilings span every source at once and this module sees one, and a dozen incidents a year do not earn pin-and-release. Named cost: `recovery_status` is self-reported (§9). **Exit flags, never blocks** — blocking is unimplementable (BR-EMP-006 runs in a transaction asset is not in, and ADR-0010 events are async), auto-closing destroys the record. **Zero list, each stated:** no cron (asset owns no time-varying state — expense's engine-SLA argument does not transfer), no emitted events, no settings keys, no ports served, no `PeriodLockPort`, no `OrgQueryPort`, no gated export columns, no offline write class. **No new URL verbs** — `assign`/`return`/`acknowledge`/`export` reserved, retirement and resolution use the sub-resource shape on expense's `payments` precedent. Custody is deliberately not importable while the registry is (import-export note added). A-048–A-053 logged, **A-052 flagged**. |
| `docs/06-modules/recruitment-candidate.md` | 3 | done | 2026-08-03 | **Grilled before generation (19 rounds, `/grill-with-docs`).** Seven tables, 15 permission keys, 38 endpoints, 11 `REC_` codes, 2 crons, 1 notification, 2 exports, 1 setting, **0 imports, 0 events emitted, 0 ports served, 0 new URL verbs, 0 mobile surface**. Decided: **no public careers surface at all** — no listing, no application form, no candidate account, no candidate email (A-054), because every endpoint here resolves a tenant from an authenticated identity and RLS binds on it; `requisition_publications` is what spec §10.13's "publishing metadata" actually asks for. **Person and pipeline are separate tables** so a re-applicant is a match rather than a duplicate. **Two axes** — `stage` forward-only and skippable, `status` terminal — so a rejection freezes where it happened and the funnel needs no extra column. **Offers are versioned rows, one live per application** by partial unique index, because a renegotiated figure under an `approved` instance is the exact failure expense was built to prevent. **Chain approval is what extends an offer** — no separate send step, since there is no candidate-facing channel. **Conversion is dual-gated** (`recruitment.candidate.update` + `employee.master.create`) and runs `EmployeeHirePort.hire` inside one transaction, so a recruiter alone cannot mint a row carrying an encrypted NIK and a bank account. **No internal-applicant model** — BR-EMP-001's NIK uniqueness already prevents two employee rows for one human, one module over, with nothing added here (A-055). **Erasure is anonymization in place**, and the audit trail is redacted for the same three columns, because a full diff would file in `audit_logs` exactly what the purge deleted. **Two forward duties discharged:** `EmployeeHirePort` added to employee.md §13 (UC-EMP-001's "recruitment conversion (forward)" actor since 2026-08-02) and organization.md BR-ORG-002's third `assignOnHire` caller, which reaches the port *through* employee rather than directly — one hire path, not two. **Two anchor gaps found and fixed:** the URL verb `export` was in live use by payroll, tax, and asset without ever being registered in naming §3 (registered retroactively — the exact drift that list exists to prevent), and A-050 still carried last session's corrected superlative. `REC_NOT_A_PANELIST` is the first deliberate 403 exception to the catalog's existence-hiding default, stated in §26 with its reason. **Catalog: 24 prefixes, 147 codes.** |
| `docs/06-modules/performance-goals.md` | 3 | done | 2026-08-03 | **Grilled before generation (19 rounds, `/grill-with-docs`).** Eight tables, 16 permission keys, 35 endpoints, 10 `PRF_` codes, 1 cron, 5 notifications, 1 import, 2 exports, 1 setting, **0 approval chains, 0 events emitted, 0 events consumed, 0 ports served, 0 file categories, 0 new URL verbs, 0 new permission actions, 0 retention jobs**. Decided: **the cycle is the spine** — no goal, review, or rating exists outside one, because collecting goals into a review by date overlap recomputes the reviewed set on every render and lets a December edit change what was reviewed in November. **Participation is materialized at launch** and is where the state lives; resolving it live has nowhere to put "this person is at `manager_review`" and silently moves the completion denominator. **One goal table with one level of nesting, same participant** — a KPI is a measured leaf, an OKR objective an unmeasured parent — because the two models differ in arity, not in kind. **Weights block at exactly 100**, unlike expense's advisory limits, since a weight is an arithmetic input and a set summing to 85 yields a score silently 15% low. **The rating is judgment weighted by goals, never achievement arithmetic**: the manager picks a level per goal, `calculated_score` is the weighted mean of those levels and only *pre-fills* the overall, and achievement is displayed and never computed — which dissolves lower-is-better inversion, `binary` goals with no percentage, and the 300%-of-target outlier in one move, with no achievement cap to configure. **Scales are FK'd tenant tables frozen once a cycle leaves draft** rather than jsonb-snapshotted, because ratings are the most-joined rows in the module and a snapshot degrades every distribution grid to unpacking jsonb per cycle. **Two review seats keyed by `kind`, not by reviewer** — deliberately unlike `interview_scorecards`, since a panel is N people with no role distinction while this is two roles with one seat each, and keying by reviewer would permit two people filing a manager review. **Calibration writes beside the rating, never over it**; the manager sees both, **the employee sees only the final**, and `calculated_score` is omitted from the subject's payload server-side. **Release is a cohort act by HR**, because staggered release leaks which ratings calibration did not touch. **Development items are keyed by employee and sourced from a participant**, which is what makes "carried forward" true. **No approval chains — a conflict avoided, not a feature declined:** the engine resolves approvers at step activation (BR-APRV-006) while this module pins the reviewer at launch (BR-PRF-003), so a chain would hand goal agreement to February's manager while the review form still belongs to January's. **No path from a rating into pay** (A-063), the third module in a row drawing that line from a different side after asset's A-052 and recruitment's A-059. **No appraisal PDF** — ADR-0014's consumer list checked and left untouched (A-064), the same check that produced A-056 one module earlier. `PRF_NOT_THE_REVIEWER` is the **second** deliberate 403 exception to existence-hiding after `REC_NOT_A_PANELIST`, and catalog §27 now states the shared rule — *a seat on a row the caller can see is a row the caller can see* — so a third module follows it rather than inventing a third answer. `performance.goal` is the **first ImportDefinition whose atomic unit is not the row**: partial commit at employee granularity, because the weight balance is a property of a set. **Catalog: 25 prefixes, 158 codes** — the `VAL_` block was found to have been under-counted by one since the Phase 1 seed (the `VAL_TOO_SHORT / VAL_TOO_LONG` row holds two codes and every tally counted it once); ledger corrected and the hazard named in §29. |
| `docs/06-modules/training.md` | 3 | done | 2026-08-03 | **Grilled before generation (19 rounds, `/grill-with-docs`).** Five tables, 20 permission keys, 31 endpoints, 6 `TRN_` codes, 1 cron, **1 approval request type** (`training.enrollment`), 4 event handlers, 4 notifications, 1 import, 2 exports, 2 settings keys, 1 port served, **0 events emitted, 0 new URL verbs, 0 new permission actions, 0 gated export columns, 0 retention jobs, 0 403 exceptions**. Decided: **catalog and instance are separate rows** — a course is browsed, a session is scheduled — because collapsing them makes running the same course twice a copied row, so the cost report becomes a string match and a course with no upcoming date is invisible to the employee who wanted to request it. **Capacity binds at the seat, never at the request**, under a `FOR UPDATE` on the session against a **live count with no denormalized counter anywhere**; deliberately unlike leave's `pending_days`, because a leave day is money-equivalent and a double-spend unrecoverable while a chair is recoverable, and a forgotten pending request silently occupying a physical seat for three weeks is the worse failure. **Single status axis** on asset's argument — nothing runs backwards, and **a no-show never frees its seat retroactively**, which is correct rather than incidental since the chair was reserved and wasted. **One attendance verdict per enrollment, no `partial`, no per-day roster** (A-071): every output of the module — certificate, cost, hours — is unaffected by which day someone skipped. **Closing a session blocks while any seat is unmarked** (`TRN_ATTENDANCE_INCOMPLETE`): auto-marking `attended` issues completion certificates to absentees, which is the one thing the module's artifact must not be wrong about, and auto-marking `no_show` writes HR's forgetfulness onto the employee's record; the friction lands on the person who can fix it, at the screen they are already on. **Cost is one column on the enrollment**, pre-filled from the session's default and never totalled there, which is the only shape in which a no-show's wasted fee is representable — and money is recorded, never moved (A-072), the **fourth** consecutive module drawing that line from a different side after asset's A-052, recruitment's A-059, and performance's A-063. **A certification is a state, not an event**: `training_certifications` keyed by employee with nullable `source_enrollment_id`, on performance's `development_items` shape, because credentials arrive with new hires and have no enrollment to hang on, the compliance query runs against a row, and **renewal is a new row** so a lapsed card and a live one both survive. **The credential row owns `expires_on`, not the file** (BR-TRN-013) — document-storage's `training_certificate` `expiryReminders` flag flipped ✅→❌ with the reason recorded there, a Phase 2 promise **moved to a better owner** rather than deleted, and `employee_document`'s reservation of `document_expires_at` for KTP/cert validity left untouched since there the scan genuinely is what expires. **ADR-0014 checked and the answer came back yes this time** — training certificates are one of its five enumerated consumers, so the completion certificate is generated on demand (payslip pattern, not a batch of 200 at session close) from an `attended` seat on a `completed` session; the identical check produced A-056 and A-064 the other way one and two modules earlier. Branding was verified rather than assumed: **no tenant logo column exists anywhere in the schema**, so the document carries `companies.legal_name` and no logo (A-073) — tolerable here and not for an offer letter, because a completion certificate asserts facts the system already holds and is not a legal instrument whose wording counsel must approve. **One approval chain routing on money**: `training.enrollment` with `costAmount` in the context playing the `overPolicyLineCount` / `revisionNumber` role, and `categoryId` as the second dimension since compliance and discretionary development are approved by different people. **Assignment skips the engine entirely** — BR-APRV-003 seeds `direct_manager(1)` on every registered type, so routing an HR-budgeted nomination through the nominee's own line manager inverts who is deciding. **The seat is allocated inside the transaction that records the completing approval**, not in the terminal event handler: two approvers on the last seat serialize on the same row lock, one commits and the other gets `TRN_SESSION_FULL` **before recording a decision**, so there is never an "approved but the room filled" state to invent an answer for. **A-070 flagged for review** — training writes nothing to `attendance_days`, so a 20-person 3-day off-site costs 60 attendance corrections; both alternatives were worse (a `training` status value makes this the second external writer into a lock-frozen derived record and asserts attendance facts from a *registration*, since an enrollment knows a session has dates and not whether anyone travelled; a seeded `training` leave type spends balance or needs another `unlimited` quota and routes one absence through two chains). The clean V2 is a `business_trip` day type **owned by attendance**, named in §15 as an attendance amendment rather than smuggled in from here. **A-074**: no catalog targeting, decided on timing rather than principle — targeting is `announcement.md`'s declared scope and announcement is the very next file in the manifest, so an audience model here ships two implementations one file apart with the second one being the good one; `self_enrollment_enabled` gates the button instead. **A-068**: no waitlist, because under the capacity model the **pending set already is one**, ordered by an approver's judgment rather than FIFO. **A-069**: no provider registry, on asset's repair-vendor line and recruitment's `channel: text` — with the cost written down rather than hidden, since vendor spend fragments across `PT Trainindo` / `PT. Trainindo` / `Trainindo`. Forward duty discharged: **performance-goals §4.3 gains `DevelopmentItemPort.openItemsFor`** — its §13 had explicitly declined to build a port ahead of a caller and named the FK as training's to declare; the FK sits on `training_enrollments` and the port is added by the owner on first real use, the employee.md/`EmployeeHirePort` and leave.md/`LeaveBalancePort` pattern. `training.certification` is the module's **only** import and splits the question a third way (registry ✅ / completions ❌): the part of a training history that still matters is the credential, and importing completions would mint sessions that never ran with attendance verdicts nobody recorded — asset's custody reasoning pointed at a different fact. Training registers **no 403 exception**, and the catalog says so explicitly: `REC_NOT_A_PANELIST` and `PRF_NOT_THE_REVIEWER` exist because a named individual is seated against a visible parent row, nothing here seats anyone that way, and manufacturing a third instance to match the pattern would be the pattern using the module. **Catalog: 26 prefixes, 164 codes**; `TRN` left the reserved-empty list, five remain. |
| `docs/06-modules/announcement.md` | 3 | done | 2026-08-03 | **Grilled before generation (19 rounds, `/grill-with-docs`).** Three tables, one enum, 5 permission keys, 11 endpoints, 2 `ANN_` codes, 2 crons, 1 command job, 1 event handler, 2 notification templates, 1 export, 2 settings keys, 1 new document category, 1 port served, **0 approval chains · 0 events emitted · 0 imports · 0 categories · 0 gated export columns · 0 403 exceptions**. **The audience is resolved once at publish into rows** (BR-ANN-001) — criteria in, a frozen recipient set out, which is simultaneously the fan-out roster, the visibility check, and the acknowledgment **denominator**; live rule evaluation was rejected because a rate whose denominator moves when someone transfers can never be reproduced, the same argument performance-goals used to materialize participation at launch. **Department rules descend the subtree, positions and job levels are exact**, decided on failure asymmetry: over-inclusion is visible and self-correcting, under-inclusion is invisible and nothing in the system will ever surface it. **Targeting resolves through a new `OrgQueryPort.audienceEmployeeIds`** rather than a published view, because the subtree walk is organization's tree and its depth cap — a flat view cannot express ancestry and resolving it here would mean joining `departments`. **Acknowledgment is inbox's action and this module's fact**: no acknowledge endpoint is registered here, since inbox already owns the platform's only queueable offline write (BR-INB-007) and duplicating it would duplicate a queue class, an idempotency scheme, and a terminal-rejection path; the same-transaction alternative was rejected because it would invert the layering by having a Phase 2 platform module call into a Phase 3 business one. **Content is frozen at publish** (A-077) — an acknowledgment attesting to text that was later rewritten is worthless, and the same holds for posts nobody acknowledged, since an announcement is the record of what the company told people. **Publish flips state in the request and fans out in a job, with `recipient_count` NULL as the completion marker** — no `publishing` status, because a status is a claim someone has to retract when the job dies, and the nullable count carries the same information with nothing to strand (the trick training used to avoid a certification status column). **Publish refuses a zero-resolution audience** (`ANN_EMPTY_AUDIENCE`, 422) on both the endpoint and the scheduler path, because a post delivered to nobody is otherwise indistinguishable from one still delivering. **Body is markdown with raw HTML disabled**, forced rather than chosen: security-standards §6 says the API never emits HTML, and a WYSIWYG storing HTML would make a tenant admin a stored-XSS author against every employee in the tenant. **`retract` registered as the fifth new URL verb** (naming §3 + the api-standards §1 mirror) rather than reusing `revoke`, because inbox had already written `closed_reason = 'retracted'` into its schema, rules, and catalog before this module existed. **Two retention keys, not one** (BR-ANN-014) — the acknowledgment register is the module's only compliance artifact and its default carries a ⚠️ VERIFY rather than an invented number. **Two ⚠️ VERIFY markers**: the retention of proof-of-notification, and whether an in-app acknowledgment satisfies any statutory socialization obligation for *peraturan perusahaan* — with an explicit rule that no UI copy may imply it does. **No approval chain, as a refusal**: a chain would sit between "the Jakarta office is closed, flooding" and the people driving to it, and unlike every module that does route through the engine, this one's effect cannot be undone by a later decision because retracting does not unread. **`announcement_recipients` is deliberately not audited** on `leave_ledger_entries`' reasoning — an append-only fact that is already its own trail, and a million `created` rows a year would bury the log. Three anchor amendments, all first-caller pattern: `OrgQueryPort.audienceEmployeeIds` (organization §4.2), `employee_directory` gains **`user_id`** (employee §4.4 — the addition that view's own text invited, and the first consumer to read the view for something other than painting a grid), and `InboxPort.createAckItems` gains an optional `dueAt` (inbox UC-INB-005). Two forward promises discharged: inbox's "ack-rate reporting belongs to announcement.md" (§13) and A-074's deferral of targeting from training. One drift repair found in passing: **api-standards §1's verb mirror had never been updated with `export`**, registered retroactively in naming §3 during the recruitment session — the mirror is now level. A-076–A-083 logged. **Catalog: 27 prefixes, 166 codes**; `ANN` off the reserved-empty list, four remain (`TEN RPT DSH ADM`) |
| `docs/06-modules/reports.md` | 3 | done | 2026-08-03 | **Grilled before generation (19 rounds, `/grill-with-docs`).** Registry of 94 code-owned `ReportDefinition`s (93 at authoring; `employee.turnover` added 2026-08-04) covering all 93 reports promised across nineteen `done` module §13s — **zero tables, zero permission keys, zero jobs, zero crons, zero settings keys, zero events, zero notifications, zero imports, zero approval chains, zero PDFs, zero new URL verbs, zero new permission actions, zero mobile surface**. One definition, two surfaces: a paged screen result and an xlsx file, off the same query, so the two can never disagree. **Two kinds of row** — *derived* (8: an existing `ExportDefinition` given a screen surface, reading through its declared `queryPort`, ADR-0001's default channel) and *owned* (85: a cross-module aggregate through read-model repositories, the §6 exception) — with the rule that **the exception is used only where no port already answers the question**, which keeps the §6 blast radius as small as the facts allow rather than as large as the licence permits. **A report never grants access the owning module would refuse** (BR-RPT-002): every `requiredPermission` is an existing key in the *owning* namespace, so a blanket `report.run` privilege-escalation surface never exists, and the catalog is authenticated-and-filtered rather than gated by an immortal key that gates nothing. **`minimumScope` + applied predicate + stamped output** (BR-RPT-005/006): a runner narrower than the definition is **refused, never silently filtered**, because a narrowed aggregate is not a partial answer — "Headcount 12" renders identically for a manager's team and a 400-person company, and only the stamp says which. **No encrypted or masked column, ever** — ADR-0001 §6(b) extended in place from published views to the designated read-model consumers, which also binds dashboard-analytics in advance; derived reports read `columnSets.base` only, so **no report output ever carries a gated column set** and `document.download.gated_export` is unreachable from here by construction. **Owner predicates travel with the rows** (BR-RPT-008), discharging payroll's BR-PAY-026 forward duty: an unfiltered `SUM(amount)` over `payroll_run_lines` reports employer statutory cost as employee gross, silently. **Nine sensitive-flagged** definitions on a stated boundary — *renders an individual's compensation or statutory tax position* — writing `report.sensitive.viewed`; the expense counter-precedent (BR-EXP-015's unconditional receipt trail, which rejected a flag) was confronted rather than ignored, and the distinction is that expense's flag would have been **tenant data in a tenant-edited table with nobody reviewing the omission**, while a report's flag is code, in the same object as its permission and columns, printed in a registry table where an unflagged salary report is visible on the page. **Bounded twice, one code**: row cap **derived from api-standards §5.2's existing depth cap rather than invented**, plus a duration bound, both one rule → `RPT_RESULT_TOO_LARGE` with `details.bound`. **`RPT_SCOPE_INSUFFICIENT` is the third 403 exception** and the first to arrive *under* the rule catalog §27 promoted — and it earns a code on a stronger ground than convention: `AUTHZ_FORBIDDEN` would be **factually wrong**, since the caller holds the permission and lacks the *other* ADR-0005 axis, so telling them "forbidden" sends them to request a key they already have. **Files ride the export framework wholesale**, discharging import-export §13's Phase 2 promise on inspection: one row added to §4.3 (`report.result`, **the registry's first definition-resolved definition** — permission, columns, and query read from the `ReportDefinition` at enqueue), one routing line to §12, **no endpoint of its own**, and the eight derived reports needed nothing at all. **`report.result` routes to the `reports` queue**, the workload ADR-0010 named that queue for and which only training's certificates had been using — measured rather than speculative isolation, since a row-stream cursor and a three-year `GROUP BY` over ten thousand employees do not belong in one lane. **Three anchor amendments, all first-caller or first-exerciser:** ADR-0001 §6(b) extension; **`AuditQueryPort` added to audit-log §4.4**, giving its Phase 2 "reports consume the query API, never the table" rule an actual mechanism — offset-paged where the endpoint stays cursor-paged, and firing `audit.log.queried` *inside the port* so a consumer cannot read the log without leaving a trace, which is also why the two audit reports carry no second flag; and api-standards §5.3 gaining a report **results** family row beside the existing report **job lists** row. **Forward duty to dashboard-analytics** (the next file): widgets are backed by report definitions and never re-derive an aggregate this registry owns — decided now because two independent aggregate implementations over the same tables *do* diverge, and a widget reading 412 against a report reading 408 destroys trust in both. A-084–A-087 (xlsx only with ADR-0014's "report exports" line explicitly left unclaimed; no caching or stored results; no saved views or scheduled runs; no mobile surface). **Catalog: 28 prefixes, 168 codes**; `RPT` off the reserved-empty list, **three remain** and all three belong to unwritten files |
| `docs/06-modules/dashboard-analytics.md` | 3 | done | 2026-08-04 | **Grilled before generation (19 rounds, `/grill-with-docs`).** 24 widgets over 4 code-declared layouts — **zero tables, zero SQL, zero permission keys, zero migrations, zero jobs, zero crons, zero events emitted or consumed, zero exports, zero imports, zero settings keys, zero audit keys, zero error codes, zero ADR changes, zero ADR-0001 §6 seams**. A widget is a **projection over a `ReportDefinition`** (`scalar`/`series`/`breakdown`/`topN`), so the module owns no query: UC-RPT-008's pre-commitment discharged, and a card cannot disagree with the report it drills into because the number *is* that report's `totals`. Access is transitive — no key is minted, so a widget can never grant what its report refuses; scope insufficiency renders as **absence**, never a refusal tile and never a silent narrowing. Cache `hris:dashboard:{tenantId}:{widgetKey}:{scopeDigest}` with the digest built from the **resolved scope predicate in the same code path as the query**; **TTL-only** (60/300/3600, single-flight), **six event subscriptions declined** and amended in attendance/leave/overtime plus three `Ports served` lists corrected; cache sits **above** the port so reports' BR-RPT-011 holds unamended. **`sensitiveRead` definitions ineligible, and `ReportQueryPort` refuses them** — a cache hit returns rows without running the query, so a flagged widget would audit on miss and not on hit. Every card stamps `appliedScope` in words, `computedAt`, and the report's caveats. 200-with-per-widget-status: one slow `GROUP BY` is one error tile, not an error page. Mobile = `manager` layout only, online-only, no Drift mirror, no ADR-0003 class — **A-087 discharged**. Cost back to reports.md: one definition (`employee.turnover`) + one param (`payroll.component_cost` `month` grain); `ReportQueryPort` added to reports §4.5 on first caller. `DSH` **owned with zero codes** beside `AUD`. A-088–A-092. |
| `docs/06-modules/system-administration.md` | 3 | done | 2026-08-04 | **Grilled before generation (23 rounds, `/grill-with-docs`).** Four platform tables + `platform_users` TOTP columns, BR-ADM-001–024, UC-ADM-001–010, 23 endpoints, 9 `sysadmin.*` keys (code constant, **not** the `permissions` catalog — a row there would be assignable to a tenant role), 4 `ADM_` codes, 1 notification template, 3 mermaid. **Zero** jobs/crons/queues, events either direction, imports/exports, approval interaction (sixth module to reach `done` with none), settings keys, mobile surface, PDF, and **ADR-0001 §6 seams**. **Scope grew by one during grilling:** platform authentication was unspecified anywhere — `platform_users` had `email` + `password_hash` and no login — so §5.1 and **ADR-0017 (Proposed)** close it. `platform_sessions` is a second identity class applying ADR-0004's law unchanged, with two stated divergences: no rotation grace window, and no `mfa_verified` column because the row cannot exist without a verified TOTP leg. **Mandatory TOTP for platform users only** — A-007 narrowed to tenant users on the ground that its evidence (spec §5.8) is the tenant login list; ADR-0004 gains a scoping line, nothing superseded. Provisioning: KMS wrap → **one transaction** (tenant + keys + 10 role templates + settings + company + branch + admin + invite) → post-commit `auth.invite`; **UC-ORG-007 discharged**; seeds through facades only, so this module reaches no other module's tables and the §6 read-model exception still has exactly two exercisers. `tenant_keys` is ADR-0016's previously unnamed platform table, owned here by sole-writer rule. Lifecycle: all three states reversible with mandatory reason, **no destroy endpoint** (A-095). `plan`/`employee_limit` recorded, never enforced (D13). Flags: platform-class table with no tenant read path, code-owned registry shipping **zero definitions** (A-096); `FeatureFlagPort` degrades to the definition's default, never `true`. Impersonation: named `active` user, reason ≥20 chars, **30 min with no refresh path** (stated divergence from ADR-0004's 15-min horizon — here the TTL is the whole session), one live per operator, **cascade-ends when the parent platform session is revoked**, bypasses `TenantStatusGuard` for `suspended`/`archived` (the only such bypass in the system), **no deny-list** (A-097 — a Payroll Admin impersonation can finalize a run, stated not hidden). New mandatory template `sysadmin.impersonation_started` to the tenant's System Administrators. Health: BullMQ failed set read direct with **no mirror table**, `job.retry()` preserving `jobId`, batch capped at 50, no retry-all, **payload body never rendered** (ADR-0011 PII discipline). Counts on the detail page only, via `employee_directory` + `StorageUsagePort` + `NotificationStatsPort`. **No platform-user CRUD** (A-093) — that is what keeps every action tenant-targeted so audit-log §9's cross-tenant rule covers it with no `audit_logs` change; 10 explicit `sysadmin.*` audit actions, channel 1 impossible here because its hook lives on `TenantScopedRepository`. Catalog **§31 `ADM_`** (Deprecated → §32, ledger → §33); `TEN_` owned with zero codes; **29 prefixes / 172 codes, reserved-empty closed 2 → 0**. CONTEXT §20 (8 terms) + three §1 entries sharpened. Leak tests L8/L9. A-093–A-098. **ADR-0017 needs review.** |

## Phase 4 — Operations, AI guide, closure

| Path | Phase | Status | Updated | Notes |
|---|---|---|---|---|
| `docs/07-operations/testing-strategy.md` | 4 | done | 2026-08-04 | **Grilled before generation (23 rounds, `/grill-with-docs`), all 23 recommendations taken.** Written as a **contract-and-enforcement document, not a manual**: the handbook already holds ~480 scenario rows across 28 module §14 tables, and the three coding-standards §9 sections already fix tiers, tooling, and file layout, so this file restates none of it and owns only what nothing else does. **Traceability is generated, never written** — no rule-to-test table exists in any document; a tag convention plus a checker parses 446 `BR-*` ids out of `docs/` and fails on any rule neither referenced nor waived. **One gate, in `hris-api`, over all 446**, because a business rule is enforced server-side by definition and a rule enforced only on a device is an exception worth seeing rather than a case worth modelling — `covered-in: hris-mobile/…` is a legal waiver, which makes that list the honest register of rules no server test covers. **`test-waivers.yml` must name a mechanism** from a closed set (`db-constraint`, `rls-policy`, `route-lint`, `dependency-lint`, `type-system`, `covered-in`); "reviewed manually" fails the schema. **The 265 UCs are tagged and never gated**; what replaces them is sharper — **every one of the 172 catalog codes must be asserted by at least one test**, since a use case's substance is its exception flows and those terminate in registered codes, and a code tag cannot be satisfied by a title string the way a `UC-*` tag can. That closes a circuit error-catalog §1 had half-built: item 4 proves a code has translations, item 6 that it has an owner, neither that it can occur. **Coverage floors are differentiated and three rows deliberately carry none** — a floor on controllers, widgets, and components is how a repo acquires tests that render a thing and assert nothing — with **diff coverage ≥80% as the gate that actually binds**. **Five kits, each a one-line parameterized invocation rather than a documented pattern**, with a lint failing any module that declares a tenant table without calling `describeTenantIsolation`; the fifth, **registry integrity**, was promised by no anchor and is the highest yield per line here: 94 reports × permission keys, 24 widgets × `sensitiveRead`, 45 templates, 8 queues, import/export definitions, settings, verbs, prefixes — pure static cross-checking of data the handbook already fixed, unable to flake, catching the one bug class that survives every other tier because each registry is individually valid. **`ADR-0018` written (Proposed, needs review)** for the statutory-vector decision, because its audience is a payroll engineer who will never open this file. **Nothing else got an ADR** — coverage numbers are reversible, flake policy is a settings change. **Two defects found and fixed:** `security-standards.md` §5 still said **"Leak-test matrix L1–L6"** when multi-tenancy §5 has carried L1–L9 since cluster D and ADR-0017 — a reader following it would have built a kit missing the platform-token and impersonation isolation cases written four days ago; and error-catalog §1 item 2 still pointed at **"§31"** for Deprecated after last session's `ADM_` insert renumbered it to §32. **Four anchor promises discharged** (ADR-0002 two-tenant tests, ADR-0005 permission matrices, ADR-0006 codes-not-strings, ADR-0010 double-delivery) plus pointer updates in multi-tenancy §5, both client coding-standards §9 E2E rows, design-system §13, error-catalog §1. **E2E is a closed numbered list** E1–E6 / M1–M4 enforced by filename, +approval decision, +payroll→payslip, +offline leave request; **scope stated honestly** — E5/E6 run against MSW and therefore prove the UI journey only. **A fourth tier was added because the hole was real:** A-011 chose hand-written wire types with no codegen, web E2E mocks the API, backend e2e has no client, so **nothing anywhere proved the three repos agree** — a 5-journey post-deploy staging smoke suite on a reset-and-reseeded tenant now does, blocking promotion rather than merge. **OpenAPI breaking-change gate in the backend repo only**, encoding two handbook rules an off-the-shelf differ gets wrong: adding an enum value is never a break (ADR-0007 open enums) and the surface versions whole. **Zero retries on every merge-gating tier** — the Playwright default is disabled deliberately, since a retry converts a real intermittent defect into permanently green CI — with retries allowed only on smoke, quarantine capped at 10 and expiry-dated. **Destructive-operation class added** because nothing covered it: **10 of the 26 crons delete data**, and idempotency proves a purge runs twice safely while saying nothing about whether its predicate is right, so purges are asserted **two-sided** including T2 untouched, and migrations apply forward from the previous release's schema **with data present** rather than against an empty database. **`CONTEXT.md` deliberately untouched** — golden vector, quarantine, and smoke suite are engineering vocabulary, not domain language, confirmed by five module docs already using "golden vectors" with no glossary entry. Zero error codes, zero schema changes, zero module-doc changes. A-099–A-103. |
| `docs/07-operations/ci-cd.md` | 4 | done | 2026-08-04 | **Grilled before generation (21 rounds, `/grill-with-docs`), all 21 recommendations taken.** Seam set first and held all the way through: **this file owns the pipeline, `environments.md` owns the target — a secret's name appears here, its value and its store never do** — mirroring testing-strategy §1's threshold/trigger split. Shape is **one spine + three delta tables + a gate-to-job matrix**, with **no full workflow YAML**: a pasted 200-line `.yml` is stale the day someone adds a cache step, and the matrix already fixes what matters. **`ADR-0019` written (Proposed, needs review)** for the release model, because its audience is whoever asks during an incident *"why is production running api v1.4 against web v1.2, and why can't I roll back two releases?"* — a question nobody answers by opening a CI document. Its core: **the digest is the release identity, the semver tag is a label for humans**, promotion re-tags the digest that passed smoke rather than rebuilding; staging deploys automatically because a post-deploy gate that runs when someone remembers is not a gate; **the three repos promote independently, backend first, with compatibility from the contract rather than from co-testing** — pinning web-to-api buys a guarantee ADR-0007 already makes impossible, since an installed app is never part of any tested pair. **Rollback depth is exactly one release, and it was derived, not chosen:** database-conventions §10 rule 5 requires release N's migration to be compatible with N−1 and promises nothing about N−2. That same rule turns out to be what makes Helm `--atomic` auto-rollback correct — stated explicitly, because otherwise someone relaxes rule 5 as over-cautious and silently removes both guarantees. **Migrations run as an in-cluster Job on the exact digest being deployed**; running `drizzle-kit migrate` from the runner would require the production database to accept connections from GitHub's public address ranges. **§7.3 supplies the production schema snapshot that two documents depended on and nothing produced** — the promotion workflow publishes `pg_dump --schema-only` per release, **schema only and never data**, with the bootstrap exemption self-clearing on the first `v*` tag so the gate can neither be permanently broken nor permanently skipped. **A payroll drain gate was added because nothing covered it:** graceful shutdown answers short jobs, but D1 allows a payroll run 30 minutes and no termination grace period covers that, so `worker` rollout waits for the `payroll` queue alone — draining all eight would block forever on `notifications` — and aborting leaves new schema + new api + old worker, a state already safe by two rules written for other reasons. **§8.5 closes a hole ADR-0010 left:** events carry `version`, **jobs do not**, so job payloads are additive-only across a release pair and a breaking change ships as a new job name. **A-103 narrowed rather than closed** — C1–C5 define advisories, Semgrep OSS SAST, gitleaks, Trivy, and a **weekly re-scan of deployed digests**, which is the only check here that can find a vulnerability in code nobody touched; **Dart gets no real SAST and that is written down** rather than papered over. **`security-exceptions.yml` mirrors `test-waivers.yml`** — same expiry discipline, third register, no new invention. **The smoke reset is a Job, never an HTTP route**, double-guarded on environment and slug: a route that erases a tenant's data is reachable by anything that can reach the API, in a product already shipping impersonation. **Production data is never restored into staging.** **Squash-merge forces the Conventional Commit onto the PR title**, which naming §12 mandated without naming the artifact. **CI never submits to store review** and the release keystore exists only in CI — a forked signing key means the app can never be updated again. **Two ADR duties discharged and one relocated:** ADR-0011's symbol upload placed (and it is not hygiene — `--obfuscate` makes every crash unreadable without it), ADR-0013's three migration gates wired, and **ADR-0014's golden-render diff moved to testing-strategy §13 as G21** with §6.6 written, because a threshold belongs there under the seam this file sets in its own §1 — substance changed too: **structural assertions, pixel diffing rejected**, since a Chromium bump changes font hinting and teams learn to re-bless baselines unread. **`CONTEXT.md` untouched, zero error codes** — a pipeline emits exit statuses, not API errors. A-104–A-110. Deltas: system-overview §3.2, testing-strategy §1/§6.6/§9/§13/§14.2, naming §12, security-standards §11/§13, ADR-0010/0011/0013/0014. |
| `docs/adr/ADR-0019-release-and-promotion-model.md` | 4 | done | 2026-08-04 | **Proposed — needs user review.** Trunk-based with no release branch (naming §12's branch-type list has no `release/`); digest as identity, semver as a human label; automatic staging, human-promoted production; independent per-repo promotion with compatibility from ADR-0007's contract rather than co-testing; backend promotes first; **guaranteed rollback depth of exactly one release, derived from database-conventions §10 rule 5**; contract steps as a one-way door; rollback restores code, never data. Seven alternatives recorded, incl. the release train, GitFlow, tag-triggered builds, auto-promotion on green smoke, and `release-please`-style derived semver. Future considerations flag the one change that could make pairing worth revisiting: a **minimum-supported-app-version mechanism, which the handbook defines nowhere**, leaving client skew unbounded. Takes number 0019, so the still-unwritten **period-lock-ownership ADR moves to 0020**. |
| `docs/07-operations/environments.md` | 4 | done | 2026-08-04 | **Grilled before generation (20 rounds, `/grill-with-docs`), all 20 recommendations taken.** Seam completes ci-cd's in the mirror direction: **this file answers *what exists and how it is configured*, its siblings answer *what you do when it misbehaves*** — and where ci-cd names the secrets CI consumes, this names the store, the path, and the rotation cadence, while **the value appears nowhere in the handbook**. **Three environments** — `local`, `staging`, `production` — with CI-ephemeral infrastructure and the GitHub `release` secret scope named explicitly as *not* environments, since both are routinely mistaken for one; `APP_ENV` is a closed set that fails boot rather than defaulting, because the value that falls back is the one that deletes a tenant. **Two GCP projects, and the split was forced before it was chosen:** ADR-0009 puts the bucket in the FCM project and a staging push must never reach a real employee's phone, so two Firebase — therefore two GCP — projects exist regardless; given that, the cluster and managed services follow, which **converts an isolation boundary ci-cd §11 asserts into one Google enforces** (one wrong ClusterRoleBinding removes a namespace split silently). Three things then fall out as derived rather than decided: **GKE Standard, not Autopilot** (node-exporter needs host mounts, the Chromium profile needs a node file), **neither database has a public IP** — which is what makes ci-cd §7.1's in-cluster migration Job structural rather than preferential — and **one registry**, since ADR-0019 promotes a digest and a digest lives in one place. **Local dev's load-bearing rule: Compose creates all three database roles and the app connects as `hris_app`** — as `postgres` the connection owns the objects, `FORCE` RLS does not bind it, and every tenant-isolation defect is invisible on a laptop until CI or production, which is exactly what multi-tenancy §5's L1–L9 exist to catch. **§6 is the reason to read the file**: the complete environment-variable registry across four environments, ~50 variables in six grouped tables, the only place the handbook answers "what does this process need in order to start" — non-secret values shown, secrets showing only their store key, and no CI gate because a missing variable already fails boot. **Secret delivery is Secret Manager → External Secrets Operator → `envFrom`, with Workload Identity for all GCP access, so no service-account JSON key exists anywhere** — including for ADR-0009's V4 signing, which goes through `signBlob`, and ADR-0016's KMS; the consequence stated as a rule rather than a footnote is that **rotation requires a rollout**, because otherwise an HPA scale-up mints pods on the new credential while the rest hold the old one. **The Chromium answer discharges a Phase 1 duty properly:** ADR-0014 prohibits `--no-sandbox` and under `RuntimeDefault` the sandbox cannot start at all, so a **`Localhost` profile adding exactly three named syscalls** — `clone` with `CLONE_NEWUSER`, `unshare`, `setns` — on one workload, with the residual admitted that a node-level file is the single place D5 portability leaks. **`maxmemory-policy = noeviction` is the finding of §9:** BullMQ stores job payloads as ordinary keys, so under the `allkeys-lru` anyone would pick for "a cache", memory pressure **silently evicts queued jobs** — a payroll run accepted and then never happening, with no error and ADR-0010's at-least-once guarantee quietly void. **Parity is enforced rather than hoped:** identical PG version, roles, RLS, migration history, image digest, variable presence, and **rate limits** — with a **closed four-item list of permitted `APP_ENV` branches**, since `if (env === 'production')` in business logic is always added for a defensible reason and is how staging stops predicting production. **Terraform in `hris-api/infra`, human-applied, with secret values never entering state** — which forced the finding that `google_sql_user` puts a password in the state file, so **database roles are created by a bootstrap script writing straight to Secret Manager**. **`ADR-0020` written (Proposed, needs review)** for environment isolation and the production data boundary, whose load-bearing half is a **refusal**: testing-strategy §7 rule 4 assigned an anonymization path here and none is built, because ADR-0016's per-tenant DEKs make a scrubber either useless (ciphertext) or catastrophic (production KMS in staging), and an anonymizer is a column allowlist that fails open the first time a migration adds a column nobody remembers to scrub — replaced by **forensic PITR inside the production project**, so nothing crosses the boundary and there is nothing to anonymize. **Three defects found and fixed:** **A-017 contradicted D7** — it claimed the spec named no email provider when D7 names Resend and prohibits Amazon SES in bold, and it had been Active for two days pointing at this very file for SPF and DKIM; corrected to Resend, absorbed entirely by `EmailPort`, which is the first time that abstraction earned its keep. **ci-cd §11 gave S5 no credential at all** — S5 is M2, an employee punching offline, and §11 provisioned only an admin — and four journeys on one email would have tripped security-standards §3's 20/hour per-email login limit, a failure whose natural "fix" is relaxing staging's limits and destroying parity; smoke credentials are now a set keyed by role. **multi-tenancy §4 said the `hris_migrator` credential "exists in CI"**, stale since ci-cd §7.1 moved migrations in-cluster and ci-cd §11 stated the pipeline holds no database credential at all — wording that reads as permission to hand CI a `DATABASE_URL`. Four forward promises discharged: ADR-0011's observability deployment and alert routing, ADR-0014's worker sizing and seccomp profile, backend-nestjs §8.1's pool sizing, security-standards §4's mesh/mTLS deferral. **`CONTEXT.md` untouched, zero error codes, zero ⚠️ VERIFY** — no regulation-dependent values exist in an infrastructure document. A-111–A-120, A-017 corrected. Deltas: ci-cd §11, multi-tenancy §4, system-overview §3.3, testing-strategy §7, security-standards §4, notification §4, ADR-0011/0014/0019. |
| `docs/adr/ADR-0020-environment-isolation-and-production-data-boundary.md` | 4 | done | 2026-08-04 | **Proposed — needs user review.** Three environments with a closed `APP_ENV` set; **two GCP projects with IAM as the enforced isolation boundary** rather than two namespaces we police ourselves — forced to start with by ADR-0009 putting the bucket in the FCM project, then chosen for the rest because ci-cd §11's privilege split only means something if the targets are separately governed; production data never leaves the production project, in any form, including anonymized; **no anonymization path is built**, refused on three grounds in descending finality — ADR-0016's per-tenant DEKs make it useless or catastrophic, an allowlist fails open, and a control exercised rarely and trusted absolutely is the worst combination available; **forensic PITR inside the boundary** replaces it; staging synthetic by construction, seeded through system-administration §5.3. Seven alternatives recorded, incl. one cluster with two namespaces, Autopilot (blocked concretely by node-exporter host mounts and the Chromium seccomp file), a scoped "safe-subset" anonymizer, a ciphertext-preserving restore, and a fourth pre-production environment. Notable consequence: **ADR-0016's per-tenant DEKs acquire a second load-bearing role** beyond crypto-shredding — they are what makes the data boundary self-enforcing rather than policy-dependent. Takes number 0020; the still-unwritten **period-lock-ownership ADR moved to 0021, and then to 0022** when the observability session filed `ADR-0021`. |
| `docs/07-operations/observability.md` | 4 | done | 2026-08-04 | **Grilled before generation (20 rounds, `/grill-with-docs`), all 20 recommendations taken.** Completes the Phase 4 seam trio in one sentence: **`environments.md` says a channel exists; this document says what arrives in it, what it means, and what you do next** — the rule and the response, with retention values *quoted* from environments §11 and never re-decided, and rollback referenced to ci-cd and never described. **The atomic unit is the alert**: OB1–OB26 in §4 and OB1–OB26 in §6 are one set in one order, under a pairing rule that makes completeness a **set difference rather than a matter of care** — no alert without an entry, no entry without an alert, and **an alert nobody would act on is deleted, not downgraded**. **§2's cardinality rule is the load-bearing constraint:** `ADR-0011` warned about `tenant_id` labels and nobody costed it — ~600 route-method pairs × 3 status classes × 14 series ≈ 25,000, times 500 tenants is **over 12 million series from one metric**, which is where Prometheus stops rather than degrades, so **`tenant_id` is banned on every histogram and high-cardinality counter without exception** and per-tenant questions go to logs and traces, which already carry `tenantId` and are built for it. **Two severities only**, defined by response expectation rather than impact, with **identical thresholds in both environments** — only the route differs, which keeps environments §12's parity promise honest. **Backlog alerts on oldest job age, never depth**, because a depth rule fires every morning on D1's attendance spike until someone silences it permanently and that silence is still there on the day it matters. **Three gaps closed that no document had asked about.** (1) **OB14 — a cron that never ran.** 26 registered crons, ten destructive; every existing signal catches *runs and fails* and none catches *never started*, which for `cron.leave.period-maintenance` is invisible until January. One gauge and one rule over all 26, tolerance exported from the registry that declares the schedule, **plus an `absent()` arm** — a never-registered job exports no series and `time() - <empty vector>` is a silent pass, so without it the rule fails in exactly the case it was built for. (2) **OB21 — the alerting itself being dead.** Everything that decides whether to alert lives inside the cluster it watches, and with no PagerDuty there is no vendor heartbeat; ingress loss means the product is 100% down, every scrape target green, and the channel **silent — byte-identical to healthy**. Watchdog with absence as the signal, plus an **external Cloud Monitoring uptime check** sharing no failure domain with Alertmanager, the only signal that survives total cluster loss. GCP-specific, accepted, named against D5. (3) **§3.2 — D3 versus the operation that has to deliver it.** 99.9% monthly is **43 minutes**; A-102 has no on-call role, environments §11 has no pager, and an outage at 23:00 WIB seen at 08:00 spends **twelve months of budget in one night**. → **`ADR-0021` (Proposed)**: the SLO is scoped to a **service window, 08:00–20:00 WIB Mon–Sat**, best-effort outside it, with **both windowed and unqualified availability computed** so the gap stays visible, and **detection unscoped** — rules fire at every hour, because the window scopes the commitment, not the detection. **§10 walks the correlation contract for the first time** — declared in five documents, executed in none: a clock-in that "didn't work" at 09:00 month-end, through literal Cloud Logging filters → Tempo → Sentry, **stating that 10% sampling means an absent trace is correct behaviour rather than a broken system**, and a **fourth step every runbook on earth omits** — no log line at all means the punch never reached the server and is sitting in Drift on a phone, which is this product's most common complaint and the one where stopping at step 3 tells a user their punch was never made. Reverse walk too: failed job → `requestId` → the originating request, which `ADR-0010`'s envelope was designed for. **§12 turns three declared redaction routes into a merge gate** — `describeTelemetryRedaction`, testing-strategy §5.6 / **G22** — parameterized over security-standards §10 so it cannot fall behind, plus a **quarterly human sample** because the gate only catches keys somebody thought of; production log scanning rejected, since the storage *is* the breach. **§14 discharges the handbook's only forward promise carrying a legal duty** (security-standards §9): six detection signals **including a report from outside**, containment whose third step is **delete nothing else**, scoping **from the audit log** because 30 days of telemetry cannot scope a breach and `ADR-0011` says audit-grade history is never the stack's job, notification **through the controller** with the **clock starting at detection rather than confirmation** — a process choice, not a regulatory number — and the window and addressees left behind the ⚠️ VERIFY marker. **§4.7 adds four security alerts** where detection previously stopped at CI, with a **named reliance** stated rather than assumed: there is no runtime alert for a cross-tenant read because `FORCE` RLS makes it impossible, so a dropped policy is caught by **G9, not telemetry**. **One defect found and fixed:** testing-strategy §14.1 counted **27 crons** when 26 are registered — the extra was `cron.contract-reminder.scan`, an illustration in ADR-0010 written before employee.md registered `cron.employee.contract-scan`; both files corrected so the same miscount does not recur. `CONTEXT.md` untouched (SLO and burn rate are engineering vocabulary), **zero error codes**, **one ⚠️ VERIFY in §14 only**. A-121–A-130. Deltas: testing-strategy §5.6/§13/§14.1, ADR-0010, ADR-0011, ADR-0019, security-standards §9, multi-tenancy §4. **Amended 2026-08-04 (backup-restore session): OB27 added** — `ADR-0011`'s alerting seed list named *"PITR/backup freshness"* and every other item on it became a rule while this one, the only signal that reports a **broken recovery capability**, did not. A Cloud Monitoring condition on OB21's precedent rather than a Prometheus rule; §15's log-based-metrics exclusion untouched. The set is now **OB1–OB27, 27 rules in six groups** (4+6+5+4+4+4). |
| `docs/adr/ADR-0021-availability-slo-service-window.md` | 4 | done | 2026-08-04 | **Proposed — needs user review.** D3's 99.9% monthly availability is **43 minutes 12 seconds**, and nobody had done that arithmetic since Phase 0. A-102 has no on-call role, `environments.md` §11 has no PagerDuty for that reason — and therefore no vendor heartbeat either — and every tenant is on WIB (A-003, D1). An outage beginning 23:00 WIB and noticed at 08:00 is nine hours, roughly **twelve months of budget in one night**; across a weekend, about eight years' worth. D3's availability clause is therefore not a target being missed but **one the designed operation cannot meet**, and no document said so — which would have left `observability.md` defining an error budget that is permanently exhausted and therefore meaningless. Decision: **99.9% within a service window of 08:00–20:00 WIB, Monday to Saturday**, best-effort outside it; **both windowed and unqualified 24×7 availability computed and displayed**, so scoping the target does not hide the measurement; **detection is unscoped** — rules, the external uptime check, and routing all run at every hour, because the window scopes the *commitment*, not the *detection*; this **scopes D3's availability clause and does not supersede D3**, whose RPO ≤ 15 min and RTO ≤ 4 h are mechanism-bound and stay `backup-restore.md`'s. Six alternatives recorded, including an unwindowed target plus a rotation (rejected on A-102 — a rotation with one name on it is one person's phone, permanently), lowering the target to ~99.5% (rejected: silently weakens a confirmed Phase 0 decision and prices overnight outages as acceptable when the real problem is that they are *unattended*), a second out-of-window SLO (rejected: a figure no process defends is the exact failure being removed, reintroduced a line lower), and relying on GKE self-healing (partly true and already relied upon, but it recovers from none of the failures that actually produce nine-hour outages — a bad release, a migration defect, Redis exhaustion under `noeviction`, an expired certificate). **Trigger to revisit is commercial and named: the first contractual uptime SLA** — shared with three of the nine §15 exclusions. Recorded as A-121. |
| `docs/07-operations/backup-restore.md` | 4 | done | 2026-08-04 | **Grilled before generation (20 rounds, `/grill-with-docs`), all 20 recommendations taken.** Seam: **`environments.md` says a backup exists; this document says what it can restore, what it cannot, what you do to use it, and the proof it still works** — backup configuration quoted and never re-decided, rollback referenced to ci-cd and never described. **§2's scope is a completeness table over eleven stateful stores**, four of which recover nothing, and the two whose loss is *unrecoverable* — the KMS KEK and Terraform state — were undocumented anywhere before this file. **§3: D3 binds one row.** Its RPO clause names PostgreSQL, so objectives for the other ten stores are derived here; write-ahead-log archival beats 15 minutes without effort, which makes **RTO the clause actually at risk**. The fact that breaks most restore runbooks at step 1: **Cloud SQL PITR does not restore in place** — it produces a new instance, so promotion is a `DATABASE_URL` secret version plus a rollout restart, which is A-111's existing rule rather than new machinery. Budget line-itemed to **≈ 3 h 35 m of 4 h with the 120-minute clone step unmeasured**, and on `ADR-0021`'s precedent **two clocks are reported** — the budget runs from the decision, detection lag sits before it, customer-visible outage is the sum. **§4's retention is the only setting in the handbook squeezed from both ends:** 35 days is the monthly `cron.audit.archive` cadence plus notice margin, and it is *also* exactly how long a completed crypto-shred stays reversible by restore, so raising it is a privacy decision and not a cost one. **§5 routes on blast radius and one symptom in ten reaches for PITR**, with a forensic clone required before any remedy that deletes or overwrites — including the forward fix, which is the one people reach for fastest. **`ADR-0022` (Proposed)** answers the incident this handbook had never named: not "the database died", which regional HA and Google already handle, but *tenant 47's admin overwrote 300 salary rows on Tuesday, noticed Thursday* — where instance-wide PITR **discards two days of writes from the other 499 tenants**. Manual extraction instead, feasible only because four mechanisms already existed: uniform `tenant_id`, **stable `uuidv7` ids with no sequences anywhere in the schema** so re-inserted rows keep every inbound FK, `ADR-0016`'s `v1:` prefix so no historical key material is needed, and re-insertion as a new audited fact. **§7 reconciles three stores at three different times:** dangling `files` rows (BR-DOC-009's object-then-row order inverted by a rewind), orphan objects `cron.document.purge` can never reach because it walks rows, outbox re-dispatch that needs nothing because G11 already asserts idempotency, resurrected `tenant_keys` **undoing a completed erasure**, and **Redis flushed before the fleet is repointed** — its idempotency envelopes otherwise assert "already processed" for work whose effect was just discarded, making a legitimate retry silently succeed while doing nothing. **Four defects found in existing documents.** (1) **`ADR-0011` seeded a backup-freshness alert that was never built** — every other seed rule became OB6/OB11/OB12/OB16 and this one, the only signal that reports a *broken recovery capability*, was missing → **OB27**, observability now 27 rules in six groups. (2) **The production bucket had no recovery of any kind** while holding payslip PDFs under a ten-year floor for which the object is the only copy → versioning + soft delete, with retention *lock* rejected because it would leave the purge crons unable to satisfy a UU PDP erasure. (3) **The audit archive job deleted before it verified** — the one destructive cron whose damage is reconstructible from nowhere → verify-then-delete. (4) **The tamper-evidence witness expired 119 months before the evidence** — BR-AUD-009 rests on the digest living outside the database, but Cloud Logging keeps 30 days → a log sink into the archive bucket. Plus **the Terraform state backend was named nowhere**, which read literally means local state on one laptop. One correction: **database-conventions §4.4 attributed a payroll cold-archive requirement to D4**, whose cold-archive clause covers the audit log only, and no module owned the mechanism → payroll and tax stay live for the full ten years. `CONTEXT.md` untouched (RPO, RTO, and PITR are engineering vocabulary — the fourth Phase 4 file in a row), **zero error codes**, **one ⚠️ VERIFY** in §11.3 on whether an unreadable-but-restorable copy counts as erased. A-131–A-143. Deltas: observability §4/§5/§6/§15, environments §9/§10.2/§11/§13.1, audit-log BR-AUD-009/010 + UC-AUD-006, database-conventions §4.4, ADR-0011/0016/0020/0021, testing-strategy §1, ci-cd §1 + §14, A-123. |
| `docs/adr/ADR-0022-tenant-scoped-recovery.md` | 4 | done | 2026-08-04 | **Proposed — needs user review.** `ADR-0002` puts 500 tenants in one PostgreSQL database and D3 supplies PITR, but every part of that assumes the failure is **instance-wide** — and the failure this product will actually meet is one tenant's HR admin running a bad import. Restoring the instance to repair them **discards every write from the other 499 tenants** in between, with no undo, using the only procedure the handbook documented. Decision: **instance-wide PITR is reserved for instance-wide damage**; single-tenant loss is a **manual extraction** through `ADR-0020`'s already-sanctioned clone inside `hris-production` — extract that `tenant_id`, re-insert in FK order carrying original ids, verify, destroy the clone. It is a procedure rather than a project only because four mechanisms already exist: uniform `tenant_id`, **stable `uuidv7` primary keys with no sequences and no minted running numbers anywhere in the schema**, so a re-inserted row keeps its id and every inbound FK still resolves; `ADR-0016`'s `v1:` ciphertext prefix, so rows encrypted under a rotated-past DEK still decrypt and the extraction needs no historical key material — with the hard corollary that **an extraction carrying encrypted columns must land in the same tenant or it carries nothing readable**; and re-insertion as a **new audited fact**, never a rewrite, which OB26 already anticipates. **Explicitly not a product feature** — no endpoint, no button, on BR-ADM-009 and A-095's precedent. Six alternatives recorded: nightly per-tenant dumps (500 a night and a second retention system with its own silent-failure mode, standing cost against a rare event, and only last night's snapshot rather than any timestamp), universal soft delete (no help against a bad *update*, taxing every query forever), accepting the collateral, `ADR-0002`'s per-tenant-database hatch (nothing provisioned, and rebuilding tenancy to ease restores is a far larger decision), tenant-level undo as a product feature (28 module designs), and a deliberately-lagged read replica. **Tradeoffs stated, not softened:** manual, hours to days, FK ordering validated by nobody, **does not fit inside D3's four hours and does not claim to**, nothing technically prevents a tired operator reaching for PITR anyway, and FK verification catches structural breakage but not semantic breakage. **Zero schema, migration, table, permission, or error-code cost** — which is the strongest argument for it over every alternative. Recorded as A-131. |
| `docs/adr/ADR-0023-table-growth-and-partitioning.md` | 4 | done | 2026-08-04 | **Proposed — needs user review.** Discharges the declarative-partitioning question `ADR-0002` and `ADR-0013` both deferred to the performance document **by name** and neither could answer, because no document had projected row counts. **No partitioning in V1**, trigger numeric — 500M rows in one table, or autovacuum on `attendance_days` failing to keep up (it takes an upsert per punch). The projection that made it answerable: `attendance_punches` ~500M rows/yr at the D1 design point, `attendance_days` ~250M, payslip lines ~240M — **two families exceeding the audit log, the only table already partitioned** — and the same table is what finally makes database-conventions §7 rule 4's *"no speculative indexes"* checkable. **The finding neither ADR could have known: hash by `tenant_id` is the only key still available on `attendance_punches`.** PostgreSQL requires the partition key in every unique constraint, and `uq_attendance_punches_tenant_id_op_id` carries no date column and cannot — an `op_id` is minted on a device with no knowledge of when the row lands — so **range-by-date, the key that makes retention a `DROP PARTITION`, is foreclosed by the constraint that makes offline dedup correct**, and hash buys vacuum and index size while buying nothing for deletion. `attendance_days` keeps both doors and is the first candidate if the trigger fires. Third decision: **attendance retention is named as a gap, not filled** — it was absent from §4.4 entirely, and the horizon is a labour-law fact, so it carries the ⚠️ VERIFY marker rather than an invented number (A-017 and A-140's precedent). Six alternatives recorded, including dropping the `op_id` unique to unlock date partitioning — rejected outright, since it trades a correctness guarantee for a storage optimization. Zero schema, zero migrations, zero code: the entire product is three recorded facts and a trigger. |
| `docs/adr/ADR-0024-tenant-onboarding-as-a-v1-capability.md` | 4 | done | 2026-08-04 | **Proposed — needs user review.** Nine module documents each refuse a bulk import and every refusal is right on its own terms; read together against the act of onboarding one tenant they leave a step with **no mechanism** — `employee.master`'s template carries no salary column and `payroll.md` §13 writes **"No import."** in bold, so a tenant at D1's typical two thousand employees types two thousand multi-line effective-dated pay packages by hand, and until it does there is no payroll run and the product has not done the thing it exists for. **Only a document whose input is every module at once could see it.** Resolution reverses nothing: payroll's argument is an argument against **supersede**, an onboarding package is the *first* for an employee who has none, and `ADR-0015`'s `create_only` — already used by `employee.master` (A-019) and described in exactly these words by `leave.balance_adjustment` — preserves the property the excluded dry-run existed to buy. Decides: onboarding is a first-class V1 capability; **import exclusions are exclusions against `supersede`, never against seeding** (stated generally so the next module does not rediscover it, with attendance's, overtime's and expense's refusals explicitly untouched because theirs are about the data); `payroll.salary_opening` registered `create_only` with `payroll.salary.import`, refusing any employee who already has salary history; bulk **revision** stays in payroll §15 behind the dry-run. Six alternatives recorded, including adding salary columns to `employee.master` and a platform-operator seeding script — both rejected on named grounds. Zero new error codes, zero schema change, zero new terms. |
| `docs/07-operations/performance.md` | 4 | done | 2026-08-04 | **Grilled before generation (19 rounds, `/grill-with-docs`), all 19 recommendations taken.** Closes `docs/07-operations/`. Seam: observability says a budget was missed; **this says what it is made of, what must exist to meet it, and how it is proven at volume**. 13 sections, arithmetic first, one sequence diagram of a morning clock-in. **D1 re-read at the fleet** — one synchronized Indonesian morning, 333 opens/s, ~2,700 req/s, and **the punch is one of two writes and one of ~8 requests**. D2 pinned **server-side** with a **closed one-member exemption** (the report inline path, excluded from OB1 by label) and the decomposition showing ~10 ms of 300 is overhead, so **97% of the read budget is the query**. Sets the three PostgreSQL timeouts nobody had defined (database-conventions §10 rule 6's *"tolerances"* at last), supplies environments §7.4's missing right-hand side, and reframes **OB5 as hold time, not pool size**. **Cache registry over seven caches invented in seven documents**, with three admission rules; **idempotency envelope cut 7 d → 24 h** on ~20 GB of arithmetic, which is what makes a 16 GB `maxmemory` floor work. Payroll gets **two clocks** and a concurrency bound; **the render budget does not fit** at 1Gi. **Permission resolution made lazy** for `@AuthenticatedOnly()`. **DDL cost table + `lock_timeout`** make ci-cd's ungated `migrate` Job safe rather than lucky. k6 capacity rehearsal on an ephemeral production-sized environment, with a **D1-scale generator that also feeds backup-restore §3.2's unmeasured clone estimate**. **Per-tenant QoS named as absent.** 15 files amended, 9 substantively. `CONTEXT.md` untouched, zero error codes, one ⚠️ VERIFY (in `ADR-0023`). A-144–A-161. |
| `docs/04-database/erd-overview.md` | 4 | done | 2026-08-04 | Moved from Phase 1/2 baseline (A-002); generated after `implementation-roadmap.md`, closing MANIFEST row 68. **Regrilled 2026-08-04 (10 rounds) — see the narrative above; every figure in this row is the corrected one.** **The handbook's only whole-schema document**: 115 tables, 277 FKs inclusive / 189 net, 126 cross-module, **39 module-to-module — the ADR-0001 §5 extraction-cost inventory, totalled for the first time** — with `files`/`branches`/`approval_instances` absorbing 30 of the 39 and only four business-to-business constraints in the entire system. Adds §5 (RLS class is not inferable from the columns — six exceptions), §7 (**ten** relation classes deliberately left unconstrained, each justified — the audit-column triple added on regrilling), §8 (five declared cascades, three more existing only as comments), §11 (recount rule, now mechanised: `scripts/erd-check.mjs`, checks `C1`–`C9`, exit 1 on disagreement). *(The original §8, deferred FKs, was deleted on regrilling — its load-order premise does not hold; §§9–12 renumbered to §§8–11.)* 10 Mermaid diagrams, all parser-validated, ≤25 nodes. A-171 records that the domain grouping is presentation, not architecture. §10 is the full defect record — 15 entries, 8 open (the MANIFEST row 70 sweep), 7 fixed on regrilling. A-172–A-174. |
| `docs/00-overview/implementation-roadmap.md` | 4 | done | 2026-08-04 | **Grilled before generation (17 rounds, `/grill-with-docs`), all 17 recommendations taken.** Generated ahead of `erd-overview.md` at user request. 11 sections, dependency-ordered, one Mermaid of three tracks converging on R1. Seam: **the only handbook file nothing defers to**, therefore the only one whose input is every module at once — owns build order, milestones, release gates, onboarding sequence; **indexes** the V1 cut and the ~200-item post-GA backlog rather than copying either (A-166). Reading rule: every ordering claim marked **forced** or **judgment**. Build order = walking skeleton through three repos **including the offline-queue skeleton** → platform spine by port-consumer count → business backbone, with the sanctioned `PeriodLockPort` fake stated once instead of implied by three modules, and the statutory calculators running **in parallel from day one** because `ADR-0018` makes them dependency-free. Milestone = capability demonstrable in staging, never a coordinated release (`ADR-0019` decision 4 forbids the train); app-store queue **attaches without gating** (A-167). **R1 = the 9 interlocking backbone modules**, R2 = the periphery — a release boundary that leaves spec §3.2's scope boundary untouched, forced by the port graph since payroll consumes no expense port and the statutory exports travel with tax and bpjs (A-168). §7's 12-step onboarding sequence and §8's 8-row first-release gate collected for the first time (A-169). §9.2's nine post-GA dependency clusters. §10 anti-scope: no dates, no estimates, no Gantt, **no status column**, plus shelf life per section (A-170). **Findings:** the unonboardable tenant → `ADR-0024` (A-165); 119 VERIFY markers counted *(recounted 121 across 28 files on 2026-08-05; `ADR-0018` §5 corrected — the original count predates the markers this session and `ADR-0023` added the same day)* → third workstream + G20 narrowed (A-163, A-164); rollback never rehearsed; the migration gate inactive for the whole build. |
| *(task)* schema-declaration sweep | 4 | done | 2026-08-05 | MANIFEST row 70, discharged — **`node scripts/erd-check.mjs` exits 0**, from 92 failing checks. Closed all eight of `erd-overview.md` §10.1: **44 comment-only foreign keys became real `.references()`** across 12 documents, `payroll.md`'s eight tables adopted the `...id, ...tenantId` spreads and gained **three `onDelete: 'cascade'`** plus every FK it had only ever declared in comments, `leave.md`'s audit-column override removed, `overtime.md`'s ADR-0001 miscite rewritten and its solid non-FK edge made dotted, `database-conventions.md` §8's `SET NULL` clause corrected (landed in row 71's session). **Six relations were declared unconstrained instead**, not constrained: three actor columns in trails, one class crossing already documented as such in `system-administration.md`, one natural-key class crossing, and the six statutory parameter sets — which turn out to be the `overtime_rate_rules` version-pin case repeated, a *row set* with no key to reference. **`naming-conventions.md` §2.1's example is now unparseable rather than renamed** (A-177): a fictitious table name was tried first and was immediately counted as a 116th real table. Every figure in `erd-overview.md` moved and §§3, 6.1, 8, 9, 10, 11 were rewritten to the derived values — **322 inclusive / 226 net / 149 cross-boundary / 8 cascades / 16 non-FK classes**, module-to-module still 39 with the same four business couplings. A-173 annotated, A-177 added. |
| `docs/08-ai-guide/ai-development-guide.md` | 4 | done | 2026-08-05 | **Grilled before generation (11 rounds, `/grill-with-docs`), all 11 recommendations taken.** 10 sections, ~330 lines. Seam: **nine of the ten rule areas `HANDBOOK_SPEC.md` §14 names already have an imperative home**, so this file restates none of them — it carries only the reading protocol, the deviation protocol, contract authority, the `⚠️ VERIFY` rule, a pointer index, ~50 pairs, and the checks. **A pair earns its place on two conditions, both required: a model's prior fights the rule, and no automated check catches the violation** — a lint error teaches faster than prose, so what survives is the violation that passes CI green. Pairs are **derived, not curated**: 135 rejected alternatives across 24 ADRs plus 78 coding-standards ban phrases, minus everything already gated. **Reading protocol is 3,200 lines before the module document** — ADR corpus read whole (1,900 ln, cheaper than the selection logic), registries grepped, dependency modules **never read whole** and resolved by grepping the port name (A-175, A-176). **`scripts/guide-check.mjs`** makes the derivation real (`G1`–`G7`) and proved itself immediately by failing the build until `ADR-0025` had a pair. Ships before MANIFEST row 70; the one place a stale rule leaked was `database-conventions.md` §8, corrected here. A-174 amended, A-175–A-176. |
| `docs/adr/ADR-0025-handbook-distribution-and-deviation-path.md` | 4 | done | 2026-08-05 | **Proposed — needs user review.** Three questions §14 cannot be answered without, none of which any document had decided. **(1) The handbook is a pinned git submodule at `docs/handbook/`** — git is the drift mechanism, so no sync script and no hash list; the pin bump is a reviewable commit, which means an agent reads the revision the code was written against. Retires ci-cd **C8** and amends `ADR-0018` decision 7: the holiday vector is read in place, because vendor-plus-hash was a hand-built substitute for what a pin guarantees. **(2) One ADR namespace, in the handbook.** Implementation repos get no `docs/adr/` and no `CONTEXT.md`; the submodule is a full clone, so the agent writes the ADR *inside it*, opens a PR on `hris-handbook`, **implements without waiting**, and marks every dependent line `// ADR-nnnn (Proposed, PR #n)` — if the PR is rejected the revert set is a grep. **(3) The handbook is authoritative for contracts and silent on implementation** — the seven contract sections named explicitly; an internal difference is not drift and is recorded nowhere. Without that split either every refactor owes an upstream PR or the handbook becomes fiction while §2 still tells agents to trust it. Eight alternatives recorded. |
| `docs/08-ai-guide/implementation-claude-md-template.md` | 4 | done | 2026-08-05 | **Grilled before generation (7 rounds, `/grill-with-docs`), all 7 recommendations taken.** Closes `docs/08-ai-guide/` and the handbook's content. 9 sections. **Three complete `CLAUDE.md` files, verbatim, ~40 lines each** — copy one, no assembly, because §14 says *ready-to-use*. Deliberately thin: `ADR-0025` put every protocol in the submodule, so the template carries only what must be true **before** any handbook read — repo identity, the mandatory gate, that stack's prohibitions, the design skill. **The gate is structural, not behavioural:** each block opens with `@docs/handbook/…/ai-development-guide.md`, which loads the guide at session start (A-179). Three signals carry it, because the import fails *silently*: import, prose fallback, and `C11`. **Found and fixed a defect in last session's guide** — six §7 pair rows cited `CLAUDE.md` as authority, which under `ADR-0025` resolves to the handbook-generation instructions, not anything an implementer should follow (A-178). All six repointed; `backend-nestjs.md` §1 gained the Prisma prohibition it never stated. Ships `docs/agents/domain.md` verbatim (`ADR-0025`'s orphan consequence) and the submodule bootstrap. **`C13` generalised** from *Result triplication* to handbook-managed regions over a path manifest — one mechanism, not two invented a session apart. `guide-check.mjs` gains `G8`–`G10`. A-178, A-179. |
| *(task)* final consistency audit | 4 | done | 2026-08-05 | MANIFEST row 73, the last row. **Ran as checks, not as reading.** Clean on first pass: **0 broken cross-references** (every backticked `.md` path in 83 documents resolves), **0 dangling ADR references**, **0 MANIFEST ghosts**, **0 MANIFEST rows missing from `PROGRESS`**, **0 failing Mermaid diagrams** across the 57 documents that carry one, and the glossary's 20 domain sections cover every module namespace with all six required Indonesian regulatory terms present. **Five defects found and fixed.** (1) `training.md` raised `APRV_INSTANCE_NOT_IN_PROGRESS` twice; the catalog registers `APRV_INSTANCE_NOT_ACTIONABLE` — an invented variant of a real code, the exact failure the same-session registry rule exists to prevent. (2) `ai-development-guide.md` cited `backend-nestjs.md` **§105**, a line number written as a section; `ADR-0025` cited **§11** for the same claim; both are **§5**. One broken section citation in 83 documents, and it was in the newest file, twice. (3) `ADR-0025` was absent from `MANIFEST` — created mid-task during row 71 and never registered. (4) `PROGRESS` row 10 still read *"71 files + final audit task"* after the sweep row was inserted. (5) `payroll_runs.paid_by` carried no foreign key while **twelve of the other thirteen non-audit actor columns did**. **One carried finding was itself the defect.** The `PeriodLockPort` served-list was reported as missing four consumers; `asset.md`, `bpjs.md`, `expense-reimbursement.md` and `tax-pph21.md` each state **No `PeriodLockPort`** in bold with reasoning. The served-list was right all along — the finding came from a grep that could not distinguish consuming a port from declining one, and `ai-development-guide.md` §2.3 now carries that caveat because its own protocol resolves contracts by grep. **The `C6` suffix gap is closed rather than carried.** `erd-check` inspected only `*_id`, while `erd-overview.md` §7 rule 2 claimed the suffix decides nothing — so the check asserted an enforcement it did not have. Widened to `*_by`; the gap cost exactly one constraint. Counts moved to **323 inclusive / 227 net / 150 crossing / `users` 33**. Both scripts exit 0. |
| *(task)* first commit, branch protection, and a reverted branching model | 4 | done | 2026-08-05 | Not a MANIFEST row — the repository had never been committed. 86 files and 24,734 lines went in as **six commits split by handbook phase** rather than one bulk commit, and tagged **`v1.0.0-rc.1`, not `v1.0.0`**: nine ADRs are still `Proposed`, and `1.0.0` is reserved for the release that ratifies them. **Branch protection applied and proved by test** rather than by reading the API back — a direct push to `main` and a force-push were each rejected by `GH006`. `enforce_admins` **on**, which is the only setting that closes the threat `ADR-0025` §2 names, since the agent it warns about holds the maintainer's admin token; **zero approvals** and **no required status checks**, both forced rather than chosen (A-180). **The branching model was wrong and is reverted** (issue #1). The work was done on gitflow at the user's request, and gitflow is prohibited here by two documents that were not read first: `naming-conventions.md` §12 fixes the branch types — `feat`, `fix`, `refactor`, `docs`, `test`, `chore`, `perf`, `ci`, with **no `develop`, no `release/`, no `hotfix/`** — and `ADR-0019` §1 decides *"Trunk-based development, no release branch"* citing that very list. It also breaks `ADR-0025`'s own mechanism, where the submodule pin bump **is** the ratification record: with the branches split, a merged pull request is not yet at `main`, and `main` did in fact lag by two commits holding a `ci-cd.md` known to be wrong. **The tell was mistaken for a decision.** The conflict surfaced as `ci-cd.md` §430's squash-only rule, and §430 was amended to accommodate gitflow — inverting `CLAUDE.md` rule 4, which says to supersede the ADR first, never to edit a dependent document into agreement. A document that must be edited to stop disagreeing with a choice is evidence against the choice. §430 and the ADR-0025 clause are restored, A-180 rewritten to record only the protection settings, `develop` deleted. History is **not** rewritten: the six import commits and their merges stand as a one-time artifact, and force-push is now correctly blocked. **One open finding, carried, not fixed:** `docs/07-operations/test-vectors/holiday-resolution.json` does not exist, while `ADR-0018` §63 and its Consequences, `testing-strategy.md` §307, `ADR-0025` §1 and `ci-cd.md` §182 all state that it does — `ADR-0018` calls it the handbook's *first* executable artifact. Row 73 could not see it: its cross-reference check reads `.md` paths only, and no MANIFEST row claims the file. The count in row 73's entry — *"2 executable artifacts, 0 open findings"* — is wrong on both figures. A-180. |
| *(task)* CI for the handbook repository | 4 | done | 2026-08-05 | Issue #3. `.github/workflows/checks.yml` — one job, two steps, running `erd-check` and `guide-check` on pull requests into `main` and pushes to `main`, then registered as a **required status check**, which discharges the gap A-180 left open. **Recorded as a new decision, not as compliance:** `ci-cd.md` specifies no pipeline for this repository — C1–C13 and G1–G22 are every one of them scoped to *"all three"* or a named implementation repository — so A-181 carries it rather than an appeal to that document. What the handbook *does* bind here are the clauses that were never scoped: `security-standards.md` §137 and `ci-cd.md` §375 pin actions by commit SHA rather than tag, and `ADR-0019` §1 fixes trunk-based squash merge. **Deliberately unfiltered by path**, which reads like a missed optimisation and is the opposite: a required check that is skipped never reports, GitHub reads never-reported as not-yet-passing, and the merge blocks permanently. **No `actions/setup-node`** — stdlib ESM, no `package.json`, and the runner ships Node, so there is one action to pin instead of two. **Limit stated in A-181 rather than left implied:** this gates mechanical drift only, and nothing in it would have caught the gitflow excursion one entry above — no script detects an anchor that went unread. Still ungated and binding: `naming-conventions.md` §12's Conventional-Commit rule on the squash-merged pull-request title, where a bad title is now unrecoverable because force-push is blocked. **Registering the check falsified two documents within the hour** — A-180 and `ci-cd.md` §437 each asserted *"no required status checks"* — and **nothing detected it**, because branch protection is a claim about GitHub settings rather than about files and neither script can see it. Both amended in place, and `ci-cd.md` §437 now carries that episode as the argument for a **C10** this repository does not have. A-180 amended, A-181. |
| *(task)* capacity revised to one engineer plus an agent | 4 | done | 2026-08-05 | Issue #6. `implementation-roadmap.md` §2.1 recorded capacity as *"roughly three to five engineers … plus a part-time statutory advisor"* and marked itself as the single place that reverses every parallelism claim if the number changes. It changed. **The reversal is narrower than it looks.** §4's sequence is ordered by dependency rather than headcount and does not move; track 2's *"no runtime dependency at all"* survives intact but now means **schedulable at any moment** rather than *running beside track 1* — an ordering freedom, not simultaneity. **A-006's three repositories stand**, decided rather than assumed: §2.1's own floor — *"below someone who can carry Flutter, someone NestJS, and someone Next.js, the split is cost without benefit"* — is now crossed and is not binding, because it was an efficiency argument about one human context-switching across three stacks, while `ADR-0019` rejected the monorepo on the mobile client's version skew, a product ground that does not weaken as a team shrinks. Zero documents changed as a result. **Track 3 gains the owner A-163 asked for and never had: the maintainer, personally.** It cannot be delegated to the agent — `CLAUDE.md` forbids inventing a regulatory value, which is the entire job, and the `⚠️ VERIFY` marker exists because that prohibition needed somewhere to point. So the separation A-163 defends survives as **a separate act rather than a separate person**: reading the regulation and signing the number is its own work with its own artifact, never done inline while implementing a calculator. The hazard A-163 named is **sharper** now, not milder, because the engineer under sprint pressure and the advisor are one person. That also makes §8's release gate the schedule's binding constraint — 119 markers, none discharged. **Separate gap closed in the same change:** §2.2's table of claims resting on Proposed ADRs did not list `ADR-0025`, and the roadmap mentions it **nowhere** — it was written first. §4.1's walking skeleton opens with *"three repos"*, which rests entirely on that ADR's submodule mount and single ADR namespace. Row added; the table was incomplete rather than wrong, which is the one thing its own preamble forbids. A-162 revised, A-163 owner named. |
| *(task)* bootstrap the three implementation repositories | 4 | done | 2026-08-05 | Issue #8. `hris-api`, `hris-admin`, `hris-mobile` exist, private, each mounting `hris-handbook` at `docs/handbook` as a submodule **pinned to `a444269`** and never tracking a branch. Every file is verbatim from `implementation-claude-md-template.md` — extracted programmatically rather than retyped, because `G10` exists precisely to catch the three `CLAUDE.md` blocks drifting apart and hand-copying is how that starts. **This repository is renamed `hris` → `hris-handbook`**: seven documents named it that way **17 times** and the name did not exist, the same defect class as a document asserting a file nobody created. One rename made all seventeen true; `CLAUDE.md`'s issue-tracker line was the only reference needing an edit. **Two deviations from template §2, both deliberate.** The submodule URL is **HTTPS, not the SSH form written there** — the handbook is public and the three are private, so a public submodule inside a private repository is fetched with **no credential at all**, which is what removes the deploy key from three places. And gate **C11** ships in the same commit rather than later, because an uninitialised submodule fails *silently*: the `@`-import resolves to nothing and the session looks entirely normal. C11 is a real assertion, not a green tick — `test -f` on an absent file exits 1, so a passing job proves the submodule checked out. It passed in all three. **Design skills split strictly per §2** — `ui-ux-pro-max` in `hris-mobile` only, `frontend-design` in `hris-admin` only, `hris-api` neither — installed as `.agents/skills/` content plus a `.claude/skills/` symlink, mirroring this repository, with the domain skills added in all three so `docs/agents/domain.md`'s redirect has something to redirect. All of it gitignored, so the three repositories track six files each. **One finding, accepted with a trigger (A-182): GitHub Free gives a *private* repository neither branch protection nor rulesets.** §13's admin enforcement, its approving review, and **C10** are unimplementable there today — the handbook assumed a paid plan and never said so. Squash-only and auto-delete applied anyway (repository settings, not protection), and C11 **reports without being able to block**. Accepted because the repositories hold no code yet and the threat `ADR-0025` §2 names is an agent rewriting *the anchors*, which live here — public, protected, verified. Trigger: install protection before the walking skeleton lands. A-182. |
| *(task)* walking skeleton — `hris-api` | 4 | done | 2026-08-05 | `implementation-roadmap.md` §4.1 **item 2, end to end and running**: login with the tenant picker, `app.tenant_id` inside the request transaction, RLS on a throwaway table, and one route through the full guard chain. Also item 1's *artifacts* — Dockerfile, the `migrate` Job entrypoint, a CI job with the gates this repository can honestly run — while item 1's actual promotion waits on infrastructure nobody has provisioned. **Item 3 (the offline mutation queue) is untouched: it lives in `hris-mobile`.** What the skeleton was built to do, it did — it found three defects that reading had not, in the exact way §4.1 predicted. **A-183, and it is the real one:** the RLS policy template promised that a request forgetting `set_config` reads zero rows, and behind a connection pool it instead *errors*, because a transaction-local GUC reverts to the empty string rather than to `NULL` and `''::uuid` raises. Fail-closed, so nothing leaks, but multi-tenancy §5's **L2 asserts the empty result** — so the leak matrix failed against the policy the first time either was executed, and `database-conventions.md` §9.2 now carries `NULLIF`. **A-184:** core-schema §10's bootstrap order created `user_roles` one migration ahead of the `companies` it references; swapped. **A-186:** a Homebrew Redis shadowed the Compose one on the default port and the application ran happily against the wrong store — both services now publish to `127.0.0.1` so the collision fails loudly. `ADR-0001` rule 4's `shared/` whitelist gains the rate-limit guard, on ordering rather than convenience: backend-nestjs §5 puts it ahead of authentication so credential stuffing pays no argon2 cost, which is precisely why it cannot live in `auth`. **The ports refactor is worth recording because a runtime error surfaced it:** `application/` was importing `infrastructure/`, invisible until an ESM-only JWT library made the unit test unloadable — the layer rule turned out to be the thing that let the login use case be tested with no database, no Redis, no argon2 binary. Proven rather than asserted: 9 unit tests over BR-AUTH-001/002/003/011/013, the L1/L2/L3/L7 leak matrix against real PostgreSQL under `hris_app`, and a route lint that was itself checked by removing a marker and watching it fail. **A-185 lists what is deliberately absent** — refresh, logout, sessions, devices, password reset, invite, device registration — so the gap between `authentication.md` §7 and the running surface is a list rather than a discovery. |
| *(task)* the three implementation repositories go public, and get protected | 4 | done | 2026-08-05 | A-182's trigger — *"install branch protection before the walking skeleton lands"* — fired the moment `hris-api` PR #2 merged, and the gap it named was unfixable on the plan: **GitHub Free gives a private repository neither branch protection nor rulesets.** Resolved by the route the maintainer had declined hours earlier and then chose: **all three are public.** Preceded by a history scan for key material, provider tokens and committed `.env` files across all three — clean, and the only credentials anywhere are the local-development literals in `docker-compose.yml` and the Postgres init script, which pass environments.md §5's *"would publishing this be a problem"* test. Protection now matches `hris-handbook` exactly and was **proven by a rejected push**, not by reading the API: `protected branch hook declined`, naming the missing `gates` check, against an admin token with `enforce_admins` on — which is what makes the clause true against the threat `ADR-0025` §2 names. **Two gates stop being aspirational.** **C3** ships as GitHub secret scanning with **push protection** on all four repositories, and push protection rather than plain scanning is the point: it refuses the commit instead of reporting it after the world has read it. **C10** now has a protection to drift from. Also true and worth stating plainly: the product source of a commercial SaaS is now readable by anyone, which was the reason for the original refusal and is a commercial judgement rather than an engineering one. |
| *(task)* track 2 — overtime pricing calculator (`hris-api` #4, `caefab5`) | 4 | done | 2026-08-06 | First of the four §4.4 calculators, chosen because track 2 blocks on nothing. `overtime.md` §4.5 as a pure function over a snapshot slice: resolve `H` with the BR-OVT-010 fallback, clamp to evidence (BR-OVT-008), walk the tiers, weight, flag the meal, convert TOIL — `Result`-returning, decimal.js per A-016, no ports, no clock. **ADR-0018 executed for the first time**, and its load-bearing clause held in an instructive way: with every `structural-fiction-v1` factor exactly `2.00`, the *total* cannot distinguish a wrong tier split from a right one — so every structural vector asserts the `tiers` trace, which is also what the payslip explain-view needs. 19 structural vectors (worked-example shapes, both sides of every tier boundary, H fallback, clamp, consent, meal threshold, TOIL conversion, rounding direction), proven by mutation rather than asserted: flipping the rounding direction fails 3, shifting the `standard_day` bound fails 5. G7 and G8 (P2 via fast-check) join the unit suite; G20's counter prints in CI — 6/6 pending-verification — informational until a promotion pipeline exists. The statutory file is a quarantined scaffold: null `input`, null `expected`, citations only, because an assistant never types a regulatory number (ai-development-guide §5); the runner refuses a verified entry until the statutory rate set is wired, so a status flipped without the work fails loudly. Implementation decisions the module doc left open recorded as **A-187** (single end rounding at 4dp half-up; TOIL days from the pinned figure; malformed rate set rides `OVT_RATE_RULES_MISSING`). Submodule pin → `a3a87b3`. Next: bpjs → tax-pph21 → payroll. |
| *(task)* track 2 — BPJS contribution calculator (`hris-api` #5, `b9f851e`) | 4 | done | 2026-08-06 | Second §4.4 calculator: `bpjs.md` §4.3's resolution ladder and §4.4's contract as a pure function — enabled → excluded → JP age → base → floor → cap → rate → round → credit, classified rows out because `income_class` on an employer premium is a BPJS fact (BR-BPJS-011). 17 structural vectors against fictional rates (10%-steps, round caps, JP ceiling 100, unit 1000); every mutation tried — credit direction, age boundary, cap-before-floor, surcharge outside the bucket — failed its pinning vector. **One found defect, fixed before merge and recorded in A-188 as a defect rather than a preference:** the first implementation credited only the main line against `mtd`, and since `mtd` carries one figure per (program, payer) with a prior run's dependent surcharge inside it, a final settlement would have re-charged that surcharge in full — writing the assumption down is what surfaced it. The credit now applies at the bucket, main line first, pinned by two vectors. Statutory scaffold: 9 pending-verification entries. |
| *(task)* track 2 — PPh 21 and payroll pipeline calculators (`hris-api` #6, `91008fe`) — **track 2 complete** | 4 | done | 2026-08-06 | Third and fourth §4.4 calculators. **tax**: TER on the month's cumulative bruto with the MTD credit (§4.6's under-withholding case is a vector), the annual sequence through PKP's down-rounding and the marginal brackets, refunds as negative withholding, a band-walking monthly gross-up whose rounded allowance IS the withholding line, a fixed-point annual gross-up through the PKP floor, the cumulative severance tariff, and the surcharge as one multiplier — 16 vectors, mutations on the band edge / PKP direction / marginal walk fail 4/1/6 (A-189). **payroll**: §4.5's eight stages over one employee's snapshot slice, composing `computeBpjs` and `computePph21` through their facades — the siblings never call each other, pipeline order is the whole of the coupling, and **ADR-0012's golden class (snapshot slice in, exact payslip out) is vectorable end to end before any run machinery exists**. 12 vectors: category bases, the floored overtime basis, proration touching lines but never bases (half a month of pay, a full month of contributions — BR-PAY-027's wire, and a mutation on it fails exactly that vector), employer premiums pushing bruto into a higher band while net goes honestly negative, the THR ladder, the gross-up pair footing, a final settlement composing a refund with severance (A-190). **The roadmap's §4.4 judgment held:** the product's largest correctness risk is now proven structurally correct — G7 vectors and all five G8 properties (P1–P5) gate every PR — while **G20 prints 31 pending-verification vectors across four statutory scaffolds, which is track 3's complete, countable task list.** The engines remain unproven against Indonesian regulation, and ADR-0018 is the document that says so plainly. Handbook pin `fd17247`. Suite: 111 passed, 31 skipped. |
| *(task)* the authentication remainder — `hris-api` #7, `4d89f9d` | 4 | done | 2026-08-06 | **A-185's deliberate-omission list, closed** — spine `implementation-roadmap.md` §4.2 order 1, chosen because it blocks on nothing and because every route written after it would otherwise be tested through a session that cannot be refreshed. The skeleton had been *issuing* a refresh token since PR #2 (`sessions.refresh_token_hash` is `NOT NULL`) with no endpoint able to consume one. **Refresh is the substance.** Single-use rotation (BR-AUTH-004) with the 10-second grace window returning the *same* successor pair (BR-AUTH-005) and the 7-day used-token history that makes a replay past grace a family revoke rather than a re-issue; the sliding half of BR-AUTH-006, which the skeleton had narrowed on purpose and `session.repository.ts` had documented as narrowed, is now enforced — 30 d mobile / 7 d web against the absolute cap. **Three orderings are load-bearing and none is obvious from the rule text:** the grace cache is consulted *before* the session lookup (a rotated hash matches no row, so a legitimate multi-tab replay would otherwise read as theft); history and grace are written *after* the commit (a rolled-back rotation must leave nothing to family-revoke on, and no cached pair that never existed); and the sliver where a hash is in **neither** store — a concurrent rotation committed but not yet recorded — answers `AUTH_REFRESH_INVALID`, never a family kill, because a false revoke logs a real user out of every device. **Devices arrive whole:** registration at mobile login, the BR-AUTH-007 limit with self-service replacement revoking the old device *and* its sessions in the login transaction, revoked installs refused terminally (BR-AUTH-014 — revocation is per install and does not wash off with a re-login), and UC-AUTH-005's cascade dropping the FCM token in the same statement as the status flip. **Credentials:** reset request answers 200 before any lookup begins, which is the mechanism BR-AUTH-010's timing equality actually needs; single-use is the consuming `UPDATE`'s row lock rather than the read that precedes it, so two racing confirms cannot both pass; reset revokes **all** sessions and clears the timed lockout ("unlock via reset"), change revokes others and feeds no lockout counter — locking an account from inside an authenticated session is a denial-of-service button. §7's two admin rows check their key **imperatively** under `@AuthenticatedOnly()`: the row says a missing key answers 404, and `@RequirePermission`'s 403 would distinguish the two probes the existence-hiding rule exists to merge. The four session/device keys resolve through a thunk, so an own-scope call still pays nothing (ADR-0005's lazy rule, asserted by a test that counts resolutions). §12 events are written to the `domain_events` outbox — the write side is the module's duty, the relay is worker machinery. **Named omissions, recorded rather than implied (A-191):** email delivery of reset and invite links rides with the notification module and the raw token never enters an outbox payload; the `admin`-policy blocked-replacement notice and the two purge crons defer the same way; `auth.*` ships as constants named by their settings keys; the §5 `Origin` allow-list waits for the CORS wiring the admin web's first deployment forces, because until an allow-list exists there is nothing to check against. 43 new unit tests (suite **156 passed / 31 skipped**), mutation-proven rather than asserted: disabling the grace cache fails the BR-AUTH-005 test, downgrading the family revoke fails BR-AUTH-004's, dropping the replacement ownership check fails the foreign-device test, and letting change revoke the acting session fails its own — each exactly one test, all reverted. Route lint 20/20. **`/auth/me` still omits `name` and `employeeId`**, and that gap stays open by design: both live on `employees`, and reaching into another module's table is the ADR-0001 violation the architecture exists to prevent. Handbook pin `24bd1dd`. Next in spine order: audit-log and settings (order 1's remainder), then organization. |
| *(task)* audit-log — the store, the sensitive-read channel and the read API — `hris-api` #8, `610abc8` | 4 | done | 2026-08-06 | **Spine `implementation-roadmap.md` §4.2 order 1, third item.** `audit_logs` and `audit_anchors` per §4.1 verbatim, RLS on both, and BR-AUD-001 made **structural**: `init-roles.sql` hands `hris_app` all four DML verbs by default privilege, so the migration takes `UPDATE` and `DELETE` back — append-only is a grant, not a convention, which is why the repository has no method for either and the §14 grant test runs against a real PostgreSQL. **Three of the module's five channels are deliberately absent, and the reasons are mechanical rather than budgetary.** Channel 1's hook lives on `TenantScopedRepository`, a class that does not exist and whose *first* audited tables arrive with organization — **not one of §4.2's fifty-odd registered tables exists in the API today**, so the hook would be a base class designed against zero callers. Channel 2 needs the outbox relay: the `domain_events` writer has existed since the auth remainder, nothing dispatches from it, and that is BullMQ wiring. The two crons defer with the worker, GCS and the Cloud Logging sink — and the **anchor computation ships without its emit on purpose**, since BR-AUD-009's 2026-08-04 amendment exists precisely because a digest living in the same database as the rows it attests to is not an external witness. `AuditQueryPort` (§4.4) waits for reports, its stated first caller. **What did land:** UC-AUD-003 fail-closed with nothing caught (a `try/catch` there converts the module's central promise into a best effort silently), BR-AUD-008's dual identity, and UC-AUD-004 — which is BR-AUD-007's recursive case, so nobody reads this log without leaving a row saying they did. **The read runs before the audit insert**: auditing first lets an unfiltered first page return its own access record, and fail-closed survives the reorder because both statements share the request transaction. UC-AUD-005 is whole apart from its schedule — the day's row set is a **uuidv7 id range** (insert time, so a late channel-2 fact anchors where it landed), the digest covers every column plus the row count and canonicalises jsonb key order, `verify` chains against the digest stored on the anchor rather than a re-resolved predecessor, and back-fill chains to the newest anchor strictly *before* the gap. **Cursor pagination arrives here** as api-standards §6's first feed: opaque base64url keyset, row-value comparison driving `idx_audit_logs_cursor` backwards, `limit + 1` for `hasMore`, `meta` never carrying a total, and every malformed cursor answered `VAL_INVALID_CURSOR` at decode so no client typo reaches the database as a cast. Unlike auth's two admin rows these three endpoints use `@RequirePermission` — §2 makes audit a tenant-level duty with no per-row scope, so there is nothing to hide and 403 is honest. `AUD_` still owns zero codes. **One handbook defect found and not resolved: BR-AUD-005 contradicts §4.2 on money columns** — §10's telemetry registry masks `/amount|salary|wage/i` while seven tables are registered "full diffs" so exactly those figures appear; filed as issue **#21**, and it must be settled before organization writes the diff builder. **Four §14 scenarios are answerable before channels 1 and 2 exist, and all four are covered**: the grant test, BR-AUD-003's dedup index (nothing else in the repository exercises `uq_audit_logs_event`, and channel 2's handler will lean on it), §9's same-millisecond tie-break (three rows on one `occurred_at` is the case the row-value comparison exists for, and no unit test can see it because the comparison is SQL), and the anchor compute/verify pair. 52 unit tests plus 8 Testcontainers assertions (suite **208 passed / 31 skipped**), mutation-proven on the row-hash column set, the read-before-audit order, the fail-closed insert, the back-fill chain, the append-only revoke, the dedup index's `UNIQUE`, and the keyset's tiebreaker — each mutation failing exactly the tests that name it. A-192. Route lint 23/23. Next in spine order: settings (order 1's remainder), then organization. |
| *(task)* settings — hierarchical config, effective dating and the code-owned registry — `hris-api` #9, `3b84f74` | 4 | done | 2026-08-06 | **Spine `implementation-roadmap.md` §4.2 order 1, closed.** The module lands whole: both tables per §4.1, the five endpoints, resolution with its cache, the three write shapes, and `settings.sync-definitions` as a release script on the migrator credential (§12 gives it one trigger and that trigger is the pipeline). **The registry ships forty-one of §4.2's fifty-five keys, and the fourteen absentees are cut by a rule rather than a budget**: a default carrying `⚠️ VERIFY` is a regulation-dependent number, and `ai-development-guide.md` §5 forbids an assistant typing one — a fixture asserts, a **seed runs**, so a placeholder would configure real tenants against a figure nobody signed. Each returns with the module that owns it, once track 3 has signed the number. **Two constraints carry the module and neither is application code.** The scope CHECK keeps a branch row from existing without a company — a row resolution could never find. The gist exclusion is the whole of BR-SET-003: history, scheduling and as-of reads are one non-overlapping interval set, and the `COALESCE` to the nil uuid is load-bearing, because `NULL = NULL` is not true under `WITH =` and without it two live tenant rows for one key would coexist. Both are proven against a real database, including §9's two-writer race. **Three interval decisions the rules imply without stating.** A same-day supersede **drops** the earlier row rather than closing it to `[today, today)` — an empty interval the constraint ignores and the timeline shows as a phantom; day is the granularity, so the second write of a day is that day's row (§14's "idempotent day granularity"). An immediate write placed under an existing schedule **closes at the schedule's start**, because the alternatives are colliding with the constraint or silently cancelling next month's change. And a row taking effect **today** is on the immutable side of BR-SET-005, not the cancellable side — requests in flight are already reading it as-of. **BR-SET-008's `tighten_only` is carried for the editor and enforced through `min`/`max`**: every direction-constrained key states its constraint as a floor or a ceiling, and two mechanisms for one sentence eventually disagree. **The bust is inside the request transaction rather than post-commit** — a named narrowing (A-193 decision 6): nothing here has a post-commit hook, the exposure is the tail of one handler, and §9 already answers a lost bust with the five-minute TTL. For the same reason a failed bust logs instead of raising, since raising it would let a Redis outage stop every settings write. **One real bug, caught by the integration test rather than by review**: the definition sync rewrote 31 of 41 rows on every run, because jsonb does not round-trip key order and `JSON.stringify` is order-sensitive — so "idempotent" was true of the data and false of the writes, and every release would have read as a configuration change. `stableStringify` in `shared/` fixes it and is now shared with the audit anchor, which needs the identical property for the identical reason. Also refactored: **ADR-0010's outbox writer moved out of auth into `src/database/`**, one writer beside the unit-of-work it rides, modules keeping their typed event ports. 76 unit tests plus 12 Testcontainers assertions (suite **284 passed / 31 skipped**, integration 24), mutation-proven on the resolution order, the same-day drop, the validation short-circuit, the as-of cache bypass, the stale-map fallback, the shared key sort and the scope CHECK. Route lint 28/28. A-193. **Spine order 1 is complete** — authentication, authorization-rbac, audit-log and settings. Next: organization (order 2, `OrgQueryPort`, eleven consumers), which is also where `TenantScopedRepository` and audit channel 1 arrive, and where handbook issue #21 must be settled first. |
| *(task)* audit diff masking — BR-AUD-005 settled against §4.2 (issue #21) | 4 | done | 2026-08-06 | Not a MANIFEST row — a contradiction found while implementing the audit-log store (A-192 item 8) and deliberately left for its own session, because the module that writes the masker is organization and reversing the rule afterwards would leave historical diffs masked the wrong way. Full reasoning in the `docs/05-platform/audit-log.md` row above. Touched: BR-AUD-005 (rewritten), §4.2 (fail-loud gate paragraph; `employees`, `leave_requests` and `candidates` notes now state their own authority instead of citing §10 for entries §10 does not contain), §5 UC-AUD-001, §7 search-port comment, §9 masking-lag edge case, §14 (+2 scenarios — money diffs in full, unregistered table throws), `security-standards.md` §10 (declared telemetry-only in one paragraph), A-192 item 8 resolved. Every other §10 citation in the handbook was checked and is telemetry-scoped, so nothing else moved. Issue #21 closed |
| *(task)* organization — structure, effective-dated placement, `OrgQueryPort`, and audit channel 1 — `hris-api` #10 | 4 | done | 2026-08-06 | **Spine `implementation-roadmap.md` §4.2 order 2**, the most-consumed port in the system: eleven module documents reference `OrgQueryPort`, and nothing downstream of it could be built first. Six tables per §4.1 verbatim, the fifteen endpoints, both ports, and the two events. **Three things arrive with it that are not organization's**, which is why this module was scheduled where it was. `TenantScopedRepository` is the first of them — audit-log §4.2 names the class and this module ships the first audited tables in the system, so **channel 1 (BR-AUD-002) exists as of this PR**: a diff written on the same handle inside the same unit-of-work, proven by an integration test that aborts a transaction and finds no audit row, which is the property a queue-based or after-commit hook cannot promise. The §4.2 registry lands as code with issue #21's fail-loud gate as a constructor assertion, so an audited table nobody classified stops the process at module init. `PeriodLockPort` is the second, stubbed in `shared/` under the roadmap's one-sentence sanction — attendance.md §4.2 owns it, and organization.md §14's locked-month scenario drives the port so the rejecting branch is real before the implementation is. The third is the `fk_setting_values_branches` deferral, closed: settings shipped `branch_id` FK-less because `branches` did not exist. `fk_holidays_branches` stays open and is holiday's own migration to write — fulfilling a deferral needs a table to alter. **Two constraints carry placement and neither is application code.** The gist exclusion is BR-ORG-002 itself — "which position did this employee hold on 3 March" has to have one answer, and `WHERE deleted_at IS NULL` on it is what lets UC-ORG-004 reopen a predecessor in the same transaction that cancels its successor. The timezone CHECK is BR-ORG-001: Indonesia has three zones, that is a fact about the country rather than tenant policy, and §1 excludes international timezones from V1. **The supersede decision the rules imply without stating**: a backdated `correction` **inherits its predecessor's `effective_to`** rather than running open-ended, or it would overlap every later placement and the constraint would refuse the whole act. Which in turn is why `ORG_ASSIGNMENT_OVERLAP` is mapped from `23P01` and never pre-checked — with the planner closing the interval it fills, a collision means the history moved under the read, and that race is exactly what the constraint is for. Duplicate codes take the opposite route for the opposite reason: §7 wants a field-named `VAL_DUPLICATE`, and a constraint name cannot say which field. **BR-ORG-003 is enforced as written, including the parts that look like omissions.** `direct_manager(n)` returns the holders of **exactly** the position n edges up — a vacancy at level 1 with a holder at level 2 answers empty, because the engine's vacancy ladder (BR-APRV-006) owns that decision and inventing a fallback here would take it away. Subject exclusion is by employee rather than by user, which covers §9's odd case where the requester also holds the landing position higher up. The chart, by contrast, does **not** filter holders on having a user account: that filter exists so a manager who cannot log in cannot be assigned an approval step, while a holder with no login still occupies the seat, and hiding them would render a vacancy that is not one. **`audienceEmployeeIds` lives here and not in announcement** for the reason §13 gives — a department rule descends this module's tree with this module's depth cap, and no flat view can express ancestry. Its dimensions **union**: "Finance" plus "Jakarta" is both groups, because a targeting rule adds an audience rather than narrowing one. **The one recorded architectural deviation is reading `employees` and `user_roles` directly** (A-194 decision 5): employee.md is spine order 3 and its `employee_directory` view — ADR-0001 rule 6's own amendment, written for this exact shape of read — does not exist yet. Every such read carries an `A-194:` marker that greps to the complete swap list, and `EmployeeLookupRepository`'s three-column shape is already the subset employee.md's facade will expose. Notably the holder joins could not have been ports even if it did exist: the employment filter is a predicate inside an aggregate, not a page of rows. **Review found three things worth recording.** §8's coordinate pairing was left to the database, so a `PATCH` carrying one coordinate would have answered `SYS_INTERNAL` where §8 asks for a field entry — it is application code now, checked against the merged row, with the CHECK as backstop. And the module was written with ten `Promise.all` pairs inside the unit-of-work, which coding-standards-nestjs §4 bans outright; the sweep that removed them also found **three pre-existing violations** in settings' value repository and auth's session and device repositories, fixed in the same PR because a CI-invisible rule that one module follows and three do not has stopped being a rule. And UC-ORG-006's *"others: forced own"* was not implemented: the chart derived its company from `companyScope`, which is **empty for an ordinary employee** — so the one surface in this module with no permission key, the one an employee opens to find their own manager, answered 404 for exactly them. It reads the caller's `employees` row now. 88 unit tests plus 38 Testcontainers assertions (suite **372 passed / 31 skipped**, integration **62**), covering both CHECKs, the exclusion constraint in all four of §14's directions including the two-writer race, RLS on the five new tables, the vacancy subquery, the as-of boundary and every archive guard against real SQL, and channel 1 end to end. Route lint 52/52. A-194. Handbook pin bumps to this PR. Next in spine order: employee (order 3), which retires `EmployeeLookupRepository`, publishes `employee_directory`, and is `assignOnHire`'s first real caller. |
| *(task)* employee — master data, ADR-0016 encryption, the status machine and `employee_directory` — `hris-api` #11 | 4 | done | 2026-08-07 | **Spine `implementation-roadmap.md` §4.2 order 3.** Three of §4.1's six tables, the master surface, both `/me` reads, the reveal path, and two of the three §13 ports. **ADR-0016 becomes real here** — until this module there was no encrypted column in the system, only a spec and a stand-in in an audit test. `encryptedText` is a Drizzle `customType`, which is the mechanism the ADR chose and which forces one non-obvious consequence: its hooks are **synchronous and context-free**, so the tenant DEK has to be resolved *before* the statement and read out of a process cache during it. Hence `ensureLoaded()` as the first line of every repository method touching the table — on reads as well as writes, because `fromDriver` is what turns a `v1:` string back into a NIK — and a cache miss **throws**, because the alternatives are a column written in the clear or ciphertext handed to a domain object as an identifier. The one thing this buys for free is worth the ceremony: **BR-AUD-005 layer 1 masks the whole encrypted set with no list**, because it derives from the column type, and the audit trail of an employee edit records that a NIK changed without ever holding one. `ptkp_status` is the exception the layer cannot reach and the reason §4.2's note exists — ADR-0016 leaves it plaintext on purpose (the tax engine reads it every run), so nothing about the schema would mask it. `tenant_keys` arrives with the crypto helper on the `TenantScopedRepository` precedent: system-administration.md owns it and will be its only writer, `shared/` is and stays its only reader, and until provisioning ships the seed mints the row. **A tenant with no key row cannot hold an employee at all**, which is the correct failure. **Three contract gaps surfaced that only this module could surface, all additive and all fixed upstream.** `Placement` returned ids only, which made both of §7's placement shapes unimplementable — a grid renders `positionTitle` and no consumer may join organization's tables to find one; it now carries six display names, three extra joins on a query that already made two. `OrgQueryPort` gained `directReports`, which employee.md §13 has listed as a consumed "team inverse" since 2026-08-02 while organization.md §4.2 never wrote it — and it answers in **employee** ids with no account filter, because a report who cannot log in is still a report and BR-ORG-003's filter exists for a different question. And `PayrollEmployeeFacts.status` listed `suspended`, which the §4.1 enum cannot produce. **Two debts closed and one deviation retired.** `employee_directory` ships with `join_date` beyond §13's column list — the addition the entry's own rule anticipated, needed by the placement rules — and with it **A-194 decision 5 is retired in full**: every direct `employees` read in organization is a view join now, leaving only `user_roles`, because authorization-rbac publishes no view. `/auth/me` returns `name` and `employeeId` again, through the same view, closing the gap the auth remainder left open by design in PR #7. **The absences are a dependency list, not a budget.** `employee_documents` needs `files` (order 5); the two request tables need `approval_instances` (order 4) — so BR-EMP-009, BR-EMP-010, BR-EMP-012 and the BR-EMP-006 clause that cancels pending requests all land with their dependency. `employee_contracts.file_id` ships FK-less on the settings precedent. `EmployeePayrollPort` is absent on a different rule, worth stating because it is the line this session used throughout: **a port that is a thin projection of machinery the module needs anyway can be written correctly with no caller; one that needs machinery only its caller can shape cannot.** `rosterFor` decrypts bank details, audits per run rather than per employee, assembles from three sources and carries an `includeExited` mode whose only definition of correct is what a THR run needs. §8's working-age floor is unimplemented for the ADR-0018 reason: it reads "today − 15 years" under a `⚠️ VERIFY`, a validator is code, and an assistant never types a regulatory number. **Two review findings, both mine.** The grid selected the whole row and dropped the encrypted set in a mapper — paying a DEK unwrap and twenty crypto hops per page for a payload that never carries them, against ADR-0016's own promise that list views do not decrypt; the projection is the boundary now, which a forgotten mapper cannot be. And `POST /employees` answered `placement: null` moments after seeding one, because the response was assembled from the create inputs rather than read back. **The idempotency decision worth recording**: UC-EMP-007 claims *before* it acts — `markApplied` guarded by `applied_at IS NULL` is an atomic claim, not a stamp — because the outbox emit is the one non-idempotent effect and a second emit is a second `eventId`, which is what every consumer's dedup guard keys on. Proven with two concurrent transactions against a real database. 73 unit tests plus 31 Testcontainers assertions (suite **462 passed / 31 skipped**, integration **93**), covering both partial uniques across every status and the soft-delete case, the contract CHECK and the inclusive-end exclusion in both directions, RLS on the three new tables, the view's `security_invoker` isolation, ciphertext at rest with cross-tenant digests, the fail-closed missing-key path, and all three masking layers on one audit row. Route lint 70/70. A-195. Next in spine order: approval-engine (order 4), which is what unblocks this module's data-change and resignation halves. |
| *(task)* approval-engine — chains, instances, resolvers, delegation and the two-gate `ApprovalPort` — `hris-api` #12 | 4 | done | 2026-08-07 | **Spine `implementation-roadmap.md` §4.2 order 4.** Six tables per §4, the eleven config and read endpoints, `ApprovalPort` in full, and UC-APRV-007's ladder. **The module ships with no consumer and a complete port, and that is a rule rather than an exception to one.** Nine module documents declare `ApprovalPort` and eight request types are registered for V1; none of those modules exists. Employee withheld `EmployeePayrollPort` on the line *a port that is a thin projection of machinery the module needs anyway can be written correctly with no caller; one that needs machinery only its caller can shape cannot* — and this is the first kind: every method is defined entirely by §5 and the `BR-APRV-*` rules, and nothing about `approve` changes depending on who calls it. **Three additive contract gaps only this module could surface.** `RoleHolderPort` is declared in **authz**, which had declared no port at all — `role_holders` and BR-APRV-006's fallback rung both read `user_roles`, which ADR-0001 rule 2 reserves to its owner, and declaring authz's port inside the engine would have been the deviation-by-comment `ai-development-guide.md` §3 rule 1 rejects; it answers by role id **and** by role key, because the resolver names one by id and `approval.fallback_role` names one by key. `OrgQueryPort.positionExists` is §8's reference check and is a different question from `positionHolders` answering empty — a live but vacant position is what the ladder exists *for*, a deleted one is a chain that can never resolve, and the difference is a field entry in the editor versus a stuck instance nobody finds until a request is late. And **audit-log §4.2 carried no `approval_*` row at all**: BR-AUD-004 keeps the *trail* out because `approval_actions` already is one and says nothing about the configuration that produced it, so `approval_chains` and `approval_delegations` are registered — a chain decides who approves a payroll run, which is the class of act §4.2 exists for — while the four execution tables stay out, structurally, because `InstanceRepository` does not extend `TenantScopedRepository` and that base asserts registration in its constructor. **Four decisions the rules imply without stating.** The §13 request-type registry lives in the **engine**: §13 says so, and a `registerRequestType()` call shaped like `registerAuditedTables` would leave it empty until Phase 3, with no chain configurable and nothing submittable — field names only, because five of the ten declarations state types and five do not, and inventing types for half a registry to fill a column nothing reads is worse than not having the column. BR-APRV-007's reroute goes up **exactly one level** and then falls into the vacancy ladder — ADR-0008 and §14 both say *next* level, singular, a walk that kept climbing would need a depth cap nothing supplies (organization caps departments, not reporting lines), and "nobody up there either" is the vacancy BR-APRV-006 already answers. The self-approval guard runs **again after** the delegation redirect, because a delegation pointing back at the requester would otherwise hand them their own request and the guard's decision was "not this person". And an empty `assigneeUserIds` on `approval.step.activated` **is** the stuck signal — §12 registers no stuck event while §13 asks for a stuck notification, the empty list is the fact, `is_stuck` is its queryable half, and inventing an event name would be adding to a contract section. **Two answers about who may act, and they differ by path.** Reaching `ApprovalPort.approve` means the module's static `@RequirePermission` already passed, so a caller with no seat holds the permission by construction — 403 `APRV_NOT_AN_APPROVER`, BR-APRV-012 as written. Reaching `GET /approval/instances/{id}` proves nothing of the kind, so outside the visibility set it hides existence; and the relationship test runs **before** the oversight test, because a company-scoped admin can be seated on another company's step by a `specific_user` resolver and checking the permission first would answer 404 for the one instance they must act on. **The `any`-quorum loser can lose in two places.** The step version guard is §9's mechanism, but under READ COMMITTED the loser may instead read its own seat one statement after the winner's `closeRemaining` — a spent seat raises `APRV_STEP_ALREADY_DECIDED` rather than `APRV_NOT_AN_APPROVER`, because having no seat and having a spent one are different facts. Both were proven with two concurrent transactions against a real database, and either way the loser's claim is undone: the failure propagates and the transaction rolls back, which is the property `ApprovalPort`'s doc comment asks every caller to preserve. **Two fixes outside the module.** `TenantScopedRepository.live` degrades to `true` on a table with no `deleted_at` — `approval_delegations` ends through `revoked_at`, and the alternative was inventing a column §4 does not declare or writing `recordChange` by hand against a port whose doc says the base is its only caller. And the two employee integration specs handed `TenantKeyService` a **frozen business clock** while the `encryptedText` column type reads the cache back through `Date.now()`: those suites only passed within five minutes of 03:00 UTC, which is a test that passes at one time of day and is therefore not a test. **The absences are dependency-shaped.** The SLA scan has no schedule — §12 asks for every 15 minutes, ADR-0010 puts crons on BullMQ, and there is no worker here yet, exactly as with the audit anchor and employee's effectuate job; nothing in it writes a step status, which is BR-APRV-010's *no auto-decide, ever*, asserted by counting calls rather than assumed. `approval.fallback_role` resolves to `hr_admin` and no tenant has such a role, because authorization-rbac seeds no templates (A-195 item 10) — so the last ladder rung is empty until it does, and that surfaces as a flagged stuck instance rather than as silence. 107 unit tests plus 18 Testcontainers assertions (suite **569 passed / 31 skipped**, integration **111**), covering the partial unique across resubmission and across tenants, the append-only revoke as `hris_app`, both CHECKs, the one-seat-per-step unique, RLS on all six tables, the same-transaction rollback that leaves no orphan instance, the snapshot surviving a chain edit, the `any`-quorum race, and channel 1 on a chain write. Route lint 81/81. A-196. Handbook pin bumps to this PR. Next in spine order: document-storage (order 5), which is what unblocks `employee_documents` and every attachment slot in the request modules — and after it notification and inbox, which order 4 forces, since an approval chain with no step notification is a chain nobody acts on. |
| *(task)* document-storage — the files pipeline, the category registry and the resolver contract nine modules were waiting on — `hris-api` #13 | 4 | done | 2026-08-07 | **Spine `implementation-roadmap.md` §4.2 order 5.** One table per §4.1, the five endpoints, the three §12 job bodies, `StorageUsagePort`, and the `employee_document` category bound end to end. **The module's real deliverable is a registry with two halves and one of them was undeclared.** §4.2's entry contract named `ownershipResolver (module port)` and nine module documents say *"this module's ownership resolver"*; no document anywhere gave it a shape, which made it the one field an implementer had to invent. `FileOwner` is declared in §4.2 now — a **consumer contract this module publishes**, the `registerAuditedTables` relationship rather than the `EmployeePayrollPort` one, because the generic endpoints have to run one check whoever owns the category. **The permission fields fold into it**, and the seed table is the argument: half its rows state a gate no static key can express — *"self, or `expense.claim.create`"*, *"or a live approver of its instance"*, *"or being a recipient of the announcement"*, *"while the claim is `draft` or `returned`"* — so keeping the expressible half as registry data would have left the rest as prose nothing enforces. Every predicate answers **false**, never an error, because §2 already made a scope miss 404 and that is why this module has no permission keys of its own to raise a 403 with. **A category nobody owns is not live**: nine of the ten ship policy-complete and refuse every request, which makes registration the gate rather than a decoration — the mirror image of the line approval drew, where a *port* was fully defined with no consumer and here the *policy* is fully defined with no owner while the *gate* is defined by nothing but the owner. **`files` is not channel-1 audited and the repository says so structurally.** audit-log §4.2 carries no row for it and §12's `document.file.committed` / `document.file.deleted` are already on audit's consumed list, so `FileRepository` does not extend `TenantScopedRepository` — whose constructor assertion is what would have caught the mistake. What *is* audited is the mint: `generated_document` writes `document.download.generated_document` **before the URL exists**, fail-closed per audit-log UC-AUD-003, proven against a real `audit_logs` row rather than a spy. `import_file`'s `document.download.gated_export` is the per-file half and belongs to the owner, because whether an export's frozen column set includes gated columns is a fact only the module that froze it holds. **Four places the contract had to be read rather than copied.** `generated_document.maxSizeBytes` is `null`, not a number — §4.2's *"— (worker-only)"* means there is no client path, and a ceiling invented to fill the dash would be a contract value nobody decided. BR-DOC-004 says the object's size must *match* the declaration while §7 says `DOC_SIZE_EXCEEDED` is *"actual object over cap/declared"*; §7 wins, because BR-DOC-010 has the client compress a selfie before it uploads, so promising a bound and delivering less is the normal case and the row stores verified bytes either way. Delete runs `canRead` → `clientDeletable` → `canDelete` so both answers stay reachable — a caller who can already see the file is told it is retained, one who cannot learns nothing including that. And §9's *"row flagged"* for an object that vanished is a log line, because `commit_failure_code` is documented staged-rows-only and writing it would file a commit failure against a file that committed fine. **The two-device confirm race lands on the `move`, not on the row.** Both readers see `staged`, both reach GCS, and the loser's source object is already gone — so a move failure is only a failure if the row is still staged, which is §9's *"first commits; second hits BR-DOC-006 idempotent success"* implemented where it actually happens. The catch is not a blanket swallow and there is a test that says so. **The absences are dependency-shaped and one is a rule.** No `DocumentPort` / `DocumentStoragePort`: four module documents name one, **none declares its shape**, and none of the four exists — the `EmployeePayrollPort` line of A-195 drawn again, while `StorageUsagePort` ships because §13 declares it in full. UC-DOC-004's worker write path goes with it, so the `generated_document` mint is tested against a seeded row. The three jobs have no schedule, for the reason the approval SLA scan and the audit anchor have none. And the expiry scan **stamps and emits nothing** — §12 registers no reminder event and notification does not exist, so the stamp is the record its template will read. **On idempotency the two contract sections disagree and the module document wins**: §7 marks both queue-reachable endpoints *accepted* / *idempotent by design* while api-standards §7 rule 1 says queue-reachable is *required* — a duplicate slot is a disposable staged row the sweep collects, so no `IdempotencyInterceptor` is built here and the first endpoint that genuinely requires one is where that subsystem lands. **Two things closed on the way through.** `employee_contracts.file_id`'s two-migration deferral, closed by the schema rather than by hand — `.references(() => files.id)` lets drizzle generate it, so the constraint carries its default name and not the `fk_…` the 0011 comment guessed. And employee's `employee_document` binding, which employee.md §2 assigned to this module's arrival: the resolver needs nothing but the directory view, so it ships, while `employee_documents` and UC-EMP-010's endpoints stay employee's own task. `@google-cloud/storage` is the one new dependency — V4 signing is a canonical-request/HMAC chain and hand-rolling it is how a signature quietly starts accepting the wrong principal; ADC resolves the signer through Workload Identity, so **no key material is mounted anywhere** and `GCS_SIGNER_SA` is an email. 74 unit tests plus 18 Testcontainers assertions (suite **643 passed / 31 skipped**, integration **129**), covering both new CHECKs, the closed foreign key in both directions, RLS and the `WITH CHECK` half on `files`, the three partial-index predicates as predicates rather than as query plans, the sanitized path landing in a column, purge removing the object before the row, and the payslip access record. Route lint 86/86. A-197. Handbook pin bumps to this PR. Next in spine order: **notification** (order 6) and **inbox** (order 7), which orders 4 and 5 both force — an approval chain with no step notification is a chain nobody acts on, and `document.expiring` is a template this module already writes the stamp for. |
| *(task)* notification — the template registry, the send pipeline and the feed — `hris-api` #14 | 4 | done | 2026-08-10 | **Spine `implementation-roadmap.md` §4.2 order 6, first half** (inbox is the other). Three tables per §4.1, §7's six endpoints, the eight `on.<event>` handler bodies, `cron.notification.purge`, `NotificationStatsPort`, and `NotificationPort` — the contract eight module documents name and the first one built before any of its consumers exist, because its own event handlers are consumers. **The registry ships whole and most of it is inert.** §13 calls §4.2 *"the platform seed"*, so all forty-five templates are registered including the thirty-odd whose modules have not been built; a template nobody sends costs a row in the preference matrix and nothing else. That is the mirror image of the line document-storage drew — there a *policy* was complete and its gate undefined, here the *definition* is complete and only its sender is missing. The message text is this module's: BR-NTF-001 says templates carry i18n keys and no document says what they say, so both locales ship and a translation pair whose placeholders drift apart fails a test. **Three contract gaps the arrival exposed.** §12's terminal approval payload was the only recipient-bearing event in its set that did not name its recipient — `approval.step.activated` carries `assigneeUserIds` and `approval.step.escalated` carries `escalatedToUserIds` for exactly the reason `approval.instance_decided` needed `requesterUserId`, since `approval_instances` is the engine's table under ADR-0001 rule 2 (PR #32, additive, no supersession). **BR-NTF-006's locale chain has two empty rungs** — no `users.locale` column, no tenant-default locale key, and `Accept-Language` is scoped to server-generated documents and unreadable in an async send — so every V1 render is `id`. And **BR-NTF-011's go_router path grammar does not exist**: it cites mobile-flutter §7, which is the sync-engine seam, so the handlers set no deep link and the port carries `deepLink?` for callers that know their own route. **Push and email have no dispatch, and the delivery rows say so.** They land `pending`, which is the state `dispatch.push` and `dispatch.email` consume; there is no FCM or email provider wired, no credentials, and ADR-0010 puts their scheduling on a worker this repository does not have. BR-NTF-007's device targeting rides with them. `in_app` is complete — `sent` at row creation — which is the channel BR-NTF-008 calls the durable one, and the feed's population is a delivery semi-join rather than a template-key list, so an email-only row and a suppressed channel both stay out. A-198 records the eighteen decisions the module document leaves open. Checks: 789 unit / 151 integration green, route lint 92/92. |
| *(task)* inbox — the unified task list, its closure rules and the acknowledge action — `hris-api` #15 | 4 | done | 2026-08-10 | **Spine `implementation-roadmap.md` §4.2 order 6, second half**, and the close of order 6. One table per §4, §7's five endpoints, the seven `on.<event>` handler bodies, `cron.inbox.purge`, and `InboxPort` for announcement.md. §1's line held all the way through: *"a notification tells you something happened; an inbox item waits for you"* — step activation produces both, and this module registers no template and imports nothing of notification's. **The approval half needed a channel that did not exist.** `approval.step.activated` carries `{ instanceId, stepId, assigneeUserIds }` and an item needs five things it does not — the **assignee row id** BR-INB-004 makes the `dedupe_key`, the `requestType` and `requestId` §4's `source_ref` holds, the `activated_at + sla_hours` sum BR-INB-009 sorts on, the delegate pairing UC-INB-001 badges, and the requester and context it titles from — all of it on the engine's tables, which ADR-0001 rule 2 puts behind its port. approval-engine §7 gains **`ApprovalTaskPort.stepTasks`** (PR #33, additive, no supersession), and §13's inbox bullet — which said items *"derive from live `approval_assignees` rows"* and so read as a licence for a cross-module table read — is corrected in place. A port rather than a wider payload, unlike A-198's `requesterUserId`: that was a *recipient*, and this event family already names its recipients; a task snapshot is five facts off three tables neither other consumer wants, and ADR-0010 names *"consumers re-read state by id"* as the channel for exactly that. **Two more contract findings.** BR-INB-006 filters on two keys §4 stores inside `source_ref` jsonb, which database-conventions §1.8 forbids — the column stays and one partial expression index answers both closure paths. And `deep_link` is `NOT NULL` while **no route grammar exists anywhere in the handbook**, the same absence A-198 recorded for notification where the column was nullable and could record it; approval tasks store `<requestType>/<requestId>`, a reference built from ids the contract already fixes. **The item copy is this module's**, on notification's precedent: no §4.2-style registry exists here and BR-INB-005 freezes a rendered title at creation, so a per-request-type title and subtitle ship in both locales over the context fields the engine already declares, and a test refuses a placeholder no module sends. Every V1 render is `id`, for A-198's reasons unchanged. **Two things ship with no caller**, deliberately: `createAckItems`/`closeAckItems` are announcement.md's and announcement is Phase 3 — §13 records that three contracts written here before that consumer existed held without amendment, and building them now is what makes that true rather than aspirational — and `inbox.item.acknowledged` is written to an outbox nothing dispatches. No relay and no scheduler, as everywhere else. Also recorded: the shared `IdempotencyInterceptor` is still absent and this is the endpoint whose arrival was supposed to build it; `app.module.ts`'s *"nothing here is queue-reachable"* is corrected, and the deferral now rests on §7 declaring the endpoint `accepted` rather than required and on BR-INB-008's 200 no-op being durable state where a stored response is a cached one (offline-sync §5). A-199; 196 unit and 23 integration tests for the module, suites 876 unit / 182 integration green. |
